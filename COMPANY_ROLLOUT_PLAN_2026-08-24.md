# خطة تعميم LetterSys على أقسام الشركة — مع إبقاء وضع «القسم الواحد»

**التاريخ:** 2026-08-24 · **الحالة:** ز−1 **والمرحلة أ** منجزتان 2026-08-27 (**924 اختباراً خضراء** · 5 هجرات · 12 بنداً من §7)؛ دفتر التنفيذ في [`ROLLOUT_EXECUTION.md`](ROLLOUT_EXECUTION.md). التالي: **ز0** (تكوينٌ لا كود) ثمّ **ب** لوحة الإدارة · **المرجع:** مسح قراءة-فقط بسبعة محاور على الكود والقاعدة الحيّة (أرقام الأسطر أدناه مقيسة لا مُخمَّنة)

**الهدف:** تحويل LetterSys من نظام مبنيّ عمليًا لقسم المتابعة (ش13) إلى نظام مراسلات مؤسّسي لشركة نفطية يعمل بوضعين من نفس الكود:
- **وضع الشركة** — كل الأقسام على خادم مركزي، بريد داخلي بين الأقسام، لوحة إدارة، تدقيق شامل.
- **وضع القسم الواحد** — تنصيب مخصَّص لقسم بعينه (كما يجري اليوم لقسم المتابعة) دون أي تعقيد إضافي ظاهر.

> **§10 «رحلةُ كتابٍ واحد»** شهادةُ موظّف البريد — **اقرأها قبل §8**: هي الحَكَم حين
> يتعارض التصميمُ معها، وقد نقضت افتراضاً جوهريّاً فيه (الإحالةُ الداخليّة **هامشٌ فقط**
> لا كتابٌ جديد) وصحّحت قاعدةَ سرّيّةٍ شُحنت في المرحلة أ.

> **§8 نسيجُ المراسلات** (التعاميم والإحالات ودورة الحياة و`@`) و**§9 نقدُ الاستشاريّ الإداريّ**
> (ما فات · ما يُسقَط · قوانين الديوان · مؤشّرات النجاح) أُضيفتا 2026-08-27 — وفيهما **أربعةُ قرارات مالكٍ جديدة**.

> **ابدأ من §7 «سجل العيوب»** (آخر الوثيقة): ستّة وعشرون عيباً مقيساً ظهرت أثناء المسح (8 حرجة · 9 متوسطة · 9 تنظيف)، وأربعة منها تعترض مراحل هذه الخطة نفسها فتُصلَح قبلها. §7.4 يبيّن أيّ إصلاحٍ يمنع أيّ مرحلة.

---

## 0) الوضع الحالي كما قِيس (لا كما يُظنّ)

### 0.1 الأقسام الداخلية كما يعرفها البرنامج فعلًا

لا يوجد نموذج «قسم» في `core/models.py` إطلاقًا — الأقسام ممثَّلة **كجهات (Entity)** ضمن فضاء واحد مع الشركات الخارجية والوزارات. القاعدة الحيّة (674 جهة، 413 نشطة) تحوي **41 وحدة داخلية مرمَّزة برموز السجل العربية** — هذه أقرب قائمة رسمية للأقسام الداخلية:

| الفئة | الوحدات |
|---|---|
| أقسام «ش» | ش1 مجلس الإدارة · ش2 الرقابة والتدقيق الداخلي · ش3 القانوني · ش4 شؤون التراخيص البترولية · ش5 العقود · ش6 رقابة عقود التراخيص · ش7 الفحص الهندسي · ش8 الصحة والسلامة المهنية والبيئة · ش9 قياسات نقل الملكية · ش10 الإخراج الكمركي · ش12 تقنية المعلومات · **ش13 المتابعة (قسم النظام الحالي)** |
| مكاتب | ش مكتب المدير العام · ش.م.د / ش.م.ف مكتبا المعاونَين |
| شُعب | ش.ت متابعة تنفيذ الموازنة · ش.ج إدارة الجودة · sh54 المتابعة الخارجية |
| هيئات | ت التخطيط والدراسات · ح الحقول · خ الخدمات والمواد · د الموارد البشرية · ع العمليات · م المالية · ه الهندسية · و نفط واسط · ت.م تشغيل المنصورية · ح.م حماية المنشآت |
| لجان | ل + 8 لجان إدارة مشتركة (ل ب، ل ج، ل ح، ل خ، ل ع، ل م، ل.ن.خ، ل8) |
| وحدات | و.ت التقارير · و.ت.ش التعاقدات · و.م مشاريع المساهمة · و.م.ب الموارد البشرية |

وإلى جوارها **364 جهة نشطة بلا رمز** بينها أقسام داخلية كثيرة (الصيانة، الحفر، الأرشفة…)، والرموز اللاتينية (ADE, ebs, kar…) شركات خارجية. الآلية الوحيدة في الكود التي «تعرف» قسمًا داخليًا هي مطابقة رمز السجل في الاستخراج (`core/extraction/matchers/entity.py:82-104` — «ش13 ← قسم المتابعة»)، وميزة الأضابير تعرض **أي** جهة لها كتب كأنها قسم بلا تمييز داخلي/خارجي (`core/views/dossiers.py:227-234`؛ السؤال موثَّق كمؤجَّل في `docs/ADABIR_DESIGN_2026-06-24.md:25,149`).

### 0.2 الفجوات البنيوية السبع أمام التعميم

| # | الفجوة | الدليل المقيس |
|---|---|---|
| 1 | **لا بُعد قسم في البيانات**: لا Department ولا ربط مستخدم↔قسم؛ الرؤية الوحيدة `created_by` الفردي (وعمليًا superuser **أو staff** يريان الكل — النمط مكرَّر يدويًا في `books_list.py:135`، `books_api.py:460`، `attachments.py:177`…) | `core/models.py` كاملًا؛ `core/roles.py:52` (`is_staff` ⟵ دور entry) |
| 2 | **7 إعدادات مفردة singleton** تفترض مؤسسة واحدة: `SystemSettings`، `NotificationSettings`، `SecuritySettings`، `BackupSettings`، `EmailSettings` (بريد واحد للجميع، 20 موضع استدعاء لـ`get()` في كود الإنتاج تمتد حتى dashboard وdossiers)، `AIIntegrationSettings`، `NetworkSettings` | `core/models.py:1113-2146` |
| 3 | **الترقيم عالمي**: `BookSequence.kind` unique (عدّاد واحد لكل نوع على النظام كله)، قيد التفرد `(our_number, kind)` عالمي، الحجز بمفتاح `(user, kind)` | `core/models.py:1358, 450-457, 1569` |
| 4 | **لا بريد داخلي**: كل إرسال يمرّ بـ SMTP خارجي ويشترط عنوان بريد؛ صندوق الوارد يعرض كل `IncomingEmail` **لكل مستخدم مسجّل بلا تصفية** وعدّاد غير المقروء على مستوى النظام | `core/messaging/views/ui.py:132-161` |
| 5 | **التدقيق غير موصول**: `UserActivityLog` معرَّف ولا يكتبه أي كود؛ middleware النشاط غير مسجَّل ويكتب للّوغ النصي لا للقاعدة؛ لا تسجيل دخول/خروج ولا قراءة ولا طباعة (PRINT غائب حتى من `ACTION_CHOICES`)؛ لا واجهة عرض للسجلات | `core/logging_models.py:11-24`؛ `core/middleware/logging_middleware.py:149-187`؛ `lettersys/settings.py:82` |
| 6 | **لا توقيع إلكتروني**: الموجود توقيعُ بريدٍ نصيّ (`EmailSettings.email_signature`) وروابط مشاركة موقّعة فقط؛ لا توقيع مستند ولا سجل تنزيل للرابط الموقّع | تقرير المحور 7؛ `core/attachment_sharing.py` |
| 7 | **النشر تطويري**: `.env` الحيّ على `DEBUG=True` و`ALLOWED_HOSTS=*`؛ لا reverse proxy ولا TLS ولا خادم ملفات ساكنة؛ النسخ الاحتياطي يغطي القاعدة فقط (لا media ولا `.env` ولا `.encryption_key` ولا نماذج ONNX)؛ سر SQL قديم في تاريخ git يلزم تدويره | `docs/LAUNCH_RUNBOOK.md:104-111`؛ `core/backup_service.py` |

### 0.3 ما هو جاهز فعلًا ويُبنى عليه (لا يُعاد بناؤه)

- **الترقيم** مصدر وحيد `core/numbering.py` — إضافة بُعد القسم تكون فيه وحده.
- **التصفية** مركزية في `BookFilterEngine` — بوابة النطاق تُضاف في مكان واحد.
- **الإشعارات** نموذج ناضج (`Notification`: فئات/أولويات/مقروء) — يحتاج توجيهًا جماعيًا فقط.
- **Channels موصول** (daphne + `asgi.py` + حارس الأصل `ws_security.py`) بمسار WebSocket واحد (حضور الحجز) — التوسعة إضافة consumers لا بنية جديدة.
- **البريد الخارجي** كامل (SMTP مربوط بكتاب + IMAP ببوابة صلة + خيوط + قوالب + روابط 18MB).
- **حلقة التصحيحات** تلتقط فعلًا (`core/extraction/capture.py` + `ScanPayload` دائم + `ExtractionFeedback`) وسكربتات الحصاد/التعبئة/القياس موجودة في `scripts/eval/` — تنقصها واجهة لا منطق.
- **مركز الإعدادات** بنية تبويبات جاهزة للتوسّع إلى لوحة إدارة (`core/views/settings_hub.py`).
- **دورة المرفقات** موحّدة (تحقق + توحيد PDF + نسخ + دمج + `MergeLog`) بنقطة إنشاء واحدة (`attachment_service.py:220`) — نقطة حقن التوقيع الطبيعية.

---

## 1) القرار المعماري الحاكم

### «بُعد القسم» داخل قاعدة واحدة — لا تعدد مستأجرين

شركة واحدة ⇒ قاعدة PostgreSQL واحدة، جدول `Department`، ومفتاح `department` على البيانات المنطاقة. **نرفض** فصل schema لكل قسم أو نسخة تطبيق لكل قسم: البريد الداخلي والإحالات بين الأقسام تتطلب أصلاً أن تكون الكتب في قاعدة واحدة، ودليل الجهات (674) ثروة مشتركة يجب ألا تتشظى.

### وضعا تشغيل من نفس الكود (Deployment Profile)

| | وضع القسم الواحد `single` | وضع الشركة `company` |
|---|---|---|
| الأقسام | قسم افتراضي واحد مخفي عن الواجهة | شجرة أقسام ظاهرة + محوّل سياق |
| الترقيم | كما اليوم تمامًا | عدّادات لكل (قسم، نوع) |
| البريد الداخلي | مخفي | مفعَّل |
| لوحة الإدارة | مركز الإعدادات الحالي + التدقيق | كاملة (أقسام/مستخدمون/عدّادات/بريد/تدريب) |
| حساب البريد | `EmailSettings` المفرد كما هو | حساب لكل قسم + سقوط للافتراضي |

الاختيار بحقل في `SystemSettings` (يظهر في مركز الإعدادات، staff فقط). **قاعدة التوافق الصارمة: تنصيب قسم المتابعة الحالي يجب أن يعمل بعد كل مرحلة دون أي خطوة يدوية** — كل هجرة تملأ القيم القديمة بالقسم الافتراضي، وكل سلوك جديد خلف الوضع `company`.

---

## 2) المراحل

> الترتيب مبني على الاعتماديات: **ز0 (تحصين الخادم) يسبق أي تعميم** لأن فتح النظام لأقسام أخرى قبل سد فجوات الأمن والنسخ يوسّع الضرر المحتمل. ثم الأساس (أ) الذي تقف عليه كل البقية.

### المرحلة ز0 — تحصين الخادم والنشر (تسبق كل شيء)

**الهدف:** خادم مركزي واحد يخدم LAN فورًا وقابل للفتح على WAN لاحقًا بقرار.

1. **تكوين الإنتاج**: قلب `DEBUG=False` وحصر `ALLOWED_HOSTS` بعناوين الخادم؛ تدوير سر SQL Server المكشوف في تاريخ git (موثَّق في `docs/LAUNCH_RUNBOOK.md:104-109`)؛ توليد `SECRET_KEY` إنتاجي.
2. **الطبقة الأمامية**: reverse proxy (يُرجَّح Caddy لبساطة TLS التلقائي، أو nginx) أمام daphne (ASGI موجود) + `SECURE_PROXY_SSL_HEADER` في settings (غائب اليوم) + خدمة الملفات الساكنة من الوكيل بعد `collectstatic` (لا شيء يخدمها اليوم في الإنتاج) — أو WhiteNoise إن أُريد تبسيط المكوّنات.
3. **الخدمات المقيمة**: PostgreSQL + Redis (يرقّي الكاش والجلسات وChannels وCelery معًا — الكود يكتشفه تلقائيًا عبر `REDIS_CACHE_URL`/`CHANNELS_REDIS_URL`) + عامل Celery + beat كخدمات نظام (NSSM على ويندوز سيرفر أو systemd على لينكس — **قرار مالك: نظام تشغيل الخادم**).
4. **العتاد**: جهاز التطوير 8GB غير كافٍ لخادم شركة (الكاشف ONNX وحده ~350MB + عمّال + Redis) — الحد المعقول 16GB والموصى به 32GB مع SSD، والقياس الفعلي بعد التشغيل التجريبي.
5. **النسخ الاحتياطي يتوسع** — ✅ **نُفِّذ 2026-08-27** (المرآة التدريجيّة + أرشيف الإعداد + إنذار المفتاح؛ إيداع `b60e7cd`). **الباقي على التشغيل لا الكود:** نقلُ `.encryption_key` إلى خزانةٍ منفصلة (بصمته `67572ad5…`)، ونسخةٌ خارج الخادم. الأصل: اليوم `pg_dump` مشفَّر فقط (`core/backup_service.py`) — يضاف: مجلد `media/` (المرفقات نفسها!)، `.env`، `.encryption_key` (فقدانه = فقدان كلمات سر البريد وكل النسخ المشفرة — يُخزَّن منفصلًا في خزانة/جهاز ثانٍ)، `var/models/`. + نسخة خارج الخادم (جهاز ثانٍ على الشبكة أو قرص خارجي بجدول).
6. **الوصول عبر WAN** — **قرار مالك** بين خيارين:
   - **VPN (الموصى به لشركة نفطية)**: WireGuard/OpenVPN إلى شبكة الشركة، النظام لا يُنشر علنًا إطلاقًا. أبسط وأكثر أمانًا؛ التكلفة إعداد VPN لكل مستخدم خارجي.
   - **HTTPS علني**: يتطلب نطاقًا وشهادة وتقييد fail2ban ومراجعة أمنية أعمق (rate limits موجودة أصلًا على الدخول والنقاط الحساسة).
7. **سد ثغرات مقيسة الآن**: `network_ping` مفتوح بلا مصادقة ويكشف الدور والإصدار وعدد الجلسات (`core/network_views.py:247-263`) — يُقيَّد أو يُقلَّص؛ آلية master/slave التي تكتب `.env` (`network_views.py:304-311`) تصبح تراثية في وضع الخادم المركزي (الكل يوجَّه للخادم؛ تبقى لوضع القسم الواحد الموزَّع إن لزم).
8. **مراقبة**: `db_healthcheck` موجود؛ يضاف endpoint صحّي مصادَق + تنبيه بريدي عند فشل النسخ الاحتياطي أو تراكم `ErrorLog`.

**بوابة القبول:** وصول من جهازين على LAN عبر HTTPS، انقطاع Redis لا يُسقط النظام (مقيس أصلًا بـ `IGNORE_EXCEPTIONS`)، استعادة نسخة احتياطية كاملة (قاعدة + media + مفاتيح) على جهاز نظيف تنجح.

---

### المرحلة أ — الأساس: القسم والعضوية والنطاق (أكبر مرحلة وأخطرها)

**أ.1 — النماذج الجديدة**
- `Department`: الاسم، الرمز (ش13…)، الأب (شجرة اختيارية: هيئة ← قسم ← شعبة)، `is_active`، وربط اختياري `entity` OneToOne بجهة الدليل — هذا الربط هو ما يجعل القسم طرف مراسلة (الإحالات والبريد الداخلي لاحقًا) ويحسم سؤال الأضابير المؤجَّل.
- `UserProfile` OneToOne بالمستخدم: `department` FK + المسمّى الوظيفي + (لاحقًا) صورة التوقيع. (لا يوجد أي Profile اليوم — `UserPassword` هو الامتداد الوحيد.)
- **البذر**: أمر إدارة يُنشئ الأقسام من الجهات المرمَّزة الـ41 (القائمة في §0.1) ويربط كلًّا بجهته، + قسم افتراضي «المتابعة» يُسند إليه كل الموجود.

**أ.2 — نطاق الكتب**
- `Book.department` FK (nullable في الهجرة، backfill = القسم الافتراضي، ثم يُفرض `NOT NULL`).
- **بوابة نطاق واحدة** `scope_books_for(user)` تُستدعى من `BookFilterEngine` ونقاط الـ API — وتستبدل النمط اليدوي المكرَّر `is_superuser or is_staff or created_by==user` في `books_list.py`، `books_api.py`، `attachments.py`، `books_detail.py` (توحيد DRY مستحق أصلًا). القاعدة الجديدة: superuser يرى الكل؛ المستخدم يرى **كتب قسمه** (لا كتبه هو فقط — هذا تغيير سلوك مقصود يطابق واقع عمل الأقسام؛ في وضع `single` النتيجة مطابقة لليوم لأن الجميع في القسم الافتراضي).
- فهارس جديدة تقود بـ `department` (الفهارس الحالية تقود بـ `created_by` — `models.py:420-434`).

**أ.3 — الترقيم لكل قسم** (كله داخل `core/numbering.py` — المصدر الوحيد)
- `BookSequence`: من `kind` unique إلى `unique_together (department, kind)` — هجرة تحوّل العدّادات الحالية (2433/358/455) إلى قسم المتابعة.
- قيد التفرد `uniq_book_our_number_kind` (`models.py:450-457`) يُعاد بناؤه بإضافة `department` — فبإمكان قسمين إصدار «2433» دون تصادم، وشروطه الحالية (استثناء المستورد الورقي والصادر الخارجي) تبقى كما هي.
- `BookNumberReservation`: المفتاح من `(user, kind)` إلى `(user, department, kind)`.
- **قرار مالك:** هل لكل قسم سلسلة ختم وارد خاصة به فعلًا على الورق (المرجَّح — عندئذ عدّاد مستقل لكل قسم يبدأ من رقم ختمه الحقيقي)، أم تُطلب سلسلة موحَّدة؟ لا نخمّن — نقيس من دفاتر الأقسام عند إدخال كل قسم.

**أ.4 — الجهات والوسوم**
- دليل الجهات يبقى **مشتركًا** على مستوى الشركة (قرار صريح: لا نشظّيه) مع حقل `is_internal` يُملأ تلقائيًا للجهات المربوطة بأقسام — يُصلح عرض الأضابير أيضًا.
- `Tag` يُضاف له `department` nullable (وسوم قسم خاصة + وسوم عامة).

**أ.5 — الإعدادات المفردة: فضّ لا هدم**
- تبقى عالمية: `SystemSettings`، `SecuritySettings`، `AIIntegrationSettings`، `NetworkSettings`، `BackupSettings`.
- تصبح «افتراضي عام + تخصيص لكل قسم»: `NotificationSettings` و`EmailSettings` (تفصيلها في المرحلة ج). دالة حلّ واحدة: إعداد القسم إن وُجد وإلا العام — فلا يلزم أي قسم بضبط شيء.

**أ.6 — أوضاع التشغيل**: حقل `deployment_profile` في `SystemSettings` + دالة `is_company_mode()` تتحكم بإظهار عناصر الواجهة القسمية.

**الاختبارات:** المجموعة الحالية (729) يجب أن تبقى خضراء بلا تعديل جوهري (القسم الافتراضي يحفظ السلوك)، + اختبارات جديدة للنطاق (مستخدم قسمٍ لا يرى كتب قسم آخر — بالمنوال العدائي في `tests_security.py`) وللترقيم المتوازي بين قسمين.

**المخاطر:** أثقل هجرات في الخطة (backfill على ~11,183 كتابًا) — تُنفَّذ بنمط هجرة 0060 المجرَّب (dry-run + حرز تراجع)؛ وتغيير سلوك الرؤية من «كتبي» إلى «كتب قسمي» يحتاج إعلامًا صريحًا للمستخدمين.

---

### المرحلة ب — لوحة الإدارة

**التوسيع على بنية مركز الإعدادات القائمة** (`core/views/settings_hub.py` — تبويبات + تضمين `?embed=1`)، لا بناء لوحة من الصفر ولا الاعتماد على Django admin الخام:

1. **تبويب الأقسام**: CRUD + الشجرة + ربط الجهة + تفعيل/تعطيل + عدّادات كل قسم (قراءة من `BookSequence` المنطاق).
2. **تبويب المستخدمين**: الإنشاء الحالي (`create_admin_user` وتدفق `UserPassword` المؤقتة) يترقى لواجهة: إنشاء/تعطيل، إسناد قسم ودور، إعادة تعيين كلمة سر.
3. **`controller` يُبعث من رماده** — «مشرف المتابعة» موجودٌ بالاسم وفارغٌ من
   المضمون، وهو **حرفيّاً** الموظّف الثاني الذي يدير البريد ويطارد الاستلام؛
   فلا دور سادس («موظّف البريد») — ازدواجٌ صافٍ. مصفوفته:
   - **يملك:** رؤية كلّ كتب قسمه (غير السرّيّة) · إدارة الإحالات (إرسال/تأشير
     استلام/إعادة) · وضعَ ورفعَ «عاجل» · تعديل `due_date` والأرشفة · مطالعة
     سجلّات الاستلام والقراءة · الإشراف على وحدة البريد.
   - **لا يملك:** إنشاء أو حذف أو تحرير متن الكتب (عملُ `entry`) · إدارة
     مستخدمين أو إعدادات (`dept_head`/`admin`) · السرّيّ إلّا بمنحٍ صريح.

4. **الأدوار تتوسع وتُنفَذ فعلًا**: تبقى الأربعة (`entry`/`controller`/`viewer`/`admin` في `core/roles.py`) ويضاف **`dept_head` (رئيس قسم)**: صلاحيات admin داخل قسمه فقط (مستخدمو قسمه، عدّاداته، بريده، تقاريره). إصلاحان مقيسان: `role_required` معرَّف وغير مستعمل في أي view (`decorators.py:75-85`) — يُسلَّك على الواجهات الحساسة؛ و`is_staff` يمنح رؤية كل الكتب ضمنًا — تُفصل «staff» الإدارية عن نطاق الرؤية (النطاق من `UserProfile.department` حصرًا).
5. **تبويب سجل التدقيق** (يُبنى في د) و**تبويب البريد** (ج) و**تبويب التدريب** (ح) — اللوحة هي الحاضنة.

---

### المرحلة ج — البريد الداخلي بين الأقسام والإحالات

**المبدأ:** الرسالة الداخلية **صف في القاعدة لا SMTP** — تسليم فوري، صفر اعتماد على خادم بريد، وقابلة للتدقيق الكامل. SMTP يبقى للخارج فقط.

1. **إصلاح فوري (يصلح تنفيذه قبل أي شيء — تسريب قائم):** صندوق الوارد `mail_inbox` يعرض كل `IncomingEmail` لكل مستخدم مسجّل بلا تصفية، والعدّاد كذلك (`core/messaging/views/ui.py:132-161`) — يُقيَّد بالقسم/الدور فورًا في المرحلة أ.
2. **نموذج `InternalMessage`**: مرسل (مستخدم/قسم) ← قسم مستلم (+ مستخدم محدد اختياريًا)، موضوع، نص، مرفقات، **ربط اختياري بكتاب** (أغلب التراسل الداخلي حول كتاب)، خيط للردود.
3. **الإحالة (توجيه كتاب بين الأقسام)** — جوهر عمل شركة حكومية نفطية: نموذج `BookReferral` (كتاب، من قسم، إلى قسم، توجيه/هامش، حالة: مُرسَل/مستلَم/مُعاد/منجَز) + فعل `referral` في `BookHistory` (بنيته الحالية 15 فعلًا تتسع — `models.py:674`). الكتاب المُحال يظهر في وارد القسم المستلم **دون نقل ملكيته** — قسم واحد مالك ونسخ إحالة للباقين.
4. **صناديق القسم**: وارد القسم = إحالات + رسائل داخلية + (لرئيس القسم) بريد SMTP الخاص بحساب قسمه. عدّادات غير المقروء لكل مستخدم لا عدّاد عام.
5. **إيصالات القراءة والاستلام**: `MessageReceipt` لكل (رسالة/إحالة، مستخدم): وقت التسليم، وقت أول قراءة، إقرار استلام صريح عند طلبه — هذا نصف متطلب «تأشير القراءة ولمن» (النصف الآخر على الكتب نفسها في د).
6. **الإشعارات الجماعية**: توجيه `Notification` لكل أعضاء قسم (اليوم `_notify_reply` يستهدف superusers + منشئ الكتاب فقط — `engines/imap.py:278-323`) + بثّ لحظي (المرحلة و).
7. **حساب بريد لكل قسم**: نموذج `EmailAccount` (FK قسم) يعمّم بنية `EmailSettings` — التشفير الشفاف للحقول (`from_db`/`save`) يعمل كما هو مع تعدد الصفوف؛ العمل الفعلي في نقاط الاستدعاء الـ20 المقيسة (smtp.py، imap.py، endpoints، ui، tasks، dashboard، dossiers، notify_overdue_books) لتمرير الحساب بدل `get()` المفرد، وجعل حالة المزامنة (`imap_last_sync`، التبريد، السقف) لكل حساب، وإضافة «عبر أي حساب وصل» على `IncomingEmail`. المفرد الحالي يبقى الحساب الافتراضي — وضع `single` لا يتغير فيه شيء.
9. **طاولة الوارد** — صفحةُ موظّف البريد، **وبوّابةُ قبول المرحلة ج كلّها**:
   إن عجزت الطاولة عن الإجابة «مَن استلم كتاب X ومتى؟» فالمرحلة لم تكتمل.
   - **لوحةُ عدّاداتٍ وطوابير لا جدولُ كتبٍ ثانٍ**: وارد اليوم · غير مستلَم ·
     متأخّر · عاجل · سرّي · إحالات معلّقة. وكلّ عدّادٍ ينقر إلى **القائمة
     الموحّدة نفسها بفلترٍ مسبق** عبر الرابط.
   - **قاعدةٌ حاكمة:** أيّ فلترٍ جديد (عاجل، حالة إحالة) يُضاف في
     `BookFilterEngine` وحده فتستفيد منه الواجهتان — ازدواجُ السطوح يُحسم
     بالتصميم لا بالانضباط.
   - **مصدرُ حقيقةٍ واحد للحالة:** «استُلم/سُلّم» هي حالات `BookReferral`
     و`MessageReceipt` — **ولا يُضاف أيّ حقل حالة تسليمٍ على `Book` نفسه**.
     ذلك هو الفخّ: حقولٌ مرتجلةٌ اليوم تناقضها `BookReferral` بعد شهر، فتُخلَّف
     قاعدةٌ بروايتين للحقيقة الواحدة — ودَينُها تصحيحُ بياناتٍ لا كود.
   - والطاولة تحترم `scope_books_for` تلقائيّاً: **لا استثناء لها من بوّابة
     السرّيّة** التي أُغلقت في المرحلة أ.

8. **إصلاحات جانبية مقيسة**: `retry_failed_emails_task` معرَّفة وغير مجدولة في beat — تُجدول؛ سقوط `api_compose` إلى «أي كتاب» عند غياب book_id (`mail_endpoints.py:108`) يُصحَّح بجعل الربط اختياريًا في `BookEmailLog`.

---

### المرحلة د — التدقيق الشامل: من وصل ومن قرأ ومن طبع، ومن أين

البنية موجودة والسلك مقطوع — الخطة وصلٌ لا بناء:

1. **تفعيل `UserActivityLog`** (`logging_models.py:11` — لا يكتبه أحد اليوم):
   - دخول/خروج/فشل دخول عبر إشارات Django (`user_logged_in`/`user_logged_out`/`user_login_failed` — صفر استخدام حاليًا).
   - **قراءة كتاب**: تسجيل في view التفاصيل + نموذج `BookReadReceipt` (كتاب، مستخدم، أول/آخر قراءة، IP) — يجيب «من قرأ هذا الكتاب؟» من صفحة الكتاب نفسها.
   - **طباعة**: يضاف `PRINT` إلى `ACTION_CHOICES` (غائب) + endpoint خفيف يستدعيه الزر قبل `window.print()` في المواضع الأربعة المقيسة (تقرير الكتاب، صفحة التقارير، تقرير الأضبارة، طباعة المستند عبر العارض) — تسجيل صادق لواقعة **طلب** الطباعة (المتصفح لا يؤكد إتمامها — يُوثَّق هذا القيد بصدق في الواجهة).
   - تصدير CSV، النسخ الاحتياطي، فتح الرابط الموقَّع (اليوم `logger.info` فقط — `attachments.py:56` — يصبح سجل قاعدة مع IP).
2. **«من أين»**: IP + user_agent (حقولهما موجودة في النموذج) + ربط هوية الجهاز — `device_identity.py` يولّد uuid+اسم للجهاز محليًا ولا يصل بأي طلب HTTP اليوم؛ يُرسَل كترويسة من عميل المسح وتُعرض تسمية الجهاز في السجل. عند تشغيل reverse proxy: الاعتماد على `X-Forwarded-For` المضبوط من الوكيل حصرًا.
3. **واجهة السجل** في لوحة الإدارة: تصفية بالمستخدم/القسم/الفعل/المدة/الكتاب، بحث بالـ IP، وتصدير. رئيس القسم يرى سجل قسمه، الـ admin يرى الكل.
4. **الاحتفاظ والحجم**: سياسة احتفاظ (افتراضي سنة، **قرار مالك**) + أرشفة/تشذيب دوري عبر Celery beat + فهرسة بالمستخدم والوقت. الكتابة غير المتزامنة (task) كي لا تبطئ كل طلب.

---

### المرحلة هـ — التواقيع الإلكترونية (على طورين)

**الطور 1 — توقيع مصادقة داخلي (يحقق الاحتياج العملي: من صادق على ماذا ومتى، وإثبات عدم التغيير):**
- `BookSignature`: كتاب/نسخة مرفق، الموقِّع، الوقت، الصفة (رئيس قسم/مدير/مفوَّض)، **بصمة SHA-256 لملف النسخة الموقَّعة**.
- ختم بصري على PDF عبر PyMuPDF (المستخدم أصلًا في `ensure_pdf_bytes`) — اسم الموقِّع وتاريخه ورمز تحقق، كنسخة `AttachmentVersion` جديدة (البنية تدعم سلسلة النسخ و`merge_metadata` JSON للبصمة) + فعل `sign` في `MergeLog` و`BookHistory`.
- صورة توقيع المستخدم في `UserProfile` (اختيارية، برفع مصادَق).
- **رابط تحقق**: على منوال `attachment_sharing` الموقَّع — يفتح صفحة تؤكد صحة البصمة وهوية الموقِّع.
- **حوكمة**: من يحق له التوقيع (دور + قسم)، ومنع تعديل المرفق بعد التوقيع إلا بنسخة جديدة تُبطل ختم القديمة بصريًا.

**الطور 2 — توقيع رقمي معياري (PAdES/X.509)** — **قرار مالك/شركة** لأنه يتوقف على سياسة خارج الكود: مرجع شهادات (داخلي أو حكومي)، عتاد (رموز USB/HSM)، ومتطلبات قانونية عراقية للاعتراف بالتوقيع. البنية من الطور 1 (النسخ + البصمات + السجل) هي الأساس الذي يُركَّب عليه، فلا عمل مُهدَر.

---

### المرحلة و — اللحظي والتراسل الفوري

1. **طبقة القنوات على Redis** في الإنتاج (`CHANNELS_REDIS_URL` — الكود يترقّى تلقائيًا؛ InMemory الحالي لعملية واحدة فقط).
2. **Consumer إشعارات** بمجموعتين: `user_{id}` و`dept_{id}` — يبثّ: إشعار جديد، إحالة/رسالة داخلية واردة، تحديث عدّاد غير المقروء، تنبيه استحقاق. يمرّ بنفس `SameOriginWebsocketValidator` القائم.
3. **الواجهة**: شارة الجرس وعدّادات الوارد تتحدث لحظيًا بلا refresh؛ قائمة الكتب الموحّدة تعرض شريط «كتب جديدة وصلت — حدّث» عند حدث قسمك (لا إعادة رسم قسرية تُربك المُدخِل).
4. **حضور الحجز الحالي** (`ReservationPresenceConsumer`) يبقى كما هو وينضم لنفس الطبقة.
5. SSE لحالة أجهزة الشبكة يبقى كما هو (يعمل).

---

### المرحلة ح — إدارة التدريبات والاستفادة من التصحيحات (داخل المنتج)

**المبدأ المحفوظ:** التدريب نفسه يبقى خارجيًا (Kaggle — صفر GPU على الخادم بالتصميم). ما يدخل المنتج هو **الإدارة**: العدّ والتصدير والاستيراد والقياس والتفعيل. الأركان الأربعة موجودة كسكربتات وتُسلَّك كأوامر إدارة + تبويب في اللوحة:

1. **عدّادات حيّة**: خدمة تعيد ما يحسبه `capture_stats` (الأزواج المؤكدة، بوابة TARGET=300 لإعادة التدريب) + تقارير دقة الحقول من `learning.py::get_learning_report` — تنقصها واجهة فقط.
2. **تصدير حزمة تدريب بزر**: تحويل `scripts/eval/harvest_t24.py` و`package_date_ds.py` إلى أوامر إدارة تنتج ملف zip جاهزًا لكاغل (القصّ بصفر حشو من الصناديق المحفوظة في `additional_data` — نمط `recrop_t24.py`).
3. **استيراد أوزان وتفعيلها**: واجهة رفع تدير أسماء `var/models/` القائمة مع بصمة تحقق ونسخة تراجع تلقائية (`*_backup.onnx`) — موصولة بنموذج `OCRModelVersion` الموجود أصلًا (`activate()` يطفئ الباقي — `models.py:1331`) بدل الاستبدال اليدوي للملفات.
4. **بوابة قبول قبل التفعيل**: أمر إدارة يلفّ `e2e_number.py` على المجموعات المجمَّدة ويقارن بالبوابات المسجَّلة مسبقًا في `docs/EVAL_REGISTRY.md` — **لا تفعيل لأوزان لم تجتز البوابة** (يحفظ انضباط «التسجيل قبل النظر» المعمول به). زر تراجع فوري.
5. **في وضع الشركة**: التصحيحات تتجمع من كل الأقسام في نفس الجداول (بُعد القسم يُضاف للتقارير لا للبيانات) — تسريع طبيعي لحلقة التعلم بحكم تعدد المُدخِلين.

---

## 3) الترتيب الزمني والاعتماديات

```
ز0 التحصين والخادم  ━━━━━━━━━►  (شرط لكل ما بعده)
أ  القسم والنطاق والترقيم  ━━━━━━━━━►  (شرط لـ ب، ج، د، و)
ب  لوحة الإدارة        ┐
د  التدقيق             ├─ متوازيتان بعد أ (د تُغذّي واجهتها في ب)
ج  البريد الداخلي والإحالات ──►  و  اللحظي (يبثّ أحداث ج)
هـ التواقيع  ─ مستقلة بعد أ (تحتاج UserProfile والأدوار)
ح  إدارة التدريبات ─ مستقلة تمامًا (يمكن تقديمها أو تأخيرها بلا أثر)
```

| المرحلة | الحجم | طبيعة الخطر الأكبر |
|---|---|---|
| ز0 | متوسط | تشغيلي (تكوين لا كود) — قابل للتراجع بسهولة |
| أ | **كبير** | هجرات backfill على 11 ألف كتاب + تغيير سلوك الرؤية — يُدار بنمط dry-run/undo المجرَّب في هجرات 0058/0060 |
| ب | متوسط | صلاحيات — تغطى باختبارات عدائية |
| ج | كبير | تعدد حسابات البريد (~15 موضع استدعاء) — يُنفَّذ آخر ج، والبريد الداخلي قبله لا يعتمد عليه |
| د | صغير–متوسط | حجم السجلات — سياسة احتفاظ من اليوم الأول |
| هـ (طور 1) | متوسط | حوكمة لا تقنية |
| و | صغير | البنية قائمة |
| ح | صغير–متوسط | صفر خطر على الإنتاج (قراءة وتقارير، والتفعيل خلف بوابة قياس) |

**مكاسب سريعة يصح اقتطاعها فورًا حتى قبل المراحل:** تقييد صندوق الوارد المشترك (تسريب قائم) · جدولة `retry_failed_emails_task` · مصادقة `network_ping` · وصل إشارات الدخول/الخروج بـ `UserActivityLog`.

---

## 4) قرارات تحتاج حسم المالك قبل التنفيذ

1. **حدود «القسم»**: هل نعتمد الوحدات المرمَّزة الـ41 كأقسام مؤسِّسة، وماذا عن الـ364 جهة الداخلية غير المرمَّزة (الصيانة، الحفر…) — أقسام كاملة أم تتبع هيئاتها؟ وهل الشجرة (هيئة←قسم←شعبة) مطلوبة من اليوم الأول أم قائمة مسطّحة تكفي؟
2. **ترقيم الأقسام الجدد**: لكل قسم دفتر ختم وارد خاص به (يُقاس من دفاتره الورقية عند إدخاله) أم سلسلة موحدة؟
3. **رؤية الكتب**: هل «كل موظفي القسم يرون كل كتب قسمه» مقبولة، أم تلزم طبقة سرية (كتب مقيدة يراها رئيس القسم فقط)؟ حقل `secret_level` موجود في الاستخراج — هل يصبح ناظمًا للرؤية؟
4. **WAN**: VPN (موصى به) أم نشر علني؟ ومن الجهة التي توفر النطاق/الشهادات إن كان علنيًا؟
5. **نظام تشغيل الخادم** وعتاده (يحدد شكل خدمات النظام والنسخ).
6. **التوقيع**: هل الطور 1 (مصادقة داخلية ببصمات) كافٍ للمرحلة الحالية، ومن الأدوار المخوَّلة بالتوقيع؟
7. **الاحتفاظ بسجل التدقيق**: سنة؟ أكثر (متطلب رقابي)؟
8. **ترتيب إدخال الأقسام**: دفعة واحدة أم تدرّج قسمًا فقسمًا (يُرجَّح: المتابعة ثم قسم تجريبي ثانٍ ثم التعميم)؟

---

## 5) خارج النطاق صراحةً (لا نفعله)

- **لا تعدد مستأجرين** (schema لكل قسم / نسخة تطبيق لكل قسم) — شركة واحدة وقاعدة واحدة.
- **لا إعادة بناء** للإطار (Django باقٍ) ولا microservices ولا SaaS متعدد الشركات.
- **لا تدريب نماذج على الخادم** — يبقى خارجيًا خلف بوابة قياس.
- **لا تطبيقات موبايل أصيلة** — الـ PWA القائمة (service worker v75، أصول محلية 100%) هي مسار الموبايل.
- **لا استبدال لهوية الواجهة** — التعميم وظيفي لا إعادة تصميم.

---

## 6) خيط الأمان العابر لكل المراحل

كل مرحلة تلتزم: اختبارات عدائية للنطاق الجديد (منوال `tests_security.py`) · صفر أسرار في الكود أو git · كل جديد خلف `login_required` + دور مناسب · rate limits على كل endpoint جديد · هجرات بـ dry-run وحرز تراجع (النمط المجرَّب في 0058/0060) · وتشغيل `security-review` قبل دمج كل مرحلة.

---

*أُعدّت هذه الخطة بمسح قراءة-فقط (7 محاور متوازية) على الكود والقاعدة الحيّة في 2026-08-24، دون أي تعديل على ملفات المشروع القائمة — احترامًا للعمل الجاري من جلسة أخرى على نفس المستودع.*

---

# 7) سجل العيوب — يُعالَج قبل البدء (المرحلة ز−1)

هذه الملاحظات لم تكن هدف المسح (كان هدفه رسم الخريطة) لكنها ظهرت أثناءه، ثم **أُعيد التحقق من كل بند منها فردياً بقراءة الكود** (كل دليل أدناه ملف:سطر مقروء لا مستنتَج). أوثّقها هنا لا في ملف منفصل لأن أغلبها **يعترض طريق مراحل الخطة نفسها**: بوابة النطاق في المرحلة أ تُبنى فوق كودٍ مكرَّر يجب توحيده أولاً، وتعدد حسابات البريد في المرحلة ج يُبنى فوق نمط كتابةٍ يتجاوز `save()`.

## 7.0 القراءة العميقة: ثلاثة جذور، لا عشرون عيباً

**الجذر الأول — «بُني ولم يوصَل».** نموذج تدقيق كامل بهجرة لا يكتبه أي سطر، ديكوريتر أدوار لا يزيّن أي view، مهمة إعادة إرسال بلا جدول، حقل `is_spam` له مرشِّح في لوحة الأدمن ولا كاتب له. النمط ليس إهمالاً بل أثر بناءٍ استباقي سبق حاجته؛ وخطره أنه **يُقرأ كتغطية**: من يرى `UserActivityLog` في النماذج يظن التدقيق قائماً.

**الجذر الثاني — «قاعدةٌ منسوخة بدل مصدر واحد».** المشروع تعلّم هذا الدرس في الترقيم وحسمه بـ`core/numbering.py` مصدراً وحيداً، لكن قاعدتين أخريين ما تزالان منسوختين يدوياً: **قاعدة الرؤية** (`is_superuser or is_staff or created_by == user` في أربعة ملفات على الأقل) و**قاعدة الحذف الناعم** (`is_deleted=False` مكتوبة يدوياً **73 مرة** في كود الإنتاج، وصفر managers مخصصة في `core/models.py`). كلتاهما تفشل بالصمت عند النسيان: استعلامٌ ينسى الشرط يُظهر بيانات لا يجوز إظهارها.

**الجذر الثالث والأخطر — «سترٌ عرضيٌّ بحجم الاستعمال».** عيوبٌ لا تظهر إطلاقاً بقسمٍ واحد وعشرة مستخدمين، وتظهر يقيناً بشركةٍ وأقسامٍ متعددة: قطيع مزامنة IMAP، علم «مقروء» عام واحد، عدّادات وقيود تفرّد عالمية، ونموّ جداولٍ بلا تشذيب. **هذا بالضبط ما يجعل السجل جزءاً من خطة التعميم لا ملحقاً بها**: التعميم هو الحدث الذي يرفع الستر.

> **تصويب لتوصية سابقة في هذه الخطة:** أُدرجت ضمن «المكاسب السريعة» (§3) جدولةُ `retry_failed_emails_task`. القراءة العميقة تُبطل ذلك: **جدولتها كما هي اليوم تُنتج إرسالاً مكرّراً بلا نهاية** (التفصيل في ح2). لا تُجدوَل إلا بعد إصلاح عدّاد المحاولات. وبقية المكاسب السريعة في §3 تبقى صحيحة.

## 7.1 حرج — يُصلَح قبل فتح النظام لأي قسم إضافي

| # | العيب | الدليل المقيس | لماذا يتفاقم بالتعميم | العلاج |
|---|---|---|---|---|
| **ح1 ✅** | **تسريب صندوق الوارد**: `mail_inbox` يعرض كل `IncomingEmail` لأي مستخدم مسجّل بلا تصفية، وعدّاد غير المقروء يُحسب على النظام كله | `core/messaging/views/ui.py:132-161` (صفر شرط ملكية)، `:153` العدّاد | اليوم قسمٌ واحد يقرأ بريد نفسه؛ غداً كل موظف يقرأ بريد كل الأقسام | تقييد بالقسم/الدور فوراً (قبل ز0)، ثم إيصالات قراءة لكل مستخدم في المرحلة ج |
| **ح2 ✅** | **حلقة إرسال مكرّر بلا نهاية** في مهمة إعادة الإرسال. التوثيق يدّعي «تحاول مرة واحدة فقط لكل سجل (تجنب حلقة لانهائية)» و**الكود لا ينفّذ ذلك**: `send_book_notification` يُنشئ **صفّاً جديداً** لكل محاولة ولا يمسّ حالة الصفّ الأصلي، فيبقى الأصل `failed` أبداً ويُعاد إرساله في كل تشغيل — وكل محاولة فاشلة تُضيف مرشَّحاً جديداً فينمو الحشد | `core/tasks.py:253-293` (لا تحديث لحالة `log`) مقابل `core/messaging/engines/smtp.py:182` (`BookEmailLog.objects.create`) و`:237,244` (يحدّثان **الصفّ الجديد** لا الأصل) | مع Redis وbeat في الخادم المركزي (ز0) تصبح المهمة مجدولة فعلياً لأول مرة — أي أن **التعميم هو ما يُفجّر العيب** | `retry_count` + `last_retry_at` + حالة `abandoned` بعد N محاولات + `retry_of` FK للأصل، ثم الجدولة. ومعه إصلاح `qs.count()` بعد التقطيع (`tasks.py:292`) |
| **ح3 ✅** | **الرسالة تُلصق بكتاب عشوائي**: عند الإرسال بلا `book_id` يسقط الكود إلى أول كتاب في القاعدة | `core/messaging/api/mail_endpoints.py:106-110`؛ الجذر: `BookEmailLog.book` غير قابل للإفراغ وبـCASCADE (`core/models.py:1613`) | سجل بريد ملوَّث عابر للأقسام: كتاب قسمٍ يحمل بريد قسمٍ آخر | `null=True, blank=True` + هجرة + حذف السقوط + رفضٌ صريح عند غياب الكتاب (يلزم أيضاً للبريد الداخلي في ج) |
| **ح4 ✅** | **لا مدير حذف ناعم**: صفر managers مخصصة في `core/models.py`، و`is_deleted=False` مكتوبة يدوياً **73 مرة** في كود الإنتاج | `grep` على `objects = / class .*Manager / get_queryset` في `core/models.py` = صفر نتائج؛ عدّ المواضع = 73 | كل استعلامٍ جديدٍ في المرحلة أ (بوابة النطاق، الإحالات، تقارير القسم) يضيف فرصة نسيانٍ جديدة | `objects` يستثني المحذوف افتراضياً + `all_objects` للاستثناءات — **قبل المرحلة أ** لأن بوابة النطاق تُبنى فوقه، مع اختبار يمنع تسرّب المحذوف |
| **ح5 ✅** | **قاعدة الرؤية منسوخة يدوياً** في أربعة ملفات على الأقل، و`is_staff` يمنح ضمناً رؤية كتب الجميع | `core/views/books_list.py:135,344`؛ `books_api.py:460,521`؛ `attachments.py:177,216,272,360`؛ `books_detail.py:239`؛ `core/roles.py:52` (`is_staff` ⟵ دور entry) | تغيير قاعدة الرؤية إلى «القسم» يتطلب مطاردة كل نسخة؛ ونسخةٌ منسيّة = تسريب عابر للأقسام | `scope_books_for(user)` مصدراً وحيداً (هي أصلاً أساس المرحلة أ) + فصل صفة staff الإدارية عن نطاق البيانات |
| **ح6 ✅** | **كشف معلومات بلا مصادقة**: `network_ping` يعيد الدور واسم الجهاز والإصدار وIP المحلي و**عدد الجلسات النشطة** لأي طارق | `core/network_views.py:247-263` | على LAN شركة (أو WAN) يصبح مسحاً مجانياً لخريطة النشر وحجم الاستعمال | تقليص الحمولة إلى `{lettersys:true}` + توكن مشترك أو تقييد بشبكة الخادم |
| **ح7 ✅** | **ازدواج معيار التشفير**: مفتاح Azure نصّ صريح في القاعدة، بينما كلمتا سرّ البريد مشفَّرتان Fernet في نفس الملف | `core/models.py:1136` مقابل `:1861-1902` | نسخة احتياطية أو وصول قراءة للقاعدة يكشف مفتاحاً مدفوع الثمن | تطبيق نفس نمط `from_db/save` على `azure_key` |
| **ح8 ✅** (جزئيّاً) | **`.encryption_key` نقطة فشل واحدة** والنسخ الاحتياطي يغطي القاعدة فقط (لا `media/` ولا `.env` ولا المفتاح ولا `var/models/`) | `core/backup_service.py`؛ `core/encryption.py:12-28` | فقدانه = فقدان كلمات سرّ البريد وكل النسخ المشفّرة، على مستوى الشركة لا قسم | ضمن **ز0/5** (موثّق أصلاً) — يُرفع هنا لأنه أخطر بند تشغيلي في الخطة |

## 7.2 متوسط — يُصلَح مع المرحلة المرتبطة

| # | العيب | الدليل | العلاج / متى |
|---|---|---|---|
| **م1 ✅** | **قطيع مزامنة IMAP**: البوابة تقرأ `imap_last_sync` الذي لا يُحدَّث إلا **بعد** انتهاء المزامنة، فكل الطلبات المتزامنة أثناء مزامنةٍ جارية تمرّ وتفتح اتصالات متوازية؛ والمزامنة تجري **متزامنة داخل طلب الويب** فتحجز عاملاً لثوانٍ | `core/messaging/views/ui.py:103-128` (البوابة) مقابل `core/messaging/engines/imap.py:501` (التحديث في النهاية) | قفل ذرّي (`cache.add`) + ختم عند **البدء** لا الانتهاء، ونقلها إلى Celery حالما توفّرت Redis (ز0). **قبل المرحلة ج** |
| **م2 ✅** | كتابة تتجاوز `save()`: `ES.objects.filter(singleton=1).update(...)` | `core/messaging/engines/imap.py:501` | يعمل اليوم ويكسر أي منطق حفظ لاحق — وسيكسر تعدد حسابات البريد في ج مباشرة |
| **م3** | **نموّ بلا تشذيب**: `ScanPayload` (حمولة JSON لكل مسحة) و`ExtractionCache` بلا أي كود أو أمر حذف/تشذيب | `core/models.py:1025-1068`؛ لا أمر تشذيب في `core/management/commands/` (جرد كامل) | أمر تشذيب مجدول يحترم «ذهب التدريب» (يُشذَّب المستهلَك/المكرَّر فقط) — **مع ز0**، فالخادم المركزي يجمع مسحات كل الأقسام |
| **م4** | تنزيل الرابط الموقَّع يُسجَّل في اللوغ النصي فقط — ملف يخرج للخارج بلا أثر قابل للاستعلام | `core/views/attachments.py:56` | سجل قاعدة مع IP — **ضمن المرحلة د** |
| **م5** | `IncomingEmail.is_read` علمٌ عام واحد (قراءة أحدهم = قراءة الجميع)، و`is_spam` له مرشِّح في الأدمن ولا كاتب له إطلاقاً | `core/models.py:1991`؛ `core/admin.py:69` | إيصالات قراءة لكل مستخدم (ج/د)؛ وإما وصل تصنيف المزعج أو إسقاط المرشِّح |
| **م6** | `error_rate` ثابت 1.0 دائماً و`average_confidence` صفر دائماً في تقرير التعلّم | `core/extraction/learning.py:40-45` | حفظ الثقة في `ExtractionFeedback` أو إسقاط الحقلين — **مع المرحلة ح**: لا يُعرض مقياسٌ بلا معنى في لوحة التدريبات |
| **م7** | `SECURE_SSL_REDIRECT=True` عند `DEBUG=False` بلا مُنهي TLS = حلقة توجيه لا نهائية | `lettersys/settings.py:327-335` | ضمن **ز0/2** — فخّ نشرٍ جاهز للانفجار يوم التدشين |
| **م8** | كتابة اعتماد قاعدة البيانات في `.env` من طلب ويب | `core/network_views.py:304-311` | تصبح تراثية في الخادم المركزي (ز0)؛ تُقيَّد أو تُعطَّل في وضع `company` |
| **م9** | **قفل الدمج عالميّ**: `RestoreJob.running()` يسمح بمهمة دمجٍ حيّة **واحدة على مستوى النظام كله** — قسمٌ يستورد سجلّه الورقيّ يحجب كل الأقسام | `core/models.py:803-806` | تقييد القفل بالقسم (أو طابور مهام) — **مع المرحلة أ**، وإلاّ صار إدخال كل قسم جديد حدثاً يوقف الشركة |

## 7.3 تنظيف وانجراف توثيق

- **ت1** `role_required` معرَّف ولا يستعمله أي view — `core/decorators.py:75` (يُسلَّك في المرحلة ب).
- **ت2** `UserActivityLoggingMiddleware` و`PerformanceMonitoringMiddleware` غير مسجَّلين في `MIDDLEWARE`، والأول يكتب للّوغ لا للقاعدة — `core/middleware/logging_middleware.py:149-187`، `lettersys/settings.py:82` (يُوصَل في د).
- **ت3** `BookSequence.prefix` موسوم بنفسه «(مهملة)» — `core/models.py:1360` (يُحذف بهجرة مع إعادة بناء العدّاد في أ).
- **ت4 ✅** CSP كانت تسمح بـ`cdn.jsdelivr.net` و`fonts.googleapis/gstatic` رغم قاعدة «صفر CDN» المعمول بها، وبـ`'unsafe-inline'` في `script-src` — `lettersys/settings.py:343-346`. سماحٌ ميت يوسّع سطح الهجوم بلا مقابل.
- **ت5 ✅** `MERGE_ADMIN_EMAILS = ['admin@example.com']` قيمة placeholder حيّة، والملف كله ثوابت غير موصولة بـ`settings` — `core/merge_settings.py:64`.
- **ت6 ✅** انجراف توثيق: `consumers.py`/`routing.py` يقولان «دورمانت افتراضياً» بينما `asgi.py:21-26` يصلهما فعلاً؛ README المراسلة يقول «كل 30 دقيقة» والجدول 10؛ وdocstring مهمة إعادة الإرسال يقول «تُشغَّل كل 30 دقيقة» وهي غير مجدولة أصلاً (ح2).
- **ت7** شذوذ بيانات: جهة «الادارة» حقل `code` فيها عنوان بريد إلكتروني بدل رمز سجل (يُنظَّف مع بذر الأقسام في أ.1).
- **ت8** **فضاءات أسماء عالمية باقية**: `ExtractionStatistics` إحصائية يومية واحدة بلا بُعد قسم (`core/models.py:1002`)، و`Tag.name/slug` و`EmailTemplate.slug` و`SuggestionCategory.key` كلها فريدة على مستوى النظام — قسمان لا يستطيعان امتلاك وسمٍ أو قالبٍ بالاسم نفسه. تُحسم مع بُعد القسم في المرحلة أ (تقاريرُ لكل قسم + تفرّدٌ مُنطَّق).
- **ت9** `network_scan_subnet` يمسح 254 عنواناً بـ64 خيطاً (8–15 ثانية) **من داخل طلب ويب** (`core/network_views.py:152,404`) — staff فقط وبحدّ 3/5د، لكنه في وضع الخادم المركزي بلا معنى أصلاً (لا اكتشاف أقران): يُعطَّل في وضع `company`.

## 7.4 بوابات الاعتماد — ما الذي يمنع ماذا

| الإصلاح | يجب أن يسبق | السبب |
|---|---|---|
| ح1 تقييد الوارد | **فوراً** (قبل ز0) | تسريبٌ قائم اليوم، لا علاقة له بالتعميم |
| ح4 مدير الحذف الناعم · ح5 بوابة الرؤية | **المرحلة أ** | بوابة نطاق القسم تُبنى فوقهما؛ بناؤها فوق كودٍ منسوخ يضاعف النسخ |
| ح2 عدّاد المحاولات · ح3 كتاب اختياري · م1 قفل المزامنة · م2 تجاوز `save()` | **المرحلة ج** | تعدد حسابات البريد والبريد الداخلي يوسّعان هذه المسارات بالضبط |
| ح6 · ح7 · ح8 · م7 · م8 | **المرحلة ز0** | كلها بنود تحصين الخادم نفسه |
| م3 تشذيب النموّ | **ز0 أو مع أول قسم ثانٍ** | الخادم المركزي يجمع مسحات كل الأقسام |
| م4 · ت1 · ت2 | ضمن **د/ب** | هي أصلاً محتوى تينك المرحلتين |
| م9 · ت8 | **المرحلة أ** | افتراضات «نطاق عالمي» تُحسم مع بُعد القسم نفسه |
| ت9 | **المرحلة ز0** | يفقد معناه في الخادم المركزي |
| م6 · ت3 · ت4 · ت5 · ت6 · ت7 | بلا اعتماد | تنظيفٌ يصحّ في أي دفعة |

## 7.5 دفعات التنفيذ المقترحة

- **دفعة ز−1/أ — أمان فوري** ✅ **منجزة 2026-08-26**: ح1 (اتّسع إلى خمسة مواضع + IDOR)، ح6، ح7، ت4، ت5.
  صفر هجرات · **829 اختباراً خضراء** · 21 اختبار انحدارٍ جديد، ثمانيةٌ منها تفشل على الكود القديم.
  التفاصيل ودفتر التنفيذ في [`ROLLOUT_EXECUTION.md`](ROLLOUT_EXECUTION.md).
- **دفعة ز−1/ب — أساسٌ تقني قبل المرحلة أ** ✅ **منجزة 2026-08-26**: ح4، ح5، ح3.
  هجرة واحدة (0061) · **857 اختباراً خضراء** · القاعدة صارت في `core/scoping.py` وحده (27 موضعاً)
  والحذف الناعم افتراضاً في `SoftDeleteManager`. المرحلة أ صارت تبني فوق موضعٍ واحد لا سبعةٍ وعشرين.
- **دفعة ز−1/ج — سلامة البريد** ✅ **منجزة 2026-08-27**: ح2، م1، م2 (وم5 مؤجَّل بقرارٍ إلى المرحلة ج).
  هجرة واحدة (0062) · **877 اختباراً خضراء** · وأربعُ توابعَ كشفتها مراجعةٌ معماريّة، أخطرها
  **فخّ `dumpdata`**: مديرُ الحذف الناعم كان سيُفقد سلّة المحذوفات صامتاً في أوّل نقلٍ إلى PostgreSQL.
- **الباقي** يُحمل داخل مرحلته بحسب جدول 7.4.

**شرط قبولٍ لكل دفعة:** المجموعة الاختبارية الحالية خضراء بلا تعديل جوهري، + اختبار انحدارٍ جديد لكل بند حرج (نموذجه اختبارات `core/tests_security.py` العدائية)، + `security-review` قبل الدمج.

## 7.6 ما لم يُفحص (صدقاً)

هذا السجل ثمرة مسحٍ لرسم الخريطة تلاه تحقّق موجَّه من الادعاءات — **وليس مراجعة جودة مكرَّسة**. لم يُفحص منهجياً: منطق الدوال وحالات الحواف داخل الأنابيب، استعلامات N+1 خارج ما وثّقته الذاكرة سابقاً، تغطية الاختبارات وفجواتها، أداء الواجهة، ولا شيفرة JavaScript. الأنوية الحديثة (`core/numbering.py`، `core/extraction/capture.py`، `core/attachment_service.py`، محرّك التصفية) بدت نظيفة ومصدرها موحّد في كل ما مرّ عليه المسح — وهذا انطباعٌ مُسنَد لا شهادةَ خلوٍّ من العيوب. مراجعة مكرَّسة (`/code-review` على أجزاء بعينها، أو ورشة متعددة الوكلاء) تبقى مستحقّة قبل التدشين.

---

# 8) نسيج المراسلات — التعاميم والإحالات ودورة الحياة

*أُضيف 2026-08-27 بعد استشارتين: معماريّة (فيبل) وإداريّة (خبير مراسلات ناقد). يُدمج في المرحلة ج ولا يستحقّ مرحلةً مستقلّة.*

## 8.0 البصيرة الحاكمة: مستويان لا يُخلطان

> **مستوى التسيير** — مَن عنده الكتاب الآن، هل استلم، هل ردّ: **حالةٌ تتغيّر وتُطارَد** ⟵ موطنها `BookReferral`.
> **مستوى النسيج الوثائقيّ** — هذا الكتاب جوابُ ذاك، وهذا إلحاقٌ بذاك: **حقيقةٌ ثابتة لا حالة لها** ⟵ يلزمها كيانٌ ثانٍ خفيف.

الجدولُ الواحد للدلالتين هو «الجدول الغامض»؛ والفصلُ عند هذا الحدّ يمنع أيضاً «ثلاثة جداول متشابهة» لأنّ لكلٍّ عقداً لا يشاركه فيه الآخر.

**فجوةٌ مقيسة تُفسّر أسئلة المالك الأربعة:** لا توجد اليوم **أيّ** علاقة من كتابٍ إلى كتاب في `core/models.py` — كلّ المفاتيح الأجنبيّة إلى `Book` تأتي من نماذجَ أخرى (مرفقات، تعليقات، تاريخ، مسحات، استخراج، بريد). فالنظام **عاجزٌ بنيويّاً** عن التعبير عن «أوّليّاتها» و«جواب كتابنا المرقّم».

## 8.1 `BookLink` — الجدول الجديد الوحيد

```
from_book (المُشير) · to_book (المُشار إليه)
relation ∈ { reply جواب · followup إلحاق · refers إشارة · amends معطوف · cancels مُلغىً بـ }
note نصّ قصير · created_by · created_at
unique (from_book, to_book, relation) · فهرسان بالاتجاهين
```

بلا حالة وبلا سير عمل — **سجلُّ حقائق**. حذفه لا يُفسد شيئاً، ووجوده يجيب «ما الذي يتّصل بهذا الكتاب؟» من فهرسٍ واحد. ويُجيب وحده عن سؤالَي المالك الثاني والرابع.

**ولا نموذج «معاملة» بترقيمٍ خاصّ**: المعاملة **عرضٌ مشتقّ** (الإغلاق التعدّيّ للروابط)، ولا تُثبَّت في القاعدة إلّا إن أثبت القياسُ بطئاً.

## 8.2 `BookReferral` الموسَّع — أربعة حقول بدل جدولٍ ثالث

```
margin/directive TEXT   ← هامش/توجيه هذه القفزة تحديداً
via_book FK→Book null   ← كتاب الإحالة المرقَّم إن صدر (وقد لا يصدر — تأشيرةٌ فقط)
closed_by_link FK→BookLink null ← الجوابُ الذي أقفل هذا الالتزام
due_date null           ← «أجيبونا خلال أسبوع» لكلّ مستلمٍ على حدة
```

**الهامشُ حقلان لا حقلٌ واحد، ولكلٍّ معناه:**
- `Book.margin` القائم = ما كُتب على **هذا المستند نفسه**.
- `BookReferral.directive` = **توجيهُ كلّ قفزة**. سبعُ قفزاتٍ = سبعةُ هوامشَ مؤرَّخةٍ بأصحابها — وهو ما لا يستطيعه حقلٌ واحدٌ إلّا بالكشط والإلحاق النصّيّ.
- وهامشُ القسم المجيب يُدوَّن على **كتاب جوابه هو**: لا نُدوّن كلامَ غيرنا عندنا، والنسيجُ يصل إليه.

> **قانونُ الديوان:** الهامش **قرارٌ لا ملاحظة**. مَن يجعله `notes` يعجز غداً عن الإجابة: **مَن أمرَ بهذا؟** فليحمل: مَن · متى · بأيّ صفة · لمن وُجِّه · ما المطلوب · وبأيّ مهلة.

## 8.3 التعميم: علمٌ + صفوفُ إحالة — لا نوعَ كتابٍ خامس

- **ليس نوعاً خامساً**: الأنواعُ الأربعة ترمّز الاتجاه، وخامسٌ يكسر الترقيم والفلاتر والعدّادات في كلّ موضع.
- **ولا يُشتقّ من تعدّد الجهات**: الـM2M يقول «إلى مَن» ولا يحمل حالة — وجوهرُ السؤال هو الحالة.
- **القرار**: `Book.is_circular` + **صفُّ `BookReferral` لكلّ قسمٍ مستقبِل** (2 أو 4 أو الـ42).
- **وصفةُ كلّ مُرسَلٍ إليه إلزاميّة**: `للتنفيذ / للإجابة / للعلم`. مكتبُ المدير العامّ يكتبها على الورق، وقسمُ المتابعة يمسك عموداً لكلٍّ.
  > **القراءةُ ليست ردّاً** — والفرقُ بينهما هو سببُ وجود هذا القسم. مَن يبني «إيصال قراءة» ويظنّه متابعةً فقد أخطأ الميزة كلّها.
- **«من ردّ ومن لم يردّ»** = صفوفُ الإحالة التي `closed_by_link IS NULL` **وصفتُها للإجابة**. وصفحةُ التعميم عند صاحبه تعرض **مصفوفة الردود**: كلّ قسمٍ وحالته — مجمّعةً تحت الأصل **بحكم البنية لا باجتهاد العرض**.
- **قوائم توزيعٍ محفوظة** («رؤساء الأقسام» · «الهيئات الأربع») — بدونها يختار الكاتب عشرين قسماً يدويّاً في كلّ تعميم.
- **الربطُ عند تسجيل الردّ**: «أهذا ردٌّ على الكتاب X؟» من قائمة ما ينتظر ردَّك، ويُقفل الالتزامَ عند الحفظ. بدونه تُصان المصفوفةُ يدويّاً فتتعفّن خلال شهر.

## 8.4 دورةُ الحياة: فوق `BookHistory` حصراً

مصدرُ أحداثٍ واحد. تُضاف أفعالٌ إلى الخمسة عشر (`referral_sent/received/returned/closed` · `link_added` · `reply_registered`)، ويُضاف حقلُ `details` JSON يحمل المعرّفات البنيويّة (`referral_id`, `link_id`, `via_book_id`, `attachment_id`) — فيصير الخطُّ الزمنيّ صفّاً غنيّاً (هامشُ القفزة، رابطُ كتاب الإحالة، معاينةُ مرفقه) **بلا جدولٍ جديد**، ويقرأ من مصدرٍ واحدٍ لا من دمج مصادر في العرض.

**و«بأيّ صفحة»** تعني حرفيّاً: رقمُ صفحةٍ اختياريّ على الهامش/التعليق + رابطٌ عميق إلى العارض (`#page=3`). عشرون سطراً وقيمةٌ يوميّة. أمّا وحدةُ السلسلة فتبقى **الكتابَ ومرفقه** — كلّ كتاب إحالةٍ يحمل مسحته.

## 8.5 `@` — إيماءةُ نداءٍ لا طريقةَ تخزين

> **الرقم يصير رابطاً بعد أن تثبّته عينُ إنسان، لأنّ الرقم ليس اسماً فريداً.**

- **الفكرة عمليّةٌ بوصفها منتقياً**: في صفحة القراءة والإدخال صندوقُ «ربط بكتاب»: يُكتب الرقم (بـ`@` أو بدونها) فيظهر بحثٌ فوريٌّ مرتَّبٌ **قسمُ الكتاب الحاليّ أوّلاً**، وكلّ نتيجةٍ **بطاقةُ إزالةِ التباس** (الرقم بعرض `core/numbering.py` + رمز القسم + وسم السنة + العنوان + الجهة) ⟵ يختار ⟵ يُكتب ضلعُ `BookLink` **ويُفتح الكتاب في حواريّة معاينةٍ بزرّ طباعة**.
- **ولم يجد؟** الصندوقُ نفسه **يسقط إلى البحث بالعنوان** — طلبُ المالك الثالث محقَّقٌ في الأداة نفسها.
- **لماذا لا نصٌّ حرٌّ يُفسَّر:** `our_number` **ليس معرِّفاً** — فريدٌ داخل القسم فقط بعد المرحلة أ، والمنقولُ من الورق مستثنى من التفرّد أصلاً (٨٢٥ مرّتين حقيقةً مقيسة)، ورقمُ الجهة يشابك رقمَنا. النصُّ المفسَّر **يخطئ بصمت**؛ والضلعُ المختار بالعين مفتاحٌ أجنبيّ لا يلتبس.
- **«الأرقام روابط»؟** في واجهات النظام: نعم — هي روابط أصلاً لأنّ خلفها مفتاحٌ أجنبيّ. أمّا **داخل نصوص المستندات وOCRها فلا رَبطَ تلقائيّاً أبداً**.
- **السرّيّة**: المنتقي لا يعرض إلّا ما يراه المستخدم عبر `scope_books_for`؛ وضلعٌ قائمٌ نحو كتابٍ محجوب يُصيَّر «كتاب مقيَّد» (رقمٌ بلا محتوى ولا معاينة).

## 8.6 ما لا يُبنى في هذه الجولة

- **لا تفسيرَ تلقائيّاً لنصّ OCR إلى روابط** — أخطرُ البنود إغراءً. (المسموح لاحقاً: مُبرِزٌ وقتَ القراءة يُظلّل ما يشبه الأرقام، والنقرُ يفتح **المنتقي نفسه** معبّأً — اقتراحٌ لا قرار.)
- **لا نوعَ كتابٍ خامس ولا نموذجَ «تعميم» مستقلّ** · **لا نموذجَ «معاملة» بترقيم** · **لا رسمَ شبكةٍ تفاعليّة للعلاقات** (لوحةٌ مسطّحة + خطٌّ زمنيّ تكفيان) · **لا نسخَ مرفقاتٍ عبر السلسلة** · **لا تصعيدَ آليّاً للمتأخّرين** · **لا حواشيَ على مستوى صفحة المرفق**.

## 8.7 ترتيبُ بطن المرحلة ج بعد هذا

1. **ج.0 — `BookLink` + المنتقي + لوحة العلاقات** — *مستقلٌّ تماماً عن البريد الداخليّ، وأصغرُ هجرة، وأسرعُ مكسبٍ لمدير البريد*: حالةُ «إلحاقاً بمذكّرتكم» تعمل من يومها.
2. **ج.1 — `BookReferral` الموسَّع** + تدفّق التعميم + `register_reply` + `is_circular` + أفعال `BookHistory` و`details`.
3. **ج.2 — مصفوفة ردود التعميم** + الخطّ الزمنيّ الموحَّد.
4. **بقيّة ج** (رسائل داخليّة، صناديق، إيصالات).
5. **ج.9 — طاولة الوارد** خاتمةً وبوّابةَ قبول.

---

# 9) نقدُ الاستشاريّ الإداريّ — ما فات وما يُسقَط

*خبيرُ مراسلاتٍ رسميّة، ناقدٌ يكره التعقيد، مطّلعٌ على أنظمةٍ مشابهة. حكمُه على الخطّة: «**هندسيّةٌ ممتازة ووثيقةُ عملٍ رديئة** — صفر صفحةٍ تصف يوم كاتب البريد».*

## 9.1 ما فات — مرتَّباً بالأثر

| # | ما فات | ماذا يوفّر عمليّاً |
|---|---|---|
| 1 | **البحث في نصّ المستند الممسوح** — `apply_search_filters` يبحث في الرقم والعنوان والهامش وأسماء الجهات، **ولا يمسّ `OCRResult` إطلاقاً** (مُتحقَّق) | العثورُ على كتابٍ لا تذكر رقمه ولا عنوانه الدقيق. **تصويبٌ مقيس:** اليوم **22 نصّاً مخزَّناً فقط** — فالقيمة تنمو مع المسح لا فوراً، والبند يبقى صحيحاً ورخيصاً |
| 2 | **اقتراحُ الإشارة المرجعيّة آليّاً** — أنبوبُ الاستخراج يقرأ النصّ أصلاً؛ عائلةُ تعابيرَ واحدة («إشارةً إلى/إلحاقاً بـ/عطفاً على») تقترح `BookLink` **بزرّ تأكيد** | يصرف استثماراً غارقاً على قيمةٍ يوميّة. والمرحلة ح لا تسأل قطّ: **ما الذي يفعله النصُّ الذي نستخرجه أصلاً؟** |
| 3 | **القيدُ السريع والإدخالُ بالدفعة** — الكاتب يستلم 30–50 كتاباً في التاسعة صباحاً | **قيدٌ في ثلاثين ثانية: رقم + تاريخ + جهة + مسح، والباقي لاحقاً.** أيّ حقلٍ إجباريٍّ إضافيّ = دفترٌ ورقيٌّ موازٍ |
| 4 | **باركود على ختم الوارد** | كلُّ مسحٍ لاحقٍ يرتبط بكتابه **بصفر كتابة**، وتتطابق السلسلةُ الورقيّة بالرقميّة |
| 5 | **قوالبُ الصادر** (تنسيب · مباشرة · إجازة · تخويل · تعقيب · إحالة) | **الصادرُ يولد رقميّاً: بلا مسحٍ ولا OCR ولا خطأ قراءة.** الخطّةُ تُنفق مرحلةً على تحسين قراءة الممسوح وصفراً على **تجنّب المسح** |
| 6 | **شاشةُ «ما يخصّني اليوم»** للموظّف العاديّ (محالٌ إليّ ولم أفتحه · مطلوبٌ ردّي · يستحقّ اليوم · متأخّر) | الطابورُ الشخصيّ هو سببُ سكنى الموظّفين في النظام. الخطّةُ تبني طاولةً لموظّف البريد ولا شيءَ للبقيّة |
| 7 | **تذكيرٌ يوميٌّ بالبريد (8 صباحاً)** | يصل مَن **لا يفتح النظام**. ثلاثون سطراً وجدولةٌ في beat — والخطّةُ تبني بدلها لحظيّاً (المرحلة و) يفيد مَن هو محدّقٌ في الشاشة أصلاً: **الترتيب معكوس** |
| 8 | **قوائمُ توزيعٍ محفوظة** | بدونها عشرون اختياراً يدويّاً في كلّ تعميم |
| 9 | **ورقةُ تسليمٍ مطبوعةٌ بحقل توقيع** مولَّدةٌ من الإحالات | **جسرُ السنة الأولى** الذي يجعل موظّف البريد يتخلّى عن دفتره الثاني |
| 10 | **التفويضُ عند الغياب** (رئيسٌ في إجازة ⟵ كلُّ شيءٍ يتوقّف) | حقلان، وأثرٌ تنظيميٌّ كبير؛ وغيابُه أشهرُ أسباب العودة إلى الورق |
| 11 | **مكانُ الحفظ الورقيّ** (مضبارة/رفّ/سنة) — حقلٌ نصّيٌّ واحد | يوفّر رحلةَ بحثٍ في الأرشيف |
| 12 | **طباعةُ دفتر الوارد مطابقاً للسجلّ الرسميّ** | القطعةُ التي يطلبها المدقّق — وبها وحدها يُتقاعَد الدفترُ الورقيّ بلا خصام |
| 13 | **صفةٌ على المرفق** (أصل · مرفق · أوّليّات) — `Attachment` بلا حقل صفةٍ إطلاقاً (مُتحقَّق) | الأصلُ ≠ المرفقُ ≠ الأوّليّات: الأوّليّاتُ كتبٌ **مقيَّدةٌ سابقاً لكلٍّ رقمُه** (⟵ `BookLink`)، والمرفقاتُ أوراقٌ بلا رقم |

## 9.2 ما يُسقَط أو يُبسَّط — **قراراتُ إسقاطٍ تنتظر المالك**

| البند | الحكم |
|---|---|
| **هـ/الطور 2 (PAdES/X.509)** | **يُشطب من الوثيقة** — يتوقّف على مرجع شهاداتٍ لا وجود له عمليّاً. بل حتى الطور 1 مبالَغ: **سجلُّ توقيعٍ + بصمة + سطرٌ في قالب الطباعة** يكفي |
| **المرحلة و (اللحظي)** | **تُقلَّص إلى الصفر** — استطلاعٌ كلّ 60 ثانية يعطي 90% من الأثر بـ2% من الكلفة وصفرِ صنفٍ جديدٍ من الأخطاء. أنفِق الموفَّر على التذكير البريديّ (9.1/7) |
| **`EmailAccount` لكلّ قسم** | **يُخرَج من ج لا يؤجَّل داخلها** — 20 موضعَ استدعاءٍ مقابل منفعةٍ لن تُستعمل في السنة الأولى. حسابٌ واحدٌ باسمِ مُرسِلٍ و`reply-to` لكلّ قسمٍ يكفي |
| **المرحلة ح (لوحة التدريبات)** | **صفر واجهة** — تبويبٌ كامل لمهمّةٍ يؤدّيها شخصٌ واحدٌ بضع مرّاتٍ سنويّاً. أوامرُ إدارةٍ وتقريرٌ نصّيّ |
| **تسجيل الطباعة (د)** | يبقى سجلُّ **التصدير** و**فتح الرابط الموقَّع** (ملفٌّ يخرج فعلاً)، **وتُسقَط نقرةُ الطباعة** — يقينٌ زائف باعتراف الخطّة نفسها |
| **هويّةُ الجهاز في التدقيق** | **تُسقَط** — ترويسةٌ يتحكّم بها العميل = طمأنينةٌ كاذبة. IP + المستخدم يكفيان على LAN |
| **`network_scan_subnet` وmaster/slave** | **تُحذف لا تُقيَّد** — كودٌ ميّتٌ بقدرةِ الكتابة في `.env` عبر HTTP التزامٌ لا أصل |
| **وضعا `single`/`company`** | أبقِ الحقل، **وتوقّف عن إضافة شرطيّاتٍ عليه**: الميزات مفعّلةٌ للجميع، والقسمُ الافتراضيّ يجعلها غير مرئيّةٍ تلقائيّاً |
| **شجرةُ الأقسام** | المفتاح `parent` مشحونٌ وكافٍ. **لا واجهةَ شجرةٍ ولا وراثةَ رؤيةٍ صعوداً** حتى تُطلب — هناك ينفجر التعقيد |
| **الأدوار** | خمسةٌ × نطاقُ قسمٍ × طبقةُ سرّيّة = ثلاثةُ أبعادٍ متفاعلة. **لا بُعدَ رابع ولا دورَ سادس** |

## 9.3 قوانينُ الديوان — وأين يُخطئ المُرقمِنون

1. **الوارد يمرّ بثلاث محطّاتٍ لا واحدة:** (أ) الختمُ والقيد — الكاتب · (ب) العرضُ على المدير الذي **يؤشّر** — التأشيرة · (ج) التنفيذُ في الشعبة. والمُرقمِنون يدمجون (أ) و(ب) لأنّهما في البرمجيّات «إنشاءُ سجلّ» — **وهما شخصان مختلفان في وقتين وشاشتين**.
2. **الهامشُ قرارٌ لا ملاحظة** (تفصيلُه في 8.2).
3. **الأصلُ واحدٌ لا يتكرّر**؛ ما يتكرّر «نسخةٌ منه إلى». والمُرقمِنون ينسخون السجلَّ لكلّ قسمٍ فينتهون بسبعة صفوفٍ كلٌّ يدّعي أنّه الكتاب. **الخطّةُ أصابت هنا** — تُرفَع إلى قانونٍ مكتوبٍ باختبارٍ يحرسه.
4. **الأصل ≠ المرفق ≠ الأوّليّات** (انظر 9.1/13).
5. **مسؤوليّةُ الردّ في التعميم**: «للتنفيذ» لبعضهم و«للعلم» للبقيّة (انظر 8.3).
6. **التعقيبُ كتابٌ مُقيَّد** لا إشعار — إن ولّده النظام فليُصدِر له رقماً ويربطه بالأصل.
7. **رقمُ الصادر يُؤخذ لحظةَ التوقيع لا لحظةَ الكتابة** (الحجزُ موجود — جيّد)، و**لا فجوةَ في الدفتر بلا تفسيرٍ مكتوب**: فليكن إلغاءُ الحجز مسجَّلاً ظاهراً — المدقّقُ سيسأل.

## 9.4 الأثر التنظيميّ — ما سيُرفَض

- **«كلُّ موظّفي القسم يرون كلَّ كتب قسمه»** — رئيسُ القسم سيقرؤها: «سكرتيري صار يقرأ بريدي». والاستجابةُ إمّا وسمُ كلِّ شيءٍ سرّيّاً، أو — وهو القاتل — **ألّا تُدخَل الحسّاسات أصلاً**؛ وسجلٌّ بثقوبٍ لا قيمة له.
  **التخفيفُ المقترح:** الواردُ الجديد مرئيٌّ للرئيس والكاتب فقط **حتى يُؤشَّر**، ثمّ للقسم. يطابق الورقَ حرفيّاً ويُبطل الاعتراض. ⟵ **قرارُ مالكٍ مطلوب.**
- **التدقيقُ الشامل** سيُقرأ مراقبةً. **التخفيف:** سجلُّ القراءة مرئيٌّ **للمُرسِل في سياق «هل استلم قسمُ س كتابي؟»** وللمدير عند حادثةٍ أمنيّة — **لا شاشةً تصفُّحيّةً تقول «ماذا فعل أحمد اليوم»**. الخطّةُ تصمّم اليوم الشكلَ الثاني.
- **الحقولُ الإجباريّة** ⟵ بياناتٌ زبالة («العنوان: كتاب»).
- **السرّيّ على الورق سجلٌّ منفصلٌ وخزانةٌ منفصلةٌ وكاتبٌ منفصل** — أيلزم سلسلةُ ترقيمٍ خاصّة؟ ⟵ **قرارُ مالكٍ مطلوب.**

**الحدُّ الأدنى للتبنّي (ما دونه يفوز الدفتر):** ① قيدٌ في 30 ثانية مع مسح · ② بحثٌ يجد أيَّ كتابٍ برقمه أو موضوعه من أيّ شاشةٍ في ثانية · ③ المستندُ يُفتح ويُطبع بنقرة · ④ دفترُ الوارد يُطبع مطابقاً للسجلّ · ⑤ الإحالةُ تُنتج ورقةَ تسليمٍ للتوقيع · ⑥ شاشةُ «ما عليّ اليوم». وكلُّ ما عداه — لحظيٌّ وتواقيعُ وبصماتُ أجهزةٍ وبريدٌ لكلّ قسم — **فوق الحدّ**.

## 9.5 القياس: ستّةُ مؤشّراتٍ تُثبت النجاح لا البناء

| المؤشّر | ما يُثبته | الهدف |
|---|---|---|
| **زمن الختم إلى القيد** (وسيط `created_at − date`) | سرعةُ نموذج الإدخال؛ ارتفاعُه = الكاتبُ يُجمِّع آخر النهار | نفس اليوم لـ>90% |
| **نسبةُ الظلّ** (عدّاد الختم الورقيّ − كتب النظام أسبوعيّاً) | الدفترُ الموازي | <5% |
| **زمنُ دورة الإحالة** (الاستلام − الإرسال · تسجيل الردّ − الإرسال) | الرقمُ الذي وُجد قسمُ المتابعة ليُنقصه | يُقاس أوّلاً ثمّ يُستهدَف |
| **نسبةُ الاستجابة للتعاميم** (ردَّ ÷ المُلزَمون عند المهلة) | الرسمُ الوحيد الذي يُعرض على المدير العامّ | — |
| **نجاحُ البحث** (بحثٌ يليه فتحُ كتابٍ خلال 30 ثانية) | قابليّةُ استعمال البحث | تصاعديّ |
| **الطباعةُ لكلّ كتاب** | التقاعدُ الفعليّ للورق؛ ثباتُه عند 1.0 = لم يتغيّر شيء | تنازليّ |

⚠️ **فخٌّ مقيسٌ سلفاً:** `created_at` للصفوف المستوردة = زمنُ الاستيراد لا زمنُ القيد — فالمؤشّر الأوّل يُحسب على الصفوف المُصدَرة داخل التطبيق حصراً.
**ومؤشّراتٌ تُرفَض:** «عدد الكتب» و«عدد المستخدمين» (يصعدان بلا تحسّن)، **ولا تُرفع دقّةُ الاستخراج إلى لوحة الإدارة** — عرضُها يُغري بضبط الرقم بدل ضبط النظام.

## 9.6 التوصيةُ الواحدة قبل أيّ سطرٍ من المرحلة ب

> اجلس صباحاً كاملاً بجانب كاتب البريد، واكتب صفحةً واحدة: **«رحلةُ كتابٍ واحدٍ من غرفة البريد إلى الأضبارة»**. كلُّ نقصٍ في 9.1 كانت تلك الصفحةُ ستكشفه.

ثمّ اشحن — **قبل لوحة الإدارة وقبل التدقيق** — الثلاثةَ الرخيصةَ عاليةَ الأثر: **حواريّة الإشارة المرجعيّة · `BookLink` · البحث في نصّ الـOCR**. أيّامٌ لا مراحل، وهي وجعُ المالك اليوميّ.

---

# 10) رحلةُ كتابٍ واحد — شهادةُ موظّف البريد

*جلسةٌ مع موظّف البريد في قسم المتابعة، 2026-08-27. أوصى بها الاستشاريّ الإداريّ قائلاً إنّ «كلّ نقصٍ في §9 كانت هذه الصفحةُ ستكشفه» — وقد كشفت أكثر: **نقضت افتراضاً جوهريّاً في §8**.*

> **قاعدةُ هذه الوثيقة:** ما دون هنا **شهادةٌ لا تصميم**. حين يتعارض التصميمُ معها، الشهادةُ هي الحَكَم.

## 10.1 المحطّات — بلفظه

> «نمسك الكتاب، نذهب به **للمدير**، ويكتب إمّا **هامشاً بالاطّلاع والحفظ**، أو **إجابةً مباشرةً بالهامش**، أو **توجيهاً للوحدات بأوامر مداولة**، أو **إعدادَ مذكّراتٍ حسب اختصاص كلّ وحدة** ليوقّعها هو لاحقاً كإجاباتٍ على ما ورد.»

فمصائرُ الكتاب أربعةٌ لا واحد: **حفظ · إجابةٌ بالهامش · مداولةٌ داخليّة · مذكّراتٌ صادرة**.

## 10.2 الترتيب — وينقض افتراضنا

> «الكتاب يُحوَّل إلى وحداتنا داخليّاً **بهامشٍ فقط**، ثمّ يأتي لنا **نختمه ونضع عليه رقم الوارد**، ثمّ **نفرّقه للوحدات حسب الاختصاص**. وبعده وللأقسام الخارجيّة يتمّ **بإعداد مذكّراتٍ جديدة تكون بروفايلاً للكتاب الوارد**، مشارٍ إلى الكتب المرفقة — **وهكذا كستاك تراكميّ**.»

**ما ينقضه:** كنّا نفترض في §8 أنّ كلّ قفزةٍ في السلسلة تُنتج **كتاباً جديداً برقم** («الأصلُ واحدٌ والملحقات ستّة»). الحقيقة:

| | داخل القسم | إلى قسمٍ خارجيّ عنّا |
|---|---|---|
| ما يُنتَج | **هامشٌ على الورقة نفسها** — لا كتابَ ولا رقم | **مذكّرةٌ جديدة (بروفايل)** برقمها، تشير إلى ما قبلها |
| التراكم | هوامشُ متتالية على البروفايل | «ستاك تراكميّ» من المذكّرات |

**وترتيبُ الترقيم مقلوبٌ عمّا يُظنّ:** الهامشُ **قبل** الرقم — المديرُ يؤشّر، ثمّ يُختَم ويُرقَّم، ثمّ يُفرَّق. (يطابق «المحطّات الثلاث» التي وصفها الاستشاريّ في §9.3/1.)

## 10.3 الهامش

> «يكتبه **مدير القسم**. أحياناً نحتاج **النصّ** وأحياناً **المضمون** يكفي. ويكون على **أصل الورقة التي تأتينا — البروفايل**، أو صفحته الثانية أو الثالثة حسب طول المذكّرة، لكن **99% على البروفايل**.»

⟵ «بأيّ صفحة» سؤالٌ حقيقيٌّ لكنّ جوابه **الصفحة الأولى في 99%** — فحقلُ رقم صفحةٍ اختياريٌّ يكفي، ولا تُبنى بنيةٌ لأجل الواحد بالمئة.

## 10.4 دفترُ التوقيع — ثلاثةُ أحداثِ عهدة

> «داخليّاً عندنا **دفترٌ خاصّ يوقّع فيه الوحدات على الاستلام**، و**موظّف الأرشفة على تمام الأرشفة والحفظ**. والخارجيّ يستلمه منّا **متعهّد البريد المركزيّ بتوقيعٍ أيضاً**.»

ثلاثةُ توقيعاتٍ حقيقيّة: **استلامُ الوحدة · تمامُ الأرشفة · تسليمُ متعهّد البريد**. وهذا ما يجب أن تُخرجه «ورقةُ التسليم المطبوعة» (§9.1/9).

## 10.5 التعميم والمتابعة

> «نعتمد على **السجلّ والمراجعة في الدفتر** إضافةً إلى المذكّرة الأصليّة.»
> «نعم، يُكتب **للتنفيذ** على بعضها و**للعلم** على بعضها، **ويقرّر ذلك مدير القسم** للشعب والوحدات.»
> «نطالب المتأخّر حاليّاً **بمذكّرةٍ جديدة اسمها تأكيد** — **ونريد تحويل الموضوع إلى تنبيهٍ إلكترونيّ**.»

⟵ يؤكّد §8.3 (صفةُ كلّ مُرسَلٍ إليه إلزاميّة، ويقرّرها المدير)، ويُعطي «التأكيد» اسمَه الحقيقيّ في العمل.

## 10.6 كيف يجد الأوّليّات اليوم

> «أجده **في السجلّ أوّلاً**، ثمّ أرجع **لأضبارته المقسّمة حسب صادرٍ أو وارد، ثمّ تقسيمٌ ثانٍ**: أوامر إداريّة، كتب شكر، مذكّرات… إلخ.»

⟵ هذا **بالضبط** ما تفعله ميزةُ الأضابير القائمة (تقسيمٌ بالاتجاه ثمّ بنوع المستند). فالبنيةُ الرقميّة تطابق عادتَه الورقيّة — ولا تُغيَّر.

## 10.7 السرّي

> «السرّي يُحفظ في **السجلّ عاديّ** — لكن فقط **مسؤول إدارة البريد والأرشفة** يحقّ لهم الاطّلاع، **أو تفويضُ موظّفٍ معيّنٍ للاطّلاع**.»

**وقرارُ المالك (2026-08-27):** التمييزُ **بالنصّ فقط ولا سلسلةَ ترقيمٍ خاصّة**، والمُدخِلُ العاديّ يرى **الرقمَ والتاريخَ فقط — بلا عنوانٍ وبلا جهةٍ وبلا صورة**.

⟵ **هذا يصحّح ما شُحن في المرحلة أ**: كنّا نُخفي الصفَّ كلَّه عمّن ليس مُنشئه أو رئيسَ قسمه. الصوابُ **حجبُ محتوىً لا حجبُ صفّ**، وصلاحيّةٌ **تُفوَّض لموظّفٍ بعينه** لا تُشتقّ من رئاسة القسم.

## 10.8 شرطُ التخلّي عن الدفتر — بلفظه

> «إذا كانت **المهامّ والمسؤوليّات واضحةً ومقسّمةً على اليوزريّة** وظاهرةً لي **كلُّ تفاصيل الاستلام وبعهدة مَن**: مَن استلم، ومَن أكّد، ومَن أعدّ… لاستغنينا عنه. **المهمّ الشفافيّة: لا يضيع مستندٌ أبداً ولا تفاصيله.**»

⟵ **«بعهدة مَن» هي الميزةُ الحاكمة** — لا «طاولة الوارد» ولا العدّادات. وهي أيضاً مقياسُ النجاح: مستندٌ واحدٌ يضيع يُبطل الثقة كلَّها.

## 10.9 دفترُ الوارد المطبوع — أعمدتُه كما طلبها

**الرقم · التاريخ · الموضوع · لمن فُرِّق حسب الاختصاص · مَن استلمه في الوحدة المختصّة.**

⟵ العمودان الأخيران **لا وجودَ لهما في النموذج اليوم** — وهما مخرجُ سجلّ العهدة (10.4).

## 10.10 ما يضيّع وقته

بطءُ الإدخال اليدويّ · ومتابعةُ حالة الكتب والإجابات: «الوحدات تتأخّر بإنجاز مهمّتها، وكذلك الأقسام الخارجيّة — أتابع حالة بريدي عندهم وأتابع معهم تنبيهاتٍ لتحصيل إجابةٍ أو تأكيد».
