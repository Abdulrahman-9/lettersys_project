# خطّة LetterSys v2 — تعميمُ النظام على الشركة (إعادةُ كتابةٍ كاملة)

**التاريخ:** 2026-08-31 · **الفرع:** `rollout-b3` (مدموجٌ فيه `main` حتى `0bec617`) · **الاختبارات:** 1432 خضراء · **الهجرات:** حتى 0076

> **عند الموافقة** يُكتب هذا الملفُّ في المستودع باسم `COMPANY_ROLLOUT_PLAN_v2_2026-08-31.md` بجذر `lettersys_rollout`، وتُحفظ الخطّةُ القديمة كما هي مرجعاً تاريخيّاً. لا كودَ يُلمَس في هذه الخطوة.

---

## 0) السياق — لماذا تُعاد كتابةُ الخطّة

الخطّةُ القديمة (`COMPANY_ROLLOUT_PLAN_2026-08-24.md`، 1286 سطراً) كُتبت **قبل** التنفيذ، وتراكمت عليها ستُّ عشرة إضافةً (§7–§16) تُصحّح بعضُها بعضاً. وأثناء التنفيذ نُقضت **ثلاثةُ أوصافٍ فيها بالقياس** (§2‑ز أدناه)، وبُنيت أشياءُ لم تكن فيها (لوحاتُ الأدوار · قصاصةُ الهامش · التبويبُ اليدويّ · شجرةُ الأبوّة)، وسقطت أشياءُ كانت فيها (الهياكلُ الموسومة). فالخطّةُ القديمة لم تعد تصف النظامَ الذي يعمل.

هذه الخطّةُ تصف **ما هو موجودٌ ومقيس** أوّلاً، ثمّ ما بقي — بالصفحة والدور والجهة والحواريّة والانتقال. والمعيارُ الذي طلبه المالك: **أفضلُ واجهات، وأفضلُ عمل، وكودٌ نظيف، وتحسينُ التقنيات** — فلها مساران مستقلّان (§9 و§10) لا يُختزلان في الميزات.

---

## 1) مراجعةُ الإنجاز — مقيسٌ لا مُدَّعى

### 1.1 المراحل

| المرحلة | الحال | الدليل |
|---|---|---|
| الدفعات 1–3 (أمان · أساس · بريد) | ✅ | 13 من 20 عيباً في §7 مُغلق |
| ز0 تحصين الخادم | ✅ كود / ⏳ تشغيل | النسخُ يعمل على `D:` (**كان معطَّلاً**) · باقٍ: نقلُ `.encryption_key` ونسخةٌ خارج الخادم |
| أ بُعد القسم | ✅ | 0063–0066 · 40 قسماً مبذوراً · شجرةٌ 6 روابط |
| ب لوحة الإدارة | ✅ | `/books/admin/` (أقسام · مستخدمون · إحالةٌ للعناقيد) |
| ج بطن التسيير | ✅ | 0067–0071 · البنود ①②③④⑥ |
| د التدقيق | ✅ | 0072 · `/books/audit/` |
| هـ التواقيع | ✅ طور 1 / 🛑 طور 2 قرارُ شركة | 0075–0076 · `/books/verify/<token>/` |
| و اللحظيّ | 🛑 موقوفةٌ على Redis | الكودُ يترقّى تلقائيّاً بـ`CHANNELS_REDIS_URL` — لا Redis في الإنتاج |
| ح إدارة التدريبات | 🛑 منطقةٌ محظورة | بنودُها في `core/extraction/**` و`scripts/eval/**` |

### 1.2 الشاشاتُ الإحدى عشرة (§15.4 القديمة): **11/11** · الألواحُ الستّة: **6/6** · وزيادةً عليها: لوحاتُ الأدوار · ورشةُ العناقيد · التبويبُ اليدويّ · قصاصةُ الهامش · صفحةُ التحقّق.

### 1.3 القاعدةُ الحيّة الآن

| | |
|---|---|
| مستخدمون نشطون | 12 (كلُّهم في «قسم المتابعة») |
| أقسام | 40 (6 منها لها أب) |
| جهات نشطة | 411 — خارجيّة 269 · أقسامُ الشركة 34 · شعبٌ ووحدات 108 |
| عناقيد | 3 |
| كتب | 13,239 · إحالات 2 · عهد 0 · روابط 0 · تواقيع 0 |

**القراءة:** البنيةُ كاملةٌ والاستعمالُ لم يبدأ. الأرقامُ الصفريّة في التسيير ليست عيباً — هي أنّ القسمَ الثاني لم يدخل بعد.

### 1.4 الفرعان

`rollout-b3` = `main` + 49 إيداعاً (الدمجُ الأوّل تمّ: `main ⟵ rollout`). **الخطوةُ الثانية** (`rollout ⟵ main`) تنتظر قرارَ المالك. والشجرةُ الأخرى `lettersys_project` نظيفةٌ الآن (`lettersys.dump` فقط غيرُ متتبَّع).

---

## 2) سجلُّ الأخطاء أثناء التنفيذ — 24 عيباً بجذورها

كلُّ عيبٍ هنا **أُصلح وحُرِس** (المرجع: `IMPLEMENTATION_NOTES.md`). ما يهمّ الخطّةَ هو **الجذر**، لأنّ الجذرَ يتكرّر والعيبَ لا.

### أ) نسخٌ متعدّدةٌ لقاعدةٍ واحدة — **8 مرّات**
قاعدةُ الرؤية وُجدت في **سبعة** مواضع خاصّة (القوائم · التفاصيل · الأضابير · اللوحة · المرفقات · البريد · الاستخراج)، والمصنِّفُ في نسختين (بايثون/استعلام) انفرجتا على «خشم الاحمر/انجانة».
**الجذر:** لا مصدرَ وحيدٌ وقتَ الكتابة.
**الحارسُ البنيويّ الآن:** `tests_scope_parity` (5 مستخدمين × 7 كتب) يقارن `can_view_book` بـ`scope_books_for` — وأيُّ شقٍّ يُضاف لأحدهما دون الآخر يسقط. **قاعدةُ الخطّة:** لا قاعدةَ صلاحيّةٍ تُكتب خارج `core/scoping.py`، ولا شرطَ دورٍ في قالب.

### ب) «مبنيٌّ ولا أحد يوصله» — **5 مرّات**
البريد · النسخُ الاحتياطيّ (`pg_dump` ليس في PATH) · قصاصةُ الهامش (حقلٌ وخدمةٌ بلا API ولا واجهة) · منتقي الربط (نقطتان بلا حواريّة) · قارئُ التاريخ.
**الجذر:** الطبقةُ الخلفيّة تُختبر وحدها وتخضرّ، والواجهةُ لا تستدعيها.
**قاعدةُ الخطّة:** «منجَز» = طرفٌ إلى طرفٍ في المتصفّح بلقطةٍ تُقرأ، لا خدمةٌ خضراء.

### ج) ما تكشفه العينُ لا الاختبار — **6 مرّات**
تبويباتٌ في الـDOM مخفيّةٌ بـ`display:none` تقدّميّ · تسطيرُ روابط · تزاحمُ ترويسة · عمودٌ ضيّق · عيبان في الورقتين المطبوعتين · ثلاثةٌ في لوحة دورة الحياة.
**قاعدةُ الخطّة:** كلُّ تغييرٍ مرئيٍّ يُتحقَّق بسكربت Playwright خارج السياق + **لقطةٌ واحدةٌ تُقرأ**. و`networkidle` لا يتحقّق في هذا التطبيق (نبضٌ دوريّ) — يُنتظر العنصر.

### د) اختباراتٌ تخدع نفسَها — **3 مرّات**
حارسُ السرّيّة جعل القارئَ صاحبَ حقّ · كتابٌ مذكورٌ مملوكٌ لقسم الغريب · مكلَّفٌ لا يرى الكتاب.
**قاعدةُ الخطّة:** كلُّ حارسٍ يُثبَت أنّه **يُخفق على الكود القديم** (طفرة) قبل قبوله — وهو ما طُبّق على 20+ حارساً هذه الجلسة.

### هـ) `except` واسعٌ يبتلع — **2**
`BookHistory(user=)` والحقلُ `by` · سقوطٌ احتياطيٌّ في فرعٍ لا يُبلَغ (WebP يرمي لا يُعيد None).
**قاعدةُ الخطّة:** لا `except Exception` حول كتابةٍ هي جزءٌ من الأثر.

### و) البيئة — **5**
قرصُ النظام بَتَرَ `IMPLEMENTATION_NOTES.md` إلى صفر (`open('w')` يقصّ قبل الكتابة) · 6 خوادمَ زومبي من **الشجرة الخطأ** · `MemoryError` في التقاط المخرَج لا في الاختبارات · `--noreload` لا يرى ملفّاتٍ جديدة · PyMuPDF بلا WebP.
**قاعدةُ الخطّة:** الإلحاقُ أو الكتابةُ الذرّيّة للوثائق · خادمٌ واحدٌ من `lettersys_rollout` · المخرَجُ الطويل إلى ملفّ.

### ز) أخطاءُ الخطّة القديمة نفسِها — **3 مقيسة**
| قالت | المقيس |
|---|---|
| «أربعُ جهاتٍ وهميّة» | **سبع** (+ واحدةٌ حقيقيّة اصطادها البحث) |
| «13 ألف كتابٍ تشير إليها» | **37** (الرقمُ أُخذ من إجماليّ القاعدة) |
| «صيَغٌ لعنوانٍ واحد» | **ثلاثةُ عناوين** — والدمجُ عبرها كان سيصهر مخاطَبين |

وأيضاً: اشتقاقُ الأبوّة من الرموز يُنتج هراءً («مجلس الادارة» تحت مكتب المدير)، و«و.*» تعني **وحدة** لا واسط.

### ح) الدمجُ الصامت — **1**
`urls.py` اندمج بلا تعارضٍ فصار فيه مسارٌ مكرّرٌ ثلاثاً، وجانغو يأخذ الأوّل — **الهياكلُ تحجب التنفيذ** بلا إخفاقِ اختبار.
**قاعدةُ الخطّة:** بعد كلّ دمجٍ: `grep` لتكرار المسارات + تشغيلُ المجموعة + لقطةٌ للصفحات المتداخلة.

---

## 3) الأدوارُ والصلاحيّات — المصفوفةُ الكاملة

### 3.1 الأدوارُ الخمسة (`core/roles.py`) وكيف تُحدَّد
| الدور | التحديد | المعنى في الشركة |
|---|---|---|
| `admin` | `is_superuser` | مديرُ النظام |
| `dept_head` | `UserProfile.is_department_head` | رئيسُ القسم/الشعبة/الوحدة **في قسمه** |
| `controller` | مجموعة «مشرف المتابعة» | **مسؤولُ البريد والأرشفة** |
| `entry` | مجموعة «مدخل الكتب» | مُدخِلُ الكتب |
| `viewer` | الافتراض | موظّفُ الشعبة/الوحدة |

**قرارٌ قائم:** `is_staff` لم تعد تُوسّع الرؤية (المرحلة أ/4). **وبقيّةٌ من الماضي:** الشريطُ الجانبيّ يُخفي 5 روابط بـ`is_staff` (سلّة المهملات · الجهات · المستخدمون · النسخ · الإعدادات) — وهي حراسةُ **عرضٍ** لا صلاحيّة؛ الصفحاتُ نفسُها محروسةٌ بـ`staff_required`. تُوحَّد في §9.

### 3.2 البوّابات (`core/scoping.py`) — المصدرُ الوحيد
| البوّابة | تُجيب عن |
|---|---|
| `scope_books_for(user)` | **الكتبُ المرئيّة** (استعلام): قسمي وشُعبي ⊕ ما أنشأتُه ⊕ ما فُرِّق إليّ ⊕ **ما ذُكر فيه اسمُ وحدتي** |
| `can_view_book` | التوأمُ الفرديّ للسابقة — محروسٌ بالتطابق |
| `secret_access` / `can_open_content` | السرّيُّ يظهر صفّاً ويُفتح بإذن: قسمُ الكتاب **نفسُه** (لا الشجرة) + رئيسُه/مختصُّ بريده + منحةٌ فرديّة |
| `subtree_ids` | الشجرةُ تسيل **نزولاً لا صعوداً** |
| `can_use_desk` | الطاولةُ والورقتان: `controller` · `dept_head` · `admin` |
| `can_view_audit` | التدقيق: `dept_head` · `admin` |
| `scope_referrals_for` | صفوفُ الإحالة: قسمُ الكتاب · المُرسِل · المُستقبِل |
| `is_mail_officer` | = `controller` |

**وحارسا الصفّ** (`core/referral_service.py`): `_guard_target` لتحريك الحالة (المكلَّف · المُحال إليه ومَن فوقه · طاولةُ قسم الكتاب) · `_guard_chaser` للتنبيه (المُرسِل ومَن فوقه · الطاولة). **الأختُ لا تُقفل التزامَ أختها، والمُلتزِمُ لا يُنبّه نفسَه.**

**والتوقيع** (`core/signature_service.py`): `can_sign` = `admin` أو `dept_head` **في قسم الكتاب**. مختصُّ البريد **لا يوقّع** — يُسلّم ويستلم ولا يُصادق.

### 3.3 مصفوفةُ الفعل × الدور

| الفعل | viewer | entry | controller | dept_head | admin |
|---|:-:|:-:|:-:|:-:|:-:|
| يرى دفترَ قسمه وشُعبِه | ✅ | ✅ | ✅ | ✅ | الكلّ |
| يرى دفترَ القسم الأمّ | ❌ | ❌ | ❌ | ❌ | ✅ |
| يفتح السرّيَّ في قسمه | ❌ | ❌ | ✅ | ✅ | ✅ |
| يُدخل كتاباً | ❌ | ✅ | ✅ | ✅ | ✅ |
| يُفرّق / يُسجّل عهدة / يقيّد | ❌ | ✅* | ✅ | ✅ | ✅ |
| يُحرّك التزاماً (استلام/إنجاز) | إن كان المكلَّف أو وحدته | ✓ | طاولةُ قسمه | شجرته | ✅ |
| يُنبّه على متأخّر | ❌ | ❌ | ✅ | شجرته | ✅ |
| طاولةُ الوارد والورقتان | ❌ | ❌ | ✅ | ✅ | ✅ |
| يربط كتاباً بكتاب | بنطاقه | ✅ | ✅ | ✅ | ✅ |
| يوقّع مستنداً | ❌ | ❌ | ❌ | قسمُه | ✅ |
| يُبطل توقيعاً | ❌ | ❌ | ❌ | توقيعَه | ✅ |
| يقرأ التدقيق | ❌ | ❌ | ❌ | شجرته | ✅ |
| البريدُ الإلكترونيّ | ❌ | ❌ | ✅ | ❌ | ✅ |
| يُدير الجهات والعناقيد | ❌ | الجهات | الجهات | الجهات | ✅ |
| يُدير الأقسام والمستخدمين | ❌ | ❌ | ❌ | ❌ (🟡 §11) | ✅ |
| يُغيّر تبويبَ جهة (`kind`) | ❌ | ✅ | ✅ | ✅ | ✅ |

\* `entry` يُفرّق عبر الحواريّة إن فتح محتوى الكتاب — وهو ما تسمح به بوّابةُ الكتاب. **سؤالٌ للمالك** إن أراد قصرَه على الطاولة (§12).

### 3.4 دورٌ سادس: **مسؤولُ الأرشفة** `archivist` 🆕 (أولويّةُ المالك 2026-08-31)

> «مهمٌّ عندي الآن أن أنتهي من دور مسؤول الأرشفة: يُدخل الكتب ويؤرشفها ويستعلم عن الكتب ويفتح الأضابير.»

**موضعُه في الدورة (من شهادة موظّف البريد):** ثلاثةُ توقيعاتٍ حقيقيّة — استلامُ الوحدة · **تمامُ الأرشفة** · تسليمُ المتعهّد. الأرشيفيُّ صاحبُ الثاني: الورقةُ تعود من الوحدة منجَزةً، فيُلحِق بها ما ينقصها (مسحٌ · دمجُ صفحات · نصّ)، ويُقيّد **تمامَ الأرشفة والحفظ**، ثمّ تُغلق. وهو أيضاً مَن **يُجيب عن «أين الكتاب؟»** — الاستعلامُ والأضابيرُ أدواتُه لا أدواتُ غيره.

| | |
|---|---|
| **التحديد** | مجموعةُ جانغو «مسؤول الأرشفة» (كـ`controller` و`entry`) — `is_archivist(user)` في `scoping.py` |
| **مع مختصّ البريد** | دوران مستقلّان يُجمعان في شخصٍ واحد إن أُريد (المجموعتان معاً). الشهادةُ تقول «مسؤول إدارة البريد والأرشفة» بصيغةٍ واحدة — **سؤال §12/16** |
| **النطاق** | كالباقي: قسمُه وشُعبُه (`scope_books_for`) |
| **السرّيّ** | كمختصّ البريد: يفتح سرّيَّ **قسمه** (الشهادة: «فقط مسؤول إدارة البريد والأرشفة يحقّ لهم الاطّلاع») |

**صلاحيّاتُه (تُضاف إلى §3.3 عموداً):**
| الفعل | archivist |
|---|:-:|
| يُدخل كتاباً (الإدخال الذكيّ · الإدخال اليدويّ) | ✅ |
| المرفقات: رفعٌ · استبدالٌ · دمجُ صفحات · إزالةُ فارغ · نسخٌ | ✅ |
| **تمامُ الأرشفة** (`record_custody(ARCHIVE_DONE)` + `is_archived=True`) | ✅ **حصريّاً له ولـadmin** |
| إعادةُ فتح مؤرشَف (`is_archived=False`) بسبب | ✅ |
| يستعلم (القائمةُ الموحّدة بكلّ مرشِّحاتها + بحثُ متن المسح) | ✅ |
| يفتح الأضابيرَ والتقاريرَ المطبوعة للأضبارة | ✅ |
| يقيّد الكتابَ في دفتر قسمه («قيِّده عندنا») | ✅ |
| يُفرّق / يُسجّل عهدةَ استلامِ وحدة / يُنبّه | ❌ (عملُ مختصّ البريد) |
| يوقّع مصادقةً | ❌ |
| سلّة المهملات: استعادةٌ | ✅ · حذفٌ نهائيّ ❌ (admin) |

**واجهتُه — «طاولةُ الأرشفة»** `/books/desk/archive/` (§6 صفحة 3هـ) — خمسةُ طوابير هي أسئلتُه اليوميّة:
1. **منجَزٌ ولم يُؤرشَف** — إحالاتٌ `DONE` بلا `ARCHIVE_DONE` (الورقةُ عادت وتنتظر الحفظ).
2. **كتبٌ بلا مرفق** — قيدٌ بلا مسح.
3. **مرفقٌ بلا نصّ** — مسحٌ لم يُستخرج (لا يُبحث في متنه).
4. **عهدةٌ خارجة** — `COURIER_PICKUP` بلا `RETURN`، أو `UNIT_RECEIPT` تجاوز موعدَه.
5. **مؤرشَفٌ حديثاً** — آخرُ ما أُغلق (للمراجعة والتراجع).
+ **زرُّ «تمامُ أرشفة»** على صفّ الطابور وفي صفحة الكتاب (بجانب «عهدة»)، يفتح حواريّةً صغيرة: موضعُ الحفظ (نصّ) · ملاحظة ⟵ يكتب حدثَ العهدة ويُؤرشف ويُقيّد في `BookHistory('archived')`.
+ **لوحةُ التحكّم** له قسمٌ «الأرشفة» (المسجّلُ `dashboard_sections.py` — بوّابتُه `is_archivist`).
+ في الشريط الجانبيّ: «طاولة الأرشفة» · «الأضابير» · «قائمة الكتب» · «الإدخال الذكي».

**ما يُحرَس:** `entry` لا يُؤرشف · مختصُّ البريد لا يُؤرشف (إلّا إن حمل المجموعتين) · الأرشفةُ لا تُقفل التزاماً مفتوحاً (تُرفض برسالة «الوحدةُ لم تُنجز») · الفتحُ بعد الأرشفة يُسجَّل بسبب.

---

## 4) الأقسامُ والشجرة

### 4.1 النموذج
`Department(name · code · parent · entity · is_active)` — **التوأمُ** (`entity`) هو الجسرُ بين الشجرة ودليل الجهات: به تمتلئ الأضبارة، وبه يُخاطَب القسمُ في كتاب.

### 4.2 الحالُ الآن — 40 قسماً مبذوراً من الرموز العربيّة، والشجرةُ:
```
ش   مكتب السيد المدير العام
└ ش13 قسم المتابعة                 ← كلُّ الكتب (13,238) والمستخدمين (12)
   └ ش.ت شعبة متابعة تنفيذ الموازنة
      ├ و.ت   وحدة التقارير
      ├ و.ت.ش وحدة التعاقدات
      ├ و.م   وحدة مشاريع المساهمة الاجتماعية
      └ و.م.ب وحدة الموارد البشرية
```
و**34 قسماً جذرٌ مسطّح** (ت · ح · خ · د · ع · ل ولجانُها السبع · م · ه · و · ش1–ش12 · ش.ج · ش.م.د · ش.م.ف) — **عمداً**: كلُّ رابطِ أبوّةٍ يُوسّع ما يراه الأبُ، فلا يُضاف إلّا حيث تُراد الرؤية.

### 4.3 الترتيبُ (قرارُ المالك): **قسم ← شعبة ← وحدة ← أفراد**. والأبوّةُ في ملفٍّ مُخلَّد `core/data/department_tree.json` بالرموز + أمر `set_department_parents` (جافٌّ افتراضاً، يمرّ من `update_department` بحارس الحلقات والسجلّ). **لا اشتقاقَ من الرموز** — مقيسٌ أنّه يُنتج هراءً.

### 4.4 دخولُ قسمٍ جديد (الإجراءُ الثابت)
1. الجهةُ في الدليل بتبويب «أقسام الشركة» ورمزٍ عربيّ (توأم).
2. `seed_departments --apply` (يتخطّى المعطَّل).
3. سطرٌ في `department_tree.json` إن أُريدت رؤيةٌ من فوق ⟵ `set_department_parents --apply`.
4. مستخدمو القسم: ملفٌّ (`UserProfile.department`) + دورٌ (مجموعة أو `is_department_head`).
5. العدّادُ يُنشأ كسولاً (`BookSequence` لكلّ قسم — 0065).
6. **الكتبُ القديمةُ لا تُنقل**: تصل القسمَ الجديد بالذكر في الصادر/الوارد (الأضبارة) وبالتفريق.

---

## 5) الجهاتُ والعناقيد

### 5.1 التبويبُ الثلاثيّ — **حقلٌ يدويّ** (`Entity.kind`، 0074)
| التبويب | المعنى | الآن |
|---|---|---|
| `external` جهات خارجية | خارج الشركة | 269 |
| `internal` أقسام الشركة | هيئاتُنا وأقسامُنا | 34 |
| `unit` الشعب والوحدات | الشعبُ والوحداتُ والأفراد | 108 |

بُذر مرّةً بقواعدَ مقيسة (صدرُ الاسم · «الأمّ / الفرع» · التوأم)، **ولا يُعاد** — الاقتراحُ للجهة الجديدة فقط. يُراجَع من صفحة الجهات بـ«الأكثر كتباً» ويُنقل بشريط «نقل إلى». **مراجعةُ البذر ما زالت على المالك** (§12).

### 5.2 العناقيد (`EntityGroup`)
- ديناميكيٌّ واحد: `all_registry_departments` (يتّسع تلقائيّاً).
- ثابتة: عضويّةٌ يدويّة. الموجود: «جميع الهيئات والأقسام» (ديناميكيّ) · «رؤساء لجان الإدارة المشتركة» · «كافة الوحدات» — **عضويّتُهما تجريبيّةٌ من هذه الجلسة، يشكّلها المالك**.
- الإدارةُ في **صفحة الجهات ← تبويب العناقيد** (إنشاء · تعديلٌ برابط · منتقٍ باحثٌ لا يُخفي المُحدَّد · تعطيل · حذفٌ لغير المستعمَل). ولوحةُ الإدارة تُحيل.
- **مسارُ الكتابة واحد** (`admin_service.save_group/delete_group`) — العيبُ الذي كان يمحو الأعضاءَ بالتعطيل محروس.

### 5.3 الجهاتُ الوهميّة — **ما فُعل وما لا يُفعل**
دُمجت عائلتان بالخطّة المُخلَّدة (24 و9 كتاباً). و«كافة الوحدات ومنتسبي شعبة…» **لا تُدمج** — مخاطَبٌ آخر. و`jmc of qurnain block` جهةٌ حقيقيّةٌ تُترك.

---

## 6) خريطةُ الصفحات — كلُّ صفحة: مَن يراها · ما فيها · إلى أين تنقر

الترتيبُ هو ترتيبُ الشريط الجانبيّ **الموصى به** (§10 يُعيد ترتيبه). الهويّةُ الموحّدة للصفحات الجديدة: **«مكتبُ الأرشيف الدافئ»** (ورقٌ · حبرٌ `#362014` · كهرمانٌ `#b45309`)، مقصورةٌ بصنفِ نطاقٍ لكلّ صفحة.

| # | الصفحة | المسار | مَن | ما فيها | ينقر إلى |
|---|---|---|---|---|---|
| 1 | **لوحة التحكّم** | `/` | الجميع | أقسامٌ بحسب الدور (`dashboard_sections.py`): ما يخصّني · الطاولة · أضبارة وحدتي · دفتر القسم · البريد · الإدارة | كلُّ عدّادٍ رابط |
| 2 | **ما يخصّني اليوم** | `/books/my/today/` | الجميع | 4 طوابير باسمي (متأخّر · اليوم · لم أستلمه · للتنفيذ) | الكتاب |
| 3 | **طاولة الوارد** | `/books/desk/` | `can_use_desk` | 5 طوابير القسم + روابطُ الورقتين | الكتاب · الدفتر · الكشف |
| 3أ | دفتر الوارد المطبوع | `/books/desk/ledger/` | `can_use_desk` | أعمدةُ الكاتب الخمسة · A4 | طباعة |
| 3ب | كشف التسليم A4 | `/books/desk/handover/?to=` | `can_use_desk` | ما فُرِّق ولم يُوقَّع · خانةُ توقيعٍ بالسنتيمتر | طباعة ⟵ تُرتجع ⟵ «عهدة» |
| 3ج | **بريدُ الوحدات** (واتساب) 🆕 | `/books/desk/chat/` | `can_use_desk` | جهاتٌ يميناً · محادثةُ كتبٍ وسطاً بعلامات ✓/✓✓/قُرئ/رُدّ/⏰ · الكتابُ يساراً (§7‑ب) | الكتاب · استلام/إنجاز/تنبيه |
| 3د | **طاولتي** (شعبة/وحدة) 🆕 | `/books/desk/mine/` | `dept_head` | الطوابيرُ الخمسة على شجرتي + لوحةُ الوحدات + الموظّفون (§7‑ج) | محادثةُ الوحدة · الكتاب |
| 3هـ | **طاولة الأرشفة** 🆕 | `/books/desk/archive/` | `archivist`·`admin` | 5 طوابير: منجَزٌ ولم يُؤرشَف · بلا مرفق · بلا نصّ · عهدةٌ خارجة · مؤرشَفٌ حديثاً (§3.4) + زرّ «تمامُ أرشفة» | الكتاب · الإدخال · الأضبارة |
| 4 | **الإدخال الذكي** | `/books/extract/smart-desktop/` | `entry`+ | مسحٌ واستخراج (منطقةُ الجلسة الأخرى) | الكتابُ الجديد |
| 5 | **قائمة الكتب** | `/books/unified/` | الجميع بنطاقهم | تبويبان وارد/صادر + 7 مرشِّحات + بحث | التفاصيل |
| 6 | **تفاصيل الكتاب** | `/books/<id>/` | `can_view_book` | ترويسة (رقمٌ حبراً · شارات · عنوان · **قُيِّد في** · **العهدة الآن**) · المستند · البيانات · دورة الحياة (تفريق/ردود/روابط/عهدة) · **التواقيع** · هوامش · تعليقات · سجلّ | الحواريّاتُ الأربع · التقرير · الطباعة |
| 6أ | الكتاب السرّي (stub) | نفسُه | من لا يفتح المحتوى | قفلٌ + رقم/تاريخ/نوع/قسم — «مغلق» لا «ممنوع» | العودة |
| 6ب | تقرير الكتاب | `/books/<id>/report/` | `can_view_book` | دورةُ الحياة مطبوعة | — |
| 7 | **الأضابير** | `/books/dossiers/` | الجميع بنطاقهم | 3 تبويبات (خارجية · أقسام · شعب) على غرار الكتب · بطاقاتٌ بعدّادَي صادر/وارد | الأضبارة |
| 7أ | الأضبارة | `/books/dossiers/<id>/` | بنطاقهم | صادر/وارد مقسَّماً بالنوع · مرشِّحات | الكتاب · التقرير |
| 8 | **التنبيهات** | `/books/notifications/` | الجميع | إشعاراتُ المستخدم | الكتاب |
| 9 | **البريد** | `/books/mail/` (+ `inbox/ sent/ compose/ templates/ thread/`) | `controller`·`admin` | مركزٌ · وارد IMAP · صادر · قوالب | الكتاب |
| 10 | **التقارير** | `/books/reports/` (+ `followup-activity/ export/`) | `dept_head`·`admin` | إحصاءاتٌ وتصدير CSV | — |
| 11 | **الجهات والعناقيد** | `/books/entities/` (+ `?view=groups`) | staff | تبويبان · شريطُ تحكّمٍ واحد (بحث · تبويب · لغة · حالة · **الأكثر كتباً**) · **نقل إلى** · ورشةُ العناقيد | تفاصيلُ الجهة · الأضبارة |
| 11أ | تفاصيل/تحرير الجهة | `/books/entities/<id>/` · `/edit/` · `/new/` | staff | حقلُ **التبويب** بين الحقول | — |
| 11ب | دمج المكرّرات | `/books/entities/merge/` | staff | `prepare_entities` بواجهة | — |
| 12 | **التدقيق** | `/books/audit/` | `can_view_audit` | سجلُّ الحركات بالشجرة | الكتاب |
| 13 | **لوحة الإدارة** | `/books/admin/?tab=` | `admin` | أقسام (يتبع · توأم) · مستخدمون (قسم · رئيس · مجموعة) · إحالةٌ للعناقيد | — |
| 14 | المستخدمون | `/books/users/` | staff | الأدوارُ الخمسة | — |
| 15 | سلة المهملات | `/books/trash/` | staff | كتبٌ ومرفقاتٌ محذوفةٌ ناعماً | استعادة |
| 16 | النسخ الاحتياطي | `/books/backup/` | staff | نسخةٌ مشفّرةٌ إلى `D:` | — |
| 17 | الإعدادات | `/books/settings/` (+ `sequences/ network/ scan/`) | staff | مركزٌ موحّد | — |
| 18 | **التحقّق من توقيع** | `/books/verify/<token>/` | **عامّ** | حكمُ البصمة · الموقِّع · الصفة · الوقت — **بلا مضمون** | — |
| 19 | الدخول / الخروج | `/login/` · `/logout/` | — | — | اللوحة |

**صفحاتٌ ميتةٌ تُزال في §9:** `book_audit.html` (هيكلٌ يتيمٌ بعد الدمج) · `dev_login` (محروسٌ بـDEBUG، يُبقى خارج الإنتاج).

---

## 7) الحواريّات — الأربع وما يفتحها ويُغلقها

| الحواريّة | تُفتح من | حقولُها | تكتب عبر | تُغلق إلى |
|---|---|---|---|---|
| **التفريق** `#distributeModal` | زرّ «تفريق» في دورة الحياة | عنقود (يُلغي اليدويّ) · أهدافٌ متعدّدة (أقسامٌ كلُّها + مكلَّفٌ من شجرتي) · للتنفيذ/للعلم · موعد · هامشٌ نصّيّ · **قصاصةُ الهامش** (سحبٌ على صورة الصفحة، كسورٌ 0–1) | `api_distribute` ⟵ `distribute()` / `send_circular()` | تحديثُ اللوحة · صفوفٌ في «التفريق والردود» |
| **العهدة** `#custodyModal` | زرّ «عهدة» | الحدث (استلام/تسليم/…) · إلى قسمٍ أو شخصٍ أو اسم · موقَّع في · ورقيّ/إلكترونيّ · ملاحظة | `api_record_custody` ⟵ `record_custody()` | «العهدة الآن» في الترويسة |
| **الربط** `#linkPickerModal` | زرّ «اربط» عند «كتبٌ مرتبطة» | بحثٌ مُمهَل (رقم/موضوع، نطاقي، قسمي أوّلاً) · نتيجةٌ تُنقر · الصفة (جواب/إلحاق/إشارة/تأكيد) · ملاحظة — **الزرُّ معطَّلٌ حتى تُنقر نتيجة** | `api_add_link` ⟵ `add_link()` | إعادةُ تحميل · ضلعٌ في القائمة |
| **قيِّده عندنا** (زرٌّ لا حواريّة) | دورة الحياة | — | `api_register_here` ⟵ `register_book_here()` (رقمٌ من عدّاد قسمي) | شريطُ «قُيِّد في» |
| الملاحظات `#notesModal` | بطاقةُ الهوامش | نصّ | `api_book_notes` | البطاقة |
| **تمامُ أرشفة** `#archiveModal` 🆕 | زرّ «تمامُ أرشفة» في طاولة الأرشفة وصفحة الكتاب (للأرشيفيّ) | موضعُ الحفظ (نصّ) · ملاحظة | `api_archive_book` ⟵ `archive_book()` (عهدة `ARCHIVE_DONE` + `is_archived` + سجلّ — **يرفض التزاماً مفتوحاً**) | شارةُ «مؤرشَف» في الترويسة · يخرج من الطابور |

**قاعدةٌ لكلّ حواريّة:** الأغلفةُ في `lifecycle_api.py` رقيقة (HTTP ⟵ استدعاء)، والحقيقةُ في الخدمة. رسالةُ الخطأ **من الخادم بنصّها**. خارجُ النطاق **404 لا 403**.

---

## 7‑ب) بريدُ المختصّ — «على غرار واتساب» (طلبُ المالك 2026-08-31)

> «أريد موظّفَ إدارة البريد يدير البريدَ كما واتساب: الجهاتُ والرسائلُ الجديدة وتقريرُ الوصول والاستلام والقراءة والردود والاستحقاق كلُّها واضحة.»

**الفكرةُ الحاكمة:** ليست ميزةً جديدةً بقدر ما هي **عرضٌ جديدٌ فوق بياناتٍ قائمة**. كلُّ ما يريده المالك موجودٌ في الجداول إلّا شيئاً واحداً (القراءة). الصفحةُ تُسمّى **«بريدُ الوحدات»** وتعيش في `/books/desk/chat/` لمن يملك `can_use_desk`.

### 7‑ب.1 التخطيط — ثلاثةُ أعمدة كواتساب
```
┌─ الجهات (يمين) ─────┬─ المحادثة (وسط) ──────────────────┬─ الكتاب (يسار، اختياريّ) ─┐
│ 🔍 بحث              │  وحدة التقارير · ✓✓ 3 · ⏰ 1        │  ترويسةٌ مصغّرة           │
│ ● وحدة التقارير  ③  │ ─────────────────────────────────  │  المستند                  │
│   وحدة التعاقدات    │  [2433] بشأن الموازنة…    ✓✓ قُرئ   │  الهامش/القصاصة          │
│   شعبة الجودة    ①  │        هامش: للتنفيذ · ⏰ 09/03      │  [استلام] [إنجاز] [تنبيه]  │
│ ─ خارجيّة ─         │  [2440] تأكيد على 2433    ✓ أُرسل   │                           │
│   وزارة النفط       │  ↩ [2451] جواب من الوحدة   ✔ رُدّ    │                           │
│   شركة EBS          │  [اكتب تنبيهاً…]           [إرسال]  │                           │
└─────────────────────┴───────────────────────────────────┴───────────────────────────┘
```
- **العمودُ الأيمن (الجهات):** الوحداتُ في شجرتي (`subtree_ids`) أوّلاً مرتّبةً بـ**آخر حركة**، ثمّ الجهاتُ الخارجيّة ذاتُ البريد. شارةٌ بعدد **غير المُستلَم** وأخرى بالمتأخّر. بحثٌ حيّ.
- **العمودُ الأوسط (المحادثة):** كلُّ إحالةٍ إلى هذه الجهة/منها = **فقاعة**: رقمُ الكتاب · الموضوع · الهامش (نصّاً + قصاصةً) · الغرض · الاستحقاق · **علاماتُ الحالة**. والجوابُ (`BookLink` جواب) يظهر **فقاعةً مقابلة** مربوطةً بأصلها. والتنبيهُ (`send_reminder`) فقاعةٌ صغيرةٌ من المختصّ. صندوقُ إدخالٍ أسفلَ المحادثة **يُرسل تنبيهاً** (لا كتاباً — الكتابُ يُدخَل من الإدخال الذكيّ).
- **العمودُ الأيسر:** الكتابُ المختار — ترويسةٌ وقصاصةٌ وأزرارُ الصفّ الثلاثة عبر الـAPI القائم.

### 7‑ب.2 علاماتُ الحالة — خريطةُ «✓» على البيانات القائمة
| العلامة | المعنى | المصدرُ اليوم | يلزم |
|---|---|---|---|
| ✓ **أُرسل** | فُرِّق | `BookReferral.created_at` | — |
| ✓✓ رماديّ **وُقّع الورق** | وقّعت الوحدةُ على كشف التسليم | `CustodyEvent(UNIT_RECEIPT).signed_at` | — (موجود) |
| ✓✓ أزرق **أُقرّ في النظام** | ضغطت الوحدةُ «استلام» | `status=RECEIVED` | طابعٌ `received_at` (**غيرُ موجود**) |
| ✓✓ أزرق **قُرئ** | فتح أحدُ الوحدة الكتابَ | **لا يوجد** | `opened_at` + `opened_by` يُختمان في `book_detail` عند أوّل فتحٍ من عضوٍ في الوحدة المُحال إليها |
| ✔ **رُدّ** | جوابٌ مربوط أو إنجاز | `closed_by_link` / `status=DONE` | طابعٌ `done_at` (**غيرُ موجود**) |
| ⏰ **مستحقّ** / 🔴 **متأخّر** | `due_date` مقابل اليوم | موجود | — |
| 🔔 **نُبِّه** | `last_reminder_at` | موجود | — |
| ↩ **أُعيد** | `status=RETURNED` | موجود | — |

**تغييرُ البيانات (هجرةٌ واحدة 0077):** `BookReferral.received_at · opened_at · opened_by · done_at`. تُملأ من `_advance` (مسارُ الكتابة الوحيد) ومن `record_custody(UNIT_RECEIPT)`. **والقراءةُ تُختم في العرض لا في الخدمة**: `book_detail` تسأل «أهذا الزائرُ من الوحدة المُحال إليها ولم يُفتح بعد؟» فتختم مرّةً واحدة. لا حذفَ ولا تعديلَ لهذه الأختام — واقعاتٌ حدثت.

### 7‑ب.3 الجهاتُ الخارجيّة في القائمة نفسِها
لجهةٍ خارجيّةٍ لها بريد: الفقاعةُ = `BookEmailLog` (✓ أُرسل · ✓✓ `success` · ✕ `failed`) و`IncomingEmail` (فقاعةٌ مقابلة، **قُرئ** = `is_read`). الردُّ عليها يفتح `mail_compose` القائم. **لا نموذجَ رسائلَ جديد** — البريدُ الخارجيّ له جداولُه، والداخليّ له الإحالة.

### 7‑ب.4 اللحظيّ
بلا Redis: **استقصاءٌ خفيف** كلَّ 20 ثانية لعدّادات القائمة فقط (`/api/desk/chat/badges/`) — رخيصٌ وصادق. مع Redis (المرحلة و): يصير بثّاً على `dept_{id}`. الواجهةُ لا تتغيّر بين الحالين.

### 7‑ب.5 ما لا يُبنى هنا
لا رسائلَ حرّةً بين المستخدمين (المحادثةُ **سجلُّ كتب**، والتنبيهُ هو النصُّ الحرُّ الوحيد — وهو ما أجاب به المالك في §16.5) · لا مرفقاتٍ في الفقاعات غيرَ مرفق الكتاب · لا حذفَ لفقاعة.

---

## 7‑ج) صفحةُ مسؤول الشعبة وصفحةُ مسؤول الوحدة (طلبُ المالك)

> «مسؤولُ الشعبة له صفحةٌ مستقلّة، ومسؤولُ الوحدة.»

**الفكرةُ الحاكمة:** آلةٌ واحدة (`queues.py`) وصفحةٌ واحدةٌ **تتسمّى بمستوى صاحبها**: `/books/desk/mine/` تقرأ قسمَ المستخدم فتُصبح «طاولةُ شعبة متابعة الموازنة» لرئيس الشعبة، و«طاولةُ وحدة التقارير» لرئيس الوحدة. **مستقلّةٌ في المسار والعنوان والشريط الجانبيّ**، لا في الكود.

| | مسؤولُ الشعبة | مسؤولُ الوحدة |
|---|---|---|
| المسار | `/books/desk/mine/` (يقرأ المستوى من `Department.parent`/الأبناء) | نفسُه |
| العنوان | «طاولةُ الشعبة — {الاسم}» | «طاولةُ الوحدة — {الاسم}» |
| الطوابير | الخمسة على **شجرته** (وحداتُه) | الخمسة على **وحدته** |
| **لوحةُ الوحدات** | ✅ صفٌّ لكلّ وحدةٍ تابعة: مفتوح · متأخّر · غيرُ مستلَم · آخرُ حركة — والنقرُ يفتح محادثتَها في 7‑ب | ❌ (لا أبناء) |
| الموظّفون | ✅ التزاماتُ كلّ موظّفٍ في الشجرة (حسابُ الوحدة يجمع — §16.5) | ✅ موظّفو الوحدة |
| الأفعال | استلام/إنجاز على صفوف شجرته · تنبيهُ وحداته · **توقيع** (رئيسُ القسم فقط اليوم — 🟡 يُوسَّع للشعبة؟) | استلام/إنجاز على صفوف وحدته |
| البوّابة | `is_department_head` + قسمُه له أبناء | `is_department_head` + قسمُه بلا أبناء |
| في الشريط الجانبيّ | «طاولتي» بدل «طاولة الوارد» | نفسُه |

**الفرقُ عن طاولة المختصّ:** المختصُّ يرى **القسمَ كلَّه** ويمسك الورقَ (الدفترُ والكشف)؛ ورئيسُ الشعبة/الوحدة يرى **عملَه وموظّفيه** بلا ورقتَي الطباعة. والاثنان يستهلكان `_queue()` نفسَها.

---

## 8) الانتقالاتُ والربطُ المنطقيّ — رحلةُ الكتاب الواحد

```
[1] الدخول ⟶ [2] لوحتي (بحسب دوري)
        │
        ├─ موظّفُ الوحدة ─▶ «ما يخصّني اليوم» ─▶ الكتاب ─▶ (استلام ▸ إنجاز) ─▶ يعود
        │                                    └▶ أضبارةُ وحدتي (ما ذُكرنا فيه)
        │
        ├─ مختصُّ البريد ─▶ الإدخال الذكي ─▶ كتابٌ جديد (رقمٌ من عدّاد قسمي)
        │        │                              └▶ التفاصيل ─▶ [تفريق] ─▶ صفوفٌ لكلّ وحدة
        │        ├▶ طاولةُ الوارد ─▶ متأخّر/غير مستلَم ─▶ الكتاب ─▶ [تنبيه] / [عهدة]
        │        ├▶ كشفُ التسليم (A4) ─▶ يُطبع ─▶ يُوقَّع ورقيّاً ─▶ يُرتجع ─▶ [عهدة: استلام وحدة]
        │        ├▶ دفترُ الوارد (A4) ─▶ جسرُ الثقة مع الدفتر الورقيّ
        │        └▶ البريدُ الإلكترونيّ ─▶ إرسالُ كتابٍ لجهةٍ خارجيّة
        │
        ├─ مسؤولُ الأرشفة ─▶ طاولةُ الأرشفة ─▶ «منجَزٌ ولم يُؤرشَف» ─▶ الكتاب ─▶ يُكمل (مسح · دمج · نصّ)
        │        │                                                    └▶ [تمامُ أرشفة] ─▶ مؤرشَفٌ مُغلَق
        │        ├▶ الأضابير ─▶ «أين الكتاب؟» ─▶ الاستعلامُ في القائمة الموحّدة
        │        └▶ الإدخالُ الذكيّ ─▶ كتابٌ جديد (يُدخل ولا يُفرّق)
        │
        ├─ رئيسُ القسم ─▶ الطاولة (شجرته) ─▶ الكتاب ─▶ [توقيع] ─▶ نسخةٌ مختومة + رمزُ تحقّق
        │        └▶ التدقيق ─▶ مَن فعل ماذا في شجرتي
        │
        └─ مديرُ النظام ─▶ الإدارة (أقسام · مستخدمون) ─▶ الجهات (تبويب · عناقيد)
```

**الروابطُ المنطقيّة الثابتة (لا تُكسر):**
1. **الجهةُ ⟷ القسم** عبر التوأم — بها الأضبارةُ والمخاطبة.
2. **الكتابُ ⟶ القسم** (`department`) ملكيّةٌ واحدة؛ **والإحالةُ** لا تنقل الملكيّة.
3. **التفريقُ = صفٌّ لكلّ وحدة** (لقطةُ العضويّة) — والتعميمُ رقمُ صادرٍ واحدٌ و`sent_to_group`.
4. **العهدةُ حدثٌ** والحقلُ الحاكم مؤشّرٌ لآخر حدث (`current_custody`).
5. **القيدُ المتعدّد** (`BookRegistration`): مستندٌ واحدٌ ورقمٌ في كلّ دفتر.
6. **الجوابُ يُقفل الإحالة** (`closed_by_link`) — ولا يُقفل التزامٌ بغير جوابٍ أو إنجازٍ صريح.
7. **التوقيعُ نسخةٌ جديدة** ولا يُحذف؛ **الإبطالُ وسم**.
8. **كلُّ فعلٍ سطرٌ في `BookHistory`** بمفتاحٍ لا نصٍّ حرّ — والتدقيقُ يُصفّي عليه.
9. **الأرشفةُ خاتمةٌ لا إخفاء**: لا تُقفل التزاماً مفتوحاً (تُرفض حتى تُنجز الوحدة)، ومؤرشَفٌ يُفتح بسببٍ مسجَّل. والأرشفةُ حدثُ عهدةٍ (`ARCHIVE_DONE`) لا علمٌ منطقيٌّ وحده — فالتوقيعُ الثاني من توقيعات الشهادة الثلاثة له أثرٌ كالأوّل.

---

## 9) مسارُ الجودة التقنيّة — كودٌ نظيفٌ وتحسينٌ للتقنيات

مرتّبٌ بالأثر. كلُّ بندٍ = إيداعٌ واحدٌ قابلٌ للتراجع + حارسٌ يُخفق على القديم.

### 9.1 إغلاقُ الجذور (من §2)
- **[ج1]** كنسُ الـ73 موضع `is_deleted=False` اليدويّة — صارت زائدةً منذ `SoftDeleteManager`. تنظيفٌ آليٌّ بـ`grep`+مراجعة.
- **[ج2]** توحيدُ النسختين المؤجَّلتين بالبروتوكول في `core/extraction/views/ui.py:88,147` على `scope_books_for` — **بعد الدمج الثاني** (تحرّرت المنطقة).
- **[ج3]** حراسةُ الشريط الجانبيّ بـ`is_staff` تُستبدل ببوّابات `scoping.py` (رابطٌ ظاهرٌ = صفحةٌ تُفتح). سياقُ قالبٍ واحد `nav_gates` يُغذّي `base.html`.
- **[ج4]** `registrations_of` و`_units_with_pending` تُصفّيان في بايثون ⟵ استعلام (🟡 4 و5).
- **[ج5]** `prune_activity_log` + تشذيبُ `ScanPayload`/`ExtractionCache` (**م3**) — أمرٌ واحدٌ `prune_growth` جافٌّ افتراضاً، مجدولٌ في beat. **القرصُ على الحافّة.**

### 9.2 التدشين (تمسّ يومَ النشر)
- **[ت1] م7** `SECURE_SSL_REDIRECT` بلا مُنهي TLS = حلقةٌ لا نهائيّة. يُربط بـ`env` صريح `BEHIND_TLS_PROXY`.
- **[ت2] م8** كتابةُ اعتماد القاعدة من طلبِ ويب تُزال (تراثيّة).
- **[ت3] م9** قفلُ الدمج عالميّ ⟵ لكلّ قسم.
- **[ت4] م4** تنزيلُ الرابط الموقَّع يُقيَّد في `ActivityLog` لا اللوغ النصّيّ.
- **[ت5]** `.encryption_key` إلى خزانةٍ + نسخةٌ خارج الخادم (تشغيليّ).

### 9.3 تحسيناتٌ تقنيّة
- **[ق1]** فهرس `pg_trgm` على `our_number` (مُعلَّقٌ منذ 05-14).
- **[ق2]** `BookLink.to_book` من PROTECT إلى منطقٍ صريح (🟡 2).
- **[ق3]** حارسُ تكرار المسارات: اختبارٌ يقرأ `urls.py` ويرفض مساراً مكرّراً — **الدمجُ الصامت لا يتكرّر**.
- **[ق4]** حارسٌ بنيويّ ثانٍ: «لا شرطَ دورٍ في أيّ قالب» (توسيعُ `test_the_template_asks_no_role_questions` إلى كلّ `templates/`).
- **[ق5]** حارسُ «مبنيٌّ ولا أحد يوصله»: لكلّ نقطةِ API في `urls.py` مستهلكٌ في `static/js` أو قالب — اختبارٌ يُحصي اليتامى.
- **[ق6]** المرحلة و: Redis في الإنتاج (قرارُ ز0) ⟵ حينها `NotificationConsumer` بمجموعتي `user_{id}`/`dept_{id}` + شارةُ الجرس + شريط «كتبٌ جديدة». **لا يُبنى قبل Redis.**
- **[ق7]** المرحلة ح: **بعد فتح منطقة الاستخراج** — عدّاداتٌ حيّة · تصديرُ حزمة · استيرادُ أوزانٍ ببصمة · بوّابةُ قبولٍ قبل التفعيل.

### 9.4 قواعدُ الحرفة (تسري على كلّ ما يُكتب)
مصدرٌ وحيدٌ لكلّ قاعدة · الأغلفةُ رقيقةٌ والحقيقةُ في الخدمة · لا `except` واسعٌ حول أثر · العددُ قبل القطع · الرسالةُ من الخادم · 404 لا 403 خارج النطاق · الإلحاقُ لا الكتابةُ فوق الوثائق · كلُّ حارسٍ يُطفَّر.

---

## 10) مسارُ الواجهات — أفضلُ واجهات

### 10.1 الهويّة
**«مكتبُ الأرشيف الدافئ»** صارت لغةَ كلّ ما بُني في هذه الدفعة (التفاصيل · الجهات · اللوحة · الطوابير · الحواريّات · الورقتان · السرّيّ). **الصفحاتُ القديمة** (القائمة الموحّدة · الإدخال الذكي · البريد · التقارير · الإعدادات) على هويّاتها السابقة — **قرارُ المالك القائم: الإحساسُ فقط بلا تغيير الهويّة**. تُوحَّد تدريجيّاً صفحةً صفحةً، كلٌّ بإيداعٍ ولقطة.

### 10.2 ترتيبُ الشريط الجانبيّ الموصى به (بحسب الدور لا قائمةٌ واحدة)
```
لوحة التحكم · ما يخصّني اليوم · [طاولة الوارد]ᶜ · الإدخال الذكي · قائمة الكتب · الأضابير
───
التنبيهات · [البريد]ᶜ · [التقارير]ʰ · [التدقيق]ʰ
───
[الجهات والعناقيد]ˢ · [لوحة الإدارة]ᵃ · [المستخدمون]ˢ · [سلة المهملات]ˢ · [النسخ]ˢ · [الإعدادات]ˢ
```
ᶜ `can_use_desk`/`is_mail_officer` · ʰ `dept_head`+ · ˢ staff · ᵃ admin — **الرابطُ الظاهر = صفحةٌ تُفتح**.

### 10.3 بنودُ الواجهات المتبقّية
- **[و1]** الشريطُ الجانبيّ بالدور (يعتمد ج3).
- **[و2]** «التدقيق» غائبٌ من الشريط — يُضاف لمن يراه.
- **[و3]** قائمةُ الكتب الموحّدة على الهويّة (أكثرُ الصفحات فتحاً؛ 7 تبويبات + جدول).
- **[و4]** الإدخالُ الذكيّ على الهويّة — **بعد فتح المنطقة**.
- **[و5]** عرضُ التوقيع في المستند نفسِه (شريطُ الختم يظهر في العارض) + زرُّ «تحقّق» بجانب كلّ توقيع.
- **[و6]** قصاصةُ الهامش: اختيارُ المرفق إن تعدّد (اليوم الأوّلُ فقط) + `--page-aspect` الحقيقيّ من الخادم.
- **[و7]** لوحةُ الإدارة: شجرةُ الأقسام **مرئيّةً** (اليوم قائمةٌ مسطّحة بحقل «يتبع») — شجرةٌ قابلةٌ للطيّ تُظهر مَن يرى مَن.
- **[و8]** تحقّقٌ بصريٌّ آليٌّ في CI: سكربتُ Playwright لكلّ صفحةٍ رئيسة + لقطةٌ مرجعيّة (الجهازُ 8GB — تُشغَّل بالتتابع).
- **[و9]** إتاحة: `scroll-margin-top` عُمِّم في التفاصيل — يُعمَّم في `base.html`؛ حلقاتُ التركيز بلون الهويّة في كلّ صفحة.

---

## 11) المتبقّي مرتّباً — الدفعاتُ القادمة

| الدفعة | البنود | يعتمد على |
|---|---|---|
| **4 — الدمجُ والتدشين** | الدمجُ الثاني (`rollout ⟵ main`) · ق3 حارسُ المسارات · ت1 م7 · ت2 م8 · ج5 التشذيب · ت5 تشغيليّ | قرارُ المالك على الدمج |
| **4‑ج — مسؤولُ الأرشفة** 🆕 **(أولويّةُ المالك)** | مجموعة «مسؤول الأرشفة» + `is_archivist` + `role_capabilities` · `archive_book()` مسارَ كتابةٍ واحداً (عهدة `ARCHIVE_DONE` + `is_archived` + `BookHistory('archived')`، يرفض التزاماً مفتوحاً) · `/desk/archive/` بخمسة طوابير · حواريّة «تمامُ أرشفة» · قسمٌ في اللوحة · الشريطُ الجانبيّ · إعادةُ فتحٍ بسبب · حرّاسُ التخويل (entry/controller لا يُؤرشفان) + لقطة | لا يعتمد على شيء — **يُبدأ به** |
| **4‑ب — بريدُ الوحدات وطاولتا الرئيسين** 🆕 | هجرة 0077 (`received_at·opened_at·opened_by·done_at`) · ختمُ القراءة في `book_detail` · `/desk/chat/` بثلاثة أعمدة + علامتا ✓✓ (ورق/نظام) + قسمُ الخارجيّة ذات البريد + استقصاءُ الشارات · `/desk/mine/` بلوحة الوحدات والموظّفين · الشريطُ الجانبيّ · حرّاسٌ + لقطاتٌ لثلاثة أدوار | الدفعة 4 (الدمجُ الثاني) — الأجوبةُ 11–14 حُسمت |
| **5 — الجذور** | ج1 كنسُ `is_deleted` · ج2 نسختا الاستخراج · ج3+و1+و2 الشريط بالدور · ق4 · ق5 | الدفعة 4 |
| **6 — التخويلُ المتبقّي** | 🟡 8 رئيسُ القسم يفتح اللوحة لقسمه · 🟡 3 تحذيرُ `record_custody` في الواجهة · 🟡 1 السرّيُّ في `api_book_detail_json` بـstub لا 403 · م5 إيصالاتُ القراءة | 5 |
| **7 — الواجهاتُ القديمة** | و3 القائمة · و7 شجرةُ الإدارة · و5 · و6 · و9 | 5 |
| **8 — اللحظيّ** | ق6 (Redis أوّلاً) | قرارُ ز0 |
| **9 — التدريبات** | ق7 · و4 · م6 | فتحُ منطقة الاستخراج |
| **10 — قرارُ الشركة** | التواقيع طور 2 (شهادات · عتاد · قانون) | قرارٌ خارج الكود |

---

## 12) قراراتٌ معلّقةٌ للمالك (لا يُمضى فيها بلا جواب)

1. **الدمجُ الثاني** `rollout-b3 ⟶ main`: الآن (نظيفٌ ومختبَر) أم بعد الدفعة 4؟
2. **مراجعةُ بذر التبويب** (269/34/108): «الادارة» و«سارة» وقعتا خارجيّتين — تُراجَع من «الأكثر كتباً».
3. **عضويّةُ العناقيد الثلاثة** — تجريبيّةٌ الآن.
4. **بقيّةُ شجرة الأقسام**: 34 جذراً مسطّحاً؛ مَن يرى مَن؟ (ل ← لجانُها السبع؟ ش ← ش2…ش12؟)
5. **`entry` يُفرّق؟** (§3.3) — أم يُقصر على الطاولة؟
6. **Redis في الإنتاج** (يفتح المرحلة و) — وهو قرارُ ز0 القديم رقم 5 (نظامُ التشغيل والعتاد).
7. **فتحُ منطقة الاستخراج** لهذه الجلسة، أم تبقى للجلسة الأخرى؟
8. **الاحتفاظُ بسجلّ التدقيق** (قرارُ ز0 القديم رقم 7) — يحدّد `prune_activity_log`.
9. **الطورُ الثاني للتواقيع** — قرارُ شركة.
10. مِن الخطّة القديمة §15.7 ما زال مفتوحاً: مَن يُدخل واردَ الشركة في وضع الشركة؟ · بوّابةُ مسلسل الأضبارة.

**أسئلةُ بريد الوحدات (§7‑ب/7‑ج) — أجاب عنها المالك 2026-08-31:**
11. ✅ **الوصول علامتان متمايزتان**: ✓✓ رماديّ = توقيعٌ ورقيّ على كشف التسليم (`UNIT_RECEIPT`) · ✓✓ أزرق = إقرارٌ في النظام (`RECEIVED`). كلاهما واقعةٌ تُعرض بوقتها.
12. ✅ **الرسالةُ كتابٌ دائماً + تنبيهٌ نصّيّ** — لا ملاحظاتٍ حرّةً بلا كتاب، فلا جدولَ جديد.
13. ✅ **الجهات = وحداتُ شجرتي + الخارجيّةُ ذاتُ البريد** — قسمان في العمود الأيمن.
14. ✅ **الإنجازُ لأيّ عضوٍ في الوحدة** (الحالُ الآن يبقى) — والسجلُّ يحفظ مَن ضغط.
15. ⏳ **رئيسُ الشعبة يوقّع؟** — مفتوح. الافتراضُ حتى الجواب: **لا** (رئيسُ القسم ومديرُ النظام فقط).
16. ⏳ **مسؤولُ الأرشفة ومسؤولُ البريد شخصٌ واحدٌ أم اثنان؟** الشهادةُ تقول «مسؤول إدارة البريد والأرشفة» بصيغةٍ واحدة. **الافتراضُ**: مجموعتان مستقلّتان تُجمعان في شخصٍ إن أُريد — فلا خسارةَ في الحالين.
17. ⏳ **أيَرى الأرشيفيُّ كلَّ أقسام الشركة** (أرشيفٌ مركزيّ) **أم قسمَه فقط**؟ **الافتراضُ**: قسمَه وشُعبَه كالباقين — والمركزيُّ يُمنح بمجموعةٍ ثانية «أرشيف الشركة» إن لزم.

---

## 13) التحقّق — كيف يُقاس كلُّ بندٍ قبل أن يُقال «منجَز»

1. **الوحدة:** `python manage.py test --settings=lettersys.settings_test > file` (المخرَجُ إلى ملفٍّ — الجهازُ 8GB) ⟵ `OK`، وصفرُ هجراتٍ معلّقة (`makemigrations --check`).
2. **الطفرة:** كلُّ حارسٍ جديد يُعطَّل الكودُ الذي يحرسه ⟵ يجب أن **يُخفق**.
3. **البصريّ:** خادمٌ واحدٌ من `lettersys_rollout` على 8000 ⟵ سكربتُ Playwright (`chromium-1223/chrome-win64/chrome.exe`، `domcontentloaded` لا `networkidle`) ⟵ تأكيداتُ الألوان المحسوبة + **لقطةٌ واحدةٌ تُقرأ** ⟵ حسابُ الفحص المؤقّت يُحذف.
4. **الأدوار:** ثلاثةُ حساباتِ فحصٍ (موظّف · مختصّ بريد · مدير) تفتح اللوحةَ والصفحاتِ المحروسة ⟵ 200/403 كما في §3.3.
4‑ج. **الأرشفة:** حسابُ `archivist` يرى «طاولة الأرشفة» (200) والطاولةَ العاديّة **لا** (403) · كتابٌ `DONE` بلا أرشفة يظهر في الطابور الأوّل · «تمامُ أرشفة» عليه ⟵ `is_archived=True` + عهدةُ `ARCHIVE_DONE` + سطرٌ في السجلّ · **المحاولةُ على التزامٍ مفتوح تُرفض برسالة** · `entry` و`controller` يُردّان بـ403 على الأرشفة (طفرة).
4‑ب. **بريدُ الوحدات:** سيناريو كامل — تفريقٌ ⟵ ✓ · عضوُ الوحدة يفتح الكتاب ⟵ «قُرئ» يُختم مرّةً واحدة · يُقرّ الاستلام ⟵ ✓✓ · يُربط جوابٌ ⟵ ✔ · وموعدٌ فائت ⟵ 🔴؛ كلُّ علامةٍ تُقرأ من الـDOM ولقطةٌ واحدة. و**طاولتي**: رئيسُ شعبةٍ يرى لوحةَ الوحدات ورئيسُ وحدةٍ لا يراها (`assertNotContains`).
5. **الدمج:** بعد كلّ دمج `grep -c` لتكرار `path(` + المجموعة + لقطةٌ للصفحات المتداخلة.
6. **القاعدةُ الحيّة:** نسخةٌ `pg_dump` إلى `D:/migration/backups/` **قبل** كلّ `migrate` أو أمرٍ كاتب.
7. **الوثائق:** `IMPLEMENTATION_NOTES.md` بالإلحاق فقط · `ROLLOUT_EXECUTION.md` سطرٌ لكلّ بند · إيداعٌ بعد كلّ بندٍ أخضر · لا `git add -A` في شجرةٍ مشتركة (القاعدةُ القديمة 3 تبقى).

---

## 14) عند الموافقة

1. يُكتب هذا المحتوى إلى `lettersys_rollout/COMPANY_ROLLOUT_PLAN_v2_2026-08-31.md`.
2. يُضاف سطرٌ في رأس `COMPANY_ROLLOUT_PLAN_2026-08-24.md`: «**نُسخت** — المرجعُ الحاليّ v2».
3. يُحدَّث `CLAUDE.md` (قسم «Current State») بثلاثة أسطر تُشير إلى v2 وقواعد §9.4.
4. يُودَع بإيداعٍ واحد `docs(plan): الخطّة v2 — مراجعةٌ وإعادةُ كتابة`.
5. ثمّ يُبدأ بالدفعة 4 بندَ الدمج الثاني **بعد جواب القرار 1**.
