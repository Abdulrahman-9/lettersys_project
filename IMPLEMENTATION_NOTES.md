# ملاحظاتُ التنفيذ — تُجمع أثناء العمل وتُستشار فيها مرّةً واحدة

**القاعدة:** كلُّ ما ألاحظه أثناء المرور على الكود ولا يخصّ البندَ الذي أنفّذه
يُسجَّل هنا **ولا يُنفَّذ في حينه** — كي لا يتضخّم البند ولا يختلط التغيير.
تُراجَع القائمةُ دفعةً واحدة مع المستشار المعماريّ عند اكتمال الدفعة.

**التصنيف:** 🔴 عيبٌ حقيقيّ · 🟡 تحسينٌ مستحقّ · 🔵 ملاحظةٌ للسجلّ · ✅ نُظِّف في حينه (تنظيفٌ رخيصٌ في ملفٍّ فتحتُه أصلاً)

---

## البند ①: تصحيحُ السرّيّة — 2026-08-27

### 🔴 عيبان حقيقيّان كشفهما التنفيذ (أُصلحا في حينهما)

**١. النسخةُ الثامنةُ والعشرون من قاعدة الرؤية — في الصفحة الرئيسة نفسها.**
`book_detail` كانت تحمل القاعدة يدويّاً موزَّعةً على أربعة أسطر
(`has_permission = is_superuser or is_staff or created_by == user`)، فنجت من
كنس الدفعة 2 لأنّ البحث كان عن النمط في سطرٍ واحد. الأثر: **الصفحةُ الرئيسة
بقيت على القاعدة القديمة** — staff يرى كلَّ شيء، والقسمُ بلا أثر.
*الدرس:* الكنسُ بالنمط النصّيّ يُخطئ الصياغاتِ المتعدّدةَ الأسطر —
والتحقّقُ الصحيح **سلوكيّ** لا نصّيّ.

**٢. القائمةُ الموحّدة لم تأخذ بُعدَ القسم إطلاقاً.** في أربعة مواضع
(`book_unified` · `api_unified_data` · التقارير · السلّة) كان البناء
`Book.objects.all() if is_privileged else filter(created_by=user)` — أي أنّ
الدفعة 2 استبدلت **المسند** ولم تستبدل **النمط**. فموظّفُ القسم كان يرى «كتبي
أنا» لا «كتب قسمي» رغم أنّ المرحلة أ أعلنت العكس.
*ولماذا لم يلتقطه اختبارُ الدفعة 2؟* لأنّه أكّد **غيابَ** كتابِ قسمٍ آخر ولم
يؤكّد **حضورَ** كتابِ زميلٍ في قسمي. **الاختبارُ السلبيّ وحده لا يحرس.**
كلُّها صارت `scope_books_for` — والاختبارُ الجديد يحرس الحضورَ والغياب معاً.

### 🟡 تحسيناتٌ مستحقّة (لم تُنفَّذ — تُستشار)

- **`api_book_detail_json` و`book_report` يرفضان السرّيّ برمز 403** بدل عرضٍ
  مقيَّد. مقبولٌ الآن (الصفُّ والتفاصيل الدنيا في القائمة وصفحة التفاصيل)، لكن
  الأنسبَ ربّما حمولةٌ مقيَّدةٌ متّسقةٌ مع بقيّة المنظومة.
- **`_is_mail_officer` يستدعي `get_user_role`** الذي يستعلم مجموعات المستخدم —
  استعلامٌ لكلّ كتابٍ في القائمة. لم يظهر أثرٌ (949 اختباراً في 15 ثانية)، لكنّ
  التخزينَ المؤقّت على الطلب يستحقّ القياس عند 13 ألف صفّ.
- **قوالبُ العرض ما زالت تقرأ `book.title` مباشرةً** في مواضع أخرى (التقارير،
  الأضابير، البريد). حُرست المسارات الأربعة الحرجة، ويلزم **جردٌ شاملٌ** لبقيّة
  القوالب قبل إعلان الطبقة مكتملة.
- **`STUB_BLANK_FIELDS` قائمةٌ يدويّة** — حقلٌ حسّاسٌ جديد في المُسلسِل لن
  يُحجب تلقائيّاً. البديلُ الأمتن: **قائمةٌ بيضاء** (ما يُسمح بمروره) بدل
  السوداء. تُراجَع مع اتّساع الحمولة.

### 🔵 للسجلّ

- `can_view_book` تغيّر معناها (صارت عن الصفّ لا المحتوى) — و`can_open_content`
  هي إذنُ المحتوى. **كلُّ مستدعٍ جديد يجب أن يختار بوعي.**
- الاختبارُ الرقميّ للبحث (`_is_numeric_query`) **يكرّر** شرطَ التفرّع في
  `apply_search_filters`. تكرارٌ مقصودٌ الآن (اقترانٌ ضعيف)، لكنّه ينجرف إن
  تغيّر أحدهما — يستحقّ توحيداً في `numbering.py` أو تعليقاً متبادلاً.

---

## البند ②: نسيجُ الوثائق (`BookLink` · المنتقي · البحثُ في المتن) — 2026-08-27

### 🔴 عيبٌ حقيقيّ كشفه التنفيذ (أُصلح في حينه)

**تصديرُ CSV كان بابَ محتوىً خلفيّاً — وكان تبويبُه يخالف الشاشة.**
`api_export_csv` يكتب `book.title` والجهاتِ **خامّاً**: القائمةُ مرّت بالمُقدِّم
وصفحةُ التفاصيل بالقالب المقيَّد، والتصديرُ لم يمرّ بشيء. فموظّفٌ عاديّ يضغط
«تصدير» فيأخذ عناوينَ كلِّ سرّيّ في قسمه في ملفٍّ يخرج من الجهاز.
ومعه عيبٌ ثانٍ في الدالّة نفسها: التبويبُ الافتراضيّ `"all"` بينما الصفحةُ
والنقطةُ على `"incoming"` — وزرُّ التصدير ينسخ `window.location.search` وأوّلُ
تحميلٍ بلا `tab`: **«صدّر ما أراه» كان يُصدّر أكثرَ ممّا يُرى.**
*الدرس ذاتُه مكرَّراً بصيغةٍ أخطر:* **كلُّ سطحٍ يُخرج محتوىً** يجب أن يمرّ
بالقرار الواحد — والقائمةُ والتفاصيلُ ليستا كلَّ السطوح. أُصلح الاثنان
وحُرسا بأربعة اختبارات (`ExportIsNotABackDoorTests`).

### 🔵 للسجلّ

- **التبويبُ الافتراضيّ `incoming` في ثلاثة مواضع** — الصفحة والنقطة والتصدير
  (الأخيرُ صُوِّب الآن). ثابتٌ مكرَّرٌ ثلاثاً يستحقّ اسماً واحداً في وحدةٍ ما.
  لم أفعل: البند لا يخصّه، وتغييرُ ثابتٍ مشتركٍ يمسّ مسارات جلسةٍ أخرى.
- **اختباري أنا أخفق أوّلاً لهذا السبب** — كتبتُ كتاباً صادراً وبحثتُ عنه فوجدتُ
  `[]`، وظننتُ الخللَ في وصلة `attachments__ocr_result__cleaned_text`. لم يكن
  فيها خلل. *الدرس:* **حين يُخفق اختبارٌ جديد، اعزل الطبقةَ قبل أن تتّهم
  الكود** — ثلاث تحرّياتٍ متدرّجة (ORM ⟵ المحرّك ⟵ النقطة) عيّنت الموضعَ بدقّة.
- **`_present` في `linking_service` تأخذ الدوالَّ معاملاتٍ** (`can_view`,
  `can_open`) بدل استيرادها. سببُه دورةُ استيرادٍ بين `scoping` و`models`؛
  يعمل، لكنّه توقيعٌ غريبٌ يستحقّ إعادةَ نظرٍ لو ارتفعت الدورةُ لاحقاً.

### 🟡 تحسينٌ مستحقّ (لم يُنفَّذ — يُستشار)

- **`BookLink.to_book` بـ`PROTECT`** يحمي السلسلة من الانقطاع، لكنّه يعني أنّ
  حذفاً صلباً لكتابٍ مُشارٍ إليه يفشل برسالةٍ خامّ. الحذفُ عندنا **ناعمٌ** في كلّ
  المسارات فلا أثرَ عمليّاً اليوم — يلزم التقاطُ `ProtectedError` برسالةٍ عربيّةٍ
  إن ظهر مسارُ حذفٍ صلبٍ في الإدارة.
- **المنتقي يرتّب بعد التقطيع** (`[:12]` ثمّ `sort`) — فقد يُقصى كتابُ قسمي إن
  سبقه اثنا عشر من غيره. مقبولٌ لأنّ النطاق قسميٌّ أصلاً (الغيرُ نادرٌ جدّاً)،
  والصحيحُ ترتيبٌ في قاعدة البيانات بـ`Case/When` عند اتّساع النطاق.

---

## البند ③أ: عمودُ التسيير — التفريق (`BookReferral`) — 2026-08-27

### 🔴 فجوةٌ بنيويّة كشفها اختبارٌ أخفق (أُصلحت في حينها)

**شجرةُ الأقسام كانت موجودةً في النموذج ولا شيء يستعملها.** `Department.parent`
مكتوبٌ منذ المرحلة أ، والنطاقُ كلُّه `department_id == dept_id` **مسطَّحاً**.
الأثر: **رئيسُ القسم ومختصُّ بريده عُميانٌ عن دفاتر شُعبهم** — كتابٌ تُنشئه
شعبةُ متابعة تنفيذ الموازنة لا يراه قسمُ المتابعة الذي يوقّع عنه ويجيب عنه أمام
الشركة. وهو ليس تفصيلاً: كلُّ تدفّق المالك (المدير يوجّه ⟵ الوحدةُ تُعدّ ⟵
المديرُ يوقّع) يمرّ عبر هذه الرؤية.

الحلّ `subtree_ids()` في `scoping` — **الشجرةُ تسيل نزولاً لا صعوداً**:
القسمُ يرى شُعبه، والشعبةُ لا ترى دفترَ الأمّ (نصُّ المالك: «للوحدات مستخدمون
يستطيعون الوصول **لأضابيرهم**… ومراجعة **بريدهم** الذي أرسلوه»). وما يخصّ
الشعبةَ من كتب القسم يصلها **بالتفريق** — التزامٌ صريحٌ لا اطّلاعٌ عامّ.
وُصلت في الدوالّ الثلاث معاً (`scope_books_for` · `can_view_book` ·
`scope_referrals_for`) — والانفراجُ بينها هو صنفُ العيب الذي كلّفنا مرّتين.

*الدرس:* **اختباري أخفق لأنّ افتراضي كان مقلوباً، لا لأنّ الكود كان خطأً** —
ومن الإخفاق خرج القرارُ الصحيح. كتبتُ التوثيقَ في الاختبار نفسه.

### ❓ سؤالٌ للمالك (مُجمَّعٌ هنا كما طُلب)

**هل يرى موظّفُ الشعبة دفترَ القسم الأمّ كاملاً؟** نفّذتُ: **لا** — يرى ما
فُرِّق إليه وما أنشأه هو. مبنيٌّ على نصّك، لكنّه قرارُ سياسةٍ لا استنتاجُ كود،
وعكسُه سطرٌ واحد. (وينبني عليه سؤالٌ ثانٍ: **هل يرى القسمُ الأمّ سرّيَّ شعبته؟**
اليوم: يرى الصفَّ ويُحجب المحتوى — لأنّ `secret_access` يقارن قسمَ الكتاب
بقسم المستخدم **بالمساواة** لا بالشجرة. عن قصد: توسيعُ السرّيّة بالشجرة قرارٌ
أثقل من توسيع الرؤية.)

### 🔵 للسجلّ

- **`subtree_ids` استعلامٌ واحدٌ لكلّ نداء** (الجدول 42 صفّاً؛ المشيُ في بايثون
  أرخصُ من استعلامٍ لكلّ مستوى). يتكرّر مرّتين في الطلب الواحد أحياناً —
  تخزينٌ مؤقّتٌ على الطلب يستحقّ القياس مع ملاحظة `_is_mail_officer` نفسِها.
- **`_referred_to` استعلامٌ فرعيّ لا وصلة** عمداً: الوصلةُ تُكرّر الكتابَ بعدد
  إحالاته فيلزم `distinct()` على كلّ قائمةٍ في النظام. محروسٌ باختبار.
- **`Notification` لكلّ موظّفٍ في الوحدة** (لا للمكلَّف وحده) — أمرُ المالك
  حرفيّاً. تعميمٌ على 42 وحدةً × 5 موظّفين = 210 صفوف في `bulk_create` واحد.
- **حارسُ الإحالة يقبل طرفَي الإحالة** إضافةً إلى مَن يفتح محتوى الكتاب: وحدةٌ
  فُرِّق إليها كتابٌ من قسمٍ آخر تستطيع تقديمَ حالته وإن لم تكن مالكةً للكتاب.

### 🟡 تحسينٌ مستحقّ (لم يُنفَّذ — يُستشار)

- **لا شيء يمنع شعبةً من إقفال التزامِ شعبةٍ أختٍ** لو كانت ترى الكتابَ (حالةُ
  التفريق إلى شعبتين). الحارسُ اليوم على مستوى الكتاب لا على مستوى الصفّ.
  محروسٌ عمليّاً بالشجرة النازلة (الشعبةُ لا ترى كتاب الأمّ)، لكنّه ليس قيداً
  صريحاً — يستحقّ `assignee`/`to_department` شرطاً في `_advance`.

---

## البند ③ب: العهدة (`CustodyEvent`) — 2026-08-27

### 🔵 قرارٌ خالف الخطّة عمداً (وهو الأهمّ في هذا البند)

**«بعهدة مَن» صار مؤشّراً لا نسخة.** الخطّة (§11.4) رسمت **حقلَي عهدةٍ** على
`Book` يُملآن من آخر حدث. نفّذتُ بدلهما `Book.current_custody` — مفتاحاً أجنبيّاً
إلى صفّ `CustodyEvent` نفسِه. السبب: الاسمُ والقسمُ والوقتُ موجودةٌ في الصفّ
أصلاً، ونسخُها في الكتاب يخلق **حقيقةً ثانيةً تنفرج عن الأولى** عند أوّل كتابةٍ
على جنب — وهو حرفيّاً الضياعُ الذي بُني الجدولُ لمنعه. الكلفة: وصلةٌ عند العرض
(`select_related`). المكسب: صفرُ احتمالِ انفراج.

**ولم أُنشئ حدثَ «قيدٍ» للـ13,236 كتاباً القديمة.** وقتُ القيد الوحيدُ المتاح
هو `created_at`، وهو للمستورد **زمنُ الاستيراد لا زمنُ القيد** (فخٌّ مقيسٌ
ومسجَّلٌ في ذاكرة المشروع). فبدل عهدةٍ مختلَقةٍ لأحد عشر ألف كتاب — لا شيء،
و«بعهدة: —» صادقة. **البيانات المختلَقة أسوأ من الغائبة** (وقد دفع المشروع
ثمنَ هذا الدرس مرّتين في وسوم التواريخ).

### 🔵 للسجلّ

- **تسجيلٌ بأثرٍ رجعيّ لا يقلب المؤشّر** (`_point_at` يقارن `signed_at`): كشفُ
  تسليمٍ يُدخَل متأخّراً بتاريخ أمس يُحفظ في السلسلة ولا يُعيد «بعهدة مَن» إلى
  الوراء. محروسٌ باختبارين (الصفُّ يُحفظ **و**المؤشّرُ لا يتحرّك).
- **`update_fields=['current_custody']` ضيّقٌ عمداً**: كتابةُ الكتاب كاملاً هنا
  تدهس تعديلاً موازياً من نافذةٍ أخرى. محروسٌ باختبار.
- **`undelivered` يُميّز توقيعَ الأرشفة عن توقيع الاستلام** — الخلطُ يُفرغ
  أخطرَ طابورٍ في الطاولة كذباً.
- **الحارسُ في الخدمة وفي القاعدة معاً** (`custody_has_a_holder`): الخدمةُ يمكن
  تجاوزُها بكتابةٍ مباشرة، والقيدُ لا.

### 🟡 تحسينٌ مستحقّ (لم يُنفَّذ — يُستشار)

- **`record_custody` تقبل `to_department` لأيّ قسم** — لا تتحقّق أنّه طرفٌ في
  إحالةٍ قائمة. مقصودٌ (العهدةُ تنتقل أحياناً بلا إحالة: المتعهّد، الأرشيف)،
  لكنّه يسمح بعهدةٍ إلى قسمٍ لا شأن له. قيدٌ ألطف: تحذيرٌ في الواجهة لا رفضٌ
  في الخدمة.
