# Merge9 — توحيدُ التبويبات وخطّةُ ترحيل البيانات إلى الإنتاج

> **هذه الوثيقةُ مصدرُ الحقيقة الموحّد.** أُنشئت لدمج عمل ثلاثة تبويبات (جلسات Claude)
> في محادثةٍ واحدة، ولتوجيه العمل القادم. **كلُّ تبويبٍ يقرؤها أوّلاً**، ويُلحِق فيها
> (لا يكتب فوقها). أُعدّت خطّةُ الترحيل باستشارة نموذج فيبل ومقيسةٌ على الجهاز.
>
> **أوّلُ ما تفعله المحادثةُ الجديدة:** اقرأ `MEMORY.md` ثمّ هذه الوثيقة، **أعد التحقّق من
> رأس الحالة أدناه** (`git rev-parse main origin/main` + `gh workflow list`)، ثمّ
> **لا تلمس الخادمَ ولا تُفعّل النشرَ** — نفّذ المراحلَ بالترتيب وبوّاباتِ التحقّق.

---

## §0 — القيودُ الصارمة (لا تُخالَف)
- **لا اتّصالَ بالخادم `217.65.145.47` إطلاقاً** — يديره مشغّلٌ آخر ([م]). كلُّ عمل الخادم يُعطى له أوامرَ جاهزةً ويُعيد المخرَج.
- **لا رفعَ لبيانات العميل** (تأكّد دوماً: GitHub = كودٌ + 4 أوزان `.onnx` LFS فقط؛ كلُّ dump/`.env`/`.encryption_key`/`media` متجاهَل).
- **لا طبعَ** لمحتوى `.env` ولا `.encryption_key` ولا أيّ سرّ.
- **لا نشرَ (deploy)** قبل اكتمال الترحيل والقبول.
- **لا فروعَ من مجلّد المشروع** — استعمل worktree مؤقّتاً قصير المسار.
- لا `git add -A` / `commit -a` (بروتوكول ROLLOUT_EXECUTION) · المخرَجُ الطويل إلى ملفّ خارج المستودع.
- **لا `dumpdata`/`loaddata` لبيانات الإنتاج إطلاقاً** — يتخطّى `save()` (`raw=True`) فيكتب الأسرارَ **صريحة**، ويُسقط المحذوفَ ناعماً، ويدمج المستخدمين بالاسم. `pg_dump -Fc`/`pg_restore` حصراً (السببُ مقيسٌ في §8). **وكلُّ كتابةٍ على الخادم خارج هذه الوثيقة تُسجَّل في سجلّ التنفيذ قبل تنفيذها.**

## §1 — الحالةُ الموحّدة (أعد التحقّق عند البدء)
- **`main` = `origin/main` = `236ffec`** · آخرُ هجرة **`0077_archive_events_and_history`** · **0 PR مفتوح** · النشرُ التلقائيّ **`disabled_manually`** (وطبقةٌ ثانية: `deploy.yml` نفسُه `workflow_dispatch`-فقط).
- ⚠️ **حالةُ قاعدة الإنتاج مجهولةٌ منذ 2026-09-08** — رفعٌ يدويٌّ لملفّ `dumpdata` خارج الخطّة (§8). كانت فارغةً (0 كتاب) قبله. **تُحسَم بأمر الاستطلاع S‑0 (§8.2)، ولا تُبدأ المرحلة 0 قبل عودة مخرَجه.**
- **الحزمةُ الكاملة:** كانت 1549 اختباراً خضراء على 0076؛ أعِد تشغيلها على `236ffec` قبل أيّ لقطة.

**ما أُنجز ودُمج على main:**
- الكودُ بلغ **0077** (دمج rollout-b3 → main: أدوار/أقسام/أرشيف/توقيع، هجرات 0062–0077).
- **حماية الوسائط X-Accel-Redirect** (PR #9) + `docs/DEPLOY_MEDIA.md` (كتلةُ nginx للمشغّل).
- **تعطيل النشر التلقائيّ** (PR #11 + `gh workflow disable`).
- **إصلاحُ كاش الجهات** (PR #14): الكاشاتُ تشفى ذاتيّاً بعد تحميل البيانات + فحصُ الإقلاع يصرخ عند قاعدةٍ فارغة.
- **تنظيفُ كودٍ** (PR #15): استبدالُ `_content_disposition` بدالّة Django الجاهزة.

## §2 — الحقائقُ الحاكمة (مقيسة)
- **مصدرُ الحقيقة = القاعدةُ المحلّيّة `lettersys` @0077** (عُدَّ بـ`Book.all_objects` — المدير `objects` يُخفي المحذوف): الكلّ **13,239** · محذوفٌ ناعماً **45** · تدريب **131** (منها 32 محذوفة) · نشطٌ غيرُ تدريبيّ **13,095** · جهات **674** (أعلى id 722) · ذاكرةُ ترويسة **5,395** · مرفقاتٌ نشطة **13,173** (0 مفقود على القرص). آخرُ كتابٍ أُنشئ 2026-08-30.
- **الوسائط:** `D:\lettersys_data\media` = 14,044 ملفّاً؛ **`books/` وحدَه ≈15,939 MiB / 13,932 ملفّاً** (مساراتٌ نسبيّة `books/<سنة>/<رقم>_<اسم>`، 0 مطلقة). لا يُشحَن: `scan_inbox` · `tmp` · `training_datasets` · `MD. O. C.png`.
- **مفتاحُ التعمية:** `project/.encryption_key` sha **`1e639b05`** (44 بايت) يفكّ `EmailSettings.smtp/imap_password` الحقيقيّين؛ مفتاحُ `../lettersys_rollout/.encryption_key` (`67572ad5`) **يفشل** ولا يُشحَن. كلاهما غائبٌ عن الخادم ⟵ يُشحَن `1e639b05` وحده (chmod 600).
- **معرّفاتُ الجهات في `var/*.json`:** `entity_doc_profiles.json` 143/143 · `entity_extraction_profiles.json` 136/136 — **0 انحراف**؛ يبقى كذلك ما دامت الاستعادةُ `pg_restore` (تحفظ المفاتيح الأوّليّة) لا إعادةَ استيراد.
- **البريدُ في القاعدة:** `is_active=True` · **`imap_sync_enabled=True`** (imap.gmail.com) · beat كلَّ 10 دقائق + مزامنةٌ عند فتح الوارد ⟵ **يُطفَأ في نسخة الشحن**. `BookEmailLog`: صفٌّ `sent` واحد (لا إعادةَ إرسال). `BackupSettings.enabled=False`.
- **SECRET_KEY:** لا شيءَ دائمٌ موقّعٌ به ⟵ **يبقى مفتاحُ الخادم**؛ كلماتُ المرور PBKDF2 مستقلّة؛ الجلساتُ تُقصّ.
- **البطء 15s على الإنتاج** = Tesseract (الافتراضيّ، لا EasyOCR) حتّى 3 تمريراتٍ على 1 vCPU. تخفيفٌ بلا كود: `TESSERACT_ADAPTIVE_THRESHOLD=0` · `OMP_THREAD_LIMIT=1` · gunicorn `--timeout≥180` · swap. **«لا جهات» = عرَضُ القاعدة الفارغة**، يزول بالاستعادة.
- **مفتاحُ النشر مكسور:** الخادم فيه pubkey `g4MZOdBd` بينما سرُّ GitHub يتوقّع `F/jfafPX` ⟵ النشرُ التلقائيّ كان يفشل. سكربتُ `lettersys-deploy`: `git reset --hard origin/main` + `lfs pull` + `migrate` + `collectstatic` + restart.
- **الترميز:** القاعدةُ المحلّيّة UTF8/ويندوز؛ الخادم `C.UTF-8` ⟵ يتغيّر ترتيبُ الفرز العربيّ فقط، لا المحتوى.
- **⚠️ المستودعُ على GitHub `PUBLIC`** (قِيس 2026-09-09 بـ`gh repo view`) — لا خاصّاً كما ظُنّ. **مُتحقَّقٌ أنّه نظيف**: صفرُ ملفّاتِ بياناتٍ أو نسخٍ في التاريخ كلِّه (كلُّ الفروع)، صفرُ ملفٍّ >5MB، و`.env`/`.encryption_key` **لم يُودَعا قطّ**؛ وLFS أربعةُ أوزان `.onnx` لا غير. لكنّ أيَّ `git add` خاطئٍ لملفّ بياناتٍ = **نشرٌ علنيٌّ فوريّ** لا تسريبٌ داخليّ — ولا يُمحى من التاريخ بسهولة.

---

# خطّةُ الترحيل (cutover)

> **تعديلٌ 2026-09-09 بأمر المالك**: **المرحلتان 2 و3 مُستبدَلتان بـ§10** (رفعُ البيانات بملفّ النسخة المشفَّرة الذي يُنتجه التطبيق). وتبقيان هنا **مرجعاً للبوّابات**. والمراحل **0 و1 و4 و5 سارية** وتُقرأ مع §10.

الرموز: **[م]** مشغّلُ الخادم (طرفٌ آخر) · **[ن]** نحن محلّيّاً · **[مالك]** قرارٌ لا يُتجاوز.
قاعدة: **كلُّ خطوةٍ تنتهي بمخرَجٍ يُلصق هنا** (لا «تمّ»)، ولا خطوةَ تالية قبل خضرة بوّابة السابقة.

## المرحلة 0 — ما قبل النافذة (T-3 ⟶ T-1)

| # | الخطوة | مَن | بوّابة | الرجوع |
|---|---|---|---|---|
| 0.1 | حسمُ قرارات §6 وتثبيتُ موعد النافذة | مالك | جوابٌ مكتوبٌ على كلّ بند | — |
| 0.2 | توحيدُ التبويبات وتجميدُ git (§«التوحيد») | ن | `git status` نظيف · `main`=`origin/main` · السيرُ disabled | — |
| 0.3 | **استطلاعُ الخادم بلا تغيير** — يُرسل [م]: `df -h` · `free -m` · `swapon --show` · `psql -c "select version()"` · `\l+ lettersys` · `cd /var/www/lettersys && git rev-parse HEAD && git lfs ls-files` · أسماءُ مفاتيح `.env` فقط (`cut -d= -f1`) · `cat $(which lettersys-deploy)` · وحداتُ systemd · كتلةُ nginx للوسائط · `ls -la .encryption_key` · `which rsync pg_restore` · `models_healthcheck` | م | حرّ ≥ **20 GB** · HEAD=main · LFS أوزانٌ لا مؤشّرات · `DEBUG=False` · `MEDIA_ROOT=/var/www/lettersys/media` · `USE_X_ACCEL_REDIRECT=True` مع كتلة `internal` · `pg_restore` 18 | — |
| 0.4 | **الأساسُ المحلّيّ**: `showmigrations core\|tail -3` (0077 [X]) · `db_healthcheck --strict` · `models_healthcheck --strict --load --hash` · الحزمةُ الكاملة خضراء · الأعدادُ بـ`all_objects` · فحصُ الوسائط · فحصُ var/*.json · sha المفتاح=1e639b05 | ن | كلُّ الأرقام مطابقةٌ لـ§2 | — |
| 0.5 | **بيانُ الوسائط** (WSL): `cd /mnt/d/lettersys_data/media/books && find . -type f -print0\|sort -z\|xargs -0 sha256sum > /mnt/d/lettersys_data/manifests/books_<ts>.sha256` | ن | الأسطر = 13,932 | — |
| 0.6 | **المزامنةُ الكبرى للوسائط** (خارج النافذة، قابلةٌ للاستئناف): `rsync -rlt --partial --partial-dir=.rsync-partial --info=progress2 --chmod=D755,F644 --exclude '.rsync-partial' /mnt/d/lettersys_data/media/books/ <user>@<host>:/var/www/lettersys/media/books/` (لا `-a`؛ `--bwlimit` نهاراً) | ن+م | [م]: `sha256sum -c books_<ts>.sha256\|grep -vc ': OK$'`=**0** · `find\|wc -l`=13,932 · `df -h`≥4GB | `rm -rf` المجلّد المنسوخ |
| 0.7 | **شحنُ المفتاح** (scp أو لصقٌ في ssh — لا بريد/دردشة/git): `project/.encryption_key` وحده ⟶ `/var/www/lettersys/.encryption_key`؛ أيُّ مفتاحٍ ذاتيّ السكّ يُعاد تسميتُه | ن+م | [م]: sha[:8]=**1e639b05** · `wc -c`=**44** · `stat -c '%U %a'`=مستخدمُ الخدمة/600 | إعادةُ المُعاد تسميتُه |
| 0.8 | **تحصينُ الخادم** (لا يمسّ القاعدة): swap 2–4GB · `.env`: `TESSERACT_ADAPTIVE_THRESHOLD=0`, `OMP_THREAD_LIMIT=1`, `BACKUP_DIR` خارج `/` · gunicorn `--timeout 180` · celery `--concurrency 1` | م | `swapon --show` · ExecStart فيه timeout | إرجاعُ `.env.bak` |

## المرحلة 1 — التجميد (بدايةُ النافذة T-0)

| # | الخطوة | مَن | بوّابة | الرجوع |
|---|---|---|---|---|
| 1.1 | إبلاغُ المُدخِلين (sarah · zainab · abdulrahman) بوقف الإدخال HH:MM + تسجيلُ آخر رقمٍ لكلّ سجلّ | مالك | رسالةٌ مؤرّخة + الأرقام | — |
| 1.2 | إيقافُ الخادم المحلّيّ: `schtasks /End /TN LetterSysServer; schtasks /Change /TN LetterSysServer /DISABLE` + قتلُ runserver/celery | ن | لا مستمعَ على 8000 | `/Change /ENABLE; /Run` |
| 1.3 | **قفلُ القاعدة قراءةً فقط**: `ALTER DATABASE lettersys SET default_transaction_read_only=on;` + `pg_terminate_backend` للجلسات | ن | فحصٌ مزدوجٌ بفاصل 5د: `count,max(created_at)` ثابت · `auth_user`=12 | `... RESET default_transaction_read_only;` |

## المرحلة 2 — اللقطةُ النهائيّة ونسخةُ الشحن [ن]  **⟶ §10 — مُستبدَلة**
المبدأ: **القاعدةُ المجمّدة لا تُلمَس** (مصدرُ الرجوع)؛ التنظيفُ على نسخةٍ `lettersys_ship` = بروفةُ استعادةٍ كاملة.

| # | الخطوة | بوّابة | الرجوع |
|---|---|---|---|
| 2.1 | لقطةٌ خام (`PGPASSWORD` لا سطرَ الأوامر): `pg_dump(16) -U lettersys_user -d lettersys -Fc --no-owner --no-privileges -f D:\lettersys_data\backups\cutover_raw_<ts>.dump` | `pg_restore -l` يعمل · sha مسجَّل | — |
| 2.2 | بروفةُ استعادة: `createdb -T template0 -E UTF8 --locale=C lettersys_ship` ثمّ `pg_restore --no-owner --no-privileges --role=lettersys_user --exit-on-error -d lettersys_ship <raw>` | خروجٌ 0 · `DB_NAME=lettersys_ship migrate --check`=0 · 0077 | `dropdb lettersys_ship` |
| 2.3 | **تنظيفٌ في `lettersys_ship` فقط**: (أ) `purge_dev_seed_books` معاينةً ثمّ `--yes` (131+117 مرفق+إعادةُ بذر العدّادات) · (ب) `UPDATE core_emailsettings SET imap_sync_enabled=false;` · (ج) `UPDATE auth_user SET is_active=false, password='!'||md5(random()::text) WHERE username IN ('ui_probe_tmp','booklet_shot','agent_flow_79f151b1','_pwverify','admin10','testuser'[,'admin12' حسب §6]);` (تعطيلٌ لا حذف — CASCADE يشلّ سجلّات) · (د) `TRUNCATE django_session;` · (هـ) `core_backupsettings.enabled=false` | الأعداد `all_objects`: الكلّ **13,108** · نشط 13,095 · محذوف 13 · تدريب 0 · جهات 674 · ذاكرة 5,395 · `imap_sync_enabled=false` · `bookemaillog` failed/pending=0 · العدّادات 2433/358/455 | `dropdb` وإعادةُ 2.2 |
| 2.4 | نسخةُ الشحن: `pg_dump(16) -U postgres -d lettersys_ship -Fc --no-owner --no-privileges -f cutover_ship_<ts>.dump` + sha | الملفّان وبصمتاهما هنا | — |
| 2.5 | دلتا الوسائط: أمرُ 0.6 مع `-n -i` أوّلاً ثمّ فعليّاً | الجافّ 0 نقلٍ (أو ملفّاتٌ معدودةٌ تحت `books/`) ثمّ `sha256sum -c`=0 فشل | — |
| 2.6 | رفعُ `cutover_ship_<ts>.dump`+`.sha256` إلى `/var/backups/lettersys/incoming/` | [م]: `sha256sum -c` OK | — |

## المرحلة 3 — الاستعادةُ في قاعدةٍ جانبيّة [م] (التطبيقُ يعمل على القديمة بلا انقطاع)  **⟶ §10 — مُستبدَلة**

| # | الخطوة | بوّابة | الرجوع |
|---|---|---|---|
| 3.1 | نسخةٌ ممّا هو قائم: `pg_dump -Fc -f /var/backups/lettersys/pre_cutover_<ts>.dump lettersys` + `cp .env .env.pre_cutover` + لقطةُ VPS إن قرّرها المالك | ملفّان >0 | — |
| 3.2 | `CREATE DATABASE lettersys_new WITH OWNER lettersys_user ENCODING 'UTF8' LC_COLLATE 'C.UTF-8' LC_CTYPE 'C.UTF-8' TEMPLATE template0;` | `\l+` يُظهر C.UTF-8 | `DROP DATABASE lettersys_new` |
| 3.3 | `pg_restore -h localhost -U lettersys_user --no-owner --no-privileges --exit-on-error -d lettersys_new <ship>` ثمّ `ANALYZE;` (إن تعثّر على pg_trgm: أنشئه بـpostgres ثمّ أعد بلا `--exit-on-error` مع `2>restore.err` وتأكّد الأخطاءُ سطرا الامتداد فقط) | خروجٌ 0 (أو أخطاءُ الامتداد وحدها) | `DROP DATABASE lettersys_new` |
| 3.4 | بوّابةُ SQL على `lettersys_new`: عدُّ الكتب/المحذوف/التدريب · `count(core_entity)` · آخرُ هجرة · `imap_sync_enabled` · المستخدمون النشطون | 13,108/13/0 · 674 · 0077 · false · الحقيقيّون وحدهم | — |
| 3.5 | بوّابةُ Django (المتغيّرُ يغلب `.env` لأنّ التحميل `setdefault`): `DB_NAME=lettersys_new migrate --check` · `... models_healthcheck --strict --load` · **إثباتُ المفتاح**: `shell -c "from core.models import EmailSettings as E; e=E.get(); print(len(e.smtp_password or ''), len(e.imap_password or ''))"` · **إثباتُ الجهات**: فحصُ var/*.json ضدّ القاعدة | خروجٌ 0 · «13095/674/5395» · طولان>0 بلا ValueError · not-in-db=0 | إن فشل المفتاح: راجع 0.7 قبل أيّ تبديل |

## المرحلة 4 — التبديل (انقطاعٌ ≤ 10 دقائق) [م]

| # | الخطوة | بوّابة | الرجوع (≤3د) |
|---|---|---|---|
| 4.1 | `systemctl stop celery-beat celery gunicorn` | `pg_stat_activity` للقاعدتين=0 (وإلّا terminate) | `systemctl start` |
| 4.2 | psql واحدة: `ALTER DATABASE lettersys RENAME TO lettersys_old_<date>; ALTER DATABASE lettersys_new RENAME TO lettersys;` | `\l` يُظهر التبديل | العكسُ بإعادة التسمية |
| 4.3 | مسحُ كاش Redis: `redis-cli -n <N> FLUSHDB` (لا FLUSHALL) | `DBSIZE`=0 | — |
| 4.4 | `migrate --check`⟶`migrate`(لا-عمل)⟶`collectstatic --noinput`⟶`models_healthcheck --strict --load` | كلُّها 0 · لا «Applying» | — |
| 4.5 | `systemctl start gunicorn celery celery-beat` | `journalctl -u gunicorn -n50` بلا traceback · `curl -sI /login/`=200 | إيقاف+4.2 عكسيّاً+`.env.pre_cutover` |
| 4.6 | **دخانٌ بالعين** (مالك+ن): دخولٌ · القائمة 13,095 · كتابُ 2025 يعرض `825`+وسم · 2026 مجرّد · الرقمُ التالي 2433/358/455 · مرفقٌ يُفتح (بلا جلسة⟶login؛ غيرُ مالك⟶403) · بحثُ جهة · لوحةُ الأقسام · رفعُ مستندٍ وقياسُ زمن(<30ث) · `df -h` | كلُّها خضراء | كما 4.5 |

## المرحلة 5 — بعد التبديل
- **5.1** sarah تُدخل أوّلَ كتابٍ حقيقيّ (رقم 2433) بحضور المالك؛ قياسُ زمن OCR.
- **5.2** الخادمُ المحلّيّ لا يعود: القفلُ باقٍ، المهمّةُ معطَّلةٌ دائماً، وإعلامُ الجميع بالعنوان الجديد (يمنع انقساماً).
- **5.3** تفعيلُ IMAP عمداً في ساعة عملٍ ومراقبةُ `journalctl -u celery -f` 10 دقائق.
- **5.4** الاحتفاظُ بـ`lettersys_old_<date>` والنسخ 7 أيّام ثمّ `DROP` · نسخةٌ خارج الخادم.
- **5.5** إصلاحُ مفتاح النشر ثمّ تمكينُ السير (أدناه).

## التوحيدُ (§قبل T-1)
**الحال**: A=`main` نظيف · B=`../lettersys_rollout` قد يحمل فرعَ عملٍ بإيداعاتٍ غير مدفوعة · C=`.claude/worktrees/agent-*` بلا شيءٍ فريد.
1. **B**: يُنهي بندَه ⟶ إيداعٌ بمساراتٍ صريحة ⟶ `push -u origin <فرع>` ⟶ `gh pr create --draft` («لا يُدمَج قبل الترحيل») ⟶ يُغلق التبويب (الشجرةُ تبقى — فيها `.env` والمفتاحُ الآخر).
2. **C**: `git worktree remove --force …` ثمّ `git branch -D worktree-agent-…`.
3. الفروعُ المدموجة (تأكّد `git log main..<فرع>` فارغ): تُحذف محلّيّاً وبعيداً.
4. وسمٌ مرجعيّ: `git tag cutover-2026-09 <main> && git push origin cutover-2026-09`.
5. **A هو الناجي**: قراءةٌ ووثائقُ فقط؛ أيُّ كود جديد في فرعٍ لا يُدمَج حتّى ما بعد الترحيل.

**قواعدُ النافذة** (T-1 ⟶ القبول): لا دفعَ لـmain · لا دمجَ PR · لا `gh workflow run` · السيرُ disabled · لا `runserver/shell/seed_*/import_legacy/run_restore_job/prepare_entities` على أيّ قاعدةٍ حيّة · `Merge9.md` يُلحَق لا يُكتب فوقه.

## §5 — سجلُّ المخاطر (الأخطر)
1. **المفتاح** — شحنُ 67572ad5 أو تلفُ نسخٍ (CRLF/BOM) ⟵ `ValueError` صامتٌ في البريد. المنع: بوّابةُ sha+حجم (0.7) + إثباتُ الفكّ (3.5). احتياط: إعادةُ إدخال كلمتَي البريد من الإعدادات.
2. **معرّفاتُ الجهات** — أيُّ إعادةِ استيراد/بذرٍ يقلب `var/*.json` بصمت. المنع: `pg_restore` فقط + حظرُ import/seed + فحصُ التطابق (3.5).
3. **القرص 31GB** — 16GB وسائط+torch+PG+swap+مرآةُ نسخ ⟵ امتلاء. المنع: بوّابة ≥20GB/≥4GB + `BackupSettings` معطَّل خارج `/` + لا شحنَ scan_inbox/tmp.
4. **IMAP فوراً** — سحبُ بريد الشركة من IP جديد بلا قرار. المنع: إطفاؤه في الشحن (2.3ب) + تفعيلٌ يدويٌّ مراقَب (5.3).
5. **SECRET_KEY/الجلسات/الكاش** — جلساتٌ/كاشٌ من عهد القاعدة الفارغة. المنع: يبقى مفتاحُ الخادم + `TRUNCATE django_session` + `FLUSHDB` عند التبديل.
6. **كتابةٌ عرضيّة في المصدر بعد اللقطة** ⟵ قفلُ القراءة (1.3).
7. **الاستعادةُ تتعثّر على pg_trgm/ACL** ⟵ مالكُ القاعدة ينفّذ + `--no-privileges` + بديلُ 3.3.
8. **أسماءٌ عربيّة عبر rsync** ⟵ WSL(UTF-8)+`LANG=C.UTF-8`+بيانُ sha256.
9. **تسريبُ الأسرار عبر `dumpdata`** (وقع فعلاً — §8): `from_db` يفكّ فيكتب الأمرُ الصريح. المنع: `EncryptedCharField` + حارسُ `db_healthcheck` + تظليلُ الأمر (§8.6).
12. **أعدادٌ مطابقةٌ بمحتوىً قديم** — نسخةُ 09-08 أعدادُها تطابق حرفيّاً ومحتواها أقدم (`merged_into` **198** مقابل **256**). **البصمةُ لا العدّ** (§10.1).
13. **`*.enc` لم يكن متجاهَلاً** في مستودعٍ عامّ — أُغلق 2026-09-09.
11. **المستودعُ عامّ**: خطأُ `git add` واحدٌ يُنشر للعالم. المنع: `.gitignore` مُحصَّن (§8) + **لا يُودَع dump/json بيانات أبداً** + مراجعةُ `git status` قبل كلّ إيداع.
10. **عمليّاتٌ يدويّةٌ خارج الخطّة**: تُكشف بـ`n_tup_ins`/`xact_rollback`/`ids>13386`؛ المنع قيدُ §0 الجديد.

## §6 — قراراتُ المالك المعلّقة (لا إقلاعَ قبل الحسم)
1. موعدُ النافذة ومدّتها ومَن يبلّغ المُدخِلين.
2. الحسابات: تعطيلُ الستّة؛ **`admin12` (9 كتب) تدريبٌ أم حقيقيّ؟** كتابا `testuser` يُنقلان لـadmin أم يبقيان معطَّلين؟
3. حذفُ 131 تدريب + 117 مرفقاً — نهائيٌّ على الإنتاج (الرجوعُ عبر `cutover_raw`).
4. البريد: متى IMAP، وهل SMTP من الخادم (كلمةُ تطبيق Gmail من IP جديد؟).
5. القرص: هامش ~4–5GB أم ترقيةٌ قبل الوسائط؟ ووجهةُ النسخ خارج الخادم واحتفاظُها.
6. حسابُ النقل لـrsync: يُنشئه [م]، أم يسحب من مخزنٍ نوفّره؟
7. مصيرُ النسخة المحلّيّة: أرشيفٌ للقراءة (موصى) أم احتياطٌ (انقسامٌ محتمل).
8. النشرُ التلقائيّ: إعادتُه على كلّ دفع أم يدويٌّ دائماً؟ وإصلاحُ المفتاح: تركيبُ العامّ المطابق (أ) أم تدويرُ الزوج (ب)؟
9. TLS/النطاق وأعلامُ الكوكي — لا تُغيَّر في النافذة.
10. لقطةُ VPS من Hostinger قبل التبديل.
11. دمجُ `feat/mail-archive-attribution` بعد الاستقرار فقط.
12. تدويرُ كلمة سرّ SQL Server القديمة (`docs/LAUNCH_RUNBOOK.md`).
13. **تدويرُ كلمة تطبيق Gmail — توصيةٌ حاسمة: نعم، اليوم**، بعد إطفاء IMAP مباشرةً (§8.4). القناةُ التي نُقل بها الملفُّ مجهولة.
14. إن ثبت التحميلُ وفوقه عملٌ >20 كتاباً: `pg_restore` من الصفر أم البناءُ على المُحمَّل بتجميدٍ محلّيٍّ فوريّ وترقيعِ الـ45 المحذوفة؟ (يُلغي نافذةَ الترحيل المخطّطة.)
15. **بأيّ قناةٍ نُقل `lettersys_data.json` وأين نسخُه الآن؟** — جوابٌ مكتوب.

## إعادةُ تمكين النشر (بعد الاستقرار، §5.5)
1. يومان عملٍ بلا رجوع و`lettersys_old` باقية.
2. **إصلاحُ المفتاح** (يحسم المالك): (أ) [م] يُلحق العامَّ المطابقَ لسرّ GitHub في `authorized_keys`؛ أو (ب) [ن] يولّد زوجاً جديداً ⟶ `gh secret set DEPLOY_SSH_KEY` ⟶ [م] يركّب العامّ. التحقّق: `ssh-keygen -lf` تتطابق.
3. مراجعةُ `lettersys-deploy` (لا `makemigrations` أبداً؛ `models_healthcheck --strict` بوّابة).
4. `gh workflow enable deploy.yml` (والملفُّ `workflow_dispatch`-فقط) ⟶ `gh workflow run` يدويّاً ⟶ `gh run watch` ⟶ نجاحٌ + HEAD المتوقّع.
5. بعد نجاحٍ يدويّ: PR يُعيد سطرَي `push` — دمجُه أوّلُ نشرٍ تلقائيّ.
6. دمجُ `feat/mail-archive-attribution` (بعد rebase) — لا قبل.

## توجيهُ المحادثة الجديدة
1. اقرأ `MEMORY.md` + هذه الوثيقة، وأعد التحقّق من §1.
2. **لا تلمس الخادمَ ولا تُفعّل النشر.**
3. البدءُ الطبيعيّ: **المرحلة 0** (0.2 التوحيد ⟶ 0.4 الأساس المحلّيّ) — كلُّها محلّيّةٌ/قراءة، بلا مخاطرة، وتُجهّز كلَّ ما يلزم.
4. لا تتقدّم للمرحلة 1 (التجميد) قبل حسم قرارات §6 وموعدِ النافذة من المالك.
5. أَلحِق مخرَجاتِ كلّ خطوةٍ في هذه الوثيقة تحت «سجلُّ التنفيذ» أدناه.
6. **للعمل على المنتَج لا الترحيل**: اقرأ **§7** (تبويب 1) — فيه ما بُني وما بقي
   بترتيبِ أولويّةٍ مُعلَّل، والديونُ المسجَّلة، وقواعدُ الحرفة، و**سبعةُ فخاخٍ
   مقيسة** تُوفّر ساعات. وحسمُه: **الأولويّةُ التشغيلُ لا ميزةٌ جديدة** — مجموعةُ
   «مسؤول الأرشفة» فارغةٌ حتى الساعة، فكلُّ ما بُني بلا مستخدمٍ واحد.

---

## §7 — تبويب 1 (التطوير): ما بُني · ما بقي · كيف يُستأنف

> **مالكُ هذا القسم: التبويب 1.** لا يُحرَّر من تبويبٍ آخر (يُلحَق تحته فقط) — ثلاثةُ
> كتّابٍ على ملفٍّ واحد يتعارضون إن تداخلت أقسامُهم.
> **علاقتُه بالوثائق القائمة**: هذه الوثيقةُ **للترحيل والتوحيد**؛ الخطّةُ التفصيليّة
> للمنتَج في `COMPANY_ROLLOUT_PLAN_v2_2026-08-31.md`، وسجلُّ الأخطاء وجذورُها في
> `IMPLEMENTATION_NOTES.md`. **لا يُنسَخ محتواهما هنا** — يُشار إليه، وإلّا صارت
> نسخةً ثالثةً تنحرف عنهما (وهو الجذرُ الذي كلّف هذا المشروعَ ثماني مرّات).

### 7.1 — الحالُ الآن (مقيسٌ 2026-09-09)

| | |
|---|---|
| فرعي | `feat/mail-archive-attribution` مُرصَّفٌ على `main@8230e91` · **مدفوع** · **PR #16 مسوّدة** |
| الاختبارات | **1568 خضراء** على الفرع · صفرُ هجراتٍ معلّقة · 270 مساراً بلا تكرار |
| شجرتي | `../lettersys_rollout` نظيفة — **تبقى** (فيها `.env` والمفتاحُ الآخر `67572ad5` الذي **لا يُشحَن**) |
| المُعلَّق عليّ | لا شيء. البندُ 1 من §التوحيد **مُنفَّذ** |

### 7.2 — ما بُني ودُمج في `main` (الدفعة 4‑ج وما قبلها)

**دورُ «مسؤول الأرشفة» كاملاً** — والقرارُ المعماريُّ الحاكم مقيسٌ لا ذوقيّ:

> **الأرشفةُ حدثُ عهدةٍ (`CustodyEvent.ARCHIVE_DONE`) لا علَمٌ منطقيّ.** الحقلُ
> `Book.is_archived` مرفوعٌ على **13,188 من 13,194** كتاباً، **13,174** منها لمجرّد
> غياب `due_date` (القاعدةُ الذهبيّة في `Book.save()`). فمعناه «انتهت المتابعة» لا
> «حُفظت الورقة»، وإعادةُ استعماله كانت تُنتج: طابوراً يولد كاذباً · إعادةَ فتحٍ
> مستحيلةً لـ13,174 كتاباً · علَماً بلا وقتٍ ولا فاعلٍ ولا موضعِ حفظ.

- `core/archive_service.py` — مسارُ الكتابة الوحيد: `archive_book` · `reopen_archive`
  (بسببٍ إلزاميّ، وحدثٌ **مخصَّص** `ARCHIVE_REOPEN` لأنّ `RETURN` المشترك لو أُخذ
  علامةَ خروجٍ لألغى رجوعُ متعهّدِ بريدٍ حفظَ الورقة صامتاً) · `is_archived` وتوأمُها
  الاستعلاميّ بـ`Subquery` على **آخر** حدثٍ أرشيفيّ.
- **البوّابةُ ثلاثيّة**: يفتح المحتوى · أرشيفيّ · **والكتابُ من أرشيف شجرته**
  (الثالثُ كان ناقصاً: الكتابُ المُفرَّق إلى وحدةٍ مرئيٌّ لأرشيفيّها وليس له إغلاقُ
  ملفّ القسم المالك). والإذنُ **قبل** فحص الحال وإلّا سرّبت رسالةُ الرفض.
- **البابُ المجاور مُغلق**: حواريّةُ العهدة تُغذّى من `EVENT_CHOICES` كلِّها، فمختصُّ
  البريد كان يختار «تمامَ أرشفة» ويتجاوز كلَّ القواعد. الآن `record_custody` ترفض
  حدثَي الأرشفة، و`record_archive_event` بابٌ خاصّ، والجسدُ المشترك `_write_event`.
- `/books/desk/archive/` — **خمسةُ طوابيرَ مقيسة**، وفيها فعلٌ في صفّ الطابور.
- شرطُ **إغلاق المتابعة قبل الحفظ** (قرارُ المالك) · «أرشيف الشركة» موسّعَ نطاقٍ لا دوراً رابعاً.
- هجرة **0077** (مفتاحا سجلّ + حدثُ عهدةٍ سادس) — مُطبَّقةٌ على القاعدة المحلّيّة.

**وثلاثةُ إصلاحاتِ واجهةٍ عامّة**: رابطُ «سجلّ الحركات» (بوّابةُ `nav.audit` كانت
مبنيّةً ولا يستعملها أحد) · توحيدُ لهجة الإشعار على `ToastCenter` (كانت خمسَ لهجات) ·
`nav_gates` — الشريطُ كان يعرض «طاولة الوارد» للجميع ويردّ 403 لأكثرهم.

**والقياسُ أسقط ثلاثةً من طوابير الخطّة الخمسة** — وهذا الدرسُ يسري على كلّ طابورٍ قادم:
| الطابورُ المقترَح | المقيس | الحكم |
|---|---|---|
| «مرفقٌ بلا نصّ» | 13,148 صفّاً · تغطيةُ المسار الحيّ **19/19** | تقريرُ إرثٍ لا طابورَ عمل |
| «مسحٌ حديثٌ بلا نصّ» | `uploaded_at` على الإرث = **وقتُ تشغيل سكربت الاستيراد** | كاذب |
| «عهدةٌ خارجة» | هو `undelivered` — طابورُ مختصّ البريد | مكرّر |

> **قاعدةٌ لكلّ استعلامٍ يمرّ على الكتب**: يحمل `source_ref=''` و`is_training=False`
> وإلّا وُلد كاذباً بـ13 ألف صفّ. **وطابورٌ لا يُفرَغ يُهجَر في أسبوع، ويُهجَر الدورُ معه.**

### 7.3 — ما في PR #16 (مسوّدة، لا تُدمَج قبل القبول)

**«مسؤول إدارة البريد والأرشفة» = اجتماعُ عضويّتين لا مجموعةٌ ثالثة** — كي يبقى بابُ
الفصل مفتوحاً حين يقتسمون العملَ بحسابين (وهو ما أعلن المالكُ أنّه قادم). وشرطُه كان
عيباً قائماً: `is_mail_officer` على التسمية الأحاديّة تُسقط قسمَ البريد عن رئيس شعبةٍ
خانتُه مؤشَّرة ⟵ صارت **عضويّة**.

**و«مَن قام بهذا العمل»** — النسبةُ كانت محفوظةً في القاعدة (`by_snapshot` ·
`recorded_by` · `created_by`) **ولا تُعرض في سطرٍ واحد**؛ وبطاقةُ السجلّ كانت تعرض
المفتاحَ الإنكليزيَّ الخام بلا اسمِ فاعلٍ إطلاقاً. صارت «تسميةٌ عربيّة — اسمُ الفاعل —
الوقت»، و«قيّده فلان» في العهدة و«فرّقه فلان» في التفريق، والحدُّ انتقل من القالب إلى SQL.

### 7.4 — ما بقي من خطّة المنتَج (بعد الترحيل)

**والحسمُ الصريح: الأولويّةُ التشغيلُ لا ميزةٌ جديدة.** النظامُ لم يُشغَّل على قسمٍ
ثانٍ بعد، ومجموعةُ «مسؤول الأرشفة» **فارغةٌ حتى الساعة** — فكلُّ ما بُني في 4‑ج بلا
مستخدمٍ واحد. بناءُ 4‑ب قبل أن يعمل أحدٌ على 4‑ج يُراكم كوداً غيرَ مُجرَّبٍ بيدِ بشر.

| # | البند | الحجم | يعتمد على |
|---|---|---|---|
| **أ** | **تعيينُ مسؤول الأرشفة** من `/books/admin/?tab=users` (خانتا «طاولة البريد» و«طاولة الأرشفة») + **اصطلاحُ تسمية الرفوف** (`خ2/ر3/ص7` مثلاً) | دقائق — **قرارُ مالكٍ لا كود** | القبول |
| **ب** | أسبوعُ استعمالٍ حقيقيّ ثمّ **مراجعةُ ما استُعمل فعلاً** من الطوابير الخمسة (أيُّها فُرِّغ؟ أيُّها هُجر؟) | يومٌ رصد | أ |
| **ج** | دمجُ PR #16 بعد رَصفٍ جديد | ساعة | القبول |
| **د** | **4‑ب**: `/books/desk/chat/` بريدُ الوحدات (ثلاثةُ أعمدة، علاماتُ ✓/✓✓ ورقيّ ونظاميّ/قُرئ/رُدّ) + `/books/desk/mine/` صفحةُ رئيسَي الشعبة والوحدة + هجرة 0078 (`received_at`·`opened_at`·`opened_by`·`done_at`) | **الأكبر** — 3–4 جلسات | ب (وإلّا بُني على تخمين) |
| **هـ** | **الدفعة 5** — الجذور: كنسُ 73 موضع `is_deleted=False` · الشريطُ بالدور · حارسُ «مبنيٌّ ولا يوصله أحد» · حارسُ «لا شرطَ دورٍ في قالب» | جلسة | ج |
| **و** | 6 التخويلُ المتبقّي · 7 الواجهاتُ القديمة · 8 اللحظيّ (**يحتاج Redis** — قرارُ ز0) · 9 التدريبات · 10 التواقيع طور 2 | لاحقاً | — |

### 7.5 — ديونٌ مسجَّلةٌ (لا تُنسى ولا تُصلَح ارتجالاً)

1. **`_unauthorized_secret_q` لا تعرف `_has_live_grant`** بينما `secret_access` تمنح
   `FULL` بمنحةٍ فرديّة — انفراجٌ **قائمٌ قبل عملي**، اتّجاهُه محافظ (يحجب عن صاحب
   الحقّ ولا يُسرّب). الإصلاح: شرطُ المنحة في الحارس + فاعلٌ عاشرٌ بمنحةٍ في حارس التطابق.
2. **سطران بـ`is_staff` في `core/extraction/views/ui.py`** (88 و147) يمنعان الأرشيفيَّ
   من **تعديل كتابٍ قائم** من الإدخال الذكيّ (يُدخل الجديدَ بلا مشكلة). منطقةُ التبويب 2
   — يُصلَحان بـ`can_open_content` حين تُفتح.
3. **`core/views/users.py`** نظامُ أدوارٍ **موازٍ ميّت** بأسماءِ مجموعاتٍ إنكليزيّة
   (`controller`, `data_entry`) لا تراه البوّابات — يُزال، ولا يُضاف إليه شيء.
4. **مجموعةُ «مسؤول الأرشفة» فارغة** — الدورُ جاهزٌ بلا مستخدم (البند أ أعلاه).

### 7.6 — قواعدُ الحرفة (تسري على كلّ ما يُكتب بعد اليوم)

مصدرٌ وحيدٌ لكلّ قاعدةِ صلاحيّةٍ في `core/scoping.py` · **لا شرطَ دورٍ في قالب** ·
«منجَز» = طرفٌ إلى طرفٍ في المتصفّح **بلقطةٍ تُقرأ** لا خدمةً خضراء · **كلُّ حارسٍ
يُطفَّر** (يُعطَّل الكودُ الذي يحرسه فيجب أن يحمرّ — 36 طفرةً حمراء هذه الجلسة، وثلاثٌ
منها كشفت حرّاساً **زائفين** يمرّون على كودٍ لا يعمل) · لا `except` واسعٌ حول أثر ·
العددُ قبل القطع · الرسالةُ من الخادم بنصّها · **404 لا 403** خارج النطاق · الإلحاقُ
لا الكتابةُ فوق الوثائق · المخرَجُ الطويل إلى ملفّ (الجهاز 8GB).

### 7.7 — فخاخٌ مقيسةٌ تُوفّر على المحادثة الجديدة ساعات

- **الشجرةُ الخطأ**: `main` و`lettersys_rollout` مجلّدان مختلفان. شغّل الخادمَ من
  الشجرة التي تُعدّلها وإلّا رأيتَ نظاماً بلا تعديلاتك (وقع للمالك فعلاً).
- **`networkidle` لا يتحقّق** في هذا التطبيق (نبضٌ دوريّ) — انتظر العنصر بـ
  `domcontentloaded` + `wait_for_selector`.
- **PWA**: أيُّ تعديل CSS/JS يلزمه رفعُ `?v=` في القالب **و**`CACHE_VERSION` في
  `service-worker.js` (الآن v97) وإلّا لم تظهر التغييراتُ للمستخدم.
- **`Book.save()` يرفع `is_archived` لكلّ كتابٍ بلا `due_date`** — لا تبنِ عليه شيئاً.
- **`exclude(field=v)` على تعليقٍ قابلٍ لـNULL يُسقط الصفوفَ الفارغة صامتاً** (اصطاده
  حارسٌ بعد أن أخفى طابورُ الأرشفة ما لم يُحفظ قطّ).
- **`bulk_create` يتجاوز `save()`** — مرِّر اللقطاتِ صراحةً وإلّا ضاع الأثر.
- **الطرفيّةُ التفاعليّة تُشوّه بايثون المُسنَّم**: استعمل `python -c` مع `django.setup()`
  لا `manage.py shell < file` (تعطّلت مرّتين على مسافاتٍ بادئة).

---

## §8 — الرفعُ اليدويّ 2026-09-08 (خارج الخطّة) وأمرُ الاستطلاع S‑0

> **مالكُ هذا القسم: التبويب 1.** أُضيف 2026-09-09 بعد اكتشافِ رفعٍ يدويٍّ لملفّ
> `dumpdata` إلى الخادم خارج مراحل الترحيل. **الحالةُ مجهولةٌ حتى يعود مخرَجُ S‑0.**

### 8.1 — ما حدث ومقاسُه (محلّيّاً، بالتنفيذ لا بالافتراض)

`lettersys_data.json` بجذر المشروع — **2026-09-08 10:50 · 27 MB · 55,617 صفّاً** —
ناتجُ `dumpdata --natural-primary --natural-foreign` **بلا `--all`**.

| | |
|---|---|
| كتب | **13,194** (ينقص **45** محذوفاً ناعماً — المديرُ الافتراضيّ أخفاها) · **99** تدريب |
| جهات | **674 بمفاتيحها** (`pk=2`) ⟵ خطرُ انحراف `var/*.json` **مُغلَق** |
| مستخدمون | **12 بلا `pk`** — والمفاتيحُ الأجنبيّة بالأسماء (`created_by=['admin12']`) |
| جلسات | **281** · `imap_sync_enabled` = **`True`** · `backupsettings.enabled` = `False` |
| مرفقات | 13,173 · مساراتٌ نسبيّةٌ كلُّها · **صفرُ** مساراتٍ مطلقة |
| `contenttypes`/`auth.permission` | **صفر صفّ** ⟵ لا تصادمَ مع `migrate` |

**🔴 تسريبٌ مُثبَت**: `smtp_password`/`imap_password` في الملفّ **16 حرفاً بنصٍّ صريح**
(كلمةُ تطبيق Gmail)، بينما عمودُ القاعدة المحلّيّة **125 حرفاً بادئتُه `enc::`** أي
**مشفَّرٌ سليم**. الجذرُ حلقةٌ مغلقة:

- `EncryptedFieldsMixin.from_db` (`core/models.py:1341`) **يفكّ** عند التحميل ⟵
  `dumpdata` يقرأ الكائنَ المفكوكَ فيكتب الصريح.
- `DeserializedObject.save()` في جانغو 4.2 يستدعي `save_base(raw=True)` وتعليقُها
  حرفيّاً *«This bypasses any model-defined save»* ⟵ `save()` **الذي يشفّر لا يُستدعى**،
  فتُكتب الكلمةُ صريحةً في عمود الإنتاج.
- و`from_db` تفكّ فقط `if is_encrypted(value)` ⟵ **الصريحُ يمرّ والبريدُ يعمل والعطبُ صامت**.

**والوثيقةُ دعت إلى الفعل**: رأسُ `scripts/pg_setup/pg_migrate.py` يوثّق مسارَ
`dumpdata`/`loaddata` — فالمالكُ اتّبع ما هو مكتوب. (يُصحَّح: البند 8.5‑د.)

الملفُّ **لم يُودَع قطّ** (`git log --all` فارغ)، و`.gitignore` حُصِّن اليوم.

### 8.2 — أمرُ الاستطلاع S‑0 (قراءةٌ فقط) — يُرسَل إلى [م]

`loaddata` **ذرّيّ** (يلفّ الملفَّ في `transaction.atomic`) فلا وجودَ لـ«تحميلٍ جزئيّ»:
إمّا 13,194 أو صفر. لكنّ الانقطاعَ يترك بصمةً في `n_tup_ins` و`xact_rollback`.

```bash
cd /var/www/lettersys && .venv/bin/python manage.py dbshell -- -Atc "
SELECT 'books='||count(*)||' ids>13386='||count(*) FILTER (WHERE id>13386)||' training='||count(*) FILTER (WHERE is_training)||' last_created='||coalesce(max(created_at)::text,'-') FROM core_book
UNION ALL SELECT 'entities='||count(*)||' max_id='||coalesce(max(id),0) FROM core_entity
UNION ALL SELECT 'history='||count(*)||' attachments='||(SELECT count(*) FROM core_attachment)||' sessions='||(SELECT count(*) FROM django_session) FROM core_bookhistory
UNION ALL SELECT 'users='||count(*)||' ['||string_agg(username,' ' ORDER BY username)||']' FROM auth_user
UNION ALL SELECT 'mail: enc='||(smtp_password LIKE 'enc::%')||' len='||length(smtp_password)||' imap_sync='||imap_sync_enabled||' active='||is_active FROM core_emailsettings
UNION ALL SELECT 'incoming_emails='||count(*) FROM core_incomingemail
UNION ALL SELECT 'pgstat book: ins='||n_tup_ins||' dead='||n_dead_tup FROM pg_stat_user_tables WHERE relname='core_book'
UNION ALL SELECT 'pgstat db: rollback='||xact_rollback||' size='||pg_size_pretty(pg_database_size(current_database())) FROM pg_stat_database WHERE datname=current_database()
UNION ALL (SELECT 'migration_head='||name FROM django_migrations WHERE app='core' ORDER BY id DESC LIMIT 1);"
```

**بديلٌ لا يحتاج `psql`** (مُوصىً به — صيغةُ `dbshell -- -Atc` تعتمد على عميل psql،
وغيابُه عن مسار الخادم يوقف الاستطلاع عند أوّل خطوة؛ والسكربتُ مُجرَّبٌ على القاعدة
المحلّيّة ويطبع خطَّ الأساس للمقارنة):

```bash
cd /var/www/lettersys && .venv/bin/python manage.py shell -c "exec(open('scripts/probe_s0.py').read())"
```

**خطُّ الأساس المحلّيّ** (قِيس 2026-09-09 على القاعدة المُجمَّدة — للمقارنة لا للحكم):
`books=13239 | ids>13386=0 | training=131 | last_created=2026-08-30 07:38:39` ·
`entities=674` · `history=22313 attachments=13187 sessions=282` · `users=12` ·
`mail: enc=True len=125 imap_sync=True active=True` · `incoming_emails=1` ·
`migration_head=0077_archive_events_and_history`.
لاحظ: المحلّيّةُ تعدّ **13,239** بـ`all_objects` (تشمل 45 محذوفاً ناعماً)، والملفُّ
صدَّر **13,194** — فالفارقُ 45 متوقَّعٌ لا عطب.

**وإشاراتٌ ثانويّة (قراءةٌ فقط):**
```bash
find / -xdev -name 'lettersys_data*.json' -printf '%TY-%Tm-%Td %TH:%TM %s %p\n' 2>/dev/null
grep -h 'loaddata\|lettersys_data' /root/.bash_history /home/*/.bash_history 2>/dev/null | tail -20
journalctl --since '2026-09-08' --no-pager | grep -iE 'loaddata|out of memory|IntegrityError' | tail -20
```

### 8.3 — جدولُ الحكم

| المخرَج | الحكم | الإجراء |
|---|---|---|
| `books=13194` · `ids>13386=0` · `enc=f` | **حُمِّل ولم يُعمَل عليه** | 8.4 كاملاً ثمّ **`pg_restore` من الصفر** (الخسارةُ صفر، والمكسبُ استرجاعُ السلّة والمسارُ المُبوَّب) |
| `ids>13386` بين 1 و20 | حُمِّل وفوقه عملٌ يسير | 8.4 ثمّ `pg_restore` بعد تصدير الجديد لإعادة إدخاله يدويّاً |
| `ids>13386` > 20 | **المُدخِلون بدؤوا** | 8.4 ثمّ **قرارُ مالكٍ** (§6/14): البناءُ عليه بتجميدٍ محلّيٍّ فوريّ + ترقيعِ الـ45 المحذوفة |
| `books=0` · `ins=0` · `rollback` صغير | **لم يُحمَّل** | 8.5 (إلغاءُ المسار) — والتدويرُ يبقى واجباً |
| `books=0` · `ins≈13194` أو `dead>0` | **حُوِّل وانقطع** | كـ«لم يُحمَّل»، لكنّ الملفَّ على قرص الخادم قطعاً ⟵ إتلافُه |
| `0<books<13194` | **ليس من هذا الملفّ** (الذرّيّةُ تمنعه) | استقصاءٌ: قواعدُ أخرى؟ إدخالٌ يدويّ؟ |

**و`migration_head` حاسمٌ بطريقةٍ ثانية**: الملفُّ أُنشئ على مخطّط **0077**؛ فلو كان
الخادمُ دونه لَما نجح `loaddata` أصلاً (حقولٌ مجهولة ⟵ رفضٌ وتراجع). ولو كان عنده،
فقد جرى `migrate` على الخادم خارج الخطّة أيضاً.

### 8.4 — إن حُمِّل: العلاجُ بالترتيب (الأعجلُ أوّلاً)

**الأعجلُ IMAP لا كلمةُ المرور** — الكلمةُ الصريحةُ ضررٌ ساكن، وIMAP **يُنتج ضرراً
جديداً كلَّ 10 دقائق** (بريدُ شركةٍ يُسحَب إلى قاعدةٍ لم تُقبَل وبقرارٍ لم يُتَّخذ §6.4).

| الوقت | مَن | الفعل |
|---|---|---|
| **د0** | [م] | `UPDATE core_emailsettings SET imap_sync_enabled=false, is_active=false;` — نافذٌ فوراً بلا إعادة تشغيل (`sync_inbox_task` يقرأ القاعدةَ كلَّ تشغيل) |
| **د1** | مالك | **تدويرُ كلمة تطبيق Gmail** (حذفُ القديمة يُبطلها في كلّ مكان: الملفّ · WAL · النسخُ · القناةُ المجهولة) |
| **د3** | [م] | شحنُ المفتاح `1e639b05` (Merge9 بند 0.7) — **قبل** أيّ إعادةِ تشفير، وإلّا سكّ النظامُ مفتاحاً ثالثاً |
| **د6** | مالك | إدخالُ الكلمة الجديدة من صفحة الإعدادات (يمرّ على `save()` فتُشفَّر) — تكفي |
| **د8** | [م] | إثباتٌ بلا طبع سرّ: `smtp_password LIKE 'enc::%'` = `t` وطولٌ ≈125 |
| **د9** | [م] | `shred -u -z` لكلّ مسارٍ أخرجه `find` + إعادةُ `find` = صفر · ونسخُ الخادم من 09-08 تُحذف أو تُعلَّم «ملوَّثة» |

**التدويرُ ليس مبالغة**: القناةُ مجهولة · الصريحُ في WAL والنسخِ لا تصلُه إعادةُ
التشفير · وكلمةُ التطبيق تفتح صندوقَ الشركة قراءةً وإرسالاً. الكلفةُ دقيقتان.

**التنظيف** (99 تدريباً · 6 حسابات · 281 جلسة) — **فقط إن تقرّر البناءُ على المُحمَّل**؛
ومع `pg_restore` لا يُنظَّف شيءٌ لأنّه سيُمحى. و`admin12` **لا يُعطَّل** قبل حسم §6.2
(9 كتبٍ بـ`created_by` عليه)، والحساباتُ تُعطَّل ولا تُحذف (مفاتيحُ أجنبيّة).

### 8.5 — إن لم يُحمَّل: يُلغى المسارُ لا يُصلَح الملفّ

`dumpdata` **صنفٌ معيبٌ لهذا الغرض** لا ملفٌّ معيب: بلا سلّة المحذوفات · تحميلُه
يتخطّى `save()` فأيُّ حقلٍ مشفَّرٍ قادمٍ يعود صريحاً · يدمج المستخدمين **بالاسم**
فيكتب فوق `admin` الخادم. الخطّةُ تنصّ على `pg_restore` وهذا سببُها بعينه.

- الملفُّ: `shred` على الخادم · حذفٌ محلّيّ (والتدويرُ — لا الحذفُ — هو الضمان).
- **سؤالٌ للمالك يُوثَّق جوابُه**: **بأيّ قناةٍ نُقل الملفّ، وأين نسخُه الآن؟**

### 8.6 — الوقايةُ البنيويّة (دَينٌ مُسجَّل، أربعةُ بنود)

**(أ) الإصلاحُ الجذريّ — نقلُ التشفير من `save()` إلى الحقل**: `EncryptedCharField`
بـ`from_db_value` (فكّ) · `get_prep_value` (تشفير — **يُستدعى حتّى مع `raw=True` ومع
`QuerySet.update()`** فيسدّ ثغرتين) · `value_to_string` (يُخرج `enc::` لا الصريح).
ويُحذف `EncryptedFieldsMixin` فلا يبقى مساران. هجرةٌ واحدة `AlterField` بلا SQL فعليّ.
**بخمسة حرّاسٍ مطفَّرة**: `dumpdata` لا يحوي الصريح · `loaddata` لصريحٍ يُخزَّن مشفَّراً ·
لا تشفيرَ مضاعفاً لـ`enc::` · `update()` يشفّر · مفتاحٌ مبدَّلٌ يبقى `enc::` بلا استثناء.

**(ب) حارسٌ في `db_healthcheck` وفحصِ الإقلاع**: لكلّ عمودٍ مشفَّر
`count(*) WHERE col<>'' AND col NOT LIKE 'enc::%'` = صفر، وإلّا خروجٌ ≠0 يُسمّي
الجدولَ والعمودَ **دون القيمة**. **ولا شفاءَ ذاتيّاً** — الشفاءُ يسكّ مفتاحاً ثالثاً صامتاً.

**(ج) تظليلُ `dumpdata`**: أمرٌ يرث الأصليَّ ويرفض الكتابةَ داخل المستودع، ويستثني
النماذجَ ذاتَ الأسرار، ويفرض `--all`، ويفحص مخرَجَه فيحذفه ويفشل إن وجد حقلاً مشفَّراً.

**(د) تصحيحُ الوثيقة الداعية**: رأسُ `scripts/pg_setup/pg_migrate.py` يُوسَم «تاريخيّ —
لنقل SQLite⟵PG قبل 2026-07؛ وأيُّ نقلٍ بين قاعدتَي PostgreSQL بـ`pg_dump -Fc`/`pg_restore` حصراً».

---

## §9 — تبويب 3 (الاستخراجُ وحلقةُ التعلّم): ما أُنجز · ما بقي · ما أنويه — ثمّ خطّةٌ واحدةٌ من تبويبٍ واحد

### 9.0 — قرارُ المالك (2026-09-09): **تبويبٌ واحدٌ وخطّةٌ واحدة**
العملُ من ثلاث جلساتٍ على مجلّدٍ واحدٍ سبّب فعلاً: تحريكَ `main` تحت جلسةٍ أخرى · إيداعاً وقع على
خرج تبويب 2 حين فشل worktree بامتلاء القرص · تعارضَ سياسةٍ (`is_staff` مقابل `can_open_content`)
لم يظهر إلّا عند الترصيف · **219 مجلّدَ اختبارٍ مؤقّتاً (6.2GB) ملأت C:** من ثلاثة مجاري اختبار ·
سقوطَ جلسةٍ بنفاد الذاكرة (0.5GB حرّة) أثناء تدقيقٍ بالوكلاء. **من الآن**: جلسةٌ واحدة تعمل على
`main` في مجلّد المشروع؛ worktree مؤقّتٌ قصيرُ المسار **فقط** لعملٍ متفرّعٍ يُغلق في يومه؛
`../lettersys_rollout` تبقى للقراءة (فيها `.env` والمفتاحُ `67572ad5` الذي لا يُشحَن) ولا يُعمل
منها؛ اختبارٌ واحدٌ في كلّ وقت و`OPENBLAS_NUM_THREADS=1` (جهاز 8GB). هذه الوثيقةُ هي الخطّة.

### 9.1 — تحديثُ رأس الحالة (يُغني عن §1 حيث اختلف — مقيسٌ 2026-09-09)
- **`main` = `origin/main` = `adf5669`** · **PR #9 وPR #11 مدموجان** 2026-09-08 · المفتوحُ الوحيد
  **PR #16** (مسوّدة، تبويب 1) · النشرُ `disabled_manually`.
- **القاعدةُ المحلّيّة (مصدرُ الحقيقة) صارت @0077 + دمجُ الجهات 2026-09-08 مساءً** بقرار المالك:
  النشطةُ 411 ⟵ **353** · 58 نسخةً `merged_into` · 848 رابطَ كتاب · 537 صفَّ ذاكرة · الروابطُ
  14,117/14,118 بلا ازدواج. نسخةٌ **قبل** الدمج: `D:/migration/backups/lettersys_pre_merge_2026-09-08.dump`
  (**pg_dump 16** من `C:/Program Files/PostgreSQL/16/bin` — الإصدارُ 13 على PATH يرفض الخادم 16).
  **كلُّ لقطةِ شحنٍ (المرحلة 2) تُؤخذ بعد هذا الدمج**، والخطّةُ الدائمة `core/data/entity_merge_plan.json`
  (89 عنقوداً بالأسماء) تُعاد على الإنتاج بـ`prepare_entities --apply` إن استُعيدت قاعدةٌ أقدم.
- ⚠️ **ثابتُ §2 «0 انحراف في `var/*.json`» لم يعد دقيقاً**: 25 مفتاحاً في كلٍّ من
  `entity_doc_profiles.json` و`entity_extraction_profiles.json` يشير إلى نسخٍ معطَّلة. **لا عطبَ**
  (المستهلكُ يمرّ على النشطة فقط، والأمُّ لها بروفايلُها في 24/25)، لكنّ إحصاءَ النسخ لا يُضاف إلى
  الأمّ حتّى يُعاد التوليدُ (دَين 9.5‑1). نسخٌ قبل التعديل في `D:/migration/backups/*pre_rekey*`.
- **Temp**: 219 مجلّداً `%LOCALAPPDATA%/Temp/tmp*` ما تزال؛ الجذرُ (`settings_test.MEDIA_ROOT` بلا مسح)
  أُصلح بـ`atexit` (`8230e91`)؛ الحذفُ الجماعيّ بيد المالك (حارسُ الوضع التلقائيّ يمنعه).

### 9.2 — ما أُنجز في الاستخراج (2026-09-01 ⟵ 09-09) بالأرقام المختومة
| الحقل | المختوم | الحكم |
|---|---|---|
| العدد (بصريّ T2.4) | e2e‑D 85% · e2e‑C 57% طرفاً لطرف | صالح؛ الاختناقُ صمتُ الكاشف (35/36) لا القراءة |
| العدد (المطبوع الصارم) | e2e‑F 28/58 · دقّة 93% · خاطئ 2 (حُكم بعين Claude) | صالح، تغطيةٌ نصفيّة؛ `printed_anchor` كان 0/10 فأُصلح |
| الموضوع | مسارُ العلامة 72.9% دقّة · 62% تغطية | صالح؛ الضعيفُ بطاقةُ اقتراحٍ لا ملء (`title_provenance`) |
| المستلمة | E‑100b 62.9% (+ سدُّ الصمت بالوجهة السائدة) | مقبولٌ مع تأكيد الكاتب؛ `book_kind` من الواجهة كان الفارق 30 ⟵ 63 |
| المُصدِرة | E‑100b 48.0% · Tier A 50.7 ⟵ **56.8** بعد الدمج (مقيسٌ بفئات التكافؤ) | هضبةٌ بنيويّة؛ الرافعتان الدمجُ (طُبِّق) وافتراضُ القسم (شُحن) |
| تاريخ الجهة | أخضر 7% تغطية بدقّة 4/7 | ساقط؛ الملءُ من الأخضر وحده، الخادمُ لا يكتب `sender_date` |

**بُنيةٌ وسلامة**: عقدُ العتاد (`artifacts.py` مسارات الجذر + مؤشّر LFS) و`models_healthcheck --strict --load --hash`
(+ قاعدةٌ فارغة = خطأٌ صلب) · `core` logger · أعلامُ الكوكي من البيئة · `onnxruntime` في المتطلّبات ·
`deploy.yml` (معطَّل) · عقدُ المنشأ `typed/confirmed/autofilled` للحقول الخمسة · حلقةُ الذاكرة تتعلّم من
التعديل وتُهمل `autofilled` · حصادُ الموضوع والتاريخ فاشلٌ‑مغلقاً · `pdf_first_page_text` للصارم ·
إصلاحُ تسريب MEDIA_ROOT · تدقيقُ المكرَّرات (39 + 6 بالعين، طُبِّق) · **افتراضُ القسم** لجهة الصادر
(`dominant_issuing_entities`: ترك‑واحد top‑1 41.9% · top‑3 66.5% مقابل 32%) · قياسُ حلقة التعلّم زمنيّاً
(التغطيةُ تُشبع عند ~1,000 صفّ والدقّةُ المشروطة هضبةٌ ~50/60%) · افتراضاتُ السرّيّة/النوع **مرفوضة**
بالقياس (96.5% = الأساس؛ النوعُ يختاره الكاتب) · وسومُ العين **حَكَمٌ لا معلّم** (12 صفحة: 5 مكرَّرات ·
3 وحدةٌ في الذيل · 4 غيرُ مطبوعة). التفصيلُ في `docs/EVAL_REGISTRY.md` و`docs/ENTITY_DEDUP_AUDIT_2026-09-08.md`.

### 9.3 — ما تبقّى من خطّتي (بالترتيب، مع البوّابة ومَن ينفّذ؛ [ن]=هذه الجلسة، [م]=المشغّل)
1. **[م/المالك] الإنتاج** وفق المراحل 0–5 أعلاه — خارجَ يدي (§0). حصّةُ الاستخراج منه بعد `pg_restore`:
   `models_healthcheck --strict --load` ⟵ `prepare_entities --apply` (إن كانت اللقطةُ قبل الدمج) ⟵ إعادةُ
   تشغيل gunicorn ⟵ `TESSERACT_ADAPTIVE_THRESHOLD=0` · `OMP_THREAD_LIMIT=1`. عرَضُ «لا جهات» = قاعدةٌ فارغة.
2. **[ن] قياسُ افتراض القسم على Tier A** بعد تطبيع `merged_into` (`entity_eval.py` يمرّر `department_id`) —
   بوّابةٌ تُسجَّل قبل النظر: المُصدِرةُ الكلّيّة ≥ **56%** (كانت 50.7) والصادرُ الداخليّ ≥ **40%** (كان 32.2).
3. **[ن] E‑100c مختومة** (100 لم تُرَ، OCR حيّ) تختم توحيدَ شكل الذاكرة + الدمج + افتراضَ القسم معاً —
   البوّابة قبل السحب: المُصدِرة ≥ **55** · المستلمة ≥ **63**. تُشغَّل حين تكون الذاكرةُ الحرّة ≥ 2GB.
4. **[ن] إعادةُ توليد بروفايلات `var/`** بعد الدمج (`study_entity_layouts`؛ تحقّق أنّه المولِّد) ⟵ يعيد ثابتَ §2.
5. **[المالك] أربعُ حالاتٍ لم تحسمها الورقة** (من واجهة الجهات): وحدة عكاس/شعبة المتابعة الفنيّة‑عكاس ·
   فريق التحليل الأوّل مقابل #99 (وتُنشأ جهةُ «فريق التحليل **التجاري**» إن ثبتت) · «شرقي بغداد» #624 ·
   «متابعة تنفيذ الموازنة الخارجيّة» #538. وكتبُ ZPEC الـ32 مختلطةٌ بين رقعتين بيد الكاتب — فرزٌ يدويّ إن أُريد.
6. **[المالك ثمّ ن] قارئُ التاريخ — بعد استقرار الإنتاج** (قرار 2026-09-09). الوصفة: حصادُ 7,763 زوجَ تاريخِ
   حبر (بعد إصلاح الأعمدة 0060) ⟵ **مصادقةُ عيني على 150 قصاصة/وسم** قبل التدريب ⟵ دفترُ كاغل جاهز ⟵ تدريبٌ
   واحدٌ بيد المالك ⟵ **حُكمُ عيني على date‑100 المختومة** (لا تدريبَ عليها أبداً). البوّابةُ تُسجَّل الآن:
   أخضر ≥ **30%** تغطية بدقّة ≥ **90%** (اليوم 7% بدقّة 4/7).
7. **[ن + كاغل بيد المالك] العدد**: اختناقُ صمت الكاشف ⟵ كاشفُ صنفين (عدد + موضوع) من حصاد الصناديق الجاري؛
   البوّابة نظرةُ e2e‑G واحدة: إصابة ≥ 65% طرفاً لطرف على 100 لم تُرَ (اليوم 57).
8. **[ن] الموضوع**: لا عملَ قبل 50 تصحيحاً حقيقيّاً (اليوم 3/50) — ثمّ جلسةُ تنقيبِ قواعد واحدة.
9. **[المالك] الدفعة 4‑ب** من خطّة rollout (بريدُ الوحدات + طاولتا الرئيسين) — بعد الاستقرار (§7.4).
10. **تدبيرٌ منزليّ**: حذفُ Temp (المالك) · تعيينُ مسؤول الأرشفة (`/books/admin/?tab=users`) · لا ذاكرةَ سرّيّةٍ أو نوع.

### 9.4 — ما أنوي فعله، بترتيب التنفيذ من التبويب الواحد
- **قبل الترحيل (محلّيّ، قراءةٌ وقياس)**: (أ) الحزمةُ الكاملة على `adf5669` — بوّابةُ §1 · (ب) 9.3‑4 ثمّ 9.3‑2
  ثمّ 9.3‑3 · (ج) إلحاقُ النتائج هنا وفي `EVAL_REGISTRY` بلا تعديل بوّابةٍ بعد النظر.
- **أثناء الترحيل**: لا شيءَ في الاستخراج؛ أُجهّز أوامرَ [م] لحصّة 9.3‑1 وأتحقّق من مخرَجاتها.
- **بعد الاستقرار**: 9.3‑6 (التاريخ) ثمّ 9.3‑7 (الكاشف) ثمّ 9.3‑8 عند بلوغ العتبة.
- **ما لن أفعله**: لا تدريبَ على مجموعات العين المختومة · لا ذاكرةَ سرّيّةٍ/نوع · لا لمسَ للخادم · لا تفعيلَ نشر ·
  لا `dumpdata`/`loaddata` · لا إرخاءَ بوّابةٍ بعد النظر.

### 9.5 — ديونٌ سجّلها تبويب 3
1. بروفايلاتُ `var/` بعد الدمج (9.1/9.3‑4).
2. معرّفاتُ Tier A (`entity_eval_tierA.json`) وE‑100b صارت نسخاً معطَّلة ⟵ تطبيعٌ بـ`merged_into` داخل
   `entity_eval.py`/`entity_sealed_e100.py` (ما يزال يدويّاً).
3. `Entity #526 «الادارة»` رمزُها بريدٌ إلكترونيّ (`follow-up2@…`) — يُصحَّح من الواجهة.
4. الوحداتُ الشقيقة لقسم المتابعة ليست كلُّها أبناءَه في شجرة الأقسام (اللجان/الادارة/الارشفة بلا أب)
   فافتراضُ القسم بُني على التواتر لا الشجرة؛ ضبطُ الشجرة يفيد التقارير لا الاستخراج.
5. `harvest_subject_boxes` جارٍ نحو كاشف الصنفين؛ لا يُقتبَس رقمٌ منه قبل e2e‑G.

---

## §10 — المسارُ المبسَّط: رفعُ البيانات بملفّ النسخة المشفَّرة

> **بأمر المالك 2026-09-09.** يستبدل **المرحلتين 2 و3** بمسارٍ واحدٍ يستعمل زرَّ النسخ
> في التطبيق نفسِه. **المراحل 0 و1 و4 و5 سارية** وتُقرأ معه. مالكُ القسم: التبويب 1.

### 10.0 — لماذا هذا المسارُ أبسط وماذا خسر

| يُلغى من الخطّة القديمة | يحلّ محلّه |
|---|---|
| 2.1 لقطةٌ يدويّة · 2.2 بروفةٌ محلّيّة · 2.3 تنظيفٌ في `lettersys_ship` · 2.4 نسخةُ شحن | زرُّ النسخ ⟵ `verify_backup` ⟵ نقلُ `.enc` ⟵ التنظيفُ **على الخادم** في القاعدة الجانبيّة |

**والبروفةُ المحلّيّة كانت مستحيلةً أصلاً** — مقيس: `lettersys_user` لا يملك `CREATEDB`
(`permission denied to create database`)، وحسابُ `postgres` يطلب كلمةَ مرورٍ لا نملكها.
فالبروفةُ تنتقل إلى الخادم في `lettersys_new` **بلا انقطاع** (والتطبيقُ على القديمة) —
وهي أقوى: تجري على PostgreSQL 18 و`C.UTF-8` الحقيقيَّين لا على محاكاةٍ محلّيّة.

**ما لا يُتنازَل عنه مهما بُسِّط المسار:**
- **قاعدةٌ جانبيّةٌ ثمّ تبديلٌ بإعادة تسمية** — ولا `pg_restore` فوق `lettersys` الحيّة **أبداً**.
- `--exit-on-error` · نسخةُ ما هو قائمٌ قبل أيّ لمس · بوّابةٌ رقميّةٌ قبل التنظيف وبعده.
- بوّابةُ sha للمفتاح · إطفاءُ IMAP قبل التبديل · `TRUNCATE django_session` + `FLUSHDB`.

### 10.1 — 🔴 درسٌ مقيسٌ اليوم: **الأعدادُ ليست بصمة**

النسخةُ الموجودة `pg_backup_20260908_0958.dump.enc` أعدادُها **تطابق القاعدةَ الحيّة
حرفيّاً** (13,239 كتاباً · 674 جهةً · 13,187 مرفقاً · 22,313 قيداً · 12 مستخدماً)،
ومع ذلك **محتواها أقدم**:

| البصمة | في النسخة | حيّاً |
|---|---|---|
| `core_entity.merged_into_id` غيرُ فارغ | **198** | **256** |

أي أنّ دمجَ الجهات المسائيَّ جرى بعدها. **فلا تُقبل نسخةٌ لأنّ أعدادَها تطابق** —
تُؤخذ نسخةٌ جديدةٌ عند T‑0 وتُقارَن **بالبصمات**: `merged_into` · `is_training` ·
`is_deleted` · طولُ كلمة البريد وبادئتُها · رأسُ الهجرة.

### 10.2 — المرجعُ للمقارنة

**خام** (قبل التنظيف): كتب **13,239** · تدريب **131** · محذوف **45** · جهات **674** ·
`merged_into` **256** · مرفقات **13,187** · قيود **22,313** · مستخدمون **12** ·
بريد `enc::` بطول **125** · رأسُ الهجرة **`0077_archive_events_and_history`**.

**بعد التنظيف**: كتب **13,108** · تدريب **0** · محذوف **13** · جهات **674** ·
`imap_sync=false` · جلسات **0** · عدّادات **2433 / 358 / 455**.

### 10.3 — الخطوات (١٣ خطوة)

**[ن]** نحن محلّيّاً · **[م]** مشغّلُ الخادم · **[مالك]** قرار.
كلُّ خطوةٍ تنتهي بمخرَجٍ **يُلصق في سجلّ التنفيذ** — لا «تمّ».

| # | مَن | الأمر | بوّابةُ القبول | الرجوع |
|---|---|---|---|---|
| **10.a** | ن | `git status` · التأكّد أنّ `.gitignore` يتجاهل `*.enc` و`*.dump` و`/lettersys_data*.json` | `git check-ignore x.enc` ينجح · الشجرةُ نظيفة | — |
| **10.b** | ن | إيقافُ الخادم المحلّيّ وقفلُ القاعدة قراءةً (**المرحلة 1 بنداها 1.2 و1.3**) | لا مستمعَ على 8000 · `count=13239` ثابتٌ بفحصين بفاصل 5د | 1.2/1.3 عكسيّاً |
| **10.c** | ن | **نسخةٌ جديدة** (زرُّ النسخ في اللوحة، أو):<br>`python manage.py shell -c "from core.backup_service import create_encrypted_pg_backup as b; print(b())"` | ملفُّ `D:/trackbackup/pg_backup_<ts>.dump.enc` ≥ 4 MB | — |
| **10.d** | ن | `python manage.py verify_backup D:/trackbackup/pg_backup_<ts>.dump.enc --expect-live`<br>ثمّ `sha256sum` للملفّ ⟵ `<ts>.dump.enc.sha256` | خروج **0** · الجدولُ = مرجعُ 10.2 الخام **بما فيه `merged_into=256`** · **sha(enc) وsha(plain) يُلصقان** | أعد 10.c |
| **10.e** | ن⟶م | تسليمُ `.enc` + `.sha256` بأيّ قناة · **والمفتاحُ `1e639b05` بقناةٍ أخرى وزمنٍ آخر** (لصقٌ في جلسة ssh — لا بريد ولا دردشة ولا git ولا مع الملفّ) | إقرارُ [م] بوصول الاثنين **منفصلَين** | — |
| **10.f** | م | `pg_dump -Fc -f /var/backups/lettersys/pre_cutover_<ts>.dump lettersys`<br>`cp .env .env.pre_cutover ; ls -la .encryption_key* ; df -h /` | dump > 0 · حرٌّ ≥ 4 GB · **وإن وُجد مفتاحٌ سلفاً**: `mv .encryption_key .encryption_key.selfminted_<ts>` (لا يُحذف) | — |
| **10.g** | م | كتابةُ `/var/www/lettersys/.encryption_key` (44 بايتاً بلا سطرٍ جديد) · `chown <svc>: ; chmod 600` | `sha256sum … \| cut -c1-8` = **`1e639b05`** · `wc -c` = **44** · `stat -c '%U %a'` = الخدمة/600 | إعادةُ المُعاد تسميتُه |
| **10.h** | م | `sha256sum -c pg_backup_<ts>.dump.enc.sha256`<br>`cd /var/www/lettersys && .venv/bin/python manage.py verify_backup <path>.enc --write-plain /var/backups/lettersys/incoming/ship.dump` | OK · **الجدولُ مطابقٌ حرفيّاً لمخرَج 10.d** — وهذا **إثباتُ المفتاح** في آنٍ واحد | `InvalidToken` ⟵ أعد 10.g؛ لم يُمسّ شيء |
| **10.i** | م | `CREATE DATABASE lettersys_new WITH OWNER lettersys_user ENCODING 'UTF8' LC_COLLATE 'C.UTF-8' LC_CTYPE 'C.UTF-8' TEMPLATE template0;`<br>`pg_restore --no-owner --no-privileges --exit-on-error -h localhost -U lettersys_user -d lettersys_new .../ship.dump 2>restore.err; echo rc=$?` ثمّ `ANALYZE;` | `rc=0` (أو أسطرُ `pg_trgm` وحدها ⟵ `CREATE EXTENSION pg_trgm` بحساب postgres ثمّ إعادة) | `DROP DATABASE lettersys_new` |
| **10.j** | م | `DB_NAME=lettersys_new .venv/bin/python manage.py shell -c "exec(open('scripts/probe_s0.py').read())"`<br>`DB_NAME=lettersys_new … migrate --check` · `… models_healthcheck --strict --load` | S‑0 = **مرجعُ 10.2 الخام** · `migration_head=0077` · `mail: enc=True len=125` · الأمران يخرجان 0 | DROP + 10.i |
| **10.k** | م | على `lettersys_new` **بهذا الترتيب**: ① `UPDATE core_emailsettings SET imap_sync_enabled=false;` ② `purge_dev_seed_books` (معاينةً ثمّ `--yes`) ③ تعطيلُ حسابات الفحص (`is_active=false`، **لا حذف** — مفاتيحُ أجنبيّة؛ و`admin12` يُترك حتى §6.2) ④ `TRUNCATE django_session;` ⑤ `UPDATE core_backupsettings SET enabled=false;` | S‑0 ثانيةً = **مرجعُ 10.2 بعد التنظيف** | DROP + 10.i |
| **10.l** | م | `DB_NAME=lettersys_new … shell -c "from core.models import EmailSettings as E; e=E.get(); print(len(e.smtp_password or ''), len(e.imap_password or ''))"` · `ls .encryption_key*` | **`16 16`** — و**125 تعني فكّاً فاشلاً مبتلَعاً** (`from_db` يبتلع الفشل بصمت) · **ملفُّ مفتاحٍ واحدٌ لا غير** (لا سكَّ ثالث) | إن فشل: أعد 10.g **قبل أيّ تبديل** |
| **10.m** | م+مالك | **المرحلة 4 كما هي** (4.1–4.6: إيقافُ الخدمات · إعادةُ التسمية · `FLUSHDB` · `migrate --check` · تشغيل · دخانٌ بالعين) ثمّ **المرحلة 5** | بوّاباتُ 4.x | 4.x |

### 10.4 — مفارقةُ الدجاجة والبيضة في المفتاح — **محسومة**

النسخةُ مشفَّرةٌ بمفتاحٍ **غائبٍ عن الخادم**. والقرار: **يُشحن `.enc` كما هو ويُفكّ على
الخادم** لا أن يُفكّ محلّيّاً ويُنقل خاماً — لثلاثة أسباب:
1. [م] وسيطٌ (§0) والملفُّ يستقرّ على جهازه؛ `.enc` يُبقيه نصّاً مشفَّراً عنده.
2. المفتاحُ يسافر **بقناةٍ ثانيةٍ وزمنٍ ثانٍ** — فلا يجتمعان في مكانٍ واحد.
3. **الفكُّ الناجح على الخادم هو نفسُه إثباتُ المفتاح** — فيطوي بوّابةَ 0.7 وإثباتَ 3.5 معاً.

**والحارسان ضدّ سكّ مفتاحٍ ثالث** (وهو الخطرُ الصامت): (أ) `verify_backup` يقرأ ملفَّ
المفتاح **مباشرةً** ولا يستدعي `get_or_create_encryption_key` أبداً — فيفشل صارخاً إن
غاب ولا يسكّ؛ (ب) **لا عمليّةَ جانغو تلمس `lettersys_new` قبل بوّابة 10.j**، وبالتصميم
الجانبيّ تبقى الخدماتُ على القاعدة القديمة.

> **دَينٌ بنيويٌّ (ثلاثةُ أسطر، خارج هذا المسار)**: `get_or_create_encryption_key` يجب
> أن **يرفض السكَّ** ما لم يكن `DEBUG` أو `ENCRYPTION_KEY_ALLOW_CREATE=1`. اليومَ يسكّ
> بصمت، فأيُّ قراءةٍ لـ`EmailSettings` على خادمٍ بلا مفتاح تدفن المشكلةَ بدل أن تكشفها.

### 10.5 — التنظيفُ على الخادم لا محلّيّاً — ولماذا

**القاعدةُ المحلّيّة مصدرُ الرجوع الوحيد** (ولا نسخةَ بروفةٍ منها) ⟵ **صفرُ كتابةٍ
عليها**. وعلى الخادم: `.enc` يبقى سليماً، فإن أخطأ التنظيفُ فـ`DROP DATABASE
lettersys_new` وإعادةُ 10.i — دقائقُ على ملفٍّ 3 MB. وبوّابتان رقميّتان تفصلان الخطأين:
**الخام** يُثبت وفاءَ الاستعادة، و**المنظَّف** يُثبت التنظيف.

### 10.6 — الحارسُ البرمجيّ: أمرُ `verify_backup` (عقدُه)

```
python manage.py verify_backup <ملفّ.enc|.dump> [--key PATH] [--expect جدول=عدد …]
                              [--expect-live] [--write-plain PATH] [--json]
```
- يقبل `.enc` (Fernet) أو خاماً (يُميَّز برأس `PGDMP`). **المفتاح يُقرأ مباشرةً** —
  غيابُه **خروج 3**، ولا يستدعي `get_or_create_encryption_key` أبداً (**حارسٌ مُطفَّر**).
- الصريحُ إلى `mkdtemp()` **خارج** `BASE_DIR` بوضع 0600 ويُحذف في `finally`.
  `--write-plain` يرفض الكتابةَ فوق موجودٍ ويرفض مساراً داخل المستودع.
- يعدّ صفوفَ `COPY` بدالّةٍ نقيّة `parse_copy_stats(lines)` ويطبع **البصمات** لا
  الأعدادَ وحدها: `merged_into` · `is_training` · `is_deleted` · بادئةُ كلمة البريد
  وطولُها · `imap_sync_enabled` · رأسُ الهجرة · أسماءُ المستخدمين.
- **لا يطبع قيمةَ أيّ عمودٍ مشفَّر** (حارسٌ يؤكّد غيابَ `enc::` وأيّ 16 حرفاً عن المخرَج).
- `--expect-live` يقارن بالقاعدة الحيّة (`Book.all_objects`) ⟵ **خروج 2** بقائمة كلّ
  الاختلافات، وإلّا 0. `pg_restore` غائبٌ ⟵ خروج 4.
- **وقيمتُه في التناظر**: الأداةُ نفسُها تُشغَّل محلّيّاً (10.d) وعلى الخادم (10.h)،
  فبوّابةُ التطابق تصير نسخاً ولصقاً لا اجتهاداً.

**حالُه: غيرُ مكتوبٍ بعد** — وهو أوّلُ ما يُكتب في المحادثة الجديدة (البند أ في §10.7).

### 10.7 — ما تبدأ به المحادثةُ الجديدة

1. **أمرُ الاستطلاع S‑0** (§8.2) — حالةُ الإنتاج ما زالت مجهولة، وحكمُه يقرّر إن كانت
   `pre_cutover` في 10.f ذاتَ قيمةٍ وهل يُطفأ IMAP فوراً.
2. **كتابةُ `verify_backup`** بعقد 10.6 وحرّاسه (بلا PostgreSQL في الاختبارات).
3. ثمّ 10.a ⟵ 10.m بالترتيب، ولا خطوةَ قبل خضرة بوّابة سابقتها.

---

---

## §11 — الخطّةُ الواحدة المُعادُ ترتيبها (استشارةُ فيبل، 2026-09-09)

> **خطّةٌ لا تنفيذ.** تُعيد ترتيبَ بنود §0–§10 **كلِّها** (180 بنداً مجروداً) في اثنتَي عشرةَ دفعةٍ
> متتابعة، ولا تُلغي قسماً ولا تنسخ محتواه — تُحيل إليه بمعرّفه. الأقسامُ السابقة تبقى مرجعَ
> الأوامر والبوّابات؛ **هذا القسمُ مرجعُ الترتيب ومَن ينفّذ ومتى.** عند اختلاف رأسِ الحالة تُصدَّق
> اللقطةُ المقيسة 2026-09-09: **`main = origin/main = HEAD = 7063bf6`**.
>
> **كيف قُرئت الوثيقة**: جردٌ بثلاث عدساتٍ (§0–§6 · §7–§8 · §9–§10) + عدسةُ تعارضاتٍ + عدسةُ تبعيّات
> (72 حافّة · مسارٌ حرجٌ من 24 بنداً)، ثمّ ترتيبٌ بمذهبٍ واحد: **الأطرافُ الثلاثة ([ن] · [م] · [مالك])
> لا يعطّل أحدُها الآخر** — كلُّ سؤالٍ للمالك في رسالةٍ واحدة، وكلُّ أمرٍ لـ[م] في أقلّ عددٍ من الحزم،
> وكلُّ فترةِ انتظارٍ ممتلئةٌ بعملٍ محلّيّ. وحيث يتصادم ذلك مع الأمان يغلب الأمان (ما لا يُرجَع عنه
> يتأخّر حتّى تُغلَق كلُّ بوّاباته).
>
> **اصطلاحُ المعرّفات** (يطابق الجرد): `0.6` بندُ مرحلة · `10.d` خطوةُ §10 · `D6.13` قرارُ مالك ·
> `R5.9` خطر · `U2` توحيد · `W3` قاعدةُ نافذة · `P4` إعادةُ تمكين نشر · `G3` توجيه · `C0.7` قيدُ §0 ·
> `7.4-أ` بندُ §7.4 · `T7.5-2` دَينُ §7.5 · `8.4-د0` سطرُ علاج §8.4 · `8.6-أ` وقاية · `9.3-4` بندُ
> §9.3 · `T9.5-2` دَينُ §9.5 · `10.6-5` متطلَّبُ verify_backup · `T10.4` دَينُ §10.4 · `سجلّ-1` قيدُ
> السجلّ · **`N1…N7` بنودٌ مقترَحةٌ جديدة** (سدُّ فجواتٍ كشفها الجرد؛ لا تُرخي بوّابةً قائمة).

### 11.0 — حقائقٌ مقيسةٌ اليوم تغلب نصَّ الوثيقة

| ما تقوله الوثيقة | المقيسُ 2026-09-09 | الأثرُ على الخطّة |
|---|---|---|
| `main=236ffec` (§1) · `main=adf5669` (§9.1) | **`7063bf6`** ثلاثتُها (HEAD/main/origin) | كلُّ «الحزمةُ الكاملة على X» تُقرأ «على رأس الوسم `cutover-2026-09`» (U4 يُثبَّت في د1) |
| «0 PR مفتوح» (§1) | PR #16 مسوّدةٌ مفتوحة (`feat/mail-archive-attribution` @7940ab0، إيداعان فوق main) | يبقى مسوّدةً حتّى P6 |
| «المُعلَّق عليّ لا شيء» (7.1) | worktree `agent-a2bd83f51892841ee` @f242d24 قائمةٌ (**0 إيداعٍ فريد**) · `rollout-b3` (محلّيّ+بعيد) و`origin/media-cd-cleanup` و`origin/prod-stability-0901` **0 فوق main** · **لا وسمَ `cutover*`** | U2/U3/U4 لم تبدأ — د0/د1 |
| «0 انحراف في var/*.json» (§2) | 25 مفتاحاً في كلٍّ من الملفّين يشير إلى نسخٍ مدموجة (§9.1؛ الملفّان مؤرّخان 09-01 قبل الدمج) | 9.3-4 يسبق 0.4 ويُودَع كي يصل الخادمَ في سحب الكود (د1⟵د3) |
| «219 مجلّدَ Temp» (§9.1) | **164** · و**C: حرٌّ 10 GB من 238 (96%)** · D: حرٌّ 374 GB | حذفُ Temp يصير عاجلاً (بيد المالك)؛ كلُّ مخرَجٍ طويلٍ ونسخةٍ إلى D: |
| `verify_backup` «غيرُ مكتوب» (§10.6) | مؤكَّد: `core/management/commands/verify_backup.py` غائب | د1 أوّلُ كود |
| `probe_s0.py` يكفي بوّابتَي 10.j/10.k | **لا يطبع** `merged_into` ولا `is_deleted` ولا عدّادات `BookSequence` | N4: توسيعُه في إيداع د1 نفسِه |
| مسارُ المفتاح | `core/encryption.py:12` = `Path(__file__)…/.encryption_key` **نسبيٌّ للشجرة** ⟵ شجرةُ `../lettersys_rollout` تشفّر بـ`67572ad5` | 10.c يُشغَّل من مجلّد المشروع حصراً وverify_backup يطبع sha المفتاح |
| «الإصدارُ 13 على PATH» (§9.1) | `pg_dump` **ليس على PATH**؛ التنصيبان 13 و16 قائمان و`_find_pg_dump` يختار **الأحدث (16)** بالكود | فخُّ 10.c مُغلَقٌ بالكود؛ يبقى تسجيلُ المسار المستعمَل في السجلّ |
| بوّابةُ 10.c «≥ 4 MB» تقديريّة | حجمُ `pg_backup_20260908_0958.dump.enc` = **4,247,544 بايتاً** | البوّابةُ صادقةٌ ومقيسة |
| «الملفُّ لم يُودَع» (§8.1) | صحيح — **لكنّه ما يزال على القرص**: `lettersys_data.json` 27,363,361 بايتاً (09-08 10:50) + `lettersys.dump` خامٌّ 3,076,680 بايتاً (09-01) بجذر المشروع، متجاهَلان | N5: تسجيلُ sha256 ثمّ إتلافٌ بعد D6.15 والتدوير |
| `.gitignore` يحمي الكلّ | يحمي `*.enc`/`*.dump`/`/lettersys_data*.json` — لكنّ النمطَ الأخير **جذريٌّ فقط** | لا نقلَ للملفّ إلى مجلّدٍ فرعيّ أبداً؛ يُتلَف لا يُنقَل |
| النشر | `gh workflow list` = **`disabled_manually`** (مقيس) · `deploy.yml` سطرا `push` معلَّقان (26–28) | الطبقتان قائمتان |
| ديونُ الوقاية 8.6 وT10.4 «مسجَّلة» | **كلُّها غيرُ منفَّذة بالكود**: لا `EncryptedCharField` · 0 ذكرٍ لـ`enc::` في `db_healthcheck` · لا تظليلَ لـ`dumpdata` · رأسُ `pg_migrate.py` ما يزال يدعو إلى dumpdata (س6–7، 22) · `get_or_create_encryption_key` يسكّ بصمت (س24–25) | R5.9 يُقرأ «منعٌ غيرُ قائم»؛ 8.6-د وثائقُ الآن (د0)، T10.4 مع verify_backup (د1)، 8.6-أ/ب/ج بعد أوّل نشرٍ تلقائيّ (د10) |

### 11.1 — تعارضاتٌ محسومة (تسبق كلَّ شيء)

| # | التعارض | الحسم | البندُ الذي يُغلقه |
|---|---|---|---|
| C1 | رأسُ الحالة ثلاثةُ أرقام | اللقطةُ 7063bf6؛ «رأسُ التجميد» = وسمُ `cutover-2026-09` | U4 (د1) · 9.1-1 |
| C2 | «0 PR» مقابل #16 | #16 مسوّدةٌ مفتوحة لا تُدمَج قبل P6 | W2 · P6 |
| C3 | ثابتُ var/*.json | يُعاد التوليدُ قبل 0.4، ويُنقل فحصُ التطابق من 3.5 الميتة إلى بوّابة 10.j | 9.3-4 (د1) · 10.j (د6) |
| C4 | «جهات 674» مقابل «353» | مقياسان: 674 صفٌّ · 353 نشطة · 256 `merged_into` — تُقرأ الثلاثةُ معاً | 10.d/10.j |
| C5 | ترقيمُ §5 متقطّع وإحالاتٌ إلى 2.3ب/3.3/3.5 | لا يُعاد ترقيمُ الوثيقة (إلحاقٌ لا كتابةٌ فوق)؛ **خريطةُ الإحالة**: R5.1⟵0.7+10.h/10.l · R5.2⟵10.j · R5.4⟵8.4-د0+10.k①+5.3 · R5.7⟵10.i | 11.15 |
| C6 | 8.4-د0 يُطفئ `is_active` أيضاً، و10.k① لا | يُصدَّق الأوسع: **10.k① = `imap_sync_enabled=false, is_active=false`** حتّى يُدوَّن جوابُ D6.4؛ 5.3 يصير «تفعيلُ `is_active` ثمّ IMAP بحسب D6.4» | 10.k · 5.3 |
| C7 | التدويرُ واجبٌ دائماً لكنّه مكتوبٌ داخل فرع «إن حُمِّل» | **D6.13 فعلٌ غيرُ مشروطٍ اليوم** في كلّ أحكام 8.3؛ 8.4-د1 إحالةٌ إليه | D6.13 (د2) |
| C8 | الكلمةُ الجديدة تُدخَل على قاعدةٍ ستُرمى (8.4-د6) ولقطةُ 10.c تحمل القديمة | **N3**: تُدخَل الكلمةُ الجديدة **في التطبيق المحلّيّ من صفحة الإعدادات قبل 10.b** فتحملها اللقطة؛ وإن تعذّر ⟵ تُدخَل على الإنتاج بعد 4.6 وقبل 5.3 مع إثبات [م] (8.4-د8). 8.4-د6 على الحيّة فقط إن اختار المالك D6.14=البناء | N3 (د2/د8) |
| C9 | 13,239 / 13,194 / 13,108 / 13,095 / 131 / 99 / 13,187 / 13,173 | **جدولُ المدراء**: `all_objects` 13,239 · `objects` (ما صدّره dumpdata) 13,194 · بعد purge 13,108 · نشطٌ غيرُ تدريبيّ 13,095 · تدريب 131 (99 غيرُ محذوف) · مرفقاتٌ كلُّ الصفوف 13,187 / نشطة 13,173 / بعد purge **غيرُ مقيس**. لا يُقتبَس رقمٌ بلا مديره | 10.d/10.j/10.k |
| C10 | 9.3-1 يُبيح `prepare_entities --apply` على الإنتاج | يُحذف؛ اللقطةُ بعد الدمج بالتعريف (10.d يشترط `merged_into=256`). يبقى تعليمةَ رجوعٍ لحالة استعادة `pre_merge` فقط بقرار مالك | 9.3-1 · W4 |
| C11 | المفتاحُ يُشحَن ثلاث مرّات (0.7 · 10.e/10.g · 8.4-د3) | **مرّةٌ واحدةٌ مبكّرة = 0.7** في د3 (وقتٌ لإصلاح CRLF/BOM، ويُحقّق «زمناً آخر» عن `.enc` تلقائيّاً)؛ إعادةُ تسمية المفتاح الذاتيّ (10.f) **تسبق الكتابةَ في الخطوة نفسِها**؛ 10.g يصير **تحقّقاً لا كتابة**؛ 8.4-د3 إحالة | 0.7 (د3) |
| C12 | §10 يصمت عن دلتا الوسائط (2.5) و2.6 | **N2 = 10.b′** دلتا الوسائط بعد القفل (= 2.5)؛ 2.6 ⟵ 10.e | N2 (د5) |
| C13 | بوّابتا 10.j/10.k غيرُ قابلتَين للتحقّق بـprobe_s0 | **N4**: توسيعُ `probe_s0.py` (قراءةٌ فقط) بـ`merged_into` · `is_deleted` · عدّادات `core_booksequence` · جهاتٍ نشطة · ذاكرةِ ترويسة · `bookemaillog` failed/pending · مستخدمين نشطين — في إيداع verify_backup | N4 (د1) |
| C14 | 10.h/10.j يحتاجان `verify_backup`/`probe_s0` على الخادم بينما U5 يمنع الكود والنشرُ معطَّل | **N1 (استثناءٌ موثَّقٌ من U5)**: إيداعُ verify_backup + N4 + var/*.json على main **بلا هجرة**، ثمّ [م] `git fetch && git checkout cutover-2026-09 && git lfs pull` **بلا `lettersys-deploy`** (يشغّل migrate وreset) وبلا إعادةِ تشغيل، مُسجَّلٌ قبل تنفيذه؛ بوّابة: HEAD=الوسم · `migrate --check`=0 · `verify_backup --help` يعمل | N1 (د3) |
| C15 | «الشجرةُ نظيفة» بينما worktree عالقة | U2 وU3 اليوم؛ U4 بعد إيداع د1؛ 10.a يُدمَج في 0.2 | د0/د1 |
| C16 | ثلاثُ قوائم «ابدأ هنا» | هذا القسمُ يحلّ محلَّها: د0 ⟵ د1 ⟵ … | §11 |
| C17 | `migration_head=0077` «يعني migrate خارج الخطّة» | لا يُستنتَج إلّا مع `git rev-parse HEAD` من 0.3 و**N6** `gh run list --workflow=deploy.yml -L 10` (نحن، قراءةٌ فقط) — آخرُ نشرٍ تلقائيٍّ شرعيّ قد يكون السبب | N6 (د0) |
| C18 | 10.d تشترط sha(plain) وعقدُ 10.6 لا يطبعه | يُضاف إلى العقد: **يطبع sha256 للصريح قبل حذفه** | 10.6-6 |
| C19 | «لا shell على قاعدةٍ حيّة» مقابل 10.c/10.d/10.j | تُقرأ القاعدة: **لا shell كاتباً**؛ المسموحُ قراءةً حصراً: `probe_s0` · `verify_backup` · `create_encrypted_pg_backup` على قاعدةٍ مقفولةٍ للقراءة | W4 |
| C20 | 10.l «ملفُّ مفتاحٍ واحد» و10.f يُبقي `selfminted_` | البوّابة: **ملفٌّ واحدٌ باسم `.encryption_key` بالضبط**، وكلُّ ما سواه بلاحقة `selfminted_`، ولا ملفَّ mtime بعد 10.g | 10.l |
| C21 | «≥4 MB» تقديريّ | مقيس: 4,247,544 بايتاً ⟵ البوّابةُ تبقى | 10.c |
| C22 | admin12 «يُترك حتّى §6.2» أثناء النافذة | بوّابةُ 0.1 تشترط جواباً على D6.2 قبل النافذة ⟵ 10.k③ «بحسب الجواب المدوَّن» | 0.1 · 10.k |
| C23 | 7.4-ج «ساعة» مقابل P6 «بعد نشرٍ يدويٍّ ناجح» | يُصدَّق P6 (الأشدُّ تحفّظاً)؛ 7.4-ج = P6؛ D6.11 قرارُ الموافقة | P6 (د9) |
| C24 | رجوعُ D6.3 «عبر cutover_raw» الميتة | الرجوع = `pg_backup_<ts>.dump.enc` + القاعدةُ المحلّيّة المجمَّدة | D6.3 |
| C25 | §10 يُنفَّذ «بالترتيب» بلا شرطٍ على حكم S‑0 | **شرطٌ مسبَقٌ لد5**: حكمُ 8.3 ∈ {لم يُحمَّل · حُمِّل بلا عمل · ≤20 كتاباً} وإلّا يتوقّف المسارُ عند D6.14 | د4 |
| C26 | R5.9 يُقرأ منجَزاً | يُوسَم «المنعُ غيرُ منفَّذ (دَين 8.6)»؛ المنعُ الفعليُّ اليوم قيدُ §0 + `.gitignore` | د10 |
| C27 | لقطةُ VPS (D6.10) سقطت من 10.f | تُضاف إلى 10.f قبل 10.i | 10.f (د6) |
| C28 | بوّاباتُ 2.3/3.4/3.5 المُسقَطة (var · ذاكرة 5,395 · المستخدمون النشطون · bookemaillog=0) | تُنقل: var + ذاكرة 5,395 ⟵ 10.j؛ المستخدمون النشطون + bookemaillog ⟵ 10.k | 10.j/10.k |
| C29 | 10.c لا يحدّد الشجرة ومسارُ المفتاح نسبيّ | 10.c من مجلّد المشروع حصراً + verify_backup يطبع sha[:8] المفتاح = `1e639b05` | 10.c/10.d |

### 11.2 — خريطةُ الدفعات

| الدفعة | الاسم | متى | مَن | البنود | البوّابةُ التي تُغلقها |
|---|---|---|---|---|---|
| **د0** | فتحُ القنوات وتنظيفُ الشجرة | **الآن محلّيّاً** (اليوم) | ن + مالك | 17 | حزمةُ [م]‑1 مُرسَلةٌ ومُسجَّلة · رسالةُ المالك مُرسَلة · `git status` نظيف · `git worktree list` = main + rollout · بصماتُ ملفّي التسريب في السجلّ |
| **د1** | الحارسُ البرمجيّ وأساسُ الشحن | **الآن محلّيّاً** (أثناء انتظار [م] والمالك) | ن | 27 | verify_backup خضراء + طفراتٌ حمراء · `migrate --check`=0 و`showmigrations core\|tail -1`=0077 · var/*.json not-in-db=0 و**0** مفتاحٍ لنسخة · الحزمةُ الكاملة خضراء (عددٌ يُلصق) · بيانُ الوسائط 13,932 · بروفةُ 10.c/10.d على نسخةٍ تجريبيّة · الوسمُ على origin |
| **د2** | حكمُ S‑0 وإغلاقُ النزف الأمنيّ | **بانتظار [م]** ثمّ فوراً | م + مالك + ن | 16 | مخرَجُ S‑0 مُلصَق وصفُّ 8.3 مُسمّى · إن حُمِّل: `imap_sync=False active=False` · الكلمةُ القديمة محذوفةٌ من Google · الجديدةُ مُدخَلةٌ محلّيّاً (`enc::`) · `find` على الخادم = 0 بعد الإتلاف |
| **د3** | تحصينُ الخادم واستقبالُ الوسائط والمفتاح والكود | **بانتظار [م]** + جوابَي D6.5/D6.6 + إيداع د1 | م + ن | 15 | `sha256sum -c`=0 فشل · 13,932 ملفّاً · `df -h` ≥4 GB · المفتاح sha[:8]=`1e639b05` · 44 · svc/600 · HEAD الخادم = الوسم · `migrate --check`=0 |
| **د4** | بوّابةُ الإقلاع | **بانتظار المالك** | مالك | 14 | جوابٌ مكتوبٌ على كلّ D6.x + موعدُ النافذة + حكمُ 8.3 في الفروع الثلاثة الأولى |
| **د5** | النافذة T‑0: التجميدُ واللقطة | **داخل النافذة** | ن + مالك | 14 | `count=13239` ثابتٌ ×2 · دلتا الوسائط 0 فشل · `.enc` ≥4 MB · verify_backup خروج 0 بـ`merged_into=256` وsha المفتاح `1e639b05` · sha(enc)+sha(plain) في السجلّ · إقرارُ [م] بالوصول |
| **د6** | النافذة: الاستعادةُ الجانبيّة | **داخل النافذة** | م | 18 | 10.h مطابقٌ حرفيّاً لـ10.d · `rc=0` · S‑0 خام = 10.2 خام · S‑0 منظَّف = 10.2 منظَّف · `16 16` · ملفُّ مفتاحٍ واحد |
| **د7** | التبديلُ والدخان (≤10 د) | **داخل النافذة** | م + مالك + ن | 8 | بوّاباتُ 4.1–4.6 كلُّها خضراء |
| **د8** | اليومُ الأوّل بعد التبديل | **بعد القبول** مباشرةً | مالك + م + ن | 13 | أوّلُ كتابٍ حقيقيّ (2433) · المحلّيُّ لا يعود · بريدٌ يعمل بالكلمة الجديدة (`enc::`) · مجموعةُ «مسؤول الأرشفة» غيرُ فارغة |
| **د9** | الاستقرارُ وإعادةُ تمكين النشر | **بعد الاستقرار** (يومان+) | ن + م + مالك | 11 | `ssh-keygen -lf` متطابقة · `gh run watch` ناجح · #16 مدموج · `lettersys_old` تُحذف بعد 7 أيّام ونسخةٍ خارجيّة |
| **د10** | ديونُ الكود بعد أوّل نشرٍ تلقائيّ | **بعد الاستقرار** | ن | 9 | 5 حرّاسٍ مطفَّرة للتشفير · حارسُ `enc::` في الإقلاع · لا `is_staff` في الإدخال الذكيّ · نظامُ الأدوار الميت مُزال |
| **د11** | المنتَجُ والاستخراج | **بعد الاستقرار** (بحسب العتبات) | ن + مالك | 18 | بوّاباتٌ مسجَّلةٌ قبل النظر: التاريخ ≥30%/≥90% · e2e‑G ≥65% · الموضوع 50 تصحيحاً · Tier A ≥56/≥40 · E‑100c ≥55/≥63 |

**قيودٌ سارية لا بنودَ عمل (تحكم كلَّ الدفعات):** C0.1–C0.7 · W1–W6 (بصياغة C19 لـW4) · U5 (باستثناء N1 الموثَّق) · 7.6 · 7.7 · G1–G6 · 9.0 · 9.1-2 · 9.1-4 · 10.0 · 10.1 · 10.2 (بجدول مدراء C9) · 10.4 · 10.5 · 10.7-3 · 9.3-10ج · D6.9 · 8.3 (تفرّعٌ يُقرأ في د2).

---

### 11.3 — د0: فتحُ القنوات وتنظيفُ الشجرة — **الآن محلّيّاً** (ن + مالك)

| المعرّف | البند | مَن | البوّابة (مخرَجٌ يُلصق) | الرجوع |
|---|---|---|---|---|
| 8.2-أ · 8.2-ب · 8.2-ج · 0.3 · 10.7-1 | **حزمةُ [م]‑1** (11.14-ب): استطلاعُ S‑0 بصيغتَيه + الإشاراتُ الثانويّة + استطلاعُ الخادم 0.3 كاملاً (+ `REDIS_CACHE_URL` اسماً ورقمَ القاعدة **N7**) — تُرسَل رسالةً واحدة | ن ⟶ م | نصُّ الحزمة في السجلّ بوقت الإرسال؛ المخرَجُ يُلصق حين يعود | — |
| N6 | `gh run list --workflow=deploy.yml -L 10` (قراءةٌ من GitHub لا من الخادم) لتاريخ آخر نشرٍ تلقائيّ | ن | القائمةُ في السجلّ؛ تُقرأ مع `migration_head` (C17) | — |
| 0.1 (إرسال) · D6.1…D6.15 | **رسالةُ المالك الواحدة** (11.14-أ) بتوصيةٍ لكلّ بند؛ D6.13 وD6.15 فعلان اليوم لا سؤالان | ن ⟶ مالك | الرسالةُ في السجلّ؛ الأجوبةُ تُغلق د4 | — |
| U2 | `git worktree remove --force .claude/worktrees/agent-a2bd83f51892841ee` ثمّ `git branch -D worktree-agent-a2bd83f51892841ee` (0 إيداعٍ فريد — مقيس) | ن | `git worktree list` = main + `../lettersys_rollout` · `git status` نظيف | — |
| U3 | حذفُ `rollout-b3` محلّيّاً وبعيداً، و`origin/media-cd-cleanup` و`origin/prod-stability-0901` — بعد `git log main..<فرع>` فارغاً لكلٍّ منها (مقيس: 0). **لا يُمسّ** `feat/mail-archive-attribution` (2 فوق main) | ن | `git branch -a` بلا الثلاثة؛ #16 قائم | `git push origin <sha>:refs/heads/<فرع>` من السجلّ |
| 0.2 · 10.a · 8.1 · R5.13 | بوّابةٌ واحدة: `git status` نظيف · `main`=`origin/main` · `gh workflow list`=disabled_manually · `git check-ignore x.enc y.dump lettersys_data.json` ينجح | ن | الأسطرُ الأربعة في السجلّ | — |
| N5 | تسجيلُ `sha256sum` لـ`lettersys_data.json` (27,363,361 ب) و`lettersys.dump` (3,076,680 ب) في السجلّ **قبل** أيّ إتلاف — كي يُطابَق مخرَجُ `find` على الخادم (8.4-د9/D6.15). **لا نقلَ ولا إعادةَ تسمية** (نمطُ التجاهل جذريّ) | ن | سطرا sha في السجلّ | — |
| 8.6-د | تصحيحُ رأس `scripts/pg_setup/pg_migrate.py`: «تاريخيّ — SQLite⟵PG قبل 2026-07؛ بين قاعدتَي PostgreSQL `pg_dump -Fc`/`pg_restore` حصراً» (وثائقُ فقط ⟵ جائزٌ على main) | ن | `grep -n dumpdata` يُظهر الوسمَ التاريخيّ لا دعوةً | `git revert` |
| 9.1-1 · سجلّ-1 · W5 | إلحاقُ تصحيح رأس الحالة (7063bf6) وهذا القسم في السجلّ — **إلحاقٌ لا كتابةٌ فوق** | ن | `git diff Merge9.md` إضافاتٌ فقط | — |
| 9.1-3 · 9.3-10أ | حذفُ 164 مجلّدَ `%LOCALAPPDATA%/Temp/tmp*` — **بيد المالك** (حارسُ الوضع التلقائيّ يمنع الجلسة). **عاجل: C: على 10 GB** | مالك | `ls -d Temp/tmp* \| wc -l` = 0 · `df -h /c` يرتفع | — |
| G1 · G2 | تحقّقُ البدء + لا لمسَ للخادم ولا نشر | ن | مُلتزَم | — |

**لماذا هنا:** كلُّ ما في د0 بلا مخاطرةٍ وبلا انتظار، وهو ما يفتح القناتَين غيرَ المتزامنتَين ([م] والمالك) في اليوم الأوّل كي لا تُهدَر أيّامٌ. **لا تتقدّم قبل:** لا شيء — تبدأ فوراً.

### 11.4 — د1: الحارسُ البرمجيّ وأساسُ الشحن — **الآن محلّيّاً** (ن)

> تُنفَّذ في **worktree مؤقّتٍ قصير المسار** (§0/§9.0) ثمّ تُدمَج إلى main **استثناءً موثَّقاً من U5** (N1): كودٌ **بلا هجرة** يخدم الترحيلَ نفسَه ولا يمسّ سلوكَ التطبيق. اختبارٌ واحدٌ في كلّ وقت، `OPENBLAS_NUM_THREADS=1`، المخرَجُ إلى ملفٍّ على D:.

| المعرّف | البند | مَن | البوّابة (مخرَجٌ يُلصق) | الرجوع |
|---|---|---|---|---|
| 10.7-2 · 10.6-1 … 10.6-13 | **`verify_backup`** بعقد §10.6 كاملاً: يقبل `.enc`/خام (`PGDMP`) · يقرأ المفتاح مباشرةً وخروج 3 عند غيابه ولا يستدعي `get_or_create_encryption_key` · الصريحُ إلى `mkdtemp()` خارج `BASE_DIR` بوضع 0600 ويُحذف في `finally` · `--write-plain` يرفض الموجودَ وداخلَ المستودع · `parse_copy_stats` نقيّة · **يطبع البصمات** (`merged_into` · `is_training` · `is_deleted` · بادئةُ/طولُ كلمة البريد · `imap_sync_enabled` · رأسُ الهجرة · أسماءُ المستخدمين · **+ جهاتٌ نشطة · ذاكرةُ ترويسة · عدّادات `BookSequence`**) · **+ sha256 للصريح قبل حذفه (C18)** · **+ sha[:8] للمفتاح المستعمَل (C29)** · لا يطبع أيَّ عمودٍ مشفَّر · `--expect-live` بـ`Book.all_objects` خروج 2 · `pg_restore` غائب خروج 4 (مع `PG_RESTORE_BIN` أو بحثٍ كـ`_find_pg_dump`) · `--expect` و`--json` · اختباراتٌ بلا PostgreSQL | ن | كلُّ متطلَّبٍ باختبار؛ **قائمةُ الطفرات ونتيجتُها الحمراء في السجلّ** (10.6-12) — لا حارسَ بلا طفرة | الفرعُ يُحذف |
| T10.4 | `get_or_create_encryption_key` يرفض السكَّ ما لم يكن `DEBUG` أو `ENCRYPTION_KEY_ALLOW_CREATE=1` (ثلاثةُ أسطر) + حارسٌ مُطفَّر؛ `settings_test` يضبط العلمَ إن احتاج | ن | خادمٌ بلا مفتاحٍ وبلا العلم ⟵ استثناءٌ صارخ؛ الطفرةُ تحمرّ؛ الحزمةُ خضراء | كما فوق |
| N4 | توسيعُ `scripts/probe_s0.py` (قراءةٌ فقط) بالبصمات نفسِها (C13) كي تصير بوّابتا 10.j/10.k نسخاً ولصقاً | ن | تشغيلٌ محلّيّ يطبع `merged_into=256 · is_deleted=45 · sequences 2433/358/455 · active_entities=353 · memory=5395 · bookemaillog f/p=0/0` | كما فوق |
| 9.3-4 · T9.5-1 · 9.1-2 | إعادةُ توليد `var/entity_doc_profiles.json` بـ`study_entity_layouts` و`var/entity_extraction_profiles.json` بـ`refresh_extraction_profiles` (**تحقّق أنّهما المولِّدان** بقراءة الأمرَين؛ نسخٌ قبلها في `D:/migration/backups/*pre_rekey*`) — قراءةٌ من القاعدة المحلّيّة غيرِ المقفولة بعد | ن | **0** مفتاحٍ في الملفّين يشير إلى نسخةٍ معطَّلة (كانت 25/25) · not-in-db=0 · الملفّان مُودَعان | استرجاعُ النسخ |
| N1 (الإيداع) | دمجُ verify_backup + T10.4 + N4 + var/*.json + هذا القسم إلى main بإيداعاتٍ بمساراتٍ صريحة | ن | `showmigrations core \| tail -1` = `0077` **[X]** ولا هجرةَ جديدة · `migrate --check`=0 · `git log origin/main` يحملها | `git revert` |
| U4 | `git tag cutover-2026-09 <main> && git push origin cutover-2026-09` **بعد** N1 | ن | `git tag -l cutover*` محلّيّاً وعلى origin يشير إلى الرأس الحامل للأداة | `git tag -d` + `push --delete` |
| بروفةُ 10.c/10.d | نسخةٌ تجريبيّةٌ **من مجلّد المشروع** (`create_encrypted_pg_backup()`) ثمّ `verify_backup … --expect-live` — تُثبت الأداةَ طرفاً إلى طرف. **لا تُشحَن** (درسُ 10.1) | ن | خروج 0 · sha المفتاح `1e639b05` · مسارُ `pg_dump` المستعمَل = `PostgreSQL/16/bin` · الجدولُ = 10.2 خام | حذفُ الملفّ التجريبيّ |
| 0.4 · 9.4(أ) | **الأساسُ المحلّيّ على رأس الوسم**: `showmigrations core\|tail -3` · `db_healthcheck --strict` · `models_healthcheck --strict --load --hash` · **الحزمةُ الكاملة** (مرّةً واحدة، إلى ملفّ) · أعدادُ `all_objects` (13,239/45/131/13,095/674/5,395/13,173) · فحصُ الوسائط · var/*.json · sha المفتاح=`1e639b05` و`wc -c`=44 | ن | كلُّ الأرقام مطابقةٌ لـ§2/10.2 · عددُ الاختبارات الخضراء يُلصق (آخرُ مقيس 1549@0076) | — |
| 0.5 · R5.8 | بيانُ الوسائط في WSL (`LANG=C.UTF-8`): `find . -type f -print0 \| sort -z \| xargs -0 sha256sum > /mnt/d/lettersys_data/manifests/books_<ts>.sha256` — يُشغَّل وحدَه (يقرأ 16 GB) | ن | الأسطر = **13,932** | — |
| T9.5-2 | تطبيعُ `merged_into` في `scripts/eval/entity_eval.py` و`entity_sealed_e100.py` (0 ذكرٍ اليوم) — يسبق 9.3-2/9.3-3 | ن | الإصابةُ تُحسب بفئات التكافؤ؛ اختبارٌ على زوجٍ مدموج | — |
| 9.3-2 · 9.3-3 (اختياريّان غيرُ حاجزَين) | قياسُ افتراض القسم على Tier A (≥56% · ≥40%) ثمّ E‑100c (≥55 · ≥63) **إن سمحت الذاكرةُ (≥2GB حرّة) وفي فترة انتظار** — لا يؤخّران الترحيل | ن | البوّابتان مسجَّلتان قبل النظر؛ النتائجُ في `EVAL_REGISTRY` بلا إرخاء | — |

**لماذا هنا:** verify_backup هو العقدةُ التي تُغذّي 10.d و10.h معاً ولا بوّابةَ بصماتٍ بدونه؛ وإعادةُ توليد var/ شرطُ خضرة 0.4 ووصولُها الخادمَ يكون في سحب الكود نفسِه (د3). **لا تتقدّم قبل:** د0 (شجرةٌ نظيفة). **لا تنتظر:** ردَّ [م] ولا المالك.

### 11.5 — د2: حكمُ S‑0 وإغلاقُ النزف الأمنيّ — **بانتظار [م] ثمّ فوراً** (م + مالك + ن)

| المعرّف | البند | مَن | البوّابة (مخرَجٌ يُلصق) | الرجوع |
|---|---|---|---|---|
| 8.3 · R5.10 | الحكمُ بجدول 8.3 على مخرَج S‑0 (مع `git rev-parse HEAD` من 0.3 وN6 لتفسير `migration_head` — C17) | ن + مالك | صفُّ الحكم مُسمّى في السجلّ | — |
| 8.4-د0 · R5.4 | **إن حُمِّل**: حزمةُ [م]‑2 فوراً: `UPDATE core_emailsettings SET imap_sync_enabled=false, is_active=false;` — **تُسجَّل في السجلّ قبل تنفيذها** (C0.7) | م | S‑0 ثانيةً: `imap_sync=False active=False` | `UPDATE … =true` |
| D6.13 · 8.4-د1 | **تدويرُ كلمة تطبيق Gmail — اليوم، غيرُ مشروطٍ بحكم 8.3** (C7): حذفُ القديمة من Google يُبطلها في الملفّ وWAL والنسخ والقناة المجهولة | مالك | إقرارٌ مكتوبٌ بالحذف + وقتُه | — (لا رجوع؛ هذا مقصود) |
| N3 (محلّيّاً) | إدخالُ الكلمة الجديدة **في التطبيق المحلّيّ من صفحة الإعدادات** (يمرّ على `save()` فتُشفَّر بـ`1e639b05`) **قبل 10.b** كي تحملها اللقطة | مالك + ن | عمودُ القاعدة المحلّيّة `LIKE 'enc::%'` وطولٌ ≈125 · إرسالُ بريدِ اختبارٍ من المحلّيّ ينجح | إعادةُ الإدخال |
| 8.4-د3 | ⟵ **مُدمَجٌ في 0.7** (د3)؛ لا يُنفَّذ مرّتين | — | — | — |
| 8.4-د6 · 8.4-د8 | **فقط** إن اختار المالك D6.14 = البناءُ على المُحمَّل: إدخالُ الكلمة على الحيّة بعد 0.7، ثمّ إثباتُ [م] `smtp_password LIKE 'enc::%'`=t وطولٌ ≈125 بلا طبع | مالك + م | كما نصُّ 8.4 | — |
| 8.5-ب · D6.15 | **بأيّ قناةٍ نُقل `lettersys_data.json` وأين نسخُه؟** — جوابٌ مكتوب | مالك | الجوابُ في السجلّ | — |
| 8.4-د9 · 8.5-أ | **بعد D6.15 والتدوير**: على الخادم `shred -u -z` لكلّ مسارٍ أخرجه `find` + إعادةُ `find` = 0 + وسمُ نسخ 09-08 «ملوَّثة»؛ محلّيّاً إتلافُ `lettersys_data.json` و`lettersys.dump` (بصمتاهما مسجَّلتان في N5) — **إتلافٌ لا نقل** | م + ن | `find / -xdev -name 'lettersys_data*.json'` = 0 · الملفّان غائبان محلّيّاً | لا رجوع (مقصود) |
| 8.4-ت | تنظيفُ المُحمَّل (99 تدريب · 6 حسابات تُعطَّل · 281 جلسة؛ admin12 بحسب D6.2) — **فقط** إن D6.14 = البناء (وهو يُلغي §10) | م | يُكتب حينَه | — |
| D6.14 | **يُطرح فقط** إن `ids>13386` > 20 | مالك | جوابٌ مكتوب | — |
| D6.12 | تدويرُ كلمة سرّ SQL Server القديمة (`docs/LAUNCH_RUNBOOK.md`) — مستقلٌّ عن النافذة، يُنفَّذ مع D6.13 | مالك | إقرارٌ مكتوب | — |
| C25 (شرط) | **لا تُفتح د5 إلّا إذا** كان حكمُ 8.3 في الفروع الثلاثة الأولى | ن | سطرٌ في السجلّ | — |

**لماذا هنا:** IMAP يُنتج ضرراً جديداً كلَّ 10 دقائق على قاعدةٍ لم تُقبَل، والكلمةُ الصريحة على قرصٍ وقناةٍ مجهولة — كلاهما نزفٌ لا ينتظر النافذة. والإدخالُ المحلّيّ للكلمة الجديدة (N3) يُغلق فخَّ «لقطةٌ تحمل كلمةً ميتةً تمرّ ببوّابة 16 16». **لا تتقدّم قبل:** عودةِ S‑0. **إن تأخّر [م] أيّاماً:** D6.13 وD6.12 وN3 لا تنتظره.

### 11.6 — د3: تحصينُ الخادم واستقبالُ الوسائط والمفتاح والكود — **بانتظار [م]** (م + ن)

> **حزمةُ [م]‑3.** شرطُها: جوابا D6.5 وD6.6، وإيداعُ د1 على origin (N1). كلُّها **قبل النافذة** وبلا مسٍّ للقاعدة الحيّة. **الترتيبُ داخلها ملزم**: المفتاحُ قبل الكود (T10.4 على خادمٍ بلا مفتاحٍ يصرخ عند أوّل قراءة).

| المعرّف | البند | مَن | البوّابة (مخرَجٌ يُلصق) | الرجوع |
|---|---|---|---|---|
| D6.6 | إنشاءُ حساب النقل لـrsync (أو مخزنٍ نوفّره) | م | اتّصالُ ssh تجريبيّ ينجح | حذفُ الحساب |
| 0.8 · R5.3 · N7 | تحصينٌ لا يمسّ القاعدة: swap 2–4 GB · `.env`: `TESSERACT_ADAPTIVE_THRESHOLD=0` · `OMP_THREAD_LIMIT=1` · `BACKUP_DIR` خارج `/` · gunicorn `--timeout 180` · celery `--concurrency 1` · **تسجيلُ رقم قاعدة Redis من `REDIS_CACHE_URL`** لبوّابة 4.3 | م | `swapon --show` · ExecStart فيه timeout · `.env.bak` موجود · رقمُ Redis في السجلّ | `.env.bak` |
| 0.7 · 8.4-د3 · 10.f(أ) · 10.g(كتابة) · R5.1 | **شحنُ المفتاح مرّةً واحدة**: [ن] يتحقّق محلّيّاً sha[:8]=`1e639b05` و`wc -c`=44 ثمّ يلصقه في جلسة ssh **بزمنٍ آخر** عن أيّ ملفّ (لا بريد/دردشة/git). [م] **في الخطوة نفسِها**: `ls -la .encryption_key*` ⟵ إن وُجد `mv .encryption_key .encryption_key.selfminted_<ts>` (لا يُحذف) ⟵ كتابةُ 44 بايتاً بلا سطرٍ جديد ⟵ `chown <svc>: ; chmod 600` | ن + م | `sha256sum .encryption_key \| cut -c1-8` = **1e639b05** · `wc -c` = **44** · `stat -c '%U %a'` = svc/600 | إعادةُ المُعاد تسميتُه |
| N1 (السحب) | **سحبُ الكود إلى الوسم بلا نشر**: `cd /var/www/lettersys && git fetch --tags origin && git checkout cutover-2026-09 && git lfs pull` — **لا `lettersys-deploy`** (يشغّل `reset --hard` + `migrate` + restart) **ولا `migrate` ولا إعادةَ تشغيل** — مُسجَّلٌ في السجلّ قبل تنفيذه (C0.7) | م | `git rev-parse HEAD` = sha الوسم · `.venv/bin/python manage.py migrate --check` خروج 0 · `showmigrations core \| tail -1` = 0077 · `manage.py verify_backup --help` يعمل · `ls scripts/probe_s0.py` · `models_healthcheck --strict --load` خروج 0 (إعادةُ بوّابة 0.3 HEAD) | `git checkout <HEAD السابق>` |
| 0.6 · R5.8 · 2.6⟵10.e · D6.5 | **المزامنةُ الكبرى للوسائط** (خارج النافذة، قابلةٌ للاستئناف): `rsync -rlt --partial --partial-dir=.rsync-partial --info=progress2 --chmod=D755,F644 --exclude '.rsync-partial' /mnt/d/lettersys_data/media/books/ <user>@<host>:/var/www/lettersys/media/books/` (لا `-a`؛ `--bwlimit` نهاراً؛ لا `scan_inbox`/`tmp`/`training_datasets`) | ن + م | [م]: `sha256sum -c books_<ts>.sha256 \| grep -vc ': OK$'` = **0** · `find \| wc -l` = **13,932** · `df -h` ≥ **4 GB** | `rm -rf` المجلّد المنسوخ |
| 3.1 (مرجع) | ⟵ مُستبدَلٌ بـ10.f (د6) مع نقل لقطة VPS (C27) | — | — | — |
| P3 (قراءة) | قراءةُ نصّ `lettersys-deploy` (من 0.3) الآن وتدوينُ ما يلزم تعديلُه (لا `makemigrations`؛ `models_healthcheck --strict` بوّابة) — **التعديلُ نفسُه في د9** | ن | ملاحظاتٌ في السجلّ | — |

**لماذا هنا:** الوسائطُ أطولُ بندٍ زمنيّاً (≈16 GB) ويجب أن تسبق التجميدَ وإلّا امتدّ الانقطاعُ بزمنها؛ والمفتاحُ المبكّر يمنح وقتاً لإصلاح CRLF/BOM ويطوي «زمناً آخر» تلقائيّاً؛ وسحبُ الكود قبل T‑1 يمنع اكتشافَ غياب verify_backup بعد التجميد. **لا تتقدّم قبل:** د1 مُودَعة على origin، وD6.5/D6.6.

### 11.7 — د4: بوّابةُ الإقلاع — **بانتظار المالك** (مالك)

| المعرّف | البند | مَن | البوّابة (مخرَجٌ يُلصق) | الرجوع |
|---|---|---|---|---|
| 0.1 · G4 | جوابٌ مكتوبٌ على **كلّ** بندٍ في §6 (الرسالةُ الواحدة 11.14-أ) + تثبيتُ موعد النافذة | مالك | جدولُ الأجوبة في السجلّ | — |
| D6.1 | موعدُ النافذة ومدّتُها ومَن يبلّغ المُدخِلين | مالك | تاريخ · HH:MM · مدّة · مُبلِّغ | — |
| D6.2 | الحساباتُ الستّة تُعطَّل؛ **admin12 (9 كتب) تدريبٌ أم حقيقيّ؟** كتابا testuser؟ | مالك | ثلاثةُ أجوبة (تُغذّي 10.k③ — C22) | — |
| D6.3 | حذفُ 131 تدريباً + 117 مرفقاً نهائيّاً (الرجوعُ = `.enc` + القاعدةُ المحلّيّة — C24) | مالك | نعم/لا | — |
| D6.4 | متى IMAP؟ وهل SMTP من الخادم؟ (يُغذّي 10.k① و5.3 — C6) | مالك | توقيتُ IMAP · نعم/لا SMTP | — |
| D6.5 | القرص: هامش ~4–5 GB أم ترقيةٌ قبل الوسائط؟ وجهةُ النسخ خارج الخادم واحتفاظُها | مالك | جواب (بعد `df -h` من 0.3) | — |
| D6.6 | حسابُ النقل: [م] يُنشئه أم مخزنٌ نوفّره؟ | مالك | جواب | — |
| D6.7 | مصيرُ النسخة المحلّيّة: أرشيفٌ للقراءة (**موصى**) أم احتياطٌ | مالك | جواب | — |
| D6.8 | النشرُ التلقائيّ: على كلّ دفعٍ أم يدويٌّ دائماً؟ إصلاحُ المفتاح (أ) أم (ب)؟ | مالك | جواب (يُغذّي P2/P5) | — |
| D6.10 | لقطةُ VPS من Hostinger قبل التبديل؟ (تُدرَج في 10.f — C27) | مالك | نعم/لا + توقيت | — |
| D6.11 | دمجُ #16 بعد الاستقرار فقط (= P6) | مالك | توقيت | — |
| D6.9 | TLS/الكوكي — لا تُغيَّر في النافذة (**محسوم**) | — | — | — |
| C25 (شرط) | حكمُ 8.3 (د2) في الفروع الثلاثة الأولى | ن | سطرٌ في السجلّ | — |

**لماذا هنا:** لا إقلاعَ قبل الحسم (§6 بنصّها)؛ وجمعُ الأجوبة في رسالةٍ واحدةٍ مُرسَلةٍ في د0 يجعل هذه الدفعةَ انتظاراً لا عملاً. **لا تتقدّم قبل:** د1 ود2 ود3 خضراء **و**كلُّ الأجوبة مكتوبة.

### 11.8 — د5: النافذة T‑0 — التجميدُ واللقطة — **داخل النافذة** (ن + مالك)

> **قواعدُ النافذة سارية** (W1–W6 بصياغة C19): لا دفعَ لـmain · لا دمجَ PR · لا `gh workflow run` · لا shell **كاتباً** على أيّ قاعدةٍ حيّة · Merge9.md يُلحَق محلّيّاً ويُدفع بعد القبول.

| المعرّف | البند | مَن | البوّابة (مخرَجٌ يُلصق) | الرجوع |
|---|---|---|---|---|
| 1.1 | إبلاغُ المُدخِلين (sarah · zainab · abdulrahman) بوقف الإدخال HH:MM + تسجيلُ آخر رقمٍ لكلّ سجلّ | مالك | رسالةٌ مؤرّخة + الأرقام (المتوقَّع 2433/358/455) | — |
| 10.b · 1.2 · 1.3 · R5.6 | إيقافُ الخادم المحلّيّ (`schtasks /End … ; /Change … /DISABLE` + قتلُ runserver/celery بـ`taskkill /T /F` — فخُّ الزومبي) ثمّ **قفلُ القاعدة قراءةً**: `ALTER DATABASE lettersys SET default_transaction_read_only=on;` + `pg_terminate_backend` | ن | لا مستمعَ على 8000 · `count,max(created_at)` ثابتٌ بفحصَين بفاصل 5 د (13,239 / 2026-08-30 07:38:39) · `auth_user`=12 | `/Change /ENABLE; /Run` · `RESET default_transaction_read_only` |
| N2 · 2.5 | **دلتا الوسائط** (C12): أمرُ 0.6 بـ`-n -i` أوّلاً ثمّ فعليّاً | ن + م | الجافّ 0 نقلٍ (أو ملفّاتٌ معدودة تحت `books/`) ثمّ `sha256sum -c` = 0 فشل | — |
| 10.c · 9.1-4 · 2.1⟵ · 2.4⟵ | **نسخةٌ جديدة من مجلّد المشروع** (C29): `python manage.py shell -c "from core.backup_service import create_encrypted_pg_backup as b; print(b())"` (قراءةٌ تحت القفل — مسموحة) | ن | `D:/trackbackup/pg_backup_<ts>.dump.enc` ≥ **4 MB** (القائمةُ 4,247,544 ب) · مسارُ pg_dump المستعمَل = 16 | — |
| 10.d · R5.12 · 10.1 · 10.2 | `python manage.py verify_backup D:/trackbackup/pg_backup_<ts>.dump.enc --expect-live` ثمّ `sha256sum` ⟵ `<ts>.dump.enc.sha256` | ن | خروج **0** · الجدولُ = 10.2 خام **بما فيه `merged_into=256` · `is_deleted=45` · نشطة 353 · ذاكرة 5,395** · sha المفتاح **1e639b05** · **sha(enc) وsha(plain) يُلصقان** | أعد 10.c |
| 10.e · 2.6⟵ | تسليمُ `.enc` + `.sha256` بأيّ قناة — **المفتاحُ سبق في 0.7 (د3) فلا يسافر شيءٌ معه** | ن ⟶ م | إقرارُ [م] بالوصول + `sha256sum -c` OK | — |
| 2.2 · 2.3 (مرجع) | ⟵ مُستبدَلان بـ10.i/10.k على الخادم (البروفةُ المحلّيّة مستحيلة — مقيس) | — | — | — |

**لماذا هنا:** اللقطةُ **بعد** القفل وفحصَي الثبات (درسُ 10.1 وقع فعلاً: 198≠256)؛ والقاعدةُ المحلّيّة مصدرُ الرجوع الوحيد فصفرُ كتابةٍ عليها من الآن. **لا تتقدّم قبل:** د4 كاملة (كلُّ D6.x + الموعد + شرطُ C25) ود3 خضراء (الوسائطُ والمفتاحُ والكودُ على الخادم).

### 11.9 — د6: النافذة — الاستعادةُ الجانبيّة على الخادم — **داخل النافذة** (م)

> **حزمةُ [م]‑4 (الجزء الأوّل).** التطبيقُ يعمل على القديمة بلا انقطاع. **لا `pg_restore` فوق `lettersys` الحيّة أبداً.** الرجوعُ الدائم: `DROP DATABASE lettersys_new` وإعادةُ 10.i — دقائق.

| المعرّف | البند | مَن | البوّابة (مخرَجٌ يُلصق) | الرجوع |
|---|---|---|---|---|
| 10.f(ب) · 3.1⟵ · D6.10 | `pg_dump -Fc -f /var/backups/lettersys/pre_cutover_<ts>.dump lettersys` · `cp .env .env.pre_cutover` · `ls -la .encryption_key*` · `df -h /` · **+ لقطةُ VPS من Hostinger إن قرّرها D6.10 (C27)** | م | dump > 0 · حرٌّ ≥ 4 GB · اللقطةُ موجودة | — |
| 10.g (تحقّقٌ فقط — C11) | `sha256sum .encryption_key \| cut -c1-8` · `wc -c` · `stat -c '%U %a'` | م | **1e639b05** · **44** · svc/600 | أعد 0.7 |
| 10.h · 10.6-13 · 10.4 | `sha256sum -c pg_backup_<ts>.dump.enc.sha256` ثمّ `cd /var/www/lettersys && .venv/bin/python manage.py verify_backup <path>.enc --write-plain /var/backups/lettersys/incoming/ship.dump` | م | OK · **الجدولُ مطابقٌ حرفيّاً لمخرَج 10.d** — وهو إثباتُ المفتاح (10.4) | `InvalidToken` ⟵ أعد 0.7؛ لم يُمسّ شيء |
| 10.i · 3.2⟵ · 3.3⟵ · R5.7 | `CREATE DATABASE lettersys_new WITH OWNER lettersys_user ENCODING 'UTF8' LC_COLLATE 'C.UTF-8' LC_CTYPE 'C.UTF-8' TEMPLATE template0;` ثمّ `pg_restore --no-owner --no-privileges --exit-on-error -h localhost -U lettersys_user -d lettersys_new …/ship.dump 2>restore.err; echo rc=$?` ثمّ `ANALYZE;` | م | `rc=0` (أو أسطرُ `pg_trgm` وحدها ⟵ `CREATE EXTENSION pg_trgm` بحساب postgres ثمّ إعادة) | `DROP DATABASE lettersys_new` |
| 10.j · 3.4⟵ · 3.5⟵ · R5.2 · 9.3-1(healthcheck) · 10.5 | على `lettersys_new` (المتغيّرُ يغلب `.env`): `DB_NAME=lettersys_new … shell -c "exec(open('scripts/probe_s0.py').read())"` (الموسَّع N4) · `… migrate --check` · `… models_healthcheck --strict --load` · **+ فحصُ var/*.json ضدّ القاعدة (C3/C28)** | م | S‑0 = **10.2 خام** (13,239 · تدريب 131 · محذوف 45 · جهات 674/نشطة 353/`merged_into` 256 · ذاكرة **5,395** · مرفقات 13,187 · قيود 22,313 · مستخدمون 12 · `mail: enc=True len=125` · `migration_head=0077`) · الأمران يخرجان 0 · var not-in-db=**0** | DROP + 10.i |
| 10.k · 2.3⟵ · R5.4 · R5.5 | **بهذا الترتيب**: ① `UPDATE core_emailsettings SET imap_sync_enabled=false, is_active=false;` (C6) ② `purge_dev_seed_books` معاينةً ثمّ `--yes` ③ تعطيلُ حسابات الفحص (`is_active=false`، **لا حذف**؛ admin12 وtestuser **بحسب جواب D6.2 المدوَّن** — C22) ④ `TRUNCATE django_session;` ⑤ `UPDATE core_backupsettings SET enabled=false;` | م | S‑0 ثانيةً = **10.2 منظَّف**: كتب 13,108 · تدريب 0 · محذوف 13 · جهات 674 · `imap false` · جلسات 0 · عدّادات **2433/358/455** · **المستخدمون النشطون = الحقيقيّون وحدهم (بالاسم) · `bookemaillog` failed/pending = 0** (C28) | DROP + 10.i |
| 10.l · R5.1 | `DB_NAME=lettersys_new … shell -c "from core.models import EmailSettings as E; e=E.get(); print(len(e.smtp_password or ''), len(e.imap_password or ''))"` · `ls -la .encryption_key*` | م | **`16 16`** (125 = فكٌّ فاشلٌ مبتلَع) · **ملفٌّ واحدٌ باسم `.encryption_key` بالضبط** وكلُّ ما سواه `selfminted_` ولا mtime بعد 10.g (C20) | إن فشل: أعد 0.7 **قبل أيّ تبديل** |

**لماذا هنا:** بوّابتان رقميّتان تفصلان خطأَ الاستعادة عن خطأ التنظيف (10.5)؛ و10.j أوّلُ عمليّةِ جانغو تلمس `lettersys_new` — بعد المفتاح لا قبله (خطرُ السكّ الثالث). **لا تتقدّم قبل:** 10.e (إقرارُ الوصول).

### 11.10 — د7: التبديلُ والدخان (انقطاعٌ ≤10 دقائق) — **داخل النافذة** (م + مالك + ن)

| المعرّف | البند | مَن | البوّابة (مخرَجٌ يُلصق) | الرجوع (≤3 د) |
|---|---|---|---|---|
| 10.m · 4.1 · D6.10 | `systemctl stop celery-beat celery gunicorn` (لقطةُ VPS إن D6.10 **قبله**) | م | `pg_stat_activity` للقاعدتَين = 0 (وإلّا terminate) | `systemctl start` |
| 4.2 | psql واحدة: `ALTER DATABASE lettersys RENAME TO lettersys_old_<date>; ALTER DATABASE lettersys_new RENAME TO lettersys;` | م | `\l` يُظهر التبديل | العكسُ بإعادة التسمية |
| 4.3 · R5.5 | `redis-cli -n <N من N7> FLUSHDB` (لا FLUSHALL) | م | `DBSIZE`=0 | — |
| 4.4 | `migrate --check` ⟶ `migrate` (لا-عمل) ⟶ `collectstatic --noinput` ⟶ `models_healthcheck --strict --load` | م | كلُّها 0 · **لا «Applying»** (وإلّا توقّفٌ ورجوعٌ عكسيّ) | 4.2 عكسيّاً |
| 4.5 | `systemctl start gunicorn celery celery-beat` | م | `journalctl -u gunicorn -n50` بلا traceback · `curl -sI /login/` = 200 | إيقاف + 4.2 عكسيّاً + `.env.pre_cutover` |
| 4.6 | **دخانٌ بالعين**: دخول · القائمة **13,095** · كتابُ 2025 يعرض `825`+وسم · 2026 مجرّد · الرقمُ التالي 2433/358/455 · مرفقٌ يُفتح (بلا جلسة ⟶ login؛ غيرُ مالك ⟶ 403) · بحثُ جهة (النشطة 353) · لوحةُ الأقسام · رفعُ مستندٍ وقياسُ زمن (<30 ث) · `df -h` | مالك + ن (+م لـdf) | كلُّها خضراء **بلقطاتٍ تُقرأ** | كما 4.5 |

**لماذا هنا:** كما في الوثيقة؛ التغييرُ الوحيد أنّ رقمَ Redis صار مقيساً (N7) وأنّ الجهاتَ النشطة 353 لا 411. **لا تتقدّم قبل:** 10.l خضراء حرفيّاً.

### 11.11 — د8: اليومُ الأوّل بعد التبديل — **بعد القبول** (مالك + م + ن)

| المعرّف | البند | مَن | البوّابة (مخرَجٌ يُلصق) | الرجوع |
|---|---|---|---|---|
| 5.1 | sarah تُدخل أوّلَ كتابٍ حقيقيّ (رقم **2433**) بحضور المالك؛ قياسُ زمن OCR | مالك + ن | الرقمُ 2433 مُخصَّص · زمنُ OCR مُسجَّل (<30 ث) — **بعده لا رجوعٌ نظيف** | — |
| 5.2 · D6.7 | الخادمُ المحلّيّ **لا يعود**: القفلُ باقٍ، المهمّةُ معطَّلةٌ دائماً، إعلامُ الجميع بالعنوان الجديد | ن + مالك | رسالةُ الإعلام مؤرّخة · `schtasks /Query` = Disabled | **لا يُنقَض أبداً** (مصدرا حقيقةٍ متصادمان) |
| N3 (احتياط) · 8.4-د8 | **إن لم تحمل اللقطةُ الكلمةَ الجديدة** (تدويرٌ بعد 10.b): المالكُ يُدخلها من الإعدادات على الإنتاج، و[م] يُثبت `smtp_password LIKE 'enc::%'`=t وطولٌ ≈125 بلا طبع؛ ثمّ بريدُ اختبار | مالك + م | `t` · ≈125 · بريدٌ يصل | إعادةُ الإدخال |
| 5.3 · D6.4 · D6.13 · R5.4 | **تفعيلُ `is_active` ثمّ IMAP بحسب D6.4** (C6) عمداً في ساعة عملٍ ومراقبةُ `journalctl -u celery -f` 10 دقائق — بالكلمة المدوَّرة ومن IP قُرِّر | م + مالك | 10 دقائق بلا خطأ مصادقة · أوّلُ `IncomingEmail` جديد | `imap_sync_enabled=false` |
| 7.4-أ · T7.5-4 · 9.3-10ب · G6 | **تعيينُ مسؤول الأرشفة** من `/books/admin/?tab=users` (خانتا «طاولة البريد»/«طاولة الأرشفة») + اصطلاحُ تسمية الرفوف (`خ2/ر3/ص7`) — قرارُ مالكٍ لا كود | مالك | مجموعةُ «مسؤول الأرشفة» **غيرُ فارغة** · لقطةُ `/books/desk/archive/` تُفتح للمعيَّن | — |
| 9.3-1 (البقيّة) | `TESSERACT_ADAPTIVE_THRESHOLD=0` · `OMP_THREAD_LIMIT=1` سبقا في 0.8؛ **لا `prepare_entities`** (C10)؛ «لا جهات» = قاعدةٌ فارغة لا عطب | — | زمنُ الرفع من 4.6 | — |
| T9.5-3 | Entity #526 «الادارة» رمزُها بريدٌ إلكترونيّ — يُصحَّح **من الواجهة على الإنتاج** (لا كتابةَ على المحلّيّة المقفولة) | ن | الرمزُ الجديد ظاهر | تعديلٌ عكسيّ من الواجهة |
| 9.3-5 | الحالاتُ الأربع (عكاس · فريق التحليل الأوّل/#99 · «شرقي بغداد» #624 · «متابعة تنفيذ الموازنة الخارجيّة» #538) + كتبُ ZPEC الـ32 — قرارُ المالك ثمّ دمجٌ **من الواجهة على الإنتاج** (`merge_entities` يُعيد توجيه الذاكرة) | مالك ثمّ ن | جوابٌ مكتوبٌ لكلّ حالة · `merged_into` بعدها · **دَينٌ يُسجَّل: إعادةُ 9.3-4 لاحقاً** | — |
| W1 (رفع) | دفعُ سجلّ التنفيذ المتراكم إلى origin بعد القبول (وثائقُ فقط) | ن | `git log origin/main` | — |

**لماذا هنا:** كلُّ ما يحتاج إنتاجاً حيّاً مقبولاً؛ وتعيينُ مسؤول الأرشفة **أولويّةُ المالك** وكلفتُه دقائق — كلُّ ما بُني في 4‑ج بلا مستخدمٍ حتّى الساعة. **لا تتقدّم قبل:** 4.6 خضراء.

### 11.12 — د9: الاستقرارُ وإعادةُ تمكين النشر — **بعد الاستقرار** (ن + م + مالك)

| المعرّف | البند | مَن | البوّابة (مخرَجٌ يُلصق) | الرجوع |
|---|---|---|---|---|
| P1 · 5.5 | يومان عملٍ بلا رجوعٍ و`lettersys_old_<date>` باقية | ن + م | تاريخان · `\l` يُظهرها | — |
| D6.8 ⟶ P2 | إصلاحُ مفتاح النشر: (أ) [م] يُلحق العامَّ المطابقَ لسرّ GitHub في `authorized_keys`؛ أو (ب) [ن] يولّد زوجاً ⟶ `gh secret set DEPLOY_SSH_KEY` ⟶ [م] يركّب العامّ | ن + م | `ssh-keygen -lf` تتطابق بين الخادم والسرّ (لا يُطبَع الخاصّ) | إعادةُ السرّ القديم |
| P3 · C0.7 | تعديلُ `lettersys-deploy`: لا `makemigrations` أبداً · `models_healthcheck --strict --load` بوّابةً قبل restart — كتابةٌ على الخادم **تُسجَّل قبلها** | م | نصُّ السكربت بعد التعديل في السجلّ | نسخةُ السكربت القديمة |
| P4 · C0.4 · W3 | `gh workflow enable deploy.yml` (الملفُّ `workflow_dispatch`-فقط) ⟶ `gh workflow run` يدويّاً ⟶ `gh run watch` | ن | نجاحٌ + HEAD الخادم = HEAD المتوقّع · `migrate --check`=0 | `gh workflow disable` |
| P5 | PR يُعيد سطرَي `push` في `deploy.yml:26–28` — دمجُه أوّلُ نشرٍ تلقائيّ (**يُسقَط** إن اختار المالك «يدويّ دائماً» في D6.8) | ن + مالك | PR مدموج · `gh run watch` ناجح | revert |
| P6 · 7.4-ج · D6.11 · W2 | **دمجُ PR #16** بعد `rebase` على main الحاليّ (الفرعُ @7940ab0 مُرصَّفٌ على 8230e91) — رفعُ `?v=`/`CACHE_VERSION` (main v96 · الفرعُ يفترض v97) | ن | 1568+ خضراء · صفرُ هجراتٍ معلّقة · PR مدموج · النشرُ التلقائيّ ناجح | revert |
| 5.4 · D6.5 | الاحتفاظُ بـ`lettersys_old_<date>` والنسخ **7 أيّام** ثمّ `DROP` · نسخةٌ خارج الخادم (بحسب D6.5) · نسخُ 09-08 «الملوَّثة» تُحذف | م | 7 أيّامٍ منذ 5.1 · النسخةُ الخارجيّة موجودة (sha) · `\l` بلا `lettersys_old` | لا رجوع (مقصود) |

**لماذا هنا:** الترتيبُ من الوثيقة حرفيّاً (P1⟶P6)، مع تثبيت أنّ **أيَّ إيداعٍ على main بعد P5 = نشرٌ فوريّ** — فلا تُعاد سطرا `push` قبل نجاحٍ يدويٍّ مُثبَت. **لا تتقدّم قبل:** P1.

### 11.13 — د10: ديونُ الكود بعد أوّل نشرٍ تلقائيّ — **بعد الاستقرار** (ن)

> تُكتب في فرعٍ (worktree قصير المسار) وتُدمَج **بعد P5** — لأنّ 8.6-أ يحمل هجرة `AlterField` تكسر بوّابةَ 0077 لو سبقت.

| المعرّف | البند | مَن | البوّابة (مخرَجٌ يُلصق) | الرجوع |
|---|---|---|---|---|
| 8.6-أ · R5.9 | `EncryptedCharField` (`from_db_value` / `get_prep_value` — يُستدعى حتّى مع `raw=True` و`QuerySet.update()` / `value_to_string` يُخرج `enc::`) + حذفُ `EncryptedFieldsMixin` (`core/models.py:1320`) + هجرةٌ واحدة `AlterField` بلا SQL | ن | **5 حرّاسٍ مطفَّرة**: dumpdata بلا صريح · loaddata لصريحٍ يُخزَّن `enc::` · لا تشفيرَ مضاعفاً · `update()` يشفّر · مفتاحٌ مبدَّلٌ يبقى `enc::` | revert |
| 8.6-ب | حارسُ `db_healthcheck` وفحصِ الإقلاع: لكلّ عمودٍ مشفَّر `count(*) WHERE col<>'' AND col NOT LIKE 'enc::%'` = 0 وإلّا خروجٌ ≠0 يُسمّي الجدولَ والعمودَ **دون القيمة** — ولا شفاءَ ذاتيّاً | ن | طفرةٌ تُدخل صريحاً ⟵ الأمرُ يحمرّ | revert |
| 8.6-ج | تظليلُ `dumpdata`: يرث الأصليّ · يفرض `--all` · يستثني نماذجَ الأسرار · يرفض الكتابةَ داخل المستودع · يفحص مخرَجَه فيحذفه ويفشل إن وجد مشفَّراً/صريحاً | ن | الشروطُ الأربعة مطفَّرة | revert |
| T7.5-2 | السطران 89 و147 في `core/extraction/views/ui.py` (`is_staff`) ⟵ `can_open_content` (فخّ: عدِّل اختبارَ «is_staff يرى الكلّ» لا الحماية) | ن | أرشيفيٌّ يعدّل كتاباً قائماً من الإدخال الذكيّ **بلقطةٍ تُقرأ** | revert |
| T7.5-1 | شرطُ المنحة (`_has_live_grant`) في `_unauthorized_secret_q` (`core/scoping.py:380`) + فاعلٌ عاشرٌ بمنحةٍ في حارس التطابق | ن | الحارسُ يحمرّ قبل الإصلاح ويخضرّ بعده | revert |
| T7.5-3 | إزالةُ نظام الأدوار الميت في `core/views/users.py` (`controller`, `data_entry`) — لا يُضاف إليه | ن | صفرُ مرجعٍ للاسمَين · 270 مساراً بلا تكرار · الحزمةُ خضراء | revert |
| 7.4-هـ | الدفعة 5 — الجذور: كنسُ مواضع `is_deleted=False` (مقيس اليوم **92 موضعاً في 37 ملفّاً** لا 73 — أعد العدّ) · الشريطُ بالدور · حارسُ «مبنيٌّ ولا يوصله أحد» · حارسُ «لا شرطَ دورٍ في قالب» | ن | الحارسان يُطفَّران فيحمرّان | revert |

**لماذا هنا:** كلُّها تُحسّن الوقايةَ والحرفةَ ولا تخدم الترحيلَ نفسَه؛ وواحدةٌ منها تحمل هجرة. **لا تتقدّم قبل:** P5 (أوّلُ نشرٍ تلقائيٍّ ناجح) — وP6 لـ7.4-هـ.

### 11.14 — د11: المنتَجُ والاستخراج — **بعد الاستقرار، بحسب العتبات** (ن + مالك)

| المعرّف | البند | مَن | البوّابة (مسجَّلةٌ قبل النظر) | الرجوع |
|---|---|---|---|---|
| 7.4-ب | أسبوعُ استعمالٍ حقيقيّ ثمّ يومُ رصدٍ: أيُّ الطوابير الخمسة فُرِّغ؟ أيُّها هُجر؟ (لا تقرأ الطابورَ من `is_archived` — فخّ 7.7) | ن + مالك | تقريرٌ بأعدادٍ لكلّ طابور | — |
| 7.4-د · 9.3-9 | **الدفعة 4‑ب**: `/books/desk/chat/` بريدُ الوحدات + `/books/desk/mine/` + هجرة **0078** (`received_at`·`opened_at`·`opened_by`·`done_at`) — 3–4 جلسات؛ **لا تُبنى قبل 7.4-ب** («وإلّا بُنيت على تخمين») | ن | «منجَز» = طرفٌ إلى طرفٍ في المتصفّح بلقطةٍ تُقرأ · PWA `?v=`+`CACHE_VERSION` مرفوعان | revert |
| 7.4-و | لاحقاً: 6 التخويلُ المتبقّي · 7 الواجهاتُ القديمة · 8 اللحظيّ (**يحتاج Redis — قرارُ ز0**) · 9 التدريبات · 10 التواقيع طور 2 | ن + مالك | لا بوّابةَ مكتوبة — تُسجَّل عند فتح كلٍّ منها | — |
| 9.3-6 | **قارئُ التاريخ**: حصادُ 7,763 زوجَ حبرٍ (بعد 0060) ⟵ **مصادقةُ العين على 150 قصاصة/وسم** ⟵ دفترُ كاغل ⟵ تدريبٌ واحدٌ بيد المالك ⟵ حُكمُ العين على date‑100 (لا تدريبَ عليها) | مالك ثمّ ن | أخضر ≥ **30%** تغطية بدقّة ≥ **90%** (اليوم 7% · 4/7) | — |
| 9.3-7 · T9.5-5 | **العدد**: كاشفُ صنفين (عدد + موضوع) من `harvest_subject_boxes` (لا يُقتبَس رقمٌ منه قبل النظرة) | ن + كاغل بيد المالك | نظرةُ e2e‑G واحدة: إصابة ≥ **65%** طرفاً لطرف على 100 لم تُرَ (اليوم 57) | — |
| 9.3-8 | **الموضوع**: لا عملَ قبل **50** تصحيحاً حقيقيّاً (اليوم 3/50) ثمّ جلسةُ تنقيبِ قواعد واحدة | ن | العتبةُ من `capture_stats` | — |
| 9.3-2 · 9.3-3 (إن لم تُنفَّذ في د1) | Tier A (≥56 · ≥40) ثمّ E‑100c (≥55 · ≥63) — بعد T9.5-2 | ن | كما سُجِّل؛ بلا إرخاءٍ بعد النظر | — |
| 9.3-4 (إعادة) | إعادةُ توليد var/ بعد أيّ دمجٍ لاحق (9.3-5) | ن | 0 مفتاحٍ لنسخة | — |
| T9.5-4 | شجرةُ الأقسام (اللجان/الادارة/الارشفة بلا أب) بـ`set_department_parents` — يفيد التقارير لا الاستخراج | ن + مالك | الأبناءُ ظاهرون في التقارير | revert |
| 9.3-10ج | سياسة **محسومة**: لا ذاكرةَ سرّيّةٍ أو نوع (96.5% = الأساس؛ النوعُ يختاره الكاتب) — لا تُفتح ثانية | — | — | — |
| 9.4 | ترتيبُ 9.4 **يُستبدَل بترتيب هذا القسم**؛ «ما لن أفعله» فيه يبقى سارياً | — | — | — |

**لماذا هنا:** الأولويّةُ **التشغيلُ لا ميزةٌ جديدة** (حسمُ §7.4)؛ وكلُّ بندٍ هنا مشروطٌ بعتبةٍ لا تتراكم إلّا بإدخالٍ حقيقيّ على الإنتاج. **لا تتقدّم قبل:** د8 (الإنتاجُ مقبولٌ ويعمل عليه المُدخِلون).

---

### 11.15 — رسائلُ جاهزة (تُرسَل ولا تُنفَّذ من عندنا)

**(أ) رسالةُ المالك الواحدة — تُرسَل في د0، يُجاب عنها بسطرٍ لكلّ بند:**

| # | السؤال | التوصية |
|---|---|---|
| D6.13 | **فعلٌ اليوم**: احذف كلمةَ تطبيق Gmail الحاليّة من حساب Google وأنشئ جديدة، ثمّ أدخِلها في **التطبيق المحلّيّ** من صفحة الإعدادات (N3) | نعم، اليوم، قبل أيّ شيءٍ آخر (القناةُ مجهولة والصريحُ في WAL والنسخ) |
| D6.15 | **جوابٌ مكتوب**: بأيّ قناةٍ نُقل `lettersys_data.json` إلى الخادم؟ أين نسخُه الآن (جهاز · بريد · USB · سحابة)؟ | كلُّ نسخةٍ تُتلَف بعد التدوير |
| D6.12 | تدويرُ كلمة سرّ SQL Server القديمة | نعم، مع D6.13 |
| D6.1 | موعدُ النافذة (تاريخ · HH:MM · المدّة) ومَن يبلّغ sarah/zainab/abdulrahman | بعد اكتمال الوسائط (0.6) — لا قبل |
| D6.2 | تعطيلُ الحسابات الستّة؟ `admin12` (9 كتب) تدريبٌ أم حقيقيّ؟ كتابا `testuser` يُنقلان لـadmin أم يبقيان معطَّلين؟ | تعطيلٌ لا حذف |
| D6.3 | حذفُ 131 كتابَ تدريب + 117 مرفقاً نهائيّاً على الإنتاج (الرجوع: `.enc` + القاعدةُ المحلّيّة) | نعم |
| D6.4 | متى يُفعَّل IMAP بعد التبديل؟ وهل يُرسَل SMTP من الخادم (IP جديد)؟ | IMAP في ساعة عملٍ بعد 5.1، مراقَباً |
| D6.5 | القرص 31 GB: هامش ~4–5 GB بعد الوسائط أم ترقيةٌ قبلها؟ وجهةُ النسخ خارج الخادم ومدّةُ الاحتفاظ؟ | ترقيةٌ إن أمكن؛ وجهةٌ خارجيّة إلزاميّة قبل DROP |
| D6.6 | حسابُ النقل لـrsync: يُنشئه [م] أم نوفّر مخزناً؟ | يُنشئه [م] |
| D6.7 | مصيرُ النسخة المحلّيّة بعد التبديل | **أرشيفٌ للقراءة** (الاحتياطُ الحيّ = انقسام) |
| D6.8 | النشرُ التلقائيّ على كلّ دفعٍ أم يدويٌّ دائماً؟ إصلاحُ المفتاح: (أ) تركيبُ العامّ المطابق أم (ب) تدويرُ الزوج؟ | (أ) أبسط؛ تلقائيٌّ بعد P4 |
| D6.10 | لقطةُ VPS من Hostinger قبل التبديل؟ | نعم |
| D6.11 | دمجُ PR #16 بعد الاستقرار فقط (= P6) | نعم |
| D6.14 | **يُطرح فقط** إن أظهر S‑0 >20 كتاباً جديداً على الخادم | — |
| D6.9 | TLS/الكوكي لا تُغيَّر في النافذة | محسوم |
| + | حذفُ 164 مجلّدَ Temp (C: على 10 GB) · وتعيينُ مسؤول الأرشفة بعد التبديل (7.4-أ) | اليوم · د8 |

**(ب) حزمُ [م] — كلُّ حزمةٍ رسالةٌ واحدة، وكلُّ أمرٍ فيها يُعيد مخرَجاً يُلصق:**

| الحزمة | متى | محتواها (الأوامرُ بنصّها في الأقسام المُحال إليها) | المخرَجُ المطلوبُ عودةً |
|---|---|---|---|
| **م‑1** | د0 — اليوم | **قراءةٌ فقط**: ① `cd /var/www/lettersys && git rev-parse HEAD && ls scripts/probe_s0.py` ② S‑0: إن وُجد السكربت فصيغةُ `manage.py shell -c "exec(open('scripts/probe_s0.py').read())"` وإلّا صيغةُ `dbshell -- -Atc` (§8.2) ③ الإشاراتُ الثانويّة (`find` · `bash_history` · `journalctl`) ④ استطلاعُ 0.3 كاملاً + `REDIS_CACHE_URL` اسماً ورقمَ القاعدة (N7) | كلُّ المخرَجات نصّاً؛ **لا يُطبَع سرّ** (أسماءُ مفاتيح `.env` فقط) |
| **م‑2** | د2 — **فقط إن حُمِّل** | ① فوراً `UPDATE core_emailsettings SET imap_sync_enabled=false, is_active=false;` ② بعد D6.15 والتدوير: `shred -u -z` لكلّ مسار من `find` + إعادةُ `find` + وسمُ نسخ 09-08 «ملوَّثة» ③ (إن D6.14=البناء) إثباتُ `enc::` بعد الإدخال | S‑0 ثانيةً · `find` = 0 |
| **م‑3** | د3 — بعد D6.5/D6.6 وإيداع د1 | ① حسابُ النقل ② تحصينُ 0.8 + رقمُ Redis ③ **المفتاح**: `ls -la .encryption_key*` ⟵ إعادةُ تسمية الذاتيّ إن وُجد ⟵ كتابةُ 44 بايتاً (يُلصَق في الجلسة) ⟵ `chown/chmod` ⟵ sha/wc/stat ④ **سحبُ الكود إلى الوسم بلا نشر**: `git fetch --tags origin && git checkout cutover-2026-09 && git lfs pull` ثمّ `migrate --check` · `showmigrations core\|tail -1` · `verify_backup --help` · `models_healthcheck --strict --load` — **لا `lettersys-deploy` ولا `migrate` ولا restart** ⑤ استقبالُ 0.6 والتحقّق | sha[:8]=1e639b05 · 44 · svc/600 · HEAD=الوسم · 0077 · `sha256sum -c`=0 فشل · 13,932 · `df -h` |
| **م‑4** | د6–د7 — داخل النافذة | 10.f(ب) ⟵ 10.g ⟵ 10.h ⟵ 10.i ⟵ 10.j ⟵ 10.k ⟵ 10.l ⟵ **توقّفٌ لإذن المالك** ⟵ 4.1–4.5 | كلُّ بوّابةٍ بنصّها؛ **10.h يجب أن يطابق 10.d حرفيّاً** |
| **م‑5** | د8–د9 | ① إثباتُ `enc::` بعد إدخال الكلمة إن لزم (N3 احتياط) ② تفعيلُ `is_active` ثمّ IMAP بحسب D6.4 + مراقبةُ 10 دقائق ③ بعد 7 أيّام: نسخةٌ خارجيّة ثمّ `DROP DATABASE lettersys_old_<date>` ④ P2(أ)/P3 | `journalctl` · sha النسخة الخارجيّة · `\l` |

---

### 11.16 — خريطةُ التغطية: كلُّ معرّفٍ ⟵ دفعتُه

| القسم | المعرّفات ⟵ الدفعة |
|---|---|
| §0 القيود | C0.1 C0.2 C0.3 C0.4 C0.5 C0.6 C0.7 ⟵ **سارية** (C0.5 تُغلَق بـU2 في د0؛ C0.4 تُرفَع في P4 د9) |
| المرحلة 0 | 0.1⟵د4 · 0.2⟵د0 · 0.3⟵د0 · 0.4⟵د1 · 0.5⟵د1 · 0.6⟵د3 · 0.7⟵د3 · 0.8⟵د3 |
| المرحلة 1 | 1.1⟵د5 · 1.2⟵د5 · 1.3⟵د5 |
| المرحلة 2 (مرجع) | 2.1⟵10.c(د5) · 2.2⟵10.i(د6) · 2.3⟵10.k(د6) · 2.4⟵10.c/10.d(د5) · **2.5⟵N2(د5)** · 2.6⟵10.e(د5) |
| المرحلة 3 (مرجع) | 3.1⟵10.f(د6) · 3.2⟵10.i · 3.3⟵10.i · 3.4⟵10.j · 3.5⟵10.j+10.l (د6) |
| المرحلة 4 | 4.1 4.2 4.3 4.4 4.5 4.6 ⟵ د7 |
| المرحلة 5 | 5.1⟵د8 · 5.2⟵د8 · 5.3⟵د8 · 5.4⟵د9 · 5.5⟵د9 (رأسُ P1–P6) |
| §التوحيد | U1⟵**منجَز** · U2⟵د0 · U3⟵د0 · U4⟵د1 · U5⟵سارٍ (استثناءُ N1 موثَّق) |
| قواعدُ النافذة | W1 W2 W3 W4 W5 W6 ⟵ **سارية** من د5 حتّى القبول (W4 بصياغة C19؛ W1 تُرفَع في د8؛ W2/W3 في د9) |
| §5 المخاطر | R5.1⟵د3/د6 · R5.2⟵د1/د6 · R5.3⟵د3 · R5.4⟵د2/د6/د8 · R5.5⟵د6/د7 · R5.6⟵د5 · R5.7⟵د6 · R5.8⟵د1/د3 · R5.9⟵د10 (غيرُ منفَّذٍ حتّى حينه — C26) · R5.10⟵د2 · R5.11⟵سارٍ (د0 N5) · R5.12⟵د5 · R5.13⟵**منجَز** (د0 تحقّق) |
| §6 قرارات المالك | D6.1 D6.2 D6.3 D6.4 D6.5 D6.6 D6.7 D6.8 D6.10 D6.11 ⟵ د4 (تُسأل في د0) · D6.9⟵**محسوم** · D6.12⟵د2 · D6.13⟵د2 · D6.14⟵د2 (مشروط) · D6.15⟵د2 |
| إعادةُ تمكين النشر | P1 P2 P4 P5 P6 ⟵ د9 · P3⟵قراءةٌ د3 + تعديلٌ د9 |
| توجيهُ المحادثة | G1 G2 ⟵ د0 · G3⟵**يُستبدَل بد0⟵د1** · G4⟵د4 · G5⟵سارٍ · G6⟵د8 |
| §7.4 | 7.4-أ⟵د8 · 7.4-ب⟵د11 · 7.4-ج⟵د9 (=P6) · 7.4-د⟵د11 · 7.4-هـ⟵د10 · 7.4-و⟵د11 |
| §7.5 ديون | T7.5-1⟵د10 · T7.5-2⟵د10 · T7.5-3⟵د10 · T7.5-4⟵د8 (=7.4-أ) |
| §7.6 · §7.7 | 7.6 ⟵ **قيدٌ سارٍ** · 7.7 ⟵ **تحذيراتٌ سارية** (مربوطةٌ في بنود د1/د5/د9/د11) |
| §8 | 8.1⟵**منجَز** (د0 تحقّق) · 8.2-أ 8.2-ب 8.2-ج ⟵ د0 · 8.3⟵د2 · 8.4-د0⟵د2 · 8.4-د1⟵د2 (=D6.13) · 8.4-د3⟵د3 (=0.7) · 8.4-د6⟵د2 (مشروط) · 8.4-د8⟵د2/د8 · 8.4-د9⟵د2 · 8.4-ت⟵د2 (مشروط) · 8.5-أ⟵د2 · 8.5-ب⟵د2 (=D6.15) · 8.6-أ⟵د10 · 8.6-ب⟵د10 · 8.6-ج⟵د10 · 8.6-د⟵د0 |
| §9 | 9.0⟵**قرارٌ منجَز** (تطبيقُه U2 د0) · 9.1-1⟵د0 · 9.1-2⟵د1 · 9.1-3⟵د0 · 9.1-4⟵د5 · 9.3-1⟵د6/د8 · 9.3-2⟵د1(اختياريّ)/د11 · 9.3-3⟵د1(اختياريّ)/د11 · 9.3-4⟵د1 (+إعادةٌ د11) · 9.3-5⟵د8 · 9.3-6⟵د11 · 9.3-7⟵د11 · 9.3-8⟵د11 · 9.3-9⟵د11 (=7.4-د) · 9.3-10أ⟵د0 · 9.3-10ب⟵د8 · 9.3-10ج⟵**محسوم** · 9.4⟵**يُستبدَل بهذا القسم** |
| §9.5 ديون | T9.5-1⟵د1 (=9.3-4) · T9.5-2⟵د1 · T9.5-3⟵د8 · T9.5-4⟵د11 · T9.5-5⟵د11 (=9.3-7) |
| §10 | 10.0 10.1 10.2 10.4 10.5 ⟵ **قيودٌ سارية** (10.2 بجدول مدراء C9) · 10.a⟵د0 (=0.2) · 10.b⟵د5 · 10.c⟵د5 · 10.d⟵د5 · 10.e⟵د5 · 10.f⟵د3(أ)/د6(ب) · 10.g⟵د3 كتابةً/د6 تحقّقاً · 10.h 10.i 10.j 10.k 10.l ⟵ د6 · 10.m⟵د7 · T10.4⟵د1 |
| §10.6 | 10.6-1 10.6-2 10.6-3 10.6-4 10.6-5 10.6-6 10.6-7 10.6-8 10.6-9 10.6-10 10.6-11 10.6-12 ⟵ د1 · 10.6-13⟵د6 (التناظرُ يُقاس في 10.h) |
| §10.7 | 10.7-1⟵د0 · 10.7-2⟵د1 · 10.7-3⟵**قاعدةٌ سارية** (لا خطوةَ قبل خضرة سابقتها) |
| السجلّ | سجلّ-1⟵د0 (يُملأ بمخرَج S‑0 في د2) |
| **جديد** | N1⟵د1(إيداع)/د3(سحب) · N2⟵د5 · N3⟵د2 (محلّيّاً)/د8 (احتياط) · N4⟵د1 · N5⟵د0 · N6⟵د0 · N7⟵د0/د3 |

**مؤجَّلٌ عمداً (بسببه):** 7.4-د/7.4-و/9.3-9 — لا يُبنى قبل رصد استعمالٍ حقيقيّ (حسمُ §7.4) وهجرةُ 0078 تكسر بوّابةَ 0077 · 8.6-أ — هجرةُ `AlterField` تكسر بوّابةَ 0077؛ 8.6-ب/ج معها في حزمةٍ واحدة كي لا يصرخ الحارسُ على قاعدةٍ لم تُنظَّف · T7.5-1/2/3 و7.4-هـ — كودٌ لا يخدم الترحيل (U5) · 9.3-6/7/8 — بقرار 2026-09-09 «بعد استقرار الإنتاج» وعتباتٍ لا تتراكم إلّا بإدخالٍ حقيقيّ · 9.3-2/9.3-3 — قياساتٌ اختياريّة تُنفَّذ في فترات الانتظار إن سمحت الذاكرة، وإلّا د11 · T9.5-4 — يفيد التقارير لا الاستخراج · P1–P6 — لا نشرَ قبل يومَين بلا رجوع.

### 11.17 — ما لا تفعله هذه الخطّة

- **لا تُنفَّذ إلّا بأمر المالك** — هي إعادةُ ترتيبٍ وحسمُ تعارضاتٍ، لا تنفيذ. البدءُ من د0 حين يقول.
- **لا تُرخي بوّابةً مسجَّلة** ولا تخترع رقماً؛ كلُّ رقمٍ فيها من الوثيقة أو من لقطة 2026-09-09، وكلُّ إضافةٍ موسومةٌ `N`.
- **لا تلمس الخادم** من عندنا؛ كلُّ ما يخصّه رسائلُ لـ[م] تُسجَّل قبل إرسالها ويُلصق مخرَجُها.
- **لا تدمج PR** ولا تُفعّل نشراً ولا تُعيد سطرَي `push` قبل P4/P5؛ و**استثناءُ N1** (كودُ الترحيل بلا هجرة) مكتوبٌ هنا صراحةً كي لا يُقاس عليه غيرُه.
- **لا `dumpdata`/`loaddata`** لبيانات الإنتاج، ولا `pg_restore` فوق `lettersys` الحيّة، ولا `prepare_entities` على قاعدةٍ حيّة.
- **لا كتابةَ على القاعدة المحلّيّة بعد 10.b** — هي مصدرُ الرجوع الوحيد؛ ما يلزم كتابتُه بعدها يُكتب على الإنتاج من الواجهة بعد القبول.
- **لا تدريبَ على المجموعات المختومة** ولا إرخاءَ بوّابةٍ بعد النظر (9.4 «ما لن أفعله» سارٍ).
- **لا تُعيد ترقيمَ الأقسام السابقة ولا تُحرّرها** — الإلحاقُ لا الكتابةُ فوق؛ التصحيحاتُ في 11.0/11.1 وخريطةُ الإحالة في 11.16.

---

## سجلُّ التنفيذ (يُلحَق فيه فقط)
- 2026-09-08 — أُنشئت الوثيقة (خطّةُ فيبل المقيسة)؛ main=236ffec@0077؛ النشرُ disabled_manually؛ الترحيلُ لم يُبدأ.
- 2026-09-09 — **تبويب 1**: §التوحيد بند 1 مُنفَّذ — `feat/mail-archive-attribution` مُرصَّفٌ على `8230e91`، مدفوعٌ، **PR #16 مسوّدة** (1568 خضراء · صفرُ هجراتٍ معلّقة). وأُضيف **§7** بحال التطوير وما بقي والديون والفخاخ. الشجرةُ `../lettersys_rollout` تبقى (فيها `.env` والمفتاحُ `67572ad5` الذي **لا يُشحَن**). **لا شيءَ معلَّقٌ على التبويب 1.**
- 2026-09-09 — **تبويب 1 / اكتشاف**: رفعٌ يدويٌّ 2026-09-08 خارج الخطّة. قِيس الملفُّ محلّيّاً (§8.1): 13,194 كتاباً · 674 جهةً **بمفاتيحها** · **كلمتا بريدٍ صريحتان** بينما عمودُ القاعدة `enc::` سليم. `.gitignore` حُصِّن (الملفُّ لم يُودَع قطّ). أُعدّ **أمرُ الاستطلاع S‑0** (§8.2، قراءةٌ فقط) — **بانتظار مخرَجه من [م]**؛ لا يُبدأ شيءٌ قبله. المخرَج: [يُلصَق] · الحكم: [حُمِّل/لم يُحمَّل/انقطع] · المُنفَّذ: [إطفاء IMAP HH:MM · تدوير HH:MM · مفتاح HH:MM · إثبات HH:MM · إتلاف HH:MM].
- 2026-09-09 — **تبويب 3**: أُلحق **§9** (الاستخراج: المنجَز بالأرقام المختومة · الباقي مرتّباً ببوّاباته · النيّة · الديون). **قرارُ المالك: تبويبٌ واحدٌ وخطّةٌ واحدة من الآن.** الحالة: `main=adf5669`، #9/#11 مدموجان، القاعدةُ المحلّيّة @0077+دمج الجهات (411⟵353، نسخةٌ قبله على D:)، ثابتُ `var/*.json` يحتاج إعادةَ توليد.
- 2026-09-09 — **تبويب 1**: أُضيف **§10 المسارُ المبسَّط** (نسخةُ التطبيق المشفَّرة) باستشارة فيبل — يستبدل المرحلتين 2 و3 بـ13 خطوة. **وثلاثةُ اكتشافاتٍ مقيسة**: ① نسخةُ `20260908_0958` **أعدادُها تطابق ومحتواها أقدم** (`merged_into` 198≠256) ⟵ البصمةُ لا العدّ؛ ② **`*.enc` لم يكن متجاهَلاً** في مستودعٍ عامّ (أُغلق)؛ ③ البروفةُ المحلّيّة **مستحيلة** (`lettersys_user` بلا `CREATEDB`) ⟵ تنتقل إلى الخادم. والنسخةُ الموجودة **صالحةٌ بنيويّاً**: تُفكّ بـ`1e639b05` ⟵ 3,185,594 بايتاً `PGDMP` · 65 جدولاً · 13,239/674/13,187/22,313/12 صفّاً. `verify_backup` **غيرُ مكتوبٍ بعد** (عقدُه في §10.6) وهو أوّلُ ما يُكتب.
- 2026-09-09 — **الجلسةُ الواحدة (فيبل)**: أُلحق **§11 — الخطّةُ الواحدة المُعادُ ترتيبها**: 180 بنداً مجروداً ⟵ 12 دفعة (مدقّقٌ آليّ للتغطية **180/180**) · 29 تعارضاً محسوماً (11.1) · 7 بنودٍ جديدة N1–N7 · رسائلُ [م]/المالك جاهزة (11.15). **مقيسٌ اليوم**: `main=origin/main=HEAD=7063bf6` · C: حرٌّ **10 GB** (96%) · Temp 164 · `verify_backup` غائب · `probe_s0` بلا بصمات · مسارُ المفتاح نسبيٌّ للشجرة (`core/encryption.py:12`) · `pg_dump` ليس على PATH و`_find_pg_dump` يختار 16 · `.enc` القائم 4,247,544 ب · `lettersys_data.json` (27,363,361 ب) و`lettersys.dump` (3,076,680 ب) ما يزالان على القرص متجاهَلَين · النشرُ `disabled_manually` (مقيس). **نُفِّذ من د0**: U2 (`worktree remove` + `branch -D worktree-agent-a2bd83f51892841ee` @f242d24، 0 فريد) · U3 محلّيّاً (`branch -D rollout-b3` @996b954، 0 فوق main؛ البعيدةُ الثلاث بعد المراجعة) ⟵ `git worktree list` = main + rollout. **التنفيذُ بدأ**: د0 (الرسائل/البصمات/8.6-د) ود1 (verify_backup · T10.4 · N4 · 9.3-4 · 0.4) بوكلاء أوبوس تحت مراقبة الجلسة — مخرَجاتُها تُلحَق هنا حين تعود.
