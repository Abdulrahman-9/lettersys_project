# تنفيذ خطة التعميم — قائمة العمل الدقيقة

**المرجع:** [`COMPANY_ROLLOUT_PLAN_2026-08-24.md`](COMPANY_ROLLOUT_PLAN_2026-08-24.md) — التصميم و§7 سجل العيوب (26 بنداً).
**هذا الملف:** التنفيذ فقط — يُحدَّث مع كل خطوة. الترتيب **من الأسهل إلى الأصعب** مع احترام بوابات الاعتماد (§7.4).

## بروتوكول التنفيذ (نُقِّح 2026-08-27 بمشورة فيبل)

1. **شجرة عملٍ منفصلة — انتهى عصر المشاركة.** عملُ التعميم في
   `../lettersys_rollout` على فرع `rollout-b3`؛ وجلسة det2 تحتفظ بالشجرة
   الأصلية على `det2-wiring`. الدمج عند حدود الدفعات بقرار المالك.
   *(البيئة الافتراضية تبقى في الشجرة الأصلية: `../lettersys_project/.venv/Scripts/python.exe`.)*
2. **إيداعٌ بعد كلّ بندٍ أخضر.** تراكمُ دفعتين بلا إيداع هو ما أنتج التشابك
   الذي منع الإيداع الذرّي لاحقاً — لا يُكرَّر.
3. **لا `git add -A/-u` ولا `commit -a` ولا `git rm` أبداً.** الإدراج بمساراتٍ
   صريحة، ثمّ `git status` للتحقّق أنّ المُدرَج مساراتك وحدها، ثمّ الإيداع في
   النَّفَس نفسه — لا يُترك شيءٌ مُدرَجاً.
   *وقع فعلاً قبل هذه القاعدة:* `git rm core/merge_settings.py` أدرج حذفاً في
   الفهرس فابتلعه إيداع الجلسة الأخرى `78e80bf`.
4. **مناطق ممنوعة** (منطقة عمل الجلسة الأخرى): `core/extraction/**`،
   `scripts/eval/**`، وكلّ ما يخصّ التواريخ/الكاشف/القارئ.
5. **بند واحد = تغيير واحد قابل للتراجع وحده.**
6. **بعد كلّ بند:** `python manage.py test --settings=lettersys.settings_test` — أخضر قبل الانتقال.
7. **كلّ بند حرج يلزمه اختبار انحدارٍ جديد**، ويُتحقَّق أنّه **يفشل على الكود القديم**.
8. **أرقام الهجرات تُحجز لحظة كتابتها** (آخر رقم مودَع: `0061`).

---

## الدفعة 1 — أمان فوري ✅ **منجزة** (2026-08-26)

صفر هجرات · مستقلّة عن قرارات §4 · **838 اختباراً خضراء، صفر انحدار** · 53 اختباراً جديداً.

### T1.1 — حذف `core/merge_settings.py` الميت ✅ ‹ت5›
- صفر مستوردين مؤكَّداً قبل الحذف (158 سطراً بينها `MERGE_ADMIN_EMAILS = ['admin@example.com']` حيّة).
- `python manage.py check` نظيف.
- ⚠️ الحذف ابتلعه إيداع الجلسة الأخرى `78e80bf` — سببه `git rm` (بند البروتوكول 2).

### T1.2 — تنظيف CSP من مصادر CDN الميتة ✅ ‹ت4›
- أُسقط `cdn.jsdelivr.net` من `script-src`/`style-src`/`font-src`، و`fonts.googleapis.com`، و`fonts.gstatic.com`.
- **قِيس قبل الحذف:** صفر مرجع CDN في `templates/` و`static/` (المطابقة الوحيدة تعليقٌ داخل chart.js المُعرَّب) — تحقّقٌ لا افتراض.
- `'unsafe-inline'` **باقٍ عمداً**: `base.html` يحمل سكربتاً مضمَّناً فعلاً؛ إسقاطه يحتاج ورشة `nonce` مستقلّة ⟵ **بند مؤجَّل**.

### T1.3 — تقليص كشف `network_ping` ✅ ‹ح6›
- الغريب عن الشبكة الخاصّة يحصل على `{lettersys, status}` فقط؛ وجيران الشبكة يحصلون على `role/name/version/ip` لأنّ `_ping_node` يقرؤها فعلاً لجدول الأجهزة (حُسم بالقراءة لا بالحدس).
- `active_users` أُسقط **نهائياً** من هذه النقطة: مستهلكه الوحيد صفحة الأجهزة وهي تأخذه من `network_devices` المحميّة.
- الحارس على `REMOTE_ADDR` لا `X-Forwarded-For` — قرارُ كشفٍ لا يُبنى على ترويسةٍ يتحكّم بها العميل. القيد موثَّق: خلف وكيلٍ عكسيّ يسقط أثره ⟵ تُعطَّل النقطة في وضع الخادم المركزي (ت9).
- **3 اختبارات** في `core/tests_security.py`.
- **فخّ مقيس:** `ipaddress.is_private` يعدّ نطاقات التوثيق (203.0.113.0/24) **خاصّةً** — الاختبار الأوّل كاد ينجح كذباً؛ صار على عنوانٍ عامٍّ حقيقيّ.

### T1.4 — تشفير `azure_key` ✅ ‹ح7›
- **وُحِّد بدل نسخه:** `EncryptedFieldsMixin` (نموذج مجرَّد) مصدراً واحداً لقاعدة التشفير الشفّاف، ترثه `AIIntegrationSettings` و`EmailSettings` معاً — النسخة الثانية كانت ستُثبّت الجذر «قاعدة منسوخة» بدل رفعه.
- **لا هجرة**: `makemigrations --check` ⟵ لا تغييرات. قياسٌ فعليّ لطول الناتج: 32⟵145 · 84⟵209 · 120⟵253 محرفاً ⟵ `max_length=255` يسع كلّ مفاتيح Azure الواقعيّة، ويضيق عند ~121 محرفاً فأكثر (موثَّق في docstring المِزيج).
- **3 اختبارات**: مشفَّرٌ في العمود الخام · صريحٌ في الذاكرة · لا تشفير مزدوج · والمستهلك `get_active_settings` يستلم المفكوك.

### T1.5 — نطاق وحدة البريد ✅ ‹ح1، اتّسع ثلاث مرّات أثناء التنفيذ›
**الاتّساع المقيس:** السجلّ قال «صندوق الوارد»؛ الجرد الكامل لسطح البريد كشف **اثني عشر موضعاً**:
- **العروض (5):** `mail_sent` (القائمة **والإحصاءات الأربع**) · `mail_inbox` (والعدّاد) · `mail_thread` (يفتح أيّ خيطٍ بالـid **ويؤشّره مقروءاً للجميع**) · `mail_compose` (يفتح أيّ كتاب) · وقبلها `book_email_logs` API (عناوين المستلمين).
- **نقاط API (6):** `api_thread_detail` (قراءة مواضيع وعناوين أيّ خيط) · `api_mark_read` (**كتابة** على بريد غيرك) · `api_thread_status` (**كتابة** على حالة خيط غيرك) · `api_compose` و`api_bulk_send` (تعليق رسالةٍ على كتاب غيرك، وسقوطٌ إلى «أوّل كتابٍ في النظام») · `api_template_preview` (يُصيّر رقم كتابٍ وعنوانه) · `api_mail_stats` (عدّادات عامّة) · و`send_email`/`book_email_preview`/`send_book_to_entity`.
- **الشريط العلوي (1):** `context_processors.mail_unread` — عدّادٌ عامّ **بمفتاح كاشٍ مشترك واحد** يظهر في **كلّ صفحة** لكلّ مستخدم؛ صار منطَّقاً وبمفتاحٍ لكلّ مستخدم.
- **قرار مقصود:** `entity_email_info` و`update_entity_email` تُركتا كما هي — دليل الجهات بيانات مشتركة، ودور «مُدخِل» يملك `manage_entities` في `core/roles.py`؛ تقييدها بـstaff كان سيناقض نموذج الأدوار نفسه.
- **`core/messaging/scoping.py`** ملفّاً مستقلّاً (7 دوالّ): القاعدة مكتوبة **مرّة واحدة** لاثني عشر موضعاً، وستُفوَّض إلى `scope_books_for` في الدفعة 2 ثمّ إلى بُعد القسم في المرحلة أ.
- **24 اختباراً** في `core/tests_mail_scope.py` (لم يكن قبلها **أيّ** اختبار يمسّ البريد) — و**14 منها تفشل على الكود القديم**: حراسةٌ مُتحقَّقة لا مُدَّعاة.

**بوابة الإغلاق:**
- ✅ **838 اختباراً خضراء** (خطّ الأساس 785 + 53 جديداً؛ صفر انحدار).
- ✅ **مراجعة أمنيّة على الفرق** — يدويّة: مهارة `security-review` تتطلّب `origin/HEAD` وهذا المستودع بلا مستودعٍ بعيد.
- ✅ **تحقّق حيّ من CSP** (لم يكن منجزاً حين أُشّر أوّل مرّة — صُحِّح): خادمٌ فعليّ ⟵ الترويسة تصل بلا أيّ مصدرٍ خارجيّ، وصفحة الدخول المُصيَّرة فيها **صفر مرجعٍ خارجيّ** وكلّ أصولها `/static/…`، والصفحات الأربع الرئيسة تستجيب. (فحصُ بروتوكولٍ وأصولٍ لا فحصٌ بصريّ — الأخير يبقى للواجهة عند أوّل تغييرٍ مرئيّ.)
- ✅ شُطبت البنود المنجزة في §7 من وثيقة الخطة.

---

## الدفعة 2 — أساسٌ تقني قبل المرحلة أ ✅ **منجزة** (2026-08-26)

هجرة واحدة (`0061`) · **857 اختباراً خضراء** · 16 اختباراً جديداً.

### T2.1 — مدير الحذف الناعم ✅ ‹ح4›
- `SoftDeleteManager` على `Book` و`Attachment` (النموذجان الوحيدان ذَوَا `is_deleted`)، و`all_objects` مخرجاً صريحاً.
- **الفخّ الذي كان يمكن أن يكسر التطبيق تُحقّق منه بالاختبار:** `_base_manager` يبقى مديراً عاديّاً غير مُرشَّح ما لم يُضبط `Meta.base_manager_name` — فاجتياز FK إلى صفٍّ محذوف (`attachment.book`) ما زال يعمل ولا يرمي `DoesNotExist`. اختبارٌ صريح يحرس هذا.
- **العلاقة العكسيّة** (`book.attachments`) صارت تُخفي المحذوف — وهو المقصود.
- **ستّة مواضع تريد المحذوف صراحةً** حُوّلت إلى `all_objects`: سلّة المحذوفات (قائمتا الكتب والمرفقات)، استعادة الكتاب واستعادة مرفقاته، استعادة مرفقٍ مفرد، و`purge_dev_seed_books` (كان سيترك كتب التدريب المحذوفة ناعماً في القاعدة).
- **7 اختبارات** في `core/tests_soft_delete.py` — **6 منها تفشل بلا المدير**.
- **قرارٌ مقصود:** الـ73 موضع `is_deleted=False` اليدويّة **بقيت**. صارت زائدةً لا خاطئة، وحذفها تغييرٌ تجميليّ في عشرات الملفّات (بعضها في منطقة الجلسة الأخرى) بلا مكسبِ أمانٍ إضافيّ — الأمان جاء من الافتراض نفسه. تُكنس في تنظيفٍ لاحقٍ بلا عجلة.

### T2.2 — بوابة الرؤية الواحدة ✅ ‹ح5›
- `core/scoping.py` مصدراً وحيداً: `is_privileged` · `can_view_book` · `scope_books_for`.
- **27 موضعاً منسوخاً** استُبدلت في سبعة ملفّات (`attachments` · `books_api` · `books_detail` · `books_list` · `dashboard` · `dossiers` · `scan_settings`)، وروجعت الفروق **يدوياً سطراً سطراً**: صفر تغيير معنى.
- موضعٌ ثامن كتب القاعدة بصياغةٍ مختلفة (`owner_book.created_by_id == request.user.id` على سطرين) — وُحِّد أيضاً؛ ولولا المراجعة اليدويّة لبقي.
- `core/messaging/scoping.py` صار **يفوّض** إلى المصدر الوحيد بدل أن ينسخه.
- **9 اختبارات** في `core/tests_book_scope.py`: مصفوفة الصلاحيات + كتابُ غيرك غير قابلٍ للوصول عبر (تفاصيل · تحرير · تقرير · حذف · قائمة).
- **قرارٌ مؤجَّل بوعي:** `is_staff` ما زالت تُوسّع الرؤية ضمناً (`roles.py:52`) — أُبقي السلوك كما هو (صفر تغيير)، وفصلُه قرارُ مالكٍ في المرحلة أ.
- **استثناء مقصود:** `core/extraction/views/ui.py:147` فيه نسخةٌ من القاعدة ولم تُمَسّ — منطقة عمل الجلسة الأخرى (بند البروتوكول 3). تُوحَّد حين تفرغ.

### T2.3 — `BookEmailLog.book` اختيارياً ✅ ‹ح3›
- **هجرة `0061_bookemaillog_optional_book`** (`null=True, blank=True`؛ `on_delete` بقي CASCADE — تغييره سلوكٌ آخر لا يخصّ هذا العيب).
- السقوط إلى «أوّل كتابٍ في القاعدة» **حُذف من مسارين** (`api_compose` و`api_bulk_send`).
- **أثرٌ جانبيّ اكتُشف وعولج:** الوصل عبر `book__created_by` يُسقط الصفوف الفارغة، فكانت رسائل المستخدم بلا كتابٍ ستختفي من صادره. `scope_sent_logs` صار يعترف بالمُرسِل مالكاً حين لا كتاب.
- محرّك SMTP: ثلاثة سجلّات كانت تقرأ `book.pk` بلا حارس.
- **3 اختبارات**: الإرسال بلا كتاب لا يلوّث أيّ كتاب · المُرسِل يرى رسالته · غيره لا يراها.

**بوابة الإغلاق:** ✅ 857 اختباراً خضراء · ✅ الهجرة تُطبَّق على قاعدةٍ نظيفة · ✅ مراجعة الفروق يدويّاً.

---

## الدفعة 3 — سلامة البريد ✅ **منجزة** (2026-08-27، بمشورة فيبل)

هجرة واحدة (`0062`) · **876 اختباراً خضراء** · 15 اختباراً جديداً · 3 إيداعات ذرّيّة.

### T3.1 — «مرّةٌ واحدة» صارت صادقةً ✅ ‹ح2›
- **ثغرةٌ في خطّتنا نفسها كشفها فيبل:** كنّا سنحدّث حالة الأصل ونضيف `retry_of` — لكنّ **صفوف المحاولات الجديدة** (يُنشئها المُرسِل بحالة `failed`) كانت ستُطابق مرشِّح الطابور بدورها، فيبقى الحشد ينمو. العلاج: `retry_of__isnull=True` في المرشِّح.
- ثلاثة حرّاس: وسمُ المحاولات (`retry_of`) · عدّادٌ على **الأصل** ينتهي بـ`abandoned` عند `MAX_RETRIES` · قفلٌ ذرّيّ ضدّ تراكب تشغيلتي beat. ونجاحُ محاولةٍ يُغلق الأصل فوراً.
- `qs.count()` بعد التقطيع (استعلامٌ ثانٍ ورسالةٌ مضلّلة) صار `len(candidates)`.
- **ثمّ** جُدولت المهمّة في `CELERY_BEAT_SCHEDULE` — بهذا الترتيب لا بعكسه، واختبارٌ يوثّقه.
- **7 اختبارات**، ستٌّ منها تُخفق على الكود القديم.

### T3.2 — قفل المزامنة الذرّي ✅ ‹م1›
- `cache.add` (ذرّيّ) يُؤخَذ **قبل** العمل، وختمُ التبريد يُكتب عند **البدء** لا الانتهاء — فتُغلق نافذة القطيع حتى لو تعثّرت المزامنة. القفل يُحرَّر في `finally`.
- قيدٌ موثَّق في الكود: على LocMemCache القفل لكلّ عمليّة لا لكلّ الخادم — الضمان الكامل مع Redis (ز0).
- **الاختبار يحاكي القطيع دقيقاً:** داخلٌ ثانٍ يُستدعى **أثناء** المزامنة الأولى، لا بعدها.

### T3.3 — إنهاء تجاوز `save()` ✅ ‹م2›
- ختمُ الانتهاء عبر `save(update_fields=...)` بدل `objects.filter(...).update(...)`.
- **حارس انحدارٍ بالتفتيش على المصدر** يمنع عودة النمط — لأنّ الأثر لا يظهر اليوم بل عند أوّل منطقٍ يُضاف إلى الحفظ (وتعدّدُ حسابات البريد في المرحلة ج أوّلُ الطارقين).

### T3.4 — أربع توابع كشفتها المراجعة ✅
1. **فخّ `dumpdata`** (الأخطر): `scripts/pg_setup/pg_migrate.py` يوثّق التصدير بـ`dumpdata` وهو يقرأ المدير الافتراضي ⟵ منذ الدفعة 2 صار يُسقط **كلّ المحذوف ناعماً بصمت** في أيّ نقلٍ SQLite⟵PG. `--all` يُجبره على `_base_manager`. *درسٌ عامّ: تغييرُ المدير الافتراضي يُبحث عن أثره الجانبيّ قصداً، لا يُنتظر ظهوره.*
2. مفتاح كاشٍ ميت في `sync_inbox_task` (بقايا العدّاد العامّ).
3. **سقوطٌ صامت** في `api_compose`: `book_id` خارج النطاق كان يُرسل بلا كتاب بصمت ⟵ رفضٌ صريح (404) + اختبار.
4. انجرافُ توثيق (ت6): «دورمانت» في `consumers.py`/`routing.py` وقد وُصلا فعلاً، وREADME المراسلة.

### مؤجَّلٌ بقرار ‹م5›
إيصالات القراءة لكلّ مستخدم (`IncomingEmail.is_read` علمٌ عامّ) — **إلى المرحلة ج**: بعد تنطيق البريد صار جمهور الخيط مالكَه والإداريّين فقط، فالعلم الواحد مقبولٌ مؤقّتاً، والإيصالات تحتاج نموذجاً ستبنيه ج على أيّ حال.

**بوابة الإغلاق:** ✅ 876 اختباراً خضراء · ✅ كلّ بندٍ أُودع وحده · ✅ كلّ حارسٍ تُحقِّق أنّه يفشل على الكود القديم.

---

## بند ز0 مُنفَّذ مبكّراً — تغطية النسخ الاحتياطي ✅ ‹ح8› (2026-08-27)

نُفِّذ خارج ترتيب الدفعات لأنّه **لا يحتاج أيّ قرارٍ من §4** وهو أخطر بندٍ
تشغيليّ: عيبٌ لا يظهر إلّا يوم الحاجة.

**القياس الذي حسم التصميم** (على التنصيب الحيّ): `MEDIA_ROOT=D:\lettersys_data\media`
فيه **14,041 ملفاً و15.8 غيغا** بلا أيّ حماية · `var/models` 42 ميغا · ووجهة
النسخ `D:/trackbackup` فيها 11 ملفاً كلّها لقطات قاعدة. المساحة الحرّة 388 غيغا.

- **مرآةٌ تدريجيّة** لا نسخٌ كامل: مسحُ الشجرة 0.5 ثانية، والنسخُ للجديد فقط.
  صحيحٌ بطبيعة البيانات — المرفق لا يُعدَّل بعد كتابته.
- **أرشيفٌ مشفَّر** لملفّات الإعداد (`.env`).
- **المفتاح لا يُنسَخ تلقائيّاً**: بصمةٌ للتحقّق، وصراخٌ إن كان مفقوداً أو
  مُلقىً في وجهة النسخ (مفتاحٌ بجانب معمّاه ليس تشفيراً).
- التشذيب يعرف الأرشيف الجديد **ولا يمسّ المرآة** (حالةٌ راهنة لا لقطة).
- 13 اختباراً + تجربةٌ جافّة على البيانات الحيّة. المجموعة: **890 خضراء**.

**بندان باقيان على التشغيل لا الكود:** نقلُ `.encryption_key` إلى خزانةٍ
منفصلة (بصمته `67572ad5…`)، ونسخةٌ خارج الخادم. و**أوّل تشغيلٍ حقيقيّ ينسخ
15.8 غيغا مرّةً واحدة** — ثمّ الفروق فقط.

---

## المرحلة أ — بُعد القسم ✅ **منجزة** (2026-08-27)

ثلاث هجرات (0063–0065) · **924 اختباراً خضراء** · خمسة إيداعات ذرّيّة ·
قرارات المالك الثلاثة منفَّذةٌ حرفيّاً.

| | ما تغيّر | الدليل |
|---|---|---|
| **أ/1** | `Department` + `UserProfile` + `deployment_profile` + أمرُ بذرٍ جافّ | 42 وحدةً مرمّزةً مقيسةً حيّاً |
| **أ/2** | النطاق صار **بالقسم** لا بالمُدخِل، ومعه **طبقةُ السرّيّة**، و`is_staff` لم تعد تُوسّع الرؤية | 487 كتاباً سرّيّاً · حسابان متأثّران |
| **أ/3** | عدّادٌ وقيدُ تفرّدٍ **لكلّ قسم** | العدّادات الأربعة نُسبت للافتراضيّ قبل القيد |
| **أ/4** | دورُ `dept_head`، وتمامُ فصل `is_staff` عن الأدوار | — |
| **أ/5** | الأضابير تميّز الداخليّ (الجهةُ التي لها قسم) | سؤالٌ مؤجَّلٌ منذ `ADABIR_DESIGN` |

**ثلاثة أشياء غيّرت الخطّة أثناء التنفيذ:**
1. **طبقةُ السرّيّة نُقلت من أ/4 إلى أ/2** — تفعيلُ النطاق وحده كان يكشف الـ487
   كتاباً السرّيّاً لكلّ موظّفي القسم حتى تصل الطبقة: نافذةُ تسريبٍ تصنعها
   الخطّة نفسها (نبّه إليها المستشار المعماريّ).
2. **`ensure_profile`** سدّت فجوةً كشفها التحقّق: المستخدم الجديد **بعد**
   الهجرة كان يبقى بلا ملفّ فيسقط إلى «كتبي أنا».
3. **البذرُ يربط قسماً قائماً بجهته** — كشفه اختبارٌ أخفق: الهجرة تُنشئ ش13 بلا
   جهة، وكان البذر يتخطّاه لأنّ رمزه موجود.

**قرارٌ مقصود:** توقيعا `get_next/consume_next` متساهلان (`department=None` ⟵
الافتراضيّ) — مسارات الاستدعاء القديمة، ومنها الاستخراج الذي تعمل عليه جلسةٌ
أخرى، لا تُجبَر على التعديل اليوم.

---

## ما بعد الدفعات الثلاث

المراحل الكبرى بترتيب الخطة: **ز0** تحصين الخادم (تكوين لا كود، يحتاج قرارات §4: نظام التشغيل · VPN/علني · العتاد) ⟵ **أ** بُعد القسم (الأثقل: هجرات على 11,183 كتاباً) ⟵ **ب** لوحة الإدارة ⟵ **ج** البريد الداخلي والإحالات ⟵ **د** التدقيق ⟵ **هـ** التواقيع ⟵ **و** اللحظي ⟵ **ح** إدارة التدريبات.

**بوابة الانتقال إلى المرحلة أ:** الدفعتان 1 و2 مغلقتان + قرارات المالك 1 و2 و3 (حدود القسم · ترقيم الأقسام · رؤية الكتب) محسومة.

---

## سجل التقدّم

| البند | الحالة | التاريخ | ملاحظة |
|---|---|---|---|
| خطّ الأساس | ✅ 785 اختباراً · إخفاق واحد | 2026-08-26 | `tests_handwriting.py:212` — منطقة الجلسة الأخرى، لا تُنسَب إلينا (وقد أصلحته لاحقاً) |
| T1.1 حذف `merge_settings` | ✅ | 2026-08-26 | صفر مستوردين مؤكَّداً |
| T1.2 تنظيف CSP | ✅ | 2026-08-26 | صفر CDN مقيساً قبل الحذف |
| T1.3 تقليص `network_ping` | ✅ | 2026-08-26 | + فخّ مقيس: `ipaddress.is_private` يعدّ نطاقات التوثيق (203.0.113.0/24) خاصّةً — الاختبار الأوّل كان سينجح كذباً |
| T1.4 تشفير `azure_key` | ✅ | 2026-08-26 | وُحِّد النمط في مِزيج بدل نسخةٍ ثانية |
| T1.5 نطاق البريد | ✅ | 2026-08-26 | اتّسع أثناء التنفيذ: 5 مواضع لا موضعاً واحداً (+IDOR في API) |
| T1.5 (جولة الإكمال) | ✅ | 2026-08-26 | الجرد الكامل رفع المواضع من 5 إلى **12** (6 نقاط API + عدّاد الشريط العلوي) |
| **المجموعة بعد الدفعة 1** | ✅ **838 اختباراً — OK** | 2026-08-26 | صفر انحدار · 14/24 من اختبارات النطاق تفشل على الكود القديم |
| T2.1 مدير الحذف الناعم | ✅ | 2026-08-26 | 6/7 حراسات تفشل بلا المدير · `_base_manager` مُتحقَّقٌ منه |
| T2.2 بوابة الرؤية الواحدة | ✅ | 2026-08-26 | 27 موضعاً ⟵ `core/scoping.py` · مراجعة فروقٍ يدويّة كشفت موضعاً ثامناً |
| T2.3 كتابٌ اختياريّ في سجلّ البريد | ✅ | 2026-08-26 | هجرة 0061 · أثرٌ جانبيّ في النطاق اكتُشف وعولج |
| **المجموعة بعد الدفعة 2** | ✅ **857 اختباراً — OK** | 2026-08-26 | 69 اختباراً جديداً منذ خطّ الأساس |
| إيداع الدفعتين 1+2 | ✅ `1598bf4` + `4b0874b` | 2026-08-27 | بمشورة فيبل: مساراتٌ صريحة وتحقّقُ فهرسٍ قبل الإيداع |
| شجرة عملٍ منفصلة | ✅ `rollout-b3` | 2026-08-27 | انتهى تشارك الشجرة؛ الـvenv وvar/ من الشجرة الأصلية |
| T3.1 حلقة إعادة الإرسال | ✅ `3c182a6` | 2026-08-27 | فيبل كشف ثغرةً في الخطّة: صفوف المحاولات كانت ستدخل الطابور |
| T3.2+T3.3 قفل المزامنة والختم | ✅ `ddfaaa9` | 2026-08-27 | اختبارٌ يحاكي القطيع أثناء المزامنة لا بعدها |
| T3.4 التوابع الأربع | ✅ `e70a854` | 2026-08-27 | فخّ `dumpdata` كان سيُفقد سلّة المحذوفات في أوّل نقل |
| **المجموعة بعد الدفعة 3** | ✅ **876 اختباراً — OK** | 2026-08-27 | 91 اختباراً جديداً منذ خطّ الأساس |
| م6 (المقياس الكاذب) | ↔ أصلحته الجلسة الأخرى | 2026-08-26 | إيداع `557dc77` — يُتحقَّق قبل شطبه من §7 |
| البند ① تصحيحُ السرّيّة | ✅ | 2026-08-27 | حجبُ محتوىً لا حجبُ صفّ · **عيبان حقيقيّان** (نسخةٌ ٢٨ من قاعدة الرؤية · القائمةُ بلا بُعد قسم) |
| البند ② نسيجُ الوثائق | ✅ | 2026-08-27 | هجرة 0067 · `linking_service` مسارَ كتابةٍ وحيداً · منتقي `@` · البحثُ يبلغ متنَ المسح · **تسريبُ تصدير CSV سُدّ** |
| **المجموعة بعد البند ②** | ✅ **978 اختباراً — OK** | 2026-08-27 | 193 اختباراً جديداً منذ خطّ الأساس |
| ③أ التفريق | ✅ `4f6fc9d` | 2026-08-27 | هجرة 0068 · **شجرةُ الأقسام صارت حمّالة** (كانت مكتوبةً ولا تُستعمل) |
| ③ب العهدة | ✅ `7f76c7b` | 2026-08-27 | هجرة 0069 · «بعهدة مَن» **مؤشّرٌ لا نسخة** · لا حدثَ قيدٍ مُختلَقاً للماضي |
| ③ج القيودُ المتعدّدة والردّ | ✅ `5f168d3` | 2026-08-27 | هجرة 0070 · نقضتُ قاعدتي في `status` ثمّ صحّحتُها |
| ③د الورقتان المطبوعتان | ✅ `ab215fa` | 2026-08-27 | تحقّقٌ بصريّ كشف عيبين لم تكشفهما 17 اختباراً خضراء |
| **المجموعة بعد البند ③** | ✅ **1073 اختباراً — OK** | 2026-08-27 | 288 اختباراً جديداً منذ خطّ الأساس · 4 هجرات (0067–0070) |
| البند ④ التعميمُ والعناقيد | ✅ | 2026-08-27 | هجرة 0071 · قاعدةٌ ديناميكيّةٌ لا تتيبّس · **لا `is_circular`** (مشتقٌّ بالكامل) |
| **المجموعة بعد البند ④** | ✅ **1093 اختباراً — OK** | 2026-08-27 | 308 اختباراً جديداً منذ خطّ الأساس |
| البند ⑥أ لوحة دورة الحياة | ✅ `fb5a0cb` | 2026-08-27 | تحقّقٌ بصريّ كشف 3 عيوب والاختباراتُ خضراء |
| كنسُ بوّابة المحتوى | ✅ `8f9e378` | 2026-08-27 | **5 نسخٍ حيّة** من القاعدة القديمة في مسارات كتابة |
| البند ⑦ سجلّ الحركات | ✅ `828224a` | 2026-08-27 | هجرة 0072 · بمشورة المستشار · **ثغرةُ X-Forwarded-For سُدّت** |
| البند ⑤ لوحة الإدارة | ✅ | 2026-08-27 | هجرة 0073 · التوأمةُ التلقائيّة + حارسُ حلقات الشجرة |
| **المجموعة بعد البند ⑤** | ✅ **1182 اختباراً — OK** | 2026-08-27 | 397 اختباراً جديداً منذ خطّ الأساس · 13 هجرة |
| **النشر على القاعدة الحيّة** | ✅ | 2026-08-30 | نسخةٌ احتياطيّة (2.8MB) ثمّ 13 هجرة · 13,193 كتاباً سليمة · الخادم على 8000 |

| البند ⑥ب الحواريّات | ✅ | 2026-08-30 | 5 نقاط كتابة + حواريّتان + أزرار الصفوف · فخّ الـPWA أُغلق (v79) |
| **المجموعة بعد ⑥ب** | ✅ **1214 اختباراً — OK** | 2026-08-30 | 429 اختباراً جديداً منذ خطّ الأساس |
| بذرُ شجرة العنونة | ✅ | 2026-08-31 | 39 قسماً على القاعدة الحيّة (40 بتوأمٍ كامل) · عيبُ «الجهة المعطّلة» أُصلح قبله |
| تصنيفُ الجهات الثلاثيّ | ✅ | 2026-08-31 | `entity_kinds.py` مصدراً وحيداً للأضابير وصفحة الجهات · 34/273/106 |
| البندان ⑤ و⑥ الطابوران | ✅ | 2026-08-31 | `/desk/` و`/my/today/` · **الشاشاتُ الإحدى عشرة اكتملت** |
| **المجموعة بعد ⑤+⑥** | ✅ **1237 اختباراً — OK** | 2026-08-31 | 452 اختباراً جديداً منذ خطّ الأساس |
| تبويبُ الجهة يدويّاً | ✅ | 2026-08-31 | هجرة 0074 · حقلٌ يملكه المالك + أداةُ نقلٍ جماعيّ · بذرٌ 459/34/181 |
| الأضبارةُ تمتلئ بالذكر | ✅ | 2026-08-31 | الشقُّ الرابع في `scope_books_for` · **النسخةُ السادسة من قاعدة الرؤية أُبطلت** |
| **المجموعة** | ✅ **1250 اختباراً — OK** | 2026-08-31 | 465 اختباراً جديداً منذ خطّ الأساس · 14 هجرة |
| إدارةُ العناقيد يدويّاً | ✅ | 2026-08-31 | تعديلٌ وأعضاءٌ وحذفٌ للـغير مستعمَل · **عيبُ محو العضويّة بالتعطيل أُصلح** |
| النسخُ الاحتياطيّ | ✅ | 2026-08-31 | **كان معطَّلاً (pg_dump غير موجود)** · يعمل على D: مُتحقَّقاً بنسخةٍ حقيقيّة |
| الدمجُ الدلاليّ | ✅ | 2026-08-31 | عائلتان (24 و9 كتاباً) · الثالثةُ تُركت عمداً |
| **المجموعة** | ✅ **1270 اختباراً — OK** | 2026-08-31 | 485 اختباراً جديداً منذ خطّ الأساس |
| شجرةُ الأقسام | ✅ | 2026-08-31 | قسم←شعبة←وحدة بقرار المالك · ملفٌّ مُخلَّدٌ بالرموز + أمرٌ جافّ · 34 قسماً تبقى مسطّحة |
| **المجموعة** | ✅ **1278 اختباراً — OK** | 2026-08-31 | 493 اختباراً جديداً منذ خطّ الأساس |
| ثغرةُ صفّ الإحالة | ✅ | 2026-08-31 | حارسُ الصفّ لا الكتاب · حارسان منفصلان بالاتّجاه · **تطابقُ نصفَي الرؤية محروسٌ بنيويّاً** |
| **المجموعة** | ✅ **1301 اختباراً — OK** | 2026-08-31 | 516 اختباراً جديداً منذ خطّ الأساس |
