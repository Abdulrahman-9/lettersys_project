# ابدأ من هنا

1. **`docs/STATE.md`** — أين نحن، ما التالي، ما ينتظر قراراً (صفحةٌ واحدة، تُستبدل).
2. **`docs/RULES.md`** — القواعدُ التي لا تُخالَف.
3. **`docs/log/`** — سجلُّ التنفيذ يوماً بيوم (يُلحَق فيه فقط؛ ملفٌّ جديدٌ لكلّ يوم).
4. `docs/HANDOFF_SESSION_2026-09-14.md` — تسليمُ جلسة 10–14 أيلول (لقطاتُ التحقّق البصريّ §7).
5. `Merge9.md` — **أرشيفٌ مختوم**؛ للمرجع التاريخيّ لا للعمل.

قبل أيّ سطرِ كود: اقرأ 1 و2، ثمّ أنشئ/ألحِق ملفَّ اليوم في `docs/log/`.
