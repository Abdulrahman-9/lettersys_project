# تسليمٌ إلى Claude Code (VS Code) — 2026‑09‑13

انسخ ما بين السطرين إلى أوّل رسالةٍ في Claude Code من جذر المستودع:

---
اقرأ أوّلاً `docs/SUNDAY_RUNBOOK_2026-09-13.md` ثمّ `Merge9.md` (السطورُ المؤرَّخة 2026‑09‑10 و11 في سجلّ التنفيذ) ثمّ `CLAUDE.md`. الحالُ: `main@01bae1a` لم يتغيّر؛ الخادمُ يحمل الوسمَ `cutover-2026-09` (`aa9794c`) ببيانات البروفة؛ النشرُ معطَّلٌ من طبقتين؛ رأسُ الهجرة 0077. ثلاثةُ فروعٍ جاهزةٌ في حزم git بالترتيب: `feat/is-deleted-sweep` (21 إيداعاً، صفرُ هجرات) ⟵ `feat/lifecycle-routing` (يحويه، 38 إيداعاً، صفرُ هجرات) ⟵ `feat/encrypted-char-field` (يحويهما، يحمل الهجرة 0078 — **لا يُدمَج قبل T‑0**). أنزِلها بـ`git fetch <bundle> <branch>:<branch>` ثمّ ادفعها وافتح PR لكلٍّ. قواعدُ لا تُخالَف: لا هجرةَ جديدة على `main` قبل T‑0؛ لا `migrate` ولا تشغيلَ لـ`lettersys-deploy` على الخادم؛ كلُّ تغييرٍ في الاستخراج يُقاس على المجموعات المختومة (`docs/EVAL_REGISTRY.md`) ولا يُقبل بتراجعٍ في أيّ حقل؛ حارسُ الوصل (`core/tests_wiring.py`) وحارسُ القوالب (`core/tests_template_role_gates.py`) وحارسُ `is_deleted` — لا تُوسَّع قوائمُ سماحها إلّا بسببٍ مكتوب؛ الحزمةُ تُشغَّل بـ`--settings=lettersys.settings_test` وتوقُّعُها 1688 اختباراً (فشلان بيئيّان فقط إن غابت أوزانُ LFS). المرجعُ الناسخُ لدورة الكتاب `docs/LIFECYCLE_DECISIONS_2026-09-11.md` — ما خالفه في خطّة الطرح v2 ومواصفة الأضابير منسوخ. المراجعاتُ التي تنتظر قراري قبل أيّ كود: `docs/UI_PRO_REDESIGN_2026-09-10.md` §6 (تسعة قرارات) · `docs/EXTRACTION_REVIEW_2026-09-11.md` (§11 السرعة و§12 الجهة: نفّذ التشخيصَ أوّلاً ولا تبنِ قبل توزيع الأخطاء) · `docs/NAV_AUDIT_2026-09-10.md` (د4 وط). لتصميم صفحة الأضابير استعمل `/design` بالموجز في `docs/DESIGN_BRIEF_DOSSIERS_2026-09-11.md`. ابدأ بالخطوة 1 من دفتر الأحد ولا تتجاوز بوّابةَ خطوةٍ حمراء. أَلحِق مخرَجاتِك في سجلّ التنفيذ في `Merge9.md` كما هو العرف.
---

## عن حملة التنظيف (سؤالُ المالك 2026‑09‑13)
**لا تحتاج حملةً شاملة — تحتاج ثلاثةَ أشياءَ موجَّهة**، والدليلُ قياسٌ لا رأي:
- pyflakes على `core`+`lettersys` (بلا اختبارات): **165 تحذيراً**، منها 4 «أسماءٌ غيرُ معرَّفة» — واحدٌ منها **خطأٌ حقيقيّ أُصلح اليوم** (`EntityResolver.get`: كاشُ الجهات لم يكن يُعاد بناؤه بعد تحميل البيانات — سببٌ محتمَل لضعف الجهة في الإنتاج)، وواحدٌ كودٌ ميّت بعد `raise` (حُذف)، و`harvest_number_strips.py:46` (`options` غيرُ معرَّف — سكربتُ تدريبٍ، يُصلَح عند استعماله). الباقي استيراداتٌ غيرُ مستعملة — تُنظَّف بأمرٍ واحد (`autoflake`) في إيداعٍ منفصلٍ صغيرٍ **بعد** T‑0.
- الأحجام: `extraction_smart.js` 5,410 سطراً و`models.py` 2,917 و`pipeline.py` 2,229 — كبيرةٌ لكنّها **موثَّقة ومقيسة**؛ تقسيمُها إعادةُ هيكلةٍ لا تنظيف، ولا تُقرَّر في أسبوع الإطلاق.
- الكودُ الميّت المعروف: مسارُ الحذف القديم و`archive_service` والقالبُ اليتيم حُذفت هذا الأسبوع؛ 26 مساراً مسمّىً بلا مدخل مجمَّدةٌ بأسبابها في `KNOWN_UNWIRED` — قرارُ «وصلٌ أم حذف» لكلٍّ قرارُ مالك.
**الحكم**: بعد T‑0 واستقرار أسبوع: إيداعُ `autoflake` واحد، وحذفُ ما يُقرَّر حذفُه من القائمة المجمَّدة. لا شيءَ الآن.
