# ابدأ هنا — LetterSys · محدَّثٌ 2026-09-14

> **هذا الملفُّ هو نقطةُ البدء الوحيدة لأيّ جلسةٍ جديدة.** اقرأه كاملاً (قصيرٌ عمداً)،
> ثمّ **لا تقرأ غيرَه إلّا عند الحاجة** — `Merge9.md` صار 1,431 سطراً وقراءتُه كاملاً هدرٌ.
> القيودُ في `Merge9.md` §0 و**قواعدُ الحرفة في §7.6 سارية دائماً**.

## 1. أين نحن (مقيسٌ 2026-09-14)

| | |
|---|---|
| `origin/main` | **وثائقُ فقط فوق `01bae1a`** — الكودُ مطابقٌ لكود البروفة · رأسُ الهجرة **0077** |
| الخادم (`217.65.145.47`) | يحمل الوسمَ **`cutover-2026-09c` (`d03a314`)** = `d80f339` + رقعةٌ مسمّاة، منشورٌ 09-14 ليلاً (بوّاباتٌ خضراء · دخان · فحصٌ طرفيّ 404/200) و**بياناتِ بروفةٍ حيّة** (13,241 كتاباً) · البريدُ مُطفَأ عمداً · النشرُ معطَّلٌ من طبقتين · `lettersys-deploy` مقفولٌ `chmod 000` |
| رفعُ الوسائط | ✅ **مختوم 2026-09-14**: 13,932 / 13,932 · إعادةُ الفحص على الخادم **0 فاشل/مفقود** · N2 دلتا **0** |
| ✅ **الأعطابُ الحيّة أُصلحت (09-14 ليلاً)** | **B‑1** · **D‑15** (404) · **D‑1** (للجديد؛ sarah/zainab تبقيان `is_staff` بقرار المالك) + إصلاحٌ رابعٌ مسمّى — فرعُ `fix/t0-named-patch` · 1,675 OK · ستُّ طفراتٍ حمراء · **حكمُ فيبل: لا شيءَ يُرجَع** — السجلُّ في آخر `Merge9.md`. **الفروعُ اللاحقة ما تزال تحمل الأعطاب** ⟵ تُنقل الرقعةُ إليها قبل دمجها |
| 🗓️ **T‑0** | **2026-09-15 قبل الدوام** بدفتر `D:/migration/outbox/T0_WINDOW_2026-09-15.md` (25 خطوة، بمراجعة فيبل) · قبلها: لقطةُ Hostinger · إبلاغُ الكاتبات · قفلُ القاعدة المحلّيّة (دورُ postgres) |
| الفروعُ والـPRs | **مدفوعةٌ كلُّها**: #17 ⟵ #18 ⟵ #19 متسلسلةٌ بالاحتواء · **#20** طاولاتُ العمل (كاغل 1,688 OK) · `feat/subject-crop-fixes` (38 OK، مُطفَّر) · #16 مسودّة — **لا دمجَ قبل قبول T‑0** |

## 2. الفروعُ الثلاثةُ الواصلة (متسلسلةٌ بالاحتواء — الترتيبُ ملزم)

| الفرع | إيداعات | هجرات | أهمُّ ما فيه |
|---|---|---|---|
| `feat/is-deleted-sweep` | 21 | **صفر** | كنسُ `is_deleted=False` (106 موضعاً) · حارسان بنيويّان (`tests_wiring` · `tests_template_role_gates`) · تدقيقُ الانتقالات · دفعاتُ الواجهات 1–3 · **اسمُ الشركة «نفط الوسط»** في الورقتين الموقَّعتين |
| `feat/lifecycle-routing` | 40 | **صفر** | **§6 الأرشفةُ تلقائيّةٌ والرفُّ أُلغي** (حُذفت `archive_service`) · §5.1 توجيهٌ تلقائيٌّ من المستلمين · §5.2/§5.5 · الأضابير · **إصلاحُ كاش `EntityResolver`** (سببٌ محتمَلٌ لضعف الجهة في الإنتاج) · «أين الموضوع؟» |
| `feat/encrypted-char-field` | 42 | **0078** | `EncryptedCharField` وحذفُ `EncryptedFieldsMixin` |

**قاعدةٌ لا تُخالَف**: **لا هجرةَ على `main` قبل T‑0** — بوّابةُ الترحيل تشترط بقاءَ رأس الهجرة **0077**. فالفرعُ الثالثُ يبقى خارجَ `main` حتّى بعد T‑0.

الحزمُ الأصليّة في `C:/Users/fwz/Downloads/*.bundle` (نسخةٌ ثانية). التسليمُ الكاملُ في `docs/CLAUDE_CODE_HANDOFF_2026-09-13.md`.

## 3. بوّاباتٌ آليّةٌ شُغِّلت على الفروع — كلُّها خضراء

الهجرةُ 0078 **حالةٌ فقط** (`state_operations` = 3 · `RunSQL`/`RunPython` = **0**) · `EncryptedFieldsMixin` حُذف فلا مسارَ ثانٍ · عقدُ الحقل في `core/fields.py` بثلاثته (`from_db_value` · `get_prep_value` · `value_to_string`) · **حرّاسُ الجمعة سليمةٌ بعددها** (`verify_backup` 47 · `encryption_key_guard` 6 · `purge_dev_seed` 3 · `encrypted_columns` 13 · `dumpdata_guard` 19⟵20) · الاختباراتُ 1,579 ⟵ **1,612** (عدّاً ساكناً؛ التسليمُ يتوقّع 1,688 عند التشغيل) · **صفرُ CDN** · صفرُ تعليقٍ `{#` ممتدّ · `bundle verify` و`fsck` نظيفان.

## 4. البندُ التالي — الحكمُ وصل

**مذكّرةُ فيبل الرابعة وصلت 2026-09-13 08:41** ⟵ `docs/FABLE_MEMO4_2026-09-13.md` (الحكمُ الملزم مختصَراً في `Merge9.md` §17). خلاصتُها:
**تُدفَع الفروعُ الثلاثة و3 PR متسلسلة الآن · ولا يُدمَج شيءٌ في `main` قبل قبول T‑0 · وN1‑ب واجبٌ الآن مستقلّاً عنها.**
سبعةُ بنودٍ جديدة **N23–N29**، وأخطرُها **N25**: فرعُ `encrypted-char-field` **يكسر `verify_backup` بصمت** (مُتحقَّقٌ بيدي) — لا يُدمَج قبل إصلاحه.
**مقيسٌ محلّيّاً بأوزان LFS**: `3c54bfc` rc=0 · 1,701 اختباراً · `0601076` rc=0 · 1,688 (الهبوطُ = حذفُ `tests_archive_service` مع الخدمة). **ولا حزمةَ كاملةً محلّيّاً أثناء الرفع** (أمرُ فيبل).

**وما لا ينتظر شيئاً:**
1. ~~إكمالُ رفع الوسائط~~ — **مختومٌ 2026-09-14** (13,932 · إعادةُ فحص الخادم 0 فاشل · N2 دلتا 0). وأداةُ الرفع فيها **عطبُ إنهاءٍ** يُصلَح قبل أيّ استعمالٍ لاحق (`Merge9.md` §17.15).
2. **قرارات المالك المعلّقة**: `docs/UI_PRO_REDESIGN_2026-09-10.md` §6 (تسعة) · `docs/EXTRACTION_REVIEW_2026-09-11.md` §11/§12 (**تشخيصٌ قبل أيّ بناء**) · `docs/NAV_AUDIT_2026-09-10.md` (د4 · ط) · وثلاثةُ أسئلةِ صلاحيّاتٍ في سجلّ د10.
3. **تحذيرٌ قائم**: الموقعُ حيٌّ على `lettersys.online` ببيانات بروفةٍ و12 حساباً نشطاً — **ما يُدخَل الآن يُمحى عند T‑0**؛ يجب إبلاغُ الكاتبات.

## 5. بعد T‑0 (بالترتيب)

**N1‑ب** وسمٌ جديدٌ يحمل الفروعَ المدموجة + سحبٌ على الخادم (`fetch --tags` ⟵ `checkout <tag>` ⟵ `lfs pull` كـwww-data · **بلا `migrate` وبلا `lettersys-deploy`**) ⟵ لقطةُ T‑0 (25 ثانيةً مقيسة) ⟵ تنظيفُ 10.k ⟵ تعيينُ مسؤول الأرشفة ⟵ أوّلُ كتابٍ حقيقيّ ⟵ يومان استقرارٍ ⟵ إعادةُ تمكين النشر ⟵ الفرعُ الثالث (الهجرة) ⟵ `autoflake`.

## 6. اقرأ عند الحاجة فقط

| تحتاج | اقرأ |
|---|---|
| ترتيبَ الترحيل كلَّه | `Merge9.md` §11.2 (خريطةُ الدفعات) ثمّ الدفعةَ المعنيّة |
| ما جرى ولماذا | `Merge9.md` §16 (مراجعةُ 09-10: المنجَز · الأخطاءُ · المخاطرُ · الصورةُ النهائيّة) — **و§16.6 مُبطَلٌ جزئيّاً**: الأرشفةُ صارت تلقائيّةً بلا رفّ (`docs/LIFECYCLE_DECISIONS_2026-09-11.md` هو الناسخ) |
| العملَ أونلاين | `Merge9.md` §15 |
| مذكّراتِ فيبل | `Merge9.md` §12 و§14 |
| حالةَ الخادم بالتفصيل | `Merge9.md` §13 و§15.3 |
| دورةَ الكتاب الحاكمة | `docs/LIFECYCLE_DECISIONS_2026-09-11.md` (ينسخ ما خالفه في خطّة الطرح v2 ومواصفة الأضابير) |
| دفترَ الأحد | `docs/SUNDAY_RUNBOOK_2026-09-13.md` |

**عرفُ العمل**: كلُّ خطوةٍ تنتهي بمخرَجٍ يُلصق في «سجلّ التنفيذ» في `Merge9.md` (لا «تمّ») · وكيلٌ واحدٌ يكتب في git · لا تلمس الخادمَ إلّا بأوامرَ تُسلَّم للمالك أو بالمنحة المحدودة (`Merge9.md` §12.3) · واستشر فيبل في كلّ قرارٍ معماريّ.
