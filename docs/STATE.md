# لوحةُ الحال — LetterSys (تُستبدل كاملةً عند كلّ تغيير؛ لا تُلحَق)

**آخرُ تحديث**: 2026‑09‑24 ظهراً — **الانطلاقُ المحلّيّ** (قرارُ المالك): الحاسبةُ خادمٌ على الشبكة الداخليّة · VPS مجمَّد · 24/25 انتقلتا إلى الجهاز. المرجعُ الحاكم حكمُ فيبل خارج المستودع (`D:/migration/fable_local_server_20260924.md`) معدَّلاً بقرارات المالك في `log/2026-09-24.md`. **القواعد**: `docs/RULES.md` · **السجلّ**: `docs/log/` · **الأرشيف**: `Merge9.md`.

## 1. أين نحن (مقيسٌ)

| الشيء | الحال |
|---|---|
| `main` | `1c65a75` — وثائق + دمجُ الرقعة الأمنيّة `fix/t0-named-patch` · رأسُ الهجرة **0077** |
| **الجهازُ المحلّيّ** | **الحقيقةُ منذ 09‑24 11:06** — 13,108 كتاباً · تدريب 0 · `max_id` 13,386 · **5 نشطة (3 superuser)** كالإنتاج · البريدُ والنسخُ مُطفآن · رأسُ الهجرة 0077 · **عدّادُ الوارد الداخليّ 3918** (الحجز 415 يمسك 3917) · نسخٌ دائمة في `D:/trackbackup/keep/` · خادمٌ **مؤقّت** (`runserver` بمتغيّرات بيئة، `DEBUG` مطفأ ⟵ `/dev-login/` 404) حتّى خدمة waitress |
| الخادم (VPS) | **مجمَّدٌ 09‑24 11:52 بثلاث طبقات**: الخدمتان متوقّفتان وممنوعتان من البدء (`ConditionPathExists`) · دورُ التطبيق للقراءة فقط · nginx 503 بصفحة الانتقال على كلّ مسار. بياناتُه 13,108 **بلا أيّ كتابةٍ منذ T‑0** · الوسمُ `cutover-2026-09c` · `lettersys-deploy` مقفول. **لا مسحَ قبل مرآةٍ خارجيّةٍ للمرفقات** (هو اليومَ النسخةُ الثانيةُ الوحيدة). الرجوع: `D:/migration/t0/vps_freeze_rollback.sh` |
| الوسائط | **مختومة**: 13,932 / 13,932، صفرُ مفقود (09‑14) |
| **T‑0** | 1–10 خضراء (09‑15) · **11–23 خضراء (09‑17 09:25→09:30، انقطاعٌ ~4.5 د)** — الاستعادةُ 2.4 ث مطابقةٌ للأساس حرفيّاً، 131 تدريباً حُذفت، العدّاداتُ 3917/605/715 لم تنزل، المفتاحُ سليم، مسبارُ السرّيّة 404/200. **24/25 على VPS ميّتتان — انتقلتا إلى الجهاز المحلّيّ (قرارُ المالك 09‑24).** السكربتاتُ والحالة محلّيّاً في `D:/migration/t0/` |
| مدفوعٌ غيرُ مدموج | PR #16 `feat/mail-archive-attribution` · #17 `feat/is-deleted-sweep` ⟵ #18 `feat/lifecycle-routing` (**`16ddfc4`** — 7 إيداعاتٍ مستردّة 09‑17) ⟵ #19 `feat/encrypted-char-field` (**`3a1a55d`**، مُعادُ القاعدة فوق `16ddfc4`، **يحمل 0078**) · #20 `feat/desk-ui-pro` و`feat/subject-crop-fixes` (يتفرّعان من `0601076` لا `16ddfc4`) · `fix/text-layer-and-markers` (`54f87d1`، مذكّرةُ فيبل 8) · `claude/phase-0-…` (`50f3ad1`، وثائق) |
| الرقعةُ الأمنيّة | في `main` **وليست في أيٍّ من الفروع** ⟵ تُعاد قاعدتُها فوق `main` قبل كلّ دمج |

### ⚠️ عملُ جلساتٍ أونلاين **ليس في المستودع** — يُستردّ، لا يُحذف ذكرُه
| # | العمل | الدليل | المكانُ المرجَّح |
|---|---|---|---|
| ~~O1~~ ✅ | `fix/extraction-stale-guard` + مذكّرةُ فيبل 6 للاستخراج الشارد وتفريغ الحقول (صفرُ هجرات) | **استُردّ 09‑24** من حزمة التسليم السحابيّة ⟵ `refs/recovered/stale-guard` = `4a7ea7a` (إيداعان فوق `main`: `c1f90c7` المذكّرة + `4a7ea7a` الرقعة) | **محلّيٌّ غيرُ مدفوع** — أوّلُ الدمج بعد القبول |
| ~~O2~~ ✅ | 6 إيداعاتٍ على `feat/lifecycle-routing` (زرُّ العين · الطباعةُ وعدُّ الورقات · عارضُ الصور · مركزُ الإعدادات · التسليم) | **استُردّت 09‑17** من `files.zip` في التنزيلات (منذ 09‑14، لم يُفتح) | **مدفوعةٌ إلى #18 (`16ddfc4`)** |
| ~~O3~~ ✅ | `docs/SETTINGS_HUB_REVIEW_2026-09-13.md` (مرجعُ P5) | ضمن الإيداعات المستردّة (`6d99a65`) | #18 |
| ~~O4~~ ✅ | `docs/HANDOFF_SESSION_2026-09-14.md` (11 لقطةَ تحقّقٍ بصريّ §7؛ أربعٌ منها للإيداعات المستردّة) | ضمن الإيداعات المستردّة (`16ddfc4`، مطابقٌ بالبصمة لنسخة التنزيلات) | #18 |

**قاعدةٌ مقترحة (فيبل، تنتظر قرارَ المالك قبل دخول RULES)**: كلُّ جلسةٍ سحابيّةٍ تنتهي بـ`git push` إلى `claude/<موضوع>-<تاريخ>` ولصقِ `git ls-remote --heads origin`؛ والحزمةُ/الـzip تسليمٌ ثانويّ **يُنزَل في اليوم نفسه** ويُسجَّل رأسُه في سجلّ اليوم. (السببُ الجذريّ لـO2–O4: تنزيلٌ لم يُفتح ثلاثةَ أيّام، ولوحةُ حالٍ سجّلت «حزمتُها عند المالك» بلا تحقّق.)

## 2. التالي — بالترتيب، ولا يُقفَز

1. **القسمُ ب — الجهازُ خادماً** (الجلسةُ الأخرى): الطاقة (النومُ 0 منجَز؛ BIOS وساعاتُ النشاط باقية) · الشبكة (حجزُ DHCP · اسمٌ داخليّ · قاعدةُ جدارٍ واحدة) · **تحريرُ C: بقائمةٍ يقرّها المالك — لا شيءَ ينتقل إلى D** · نسخةُ إنتاجٍ منفصلة (لا شجرةَ التطوير) + بيئةٌ وحسابُ خدمة · فرعُ بنية الإطلاق في worktree (waitress/whitenoise · `run_backup` · حارسُ القفل في `db_healthcheck` · نقلُ كتابات `.env` وقتَ التشغيل إلى القاعدة) — **بلا دمج**.
2. **د — الخدمةُ والقبول محلّيّاً**: خدمةُ waitress تقوم بعد إعادة إقلاعٍ بلا تدخّل ⟵ مسبارُ السرّيّة + HTTP من محطّةٍ أخرى ⟵ **الخطوة 24 بعين المالك هنا** ⟵ **25 القبول**: أوّلُ واردٍ داخليٍّ حقيقيّ بحضور المالك — العدّادُ **3918** ما لم يُحرَّر الحجز 415 ⟵ يومان استقرار. المسحُ عند الإطلاق NAPS2 ⟵ PDF ⟵ رفع (وكيلُ المسح أحاديُّ الجهاز في الكود الحاليّ؛ يُصلَح بعد الانطلاق).
3. **هـ — التشغيلُ الدائم**: مهامُّ المجدول (نسخ · تحقّق · جلسات · متأخّرات · إنذارُ C:) ⟵ **مرآةٌ خارجيّةٌ للمرفقات والنسخ + إيداعُ المفتاح** — شرطُ أيّ قرارٍ بمسح VPS.
4. **الدمجُ بعد القبول + يومين** بترتيب الحكم: `refs/recovered/stale-guard` ⟵ `fix/text-layer-and-markers` مع فرع العناوين ⟵ #17 ⟵ #18 (بعد N23) ⟵ #20 و`subject-crop-fixes` ⟵ **#19 آخرُ الكلّ** (0078 · كسرُ `verify_backup` · نافذةُ `migrate` محلّيّة). كلُّ فرعٍ يُطعَّم برقعة `d03a314` أوّلاً.
5. بعد الاستقرار: `50f3ad1` ثمّ `docs/split-merge9` ⟵ `main` (**دمجٌ محلّيٌّ ثمّ دفع**، `merge=union`) · `fix/entity-cache-heal` (`cherry-pick 64cecf3`) · **D6.13 تدويرُ كلمة Gmail قبل أيّ بريد** · تجميدُ الميزات أسبوعين.

## 3. سجلُّ القرارات الموحّد (مَن حسم · متى · أين يُنفَّذ)

| الرمز | القرار | الحكم | متى | الحال |
|---|---|---|---|---|
| D6.3 | حذفُ 131 كتابَ تدريب من الإنتاج | **نعم** | 09‑13 | **نُفِّذ 09‑17** (الخطوة 17) |
| D6.2 | الحساباتُ الحيّة | **تعطيلُ 7** (5 اختبار + 2 ملتبسان) · **لا إبلاغ** — ينسخ قرارَ 09‑13 «إبلاغٌ شفهيّ» | 09‑17 | **نُفِّذ** (18‑ب وبعد 23) |
| T‑0 إطلاق | الوكيلُ يُطلق والمالكُ يعتمد كلَّ أمرٍ يدويّاً؛ رمزا 12/17 في الأمر المعتمَد | المالك | 09‑17 | نُفِّذ |
| D6.4 | تفعيلُ البريد | **يوم +2 بعد T‑0** | 09‑13 | معلَّقٌ على D6.13 |
| D6.13 | تدويرُ كلمة تطبيق Gmail | **نعم** | 09‑08 | **حالةُ التنفيذ غيرُ مسجَّلة** ⟵ تُحسم قبل D6.4 |
| الإحالةُ اليتيمة | صفُّ «للعلم» بعد إزالة الجهة | **يُقفَل RETURNED** بسطر سجلّ | 09‑13 | فرعُ الدورة (لم يُنفَّذ) |
| الوكالة ١–٤ | توقيعٌ بالوكالة · مدّةٌ · سرّيّة · مَن يسجّل | لا طباعة «بالوكالة» · بلا مدّة · تُمنح السرّيّة · كلاهما | 09‑13 | 0079 (بعد T‑0) |
| الدورة §1–§8 | **المرجعُ الناسخ** لخطّة الطرح ومواصفة الأضابير | `docs/LIFECYCLE_DECISIONS_2026-09-11.md` | 09‑11 | `feat/lifecycle-routing` |
| الصادرُ الخارجيّ | رقمٌ واحد = رقمُ مكتب المدير العام بعد «ش13/»، بلا سلسلة | مُثبَت | 09‑11 | كما كان |
| الرفّ | موضعُ الحفظ الورقيّ | **مُلغى** | 09‑11 | فرعُ الدورة (مُنفَّذ) |
| بوّابةُ الموضوع المتعلَّم | ملءٌ مباشرٌ بعد 3 عيّنات | **نعم، كالتاريخ** | 09‑11 | مُنفَّذ (`subject_crop`) |
| الواجهات ق‑7 | اسمُ الشركة على الورقتين | **شركة نفط الوسط** | 09‑10 | مُنفَّذ |
| فيبل 4 | حكمُ الحزم الثلاث | «لا شيءَ يُرجَع»؛ ترتيبُ الدمج | 09‑13 | ساري |
| D‑LAN | الانطلاق: لا رفعَ على الموقع الآن؛ الحاسبةُ خادمٌ على الشبكة الداخليّة؛ 24/25 محلّيّاً | **نعم** (نصُّه في سجلّ 09‑24) | 09‑24 | جارٍ |
| D‑FRZ | تجميدُ VPS بثلاث طبقات؛ F1 بنمط المنحة («نفذ البديل») | نعم | 09‑24 | **نُفِّذ 11:46→11:52** |
| D‑DISK | لا شيءَ ينتقل إلى D (قرصٌ ميكانيكيّ)؛ C يُحرَّر بقائمةٍ تُعرض قبل الحذف | نعم | 09‑24 | القسم ب |
| D‑ENV | كتاباتُ `.env` وقتَ التشغيل ⟵ القاعدة بإصلاح كود (لا بصلاحيّات ملفّ) | نعم | 09‑24 | فرعُ بنية الإطلاق |
| D‑SCAN | المسحُ من أجهزة الكاتبات بعد الانطلاق؛ عند الإطلاق NAPS2 ⟵ رفع | نعم | 09‑24 | بعد القبول |
| D‑ACC | الحسابان الملتبسان معطَّلان محلّيّاً كالإنتاج | نعم (تأكيدُ قرار 09‑17) | 09‑24 | **نُفِّذ** (5 نشطة · 3 مديرون) |

### قراراتٌ **معلَّقة** (لا يُبنى قبلها)
| # | القرار | المرجع |
|---|---|---|
| P1 | الواجهات ق‑1…ق‑6، ق‑8، ق‑9 | `UI_PRO_REDESIGN` §6 |
| P2 | د4 زرُّ حذفٍ في صفحة الكتاب · ط وصلُ/حذفُ `followup_activity_report` و`legacy_import` و11 API بلا مستهلك | `NAV_AUDIT` · `tests_wiring.KNOWN_UNWIRED` |
| P3 | إتلافُ `lettersys_data.json` و`lettersys.dump` محلّيّاً | الأرشيف §17.5 |
| P4 | حوكمةُ المزوّد الخارجيّ (Azure): تعطيلٌ صريحٌ أم استثناءُ السرّيّ | `EXTRACTION_REVIEW` §9 |
| P5 | أتُجمع إدارةُ الجهات والأقسام في مركز الإعدادات؟ | `SETTINGS_HUB_REVIEW_2026-09-13` (على #18) |
| ~~P6~~ | موضعُ رقعة الاستخراج الشارد وتوقيتُها | **حُسم 09‑24**: مستردّةٌ (`c1f90c7` المذكّرة + `4a7ea7a` الرقعة) وهي **أوّلُ الدمج** بعد القبول + يومين (حكمُ فيبل، القرار 9) |
| P7 | N28 حدُّ الحقبة · دمجُ `feat/mail-archive-attribution` بعد الاستقرار | الأرشيف §17 · §6.11 |

## 4. الديونُ المسجَّلة (لا تُصلَح ارتجالاً)
**بعد 25**: الحساباتُ المعطَّلة بـ`is_active` وحدَه — تُخلط كلماتُ مرورها (مسجَّلةً قبل التنفيذ) · إسقاطُ قواعد `zz_*` بقرار المالك · `TESSERACT_TESSDATA_DIR` غيرُ مضبوطٍ على الخادم · رسالةُ «التالي: 24» في سكربت العبور تُطبع مع كلّ `--until`.
**الانطلاقُ المحلّيّ (09‑24)**: الخادمُ المؤقّت يعمل من **شجرة التطوير** (أيُّ تعديلٍ فيها يبلغ مَن يستعمله فوراً للساكن) — يُستبدل بالخدمة من نسخة الإنتاج · الحجزُ 415 يمسك 3917 ⟵ يُحرَّر أو يُترك لانتهائه قبل كتاب القبول · VPS النسخةُ الثانيةُ الوحيدة للمرفقات حتّى المرآة الخارجيّة · بوّابةُ curl من شبكة المكتب لا تُصدَّق (اعتراضُ TLS) — القياسُ من الخادم نفسه أو من خارج الشبكة.
**قائمة**: `var/subject_boxes.json` ⟵ جدولٌ في 0079 ويدخل النسخَ · شخصٌ جهةً ووحدتُه · غرضُ «للردّ» · ✓/✓✓ تتبّعُ الفتح · جوابُ الوحدة بمرفق · سجلُّ مَن غيّر إعداداً · `harvest_number_strips.py:46` · 10 أسطرِ `is_deleted=False` في `scripts/eval`/`training` · إعادةُ استعمال TSV في «أين الموضوع؟» · صفحةُ قياس الاستخراج.
