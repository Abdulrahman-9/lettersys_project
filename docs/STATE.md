# لوحةُ الحال — LetterSys (تُستبدل كاملةً عند كلّ تغيير؛ لا تُلحَق)

**آخرُ تحديث**: 2026‑09‑17 (جلسةٌ سحابيّة، من المستودع لا من الخادم). **القواعد**: `docs/RULES.md` · **السجلّ**: `docs/log/` · **الأرشيف**: `Merge9.md`.

## 1. أين نحن (مقيسٌ من `origin/main@1c65a75`)

| الشيء | الحال |
|---|---|
| `main` | `1c65a75` — وثائق + دمجُ الرقعة الأمنيّة `fix/t0-named-patch` · رأسُ الهجرة **0077** |
| الخادم | الوسمُ `cutover-2026-09c` (`d03a314`) · **بياناتُ بروفةٍ حيّة (13,241)** · 12 حساباً حقيقيّاً · البريدُ مُطفَأ · النشرُ معطَّل · `lettersys-deploy` مقفول `000` |
| الوسائط | **مختومة**: 13,932 / 13,932، صفرُ مفقود (09‑14) |
| **T‑0** | **الخطواتُ 1–10 خضراء (09‑15) وتوقّفٌ مقصودٌ قبل 11** — النسخةُ على الخادم مُتحقَّقةٌ ومقفولة. **ما بعد 11 غيرُ مسجَّل** (نفدت توكنزُ الجلسة بعد أمر الاعتماد) ⟵ يُحسم بسطر: `Book.objects.count()` — 13,108 = تمّت · 13,241 = موقوفة |
| الفروعُ المدفوعة | `feat/is-deleted-sweep` (PR #17) ⟵ `feat/lifecycle-routing` (#18) ⟵ `feat/encrypted-char-field` (#19، **يحمل 0078**) · `fix/extraction-stale-guard` (رقعةٌ فوق `main`) · `feat/subject-crop-fixes` · `feat/desk-ui-pro` · `feat/mail-archive-attribution` · `claude/phase-0-…` (وثائق) |
| غيرُ مدفوع | 6 إيداعاتٍ على `feat/lifecycle-routing` من 09‑14 (زرُّ العين · الطباعةُ وعدُّ الورقات · عارضُ الصور · مركزُ الإعدادات · التسليم) — حزمتُها عند المالك |
| الرقعةُ الأمنيّة | في `main` **وليست في أيٍّ من الفروع الثلاثة** ⟵ تُعاد قاعدتُها فوق `main` قبل الدمج |

## 2. التالي — بالترتيب، ولا يُقفَز

1. اقرأ عددَ الكتب على الخادم ⟵ يحدّد هل نحن قبل الخطوة 11 أم بعدها.
2. أتمم T‑0 **بسكربتٍ ذي نقاط تفتيش** (دفترُها `D:/migration/outbox/T0_WINDOW_2026-09-15.md` — يُنقل إلى `docs/`).
3. ادمج `fix/extraction-stale-guard` أوّلاً (أصغرُ الرقع، أعلاها أثراً على الكاتبات).
4. أعد قاعدةَ الفروع الثلاثة فوق `main`؛ ادفع الإيداعات الستّة؛ التحقّقُ البصريّ (11 لقطة في `docs/HANDOFF_SESSION_2026-09-14.md` §7).
5. ادمج #17 ثمّ #18. **#19 لا يُدمَج** قبل إصلاح كسره لـ`verify_backup` وبوّابة `sqlmigrate core 0078` = no‑op.
6. N1‑ب الثانية: وسمٌ جديد + سحبٌ على الخادم (بلا `migrate`) ⟵ `purge_dev_seed_books --yes` (D6.3) ⟵ د8 ⟵ يومان استقرار ⟵ إعادةُ تمكين النشر ⟵ د9.
7. **تجميدُ الميزات أسبوعين**؛ تشخيصُ السرعة والجهة (`EXTRACTION_REVIEW` §11/§12) ثمّ `autoflake`.

## 3. سجلُّ القرارات الموحّد (مَن حسم · متى · أين يُنفَّذ)

| الرمز | القرار | الحكم | متى | يُنفَّذ في |
|---|---|---|---|---|
| D6.3 | حذفُ 131 كتابَ تدريب من الإنتاج | **نعم، قبل T‑0** | 09‑13 | 10.k② على الخادم |
| D6.4 | تفعيلُ البريد | **يوم +2 بعد T‑0** | 09‑13 | د8 بندٌ مستقلّ |
| الحساباتُ الحيّة | تعطيلٌ/لافتة | **لا شيء — إبلاغٌ شفهيّ**؛ الخطرُ مقبولٌ بقرار | 09‑13 | — |
| الإحالةُ اليتيمة | صفُّ «للعلم» بعد إزالة الجهة | **يُقفَل RETURNED** بسطر سجلّ | 09‑13 | فرعُ الدورة (لم يُنفَّذ بعد) |
| الوكالة ١–٤ | توقيعٌ بالوكالة · مدّةٌ · سرّيّة · مَن يسجّل | لا طباعة «بالوكالة» · بلا مدّة · تُمنح السرّيّة · كلاهما | 09‑13 | 0079 (بعد T‑0) |
| الدورة §1–§8 | **المرجعُ الناسخ** لخطّة الطرح ومواصفة الأضابير | `docs/LIFECYCLE_DECISIONS_2026-09-11.md` | 09‑11 | `feat/lifecycle-routing` |
| الصادرُ الخارجيّ | رقمٌ واحد = رقمُ مكتب المدير العام بعد «ش13/»، بلا سلسلة | مُثبَت (عُكست خطوةٌ خاطئة) | 09‑11 | كما كان |
| الرفّ | موضعُ الحفظ الورقيّ | **مُلغى الآن** | 09‑11 | فرعُ الدورة (مُنفَّذ) |
| بوّابةُ الموضوع المتعلَّم | ملءٌ مباشرٌ بعد 3 عيّنات | **نعم، كالتاريخ** | 09‑11 | مُنفَّذ (`subject_crop`) |
| الواجهات ق‑7 | اسمُ الشركة على الورقتين | **شركة نفط الوسط** | 09‑10 | مُنفَّذ |
| S‑0 / Gmail | تدويرُ كلمة تطبيق Gmail | **نعم** (توصيةٌ حاسمة) | 09‑08 | [م] |
| فيبل 4 | حكمُ الحزم الثلاث | «لا شيءَ يُرجَع»؛ ترتيبُ الدمج | 09‑13 | §17.1 |

### قراراتٌ **معلَّقة** (لا يُبنى قبلها)
| # | القرار | المرجع |
|---|---|---|
| P1 | الواجهات ق‑1…ق‑6، ق‑8، ق‑9 (بطاقةُ الشعار · الإجراءاتُ السريعة · «قيد المتابعة» · 403/404 · الشريطُ على الهاتف · `design-system.css` · لوحُ الطاولات · أفعالُ «ما يخصّني») | `UI_PRO_REDESIGN` §6 |
| P2 | د4 زرُّ حذفٍ في صفحة الكتاب · ط وصلُ/حذفُ `followup_activity_report` و`legacy_import` و11 API بلا مستهلك | `NAV_AUDIT` · `tests_wiring.KNOWN_UNWIRED` |
| P3 | إتلافُ `lettersys_data.json` و`lettersys.dump` محلّيّاً | §17.5 |
| P4 | حوكمةُ المزوّد الخارجيّ (Azure): تعطيلٌ صريحٌ أم استثناءُ السرّيّ | `EXTRACTION_REVIEW` §9 |
| P5 | أتُجمع إدارةُ الجهات والأقسام في مركز الإعدادات؟ | `SETTINGS_HUB_REVIEW` |
| P6 | فيبل 6: موضعُ رقعة الاستخراج الشارد وتوقيتُها وإلغاءُ الطلب على الخادم | `FABLE_MEMO6_EXTRACTION_STALE` |
| P7 | N28 حدُّ الحقبة (يتوقّف على قياس N24) · دمجُ `feat/mail-archive-attribution` بعد الاستقرار | §17 · §6.11 |

## 4. الديونُ المسجَّلة (لا تُصلَح ارتجالاً)
`var/subject_boxes.json` ⟵ جدولٌ في 0079 ويدخل النسخَ · شخصٌ جهةً ووحدتُه · غرضُ «للردّ» · ✓/✓✓ تتبّعُ الفتح · جوابُ الوحدة بمرفق · سجلُّ مَن غيّر إعداداً · `harvest_number_strips.py:46` · 10 أسطرِ `is_deleted=False` في `scripts/eval`/`training` · إعادةُ استعمال TSV في «أين الموضوع؟» · صفحةُ قياس الاستخراج.
