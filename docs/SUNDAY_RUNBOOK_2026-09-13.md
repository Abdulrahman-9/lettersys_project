# دفترُ الأحد — ما يُنفَّذ حين يفتح الجهاز، بترتيبه وبوّاباته (كُتب 2026‑09‑10)

كلُّ خطوةٍ هنا **لا تُنفَّذ إلّا بعد خضرة بوّابة ما قبلها**. ما يمسّ الإنتاج مُعلَّمٌ ⚠️. الجلسةُ السحابيّة لا تملك مفتاحاً ولا جهازاً — هذا الدفترُ تسليمُها.

## 0. قبل أيّ شيء (5 دقائق)
- اقرأ Merge9 **§16.4** (المخاطرُ المفتوحة). أخطرُها: الموقعُ حيٌّ ببيانات البروفة و12 حساباً حقيقيّاً — أيُّ كتابٍ يُدخَل الآن **يُمحى** عند الرفع النهائيّ. اختر: تعطيلُ الحسابات أم لافتةٌ.
- قرّر الثلاثة: **D6.3** (حذفُ 131 كتابَ تدريبٍ من الإنتاج) · **D6.4** (متى يُفعَّل البريد) · إتلافُ `lettersys_data.json`.

## 1. إنزالُ عمل الجلسة ودفعُه (من جذر المستودع، على `main@01bae1a`)
```
git fetch feat-is-deleted-sweep.bundle feat/is-deleted-sweep:feat/is-deleted-sweep
git fetch feat-encrypted-char-field.bundle feat/encrypted-char-field:feat/encrypted-char-field
git log --oneline 01bae1a..feat/is-deleted-sweep | wc -l      # 18
git push -u origin feat/is-deleted-sweep feat/encrypted-char-field
```
- PR 1: `feat/is-deleted-sweep` ⟵ `main` — **صفرُ هجرات** (`git diff 01bae1a -- core/migrations` فارغ). يُدمَج بعد §2.
- PR 2: `feat/encrypted-char-field` ⟵ `main` — يحمل **0078** ⟵ **لا يُدمَج قبل T‑0** (§5). ضع «Do not merge» في العنوان.

## 2. التحقّقُ الذي يحتاج عينَك (نصفُ ساعة) — على `feat/is-deleted-sweep` محلّيّاً
1. **ق9**: كم كتاباً يظهر فجأةً في «بلا مرفق» بعد (د‑4)؟
   `manage.py shell -c "from core.models import *; from core.views.queues import *; print(Book.objects.live().exclude(pk__in=Attachment.objects.values('book_id')).count())"` — سجّل الرقم في Merge9 قبل الشحن.
2. **ق15 + اللقطاتُ** (`runserver` محلّيّاً، مستخدمٌ غيرُ staff ثمّ staff):
   - `/books/desk/archive/` · `/books/trash/` · `/books/<id>/` — الحذفُ الناعم لا يُظهر محذوفاً.
   - الداشبورد ⟵ «متأخّر» ⟵ القائمةُ تعرض المتأخّرَ وحدَه (`followup=overdue` في العنوان).
   - الداشبورد ⟵ عدّادُ طاولةٍ ⟵ يهبط على بطاقة الطابور (`#q-…`).
   - الطابور ⟵ «و{n} غيرها» ⟵ الطابورُ كاملاً وزرُّ «اطوِ».
   - مركزُ الإعدادات ⟵ تبويبُ السلّة ⟵ رابطُ القائمة يفتح في **النافذة العليا**.
   - القائمة ⟵ حذفُ صفٍّ ⟵ التوست ⟵ «تراجع» يُعيد الصفّ.
   - صفحةُ كتاب ⟵ تفريقٌ/عهدة ⟵ اللوحةُ تتحدّث **بلا** إعادة تحميل والتمريرُ ثابت.
   - طاولةُ الوارد ⟵ كشفُ التسليم ⟵ يطبع «شركة نفط الوسط» و«رجوع» يعود إلى الطاولة.
   - عنوانٌ لكتابٍ محذوف ⟵ 404 **بشريطٍ** وزرِّ عودة (يحتاج `DEBUG=False` محلّيّاً).
   - Tab من أوّل الصفحة ⟵ «تخطَّ إلى المحتوى» يظهر.
3. **كاغل** على رأس الفرع: الحزمةُ الكاملة **بأوزان LFS** — `EXIT=0` صفرُ فشل (الجلسةُ السحابيّة لا تستطيع LFS).
4. ثمّ **ادمج PR 1**.

## 3. N1‑ب — الخادمُ يحمل الوسمَ القديم `aa9794c` ⚠️
```
git tag -a cutover-2026-09b <sha-main-بعد-الدمج> -m "N1-ب: purge fix + حرّاس 8.6-ب/ج + N11 + T7.5 + 7.4-هـ + تدقيقُ الانتقالات"
git push origin cutover-2026-09b
```
كاغل على الوسم الجديد (`EXIT=0`) ⟵ ثمّ على الخادم **بالصيغة نفسِها التي نُفِّذت بها N1** (كـwww‑data):
```
cd /var/www/lettersys && git fetch --tags origin && git checkout --force cutover-2026-09b && git lfs pull
```
بوّابات: `git rev-parse HEAD` = sha الوسم · `.venv/bin/python manage.py verify_backup --help` rc=0 · `showmigrations core | tail -3` = **0077 مطبَّقة / 0 معلّقة** · `models_healthcheck --strict --load` rc=0 · `check` نظيف · **لا `migrate` · لا `lettersys-deploy` (يبقى 000) · لا إعادةَ تشغيلٍ الآن** (تُؤجَّل إلى د7/T‑0).

## 4. بعد D6.3: `purge_dev_seed_books --yes` على الخادم ⚠️ (10.k②) — معاينةً أوّلاً، والبوّابة 13,108 كتاباً · تدريب 0.

## 5. T‑0 ⚠️ (جهازُك): نسخةٌ جديدة ⟵ `verify_backup --expect-live` ⟵ نقل ⟵ N21 استعادةٌ في المكان (≈25 ث مقيسةً) — كما في §11/§15 حرفيّاً. ثمّ إتمامُ رفع الوسائط بسطر الاستئناف (13,932 ملفّاً بالبصمات).

## 6. بعد T‑0 حصراً
- **PR 2** (8.6‑أ): على الخادم `sqlmigrate core 0078` يجب أن يطبع **no‑op** ⟵ دمجٌ ⟵ وسمٌ ⟵ سحبٌ ⟵ `migrate` (ثوانٍ) ⟵ `db_healthcheck --strict` ⟵ `shell -c "…len(e.smtp_password)…"` = `16 16`.
- **د8**: مسؤولُ الأرشفة · أوّلُ كتابٍ حقيقيّ · البريدُ بقرار D6.4 · وحدةُ `celery-beat`.
- **د9**: مفتاحُ النشر (D6.8) · تمكينُ السير · PR #16 · `chmod 755 lettersys-deploy`.

## 7. القراراتُ المؤجَّلة للمراجعة البصريّة (تفتح الدفعاتِ 4–7 من الواجهات)
ق‑1 · ق‑2 · ق‑3 · ق‑4 · ق‑5 · ق‑6 · ق‑8 · ق‑9 — نصُّها في `docs/UI_PRO_REDESIGN_2026-09-10.md` §6. ومن تدقيق الانتقالات: **د4** (زرُّ حذفٍ في صفحة الكتاب؟) و**ط** (`followup_activity_report` و`legacy_import` والـAPI الـ11 بلا مستهلك: وصلٌ أم حذف؟ — القائمةُ في `core/tests_wiring.py`).
