# سجلُّ التنفيذ — 2026-09-14

> مُستخرَجٌ من `Merge9.md` §17 (أرشيف). يُلحَق هنا فقط.

### 17.15 — صباحُ 09-14: **بوّابةُ الوسائط مختومة** · إصلاحاتُ الموضوع مُثبَتة · و**ثلاثةُ أعطابٍ حيّةٍ على الوسم المنشور** · 2026-09-14

#### ١. الوسائطُ — مختومةٌ لـT‑0
| البوّابة | المقيس |
|---|---|
| الرفع | **13,932 / 13,932** مُتحقَّقاً بالبصمات |
| **إعادةُ الفحص الكاملة على الخادم** (`--verify`، 07:58⟵08:00) | **فاشلٌ/مفقود: 0** |
| **N2 دلتا** | على القرص **13,932** = البيان **13,932** · أحدثُ من البيان: **0** ⟵ لا رفعَ إضافيّ |
| قرصُ الخادم | 14G حرّة |

**عطبُ إنهاءٍ في أداة الرفع (بلا ضررٍ على البيانات)**: بعد الاكتمال دارت الأداةُ على دفعاتٍ فارغةٍ حتّى **الدفعة 135** وتركت قفلاً ميّتاً. الجذر: `cnt=$(grep -c . "$batch" || echo 0)` — `grep -c` على ملفٍّ فارغٍ **يطبع 0 ويخرج 1** فيُضاف `0` ثانٍ ⟵ `cnt="0\n0"` ⟵ `[ "$cnt" -eq 0 ]` يفشل مقارنةً فلا يتحقّق شرطُ الإنهاء. **يُصلَح قبل أيّ استعمالٍ للأداة** (بعد T‑0 مثلاً). القفلُ أُزيل يدويّاً بعد التأكّد من صفر عمليّات.

#### ٢. حقائقُ الخادم (قراءةٌ فقط) — تحسم «أين البطء» جزئيّاً
`nproc` = **1** · ذاكرةٌ 3910/980 MB متاحة · tesseract **5.5.0** · `ara.traineddata` = **1,432,056 بايت = fast لا best** ⟵ رافعةُ §1 (`tessdata_best`) **لم تُجرَّب في الإنتاج قطّ** · **`pandas: False` في بيئة الخادم** ⟵ **يؤكّد في الإنتاج** أنّ هندسةَ أسطر «أين الموضوع؟» كانت ميتة (مذكّرةُ فيبل الخامسة). و`intra_op=1` صحيحٌ لنواةٍ واحدة — لا يُلمَس.

#### ٣. إصلاحاتُ الموضوع — مُثبَتةٌ ومطفَّرة (فرع `feat/subject-crop-fixes`)
`179e15f` الإصلاحاتُ الثلاثة (`DICT` + `_tsv_records` · حدُّ y على «إلى/» · نافذةٌ متناظرة) · `62da5ed` حرّاسُها. **كلُّ طفرةٍ احمرّت**: M1 ⟵ حارسان · M2 ⟵ حارس · M3 ⟵ حارس. بعد الاستعادة: `tests_subject_crop` + `tests_title_emission` = **38 · OK**.

#### ٤. التدقيقُ الثلاثيّ — مُستردٌّ من السجلّ
`audit:detail` (16 ملاحظة، **مُحقِّقُه سقط بحدّ الجلسة فهي غيرُ مُتحقَّقٍ منها**) · `verify:roles` (17، مُتحقَّقة) · `verify:toast` (19، مُتحقَّقة). محفوظةٌ كاملةً في `D:/migration/audit_recovered_20260914.json`. وصحّح المدقّقون مقدّمتي: `D:/wt/lcr` كان على `feat/desk-ui-pro` لا `feat/lifecycle-routing` (يحتويه).

#### ٥. **ثلاثةُ أعطابٍ حيّةٍ على الوسم المنشور `cutover-2026-09b`** — قِستُها بنفسي على الوسم لا على الفرع

**B‑1 — نظامُ التوست يدور على نفسه (حرجة، وظيفيّة)**
`static/app.js` على الوسم: `function showToast(` في **السطر 119** و`window.showToast = function(...) { return showToast(...); }` في **السطر 1792** — **كلاهما بعمق أقواسٍ 0** (تحليلُ نطاقٍ حقيقيّ يتخطّى النصوصَ والتعليقات، لا موضعُ العمود). الإسنادُ يستبدل الربطَ العامّ فالمرجعُ الداخليُّ يُحَلّ إلى الغلاف نفسِه ⟵ **تكرارٌ لا نهائيّ**. **مُعاد إنتاجُه بـNode**: `RangeError: Maximum call stack size exceeded`. و**24 نداءً عارياً** في `app.js` وحدَه (وغيرُها في ملفّاتٍ أخرى بحسب التدقيق). فكلُّ رسالةٍ تمرّ بـ`showToast` ترمي خطأً — وإن لم يلتقطه المُنادي **توقّف الفعلُ الذي بعدها**.

**D‑15 — ستُّ نسخٍ من قاعدة `is_staff` الملغاة في عمليّات المحتوى (حرجة، أمنيّة)**
قاعدةُ المشروع أنّ `is_staff` **لا توسّع الرؤية** (`core/scoping.py:16`) — والوسمُ يحمل ستَّ نسخٍ تتخطّى نطاقَ القسم **والسرّيّة**:
| الموضع على الوسم | ما يُتاح لحامل `is_staff` على **أيّ** كتاب |
|---|---|
| `core/merge_api.py` ×4 (`get_attachment` · `merge` · `delete_file` · `restore`) | دمجُ مرفقٍ · **حذفُه (ناعم — `restore` يُعيده)** · استعادةُ نسخة · وعبر `get_attachment`: `history`/`versions` |
| `core/views/api.py:207–210` `attachment_ocr_text` | **قراءةُ نصّ OCR لأيّ مرفق — ومنه كتبُ السرّيّ** |
| `core/views/books_api.py:737–739` `api_book_inline_status` | تغييرُ حالةِ أيّ كتاب |

**والمسارُ حيٌّ وإن لم يكن له زرّ**: `router.register('attachments', AttachmentMergeViewSet)` تحت `books/api/` بـ`permission_classes = [IsAuthenticated]` وحدَه — مدرجٌ في `KNOWN_UNWIRED` (بلا واجهة) **لكنّه مُوجَّهٌ ويُبلَغ بالرابط**.

**D‑1 — كلُّ حسابٍ يُنشأ من صفحة الحسابات يولد `is_staff=True`** (`core/views/users.py:82` على الوسم) ⟵ فالثغرةُ **تتّسع مع كلّ كاتبٍ جديد**.

**التعرّضُ الفعليُّ على الخادم الآن** (قراءةٌ فقط): **12** حساباً نشطاً · **10** `is_staff` · **8** superuser · **2 `is_staff` بلا superuser** ⟵ هذان الاثنان هما مَن ينال اليومَ صلاحيّاتٍ فوق دوره (الـsuperuser يرى كلَّ شيءٍ بحقّ).

**حدودُ الادّعاء بصدق**: المصادقةُ بالجلسة وDRF يفرض CSRF على الأفعال المغيِّرة ⟵ **خطرٌ من داخل الشركة خارجَ النطاق، لا استغلالٌ من مجهول**. والحذفُ ناعمٌ يُستعاد. ولم يُجرَّب الاستغلالُ تشغيلاً — الإثباتُ نصُّ الكود على الوسم وإعادةُ إنتاجِ نمط B‑1.

**وتصحيحٌ لقياسي أنا**: عدُّ نمطِ سطرٍ واحدٍ أعطى **1** (فاته أنّ بوّابة `attachment_ocr_text` ممتدّةٌ على ثلاثة أسطر)، والجردُ متعدّدُ الأسطر أعطى **6** — فالمدقّقُ كان محقّاً. وفحصي الأوّلُ على الوسم بـ`grep -A12` أعطى **0** لأنّ البوّابةَ في السطر 209 خارجَ النافذة — **ثالثُ وقوعٍ في فخّ المخرَج المقصوص**؛ أُعيد بمدًى كاملٍ فظهرت.

#### ٦. قراران للمالك قبل T‑0
1. **رقعةٌ أمنيّةٌ مسمّاةٌ قبل T‑0؟** قاعدةُ فيبل «كودُ T‑0 = كودُ البروفة **زائدَ إصلاحاتٍ مسمّاة**» تسمح بها. الإصلاحُ المقترح تقنيٌّ بلا قرارِ سياسة: **B‑1** بإزالة الغلاف الذاتيّ · **D‑15** باستبدال النسخ الستّ بـ`can_open_content` من `core/scoping.py` (**404 لا 403** — نمطُ T7.5‑2 نفسُه) · مع حارسٍ مطفَّرٍ لكلّ موضع وكاغل على الرقعة، على فرعٍ من الوسم لا من `main`.
2. **D‑1 — قرارُ سياسةٍ لا دَين** (هو السؤالُ ① المعلَّقُ من د10): أيتوقّف إنشاءُ الحسابات بـ`is_staff=True`؟ وماذا عن الحسابَين الحاليَّين؟

- **2026-09-14 — عطبُ إنهاء أداة الرفع أُصلح** (المذكورُ في §17.15): `cnt=$(grep -c . "$batch"); cnt=${cnt:-0}` بدل `|| echo 0`. `bash -n` أخضر · نسخةٌ احتياطيّة `D:/migration/upload_media.sh.bak_20260914` · وإعادةُ فحصٍ على دفعةٍ فارغة: `cnt=0` ⟵ **ينتهي**. (المحاولةُ الأولى رفضها حارسُ الصياغة: `\n` داخل تعليقٍ صار سطراً حقيقيّاً فانكسر اقتباسٌ — فلم يُبدَّل شيء.)
- **2026-09-14 مساءً — قراراتُ المالك (§17.15 ٦ + D6.x)**: **رقعةٌ مسمّاةٌ قبل T‑0** (B‑1 · D‑15) · **D‑1**: يُوقَف `is_staff` للحسابات الجديدة ويُعرض الحسابان الحاليّان بالاسم للقرار · **موعدُ النافذة: غداً قبل الدوام** (D6.1) · **لقطةُ VPS من Hostinger قبل T‑0** (D6.10 = نعم) · **لم يُوافَق** على إتلاف `lettersys_data.json`/`lettersys.dump` ولا على تعطيل حسابات الفحص (D6.2 مفتوح ⟵ 10.k③ يُتخطّى في هذه النافذة).
  **فحصُ ما قبل T‑0 محلّيّاً (قراءةٌ فقط)**: القاعدةُ مُجمَّدة **13,239** · آخرُ `created_at` **2026-08-30 07:38:39** · تدريب 131 · مستخدمون 12 · لا مستمعَ على 8000 · **مهمّةُ `LetterSysServer` غيرُ موجودةٍ في Task Scheduler** (أسطرُ `schtasks` في 10.b تفشل — البوّابةُ الفعليّة: لا مستمع + قفلُ القاعدة) · C: حرٌّ 7.6 GB.
  **الرقعة** — فرعُ `fix/t0-named-patch` من الوسم `d80f339` في `D:/wt/t0p` (محلّيٌّ غيرُ مدفوع): D‑15 النسخُ الستُّ ⟵ `can_open_content` بـ404 · D‑1 · B‑1 إزالةُ الغلاف الذاتيّ · وإصلاحٌ رابعٌ مسمّى على الوسم نفسِه (تفصيلُه بعد النشر) · `?v=` و`CACHE_VERSION=v96-t0p`. **البوّابات**: المستهدَفة 136 OK · **ستُّ طفراتٍ كلُّها حمراء** (إرجاعُ كلّ ملفٍّ إلى الوسم) · صفرُ `.is_staff` في المواضع الستّة وصفرُ `window.showToast =` · **الحزمةُ كاملةً محلّيّاً بأوزان LFS: 1,675 · OK (skipped=2)** = 1,663 الوسم + 12 حارساً جديداً · `makemigrations --check` No changes.
  **مسبارُ خادمٍ مُسجَّلٌ قبل تنفيذه (قراءةٌ فقط، `manage.py shell` كـwww-data)**: عددُ الكتب والمرفقات المُنشأة على `lettersys.online` منذ 2026-09-10 (ما سيُمحى عند T‑0) · أسماءُ حسابات `is_staff` بلا superuser · عددُ من دخل منذ 09-10. لا كتابةَ ولا إعادةَ تشغيل.
  **المخرَج** (`D:/migration/logs/probe_t0_pre_20260914.txt`، ssh rc=0): كتب **13,241** (= 13,239 + 2) · **مُنشأةٌ بعد التجميد (> 2026-08-30 07:38:39): 2 كتاباً · 3 مرفقات** ⟵ **تُمحى عند T‑0 بالبناء** · `is_staff` بلا superuser: **`sarah` و`zainab`** (نشطتان — الكاتبتان الرئيستان) · دخل بعد التجميد: 2 · الوسم `cutover-2026-09b` · الخدمتان `active`. والرقعةُ مُودَعةٌ محلّيّاً **`d03a314`** (غيرُ مدفوعة).
  **مسبارٌ ثانٍ مُسجَّلٌ قبل تنفيذه (قراءةٌ فقط)**: تفصيلُ الكتابين (المعرّف · النوع · الرقم · المُنشئ · الزمن · عددُ المرفقات) كي يُعاد إدخالُهما بعد T‑0 — بلا نصِّ العنوان.
  **المخرَج** (`probe_t0_pre2_20260914.txt`، rc=0): **#13387 وارد داخليّ `3915`** و**#13388 وارد داخليّ `3916`** — كلاهما من **`zainab`** يوم 2026-09-10 (09:21 و09:30 UTC) · غيرُ محذوفَين · غيرُ تدريب · مرفقٌ واحدٌ لكلٍّ (#13293 · #13294). ⟵ **كتابان حقيقيّان على البروفة بختمٍ ورقيٍّ محتمَل** يُعادان يدويّاً بعد T‑0 بالرقمين نفسيهما.
  **العدّادُ والحجوزاتُ محلّيّاً (قراءةٌ فقط)**: `incoming_internal.next_number=3917` · حجزُ **3915** `active` و`is_recycled=True` (انتهى 09-13 07:45) · حجزُ **3916** `cooldown` · وهذا ما أعطى زينبَ الرقمين على البروفة ⟵ **بعد T‑0 تُدخِل زينبُ الكتابين أوّلاً قبل أيّ واردٍ داخليٍّ آخر** وإلّا ذهب 3915 لكتابٍ غيرِ الذي خُتم به (استنتاجٌ من حالة الحجز، لم يُتتبَّع في الكود). ⚠️ **وحجزا 3915/3916 كُتبا محلّيّاً يوم 09-13** (`voided_at`/`last_heartbeat`) مع أنّ الكتبَ ثابتةٌ منذ 08-30 ⟵ **القاعدةُ المحلّيّة ليست مقفولةً كتابةً** (فُتح الإدخالُ الذكيّ عليها) ⟵ قفلُ 10.b قراءةً أوّلُ خطوةٍ في النافذة بلا تأخير.
- **2026-09-14 ليلاً — قراراتُ المالك الثلاثة**: **نشرُ الرقعة الليلة** · **`sarah` و`zainab` تبقيان `is_staff`** (بعد الرقعة لا تفتح لهما محتوى أقسامٍ أخرى؛ تبقى صفحاتُ الإدارة) · **كتابا زينب 3915/3916 يُتركان للمحو** (لا إعادةَ إدخال، لا حذفَ يدويَّ الآن — T‑0 يستبدل القاعدة) ⟵ الرقمان قد يُعاد إصدارُهما لورقٍ آخر بعد T‑0.
  **خطّةُ النشر المُسجَّلة قبل تنفيذها** (منحةُ N8، بلا `migrate` وبلا `lettersys-deploy` الذي يبقى `000`): وسمٌ **`cutover-2026-09c`** = `d03a314` يُدفَع مع الفرع ⟵ على الخادم كـwww-data: `fetch --tags` ⟵ `checkout --force cutover-2026-09c` ⟵ `lfs pull` ⟵ **بوّاباتٌ توقف قبل إعادة التشغيل**: HEAD=`d03a314` · معلَّقةُ الهجرات 0 · `migrate --check` 0 · `models_healthcheck --strict --load` 0 · `check` ⟵ `collectstatic --noinput` ⟵ `systemctl restart lettersys.service lettersys-celery.service` ⟵ **دخان**: `/login/` 200 · `/media/` 302 · واجهةُ المرفقات بلا جلسة ≠ 200 · `CACHE_VERSION='v96-t0p'` مخدوم · `app.js` المخدوم بلا `window.showToast =` · صفرُ traceback · الذاكرةُ الحرّة. الرجوع: `checkout --force cutover-2026-09b` + `collectstatic` + restart.
  **الجزءُ الأوّل (`deploy_t0p_partA_20260914.txt`، ssh rc=0)**: الوسمُ دُفع (`cutover-2026-09c` ⟵ `d03a314`) · قبل: `d80f339 cutover-2026-09b` ⟵ بعد: **`d03a314 cutover-2026-09c`** · تغييراتٌ متتبَّعة 0 · **هجراتٌ معلَّقة 0** · `migrate --check` rc=0 · `models_healthcheck --strict --load` rc=0 · `check` = no issues · `collectstatic`: **2 ملفّين نُسخا** (app.js · service-worker.js) و220 بلا تغيير.
  **فحصٌ طرفيٌّ مُسجَّلٌ قبل تنفيذه (قراءةٌ فقط، بلا دخولٍ ولا جلسة)**: `manage.py shell` يستدعي `attachment_ocr_text` مباشرةً بـ`RequestFactory` و`request.user=sarah` على مرفقٍ خارج `can_open_content` لها ⟵ المتوقَّع **404**، وبحساب superuser على المرفق نفسِه ⟵ **200**. لا يُطبع نصٌّ ولا يُمسّ `last_login`.
  **الجزءُ الثاني (`deploy_t0p_partB_20260914.txt`، ssh rc=0)**: إعادةُ تشغيل الخدمتين ⟵ `active active` · `/login/` **200** · `/media/__probe__.pdf` **302** · واجهةُ المرفقات بلا جلسة **403** · OCR بلا جلسة **302** · صفحةُ الدخول تُحيل إلى **`app.js?v=20260914-t0p`** · `service-worker.js` المخدوم **`CACHE_VERSION = 'v96-t0p'`** · `app.js` المخدوم: **`window.showToast =` = 0** و`function showToast` = 1 · traceback/critical في 5 دقائق **0** · الذاكرةُ المتاحة **2,864 MB**. ⟵ **الرقعةُ حيّةٌ على `lettersys.online`**، و`lettersys-deploy` باقٍ `000`.
  **الفحصُ الطرفيّ (`deploy_t0p_e2e_20260914.txt`، rc=0)**: `sarah` (staff=True · super=False · القسم 1) على المرفق **#11074** (كتابٌ **سرّيٌّ في قسمها**) ⟵ **404** · superuser على المرفق نفسِه ⟵ **200**. ⟵ D‑15 مختومٌ على الإنتاج سلوكيّاً لا نصّاً.
  **تحقّقٌ لدفتر الغد**: `core/backup_service.create_encrypted_pg_backup` = `pg_dump` + `encrypt_file` فقط — **صفرُ كتابةٍ في القاعدة** ⟵ آمنٌ بعد قفل 10.b قراءةً. **ولم يُشغَّل كاغل على `cutover-2026-09c`** (بديلُه الحزمةُ المحلّيّة بأوزان LFS 1,675 OK). الدفترُ النهائيّ للنافذة بمراجعة فيبل: `D:/migration/outbox/T0_WINDOW_2026-09-15.md`.
- **2026-09-14 ليلاً — تذكيرُ المالك: «استشر فيبل في المهامّ المعقّدة»** (الرقعةُ نُشرت قبل الاستشارة — خطأُ ترتيبٍ من المشرف، سُجِّل قاعدةً). **حكمُ فيبل العدائيّ على نشر الليلة: لا شيءَ يُرجَع.** (أ) التوسيعُ الضمنيّ (المُنشئ ⟵ `can_open_content`) **مواءمةٌ لا توسيع**: العمليّاتُ نفسُها مفتوحةٌ بالبوّابة نفسِها في المسارات الموصولة (`attachments.py:232,271,327,415,489` · `books_api.py:794` · `comments.py:58`)؛ ودَينٌ بعد T‑0: فصلُ `can_mutate_content` عن شقّ الذكر (`scoping.py:319–336`). (ب) 403⟵404 مقبول (العميلان يقرآن رسالةَ JSON فقط). (ج) **لا نسخةَ `is_staff` باقيةً توسّع محتوى**؛ الباقي صلاحيّاتٌ إداريّةٌ خلف `staff_required` (حذفُ الحسابات بلا حارسٍ للسوبر `users.py:109–119` · كلماتُ المرور المؤقّتة · الاستعادةُ والاستيراد · حذفُ الجهات ودمجُها) ⟵ دَينٌ بعد T‑0: سوبر فقط لهذه. (د) **كاغل ليس شرطَ T‑0** (بوّابةُ دمجٍ فقط). **الدفتر**: 25 خطوةً في 56 سطراً. **أخطرُ ثلاثة**: ① الورقُ المختوم 3915/3916 يُعاد إصدارُه بعد المحو ⟵ تُسأل زينب الليلة · ② **القفلُ المحلّيّ يحتاج دورَ postgres** (مالكُ القاعدة `postgres` لا `lettersys_user`) · ③ `--write-plain` يرفض هدفاً موجوداً ⟵ أسماءٌ مؤرّخة؛ و`RENAME` يحتاج صفرَ اتّصالات. **الليلة**: لقطةُ Hostinger · رسالةُ الكاتبات · ويُستحسن القفلُ المحلّيّ.
  **تحقّقُ المشرف من الدفتر قبل اعتماده** (قُرئ كاملاً، 56 سطراً): مالكُ القاعدة المحلّيّة **`postgres`** والتطبيقُ `lettersys_user` (`fable_local_probe_20260914.txt:3`) ✓ · إعادةُ التدوير **بأصغر رقمٍ متروك** وحالاتُه `voided/expired` أو `cooldown` منقضٍ (`reservation_service.py:22–32,68–83`) ⟵ **3916 أوّلاً** و3915 بعد تعليمه منتهياً ✓ · `--write-plain` «غيرُ موجود» (`verify_backup.py:49–50,86–87`) ✓ · `D:/migration/remote/smoke.sh` موجود ✓ · مرفقُ المسبار الطرفيّ **#11074** ⟵ الكتاب #9528 سرّيّ · القسم 1 · **غيرُ تدريب** وغيرُ محذوف ⟵ ينجو من purge فتصحّ الخطوة 23 غداً ✓.
- **2026-09-14 ليلاً — قرارُ المالك: «احذف 3915 و3916» = إلغاءُ الرقمين نهائيّاً** (لا حذفَ للكتابين من الخادم — يمحوهما T‑0). **لماذا يكفي حذفُ الحجزين**: إعادةُ البذر **لا تُنزل العدّادَ أبداً** — `target = max(next_number, أكبرُ رقمِ كتابٍ+1, أكبرُ رقمٍ محجوزٍ+1)` (`core/views/dashboard.py:897–938`)، و«الفجواتُ مقبولة؛ تكرارُ الأرقام ليس كذلك» بنصّ الكود؛ ولا شيءَ في الكود يُعيد إنشاءَ حجزٍ محذوف (الإعادةُ تدويرٌ لصفوفٍ قائمةٍ فقط، `reservation_service.py:22–32`). ⟵ بعد الحذف يبقى `incoming_internal.next_number=3917` ولا يأخذ أيُّ كتابٍ بعد T‑0 الرقمَ 3915 ولا 3916.
  **الأمرُ المُسجَّل قبل تنفيذه (القاعدةُ المحلّيّة، مصدرُ T‑0)**: نسخُ الصفّين كاملَين إلى `D:/migration/logs/reservations_3915_3916_backup_20260914.json` (قراءةً) ثمّ `BookNumberReservation.objects.filter(kind='incoming_internal', number__in=[3915,3916], book__isnull=True).delete()` بحارسٍ يتوقّف إن وُجد كتابٌ محلّيٌّ بأحد الرقمين · البوّابة: صفّان محذوفان · صفرُ حجزٍ باقٍ بهما · العدّاد 3917.
  **المخرَج**: النسخة **2 صفّاً** (id 413 ⟵ 3915 `active` · id 414 ⟵ 3916 `cooldown` · `book_id` فارغ) ⟵ الملفّ أعلاه · كتبٌ محلّيّةٌ بالرقمين **0** ⟵ `deleted (2, {'core.BookNumberReservation': 2})` · باقٍ **0** · `incoming_internal.next_number` **3917** ✓.
  **اكتشافٌ جانبيّ (قراءةٌ فقط)**: المتروكُ القابلُ للتدوير الآن — وارد داخليّ **2445** `expired` · وارد خارجيّ **359** `expired` و**604** `voided` · صادر داخليّ **714** `cooldown`. و2445 و359 **مرتبطان بكتبٍ قائمة** (`objects=1`) فيتخطّاهما التدوير (`reservation_service.py:71`)؛ أمّا **604 و714 فبلا كتاب** ⟵ **أوّلُ واردٍ خارجيٍّ بعد T‑0 يأخذ 604 وأوّلُ صادرٍ داخليٍّ 714** قبل عدّادَيهما 605/715 — بالتصميم «بلا فجوات» لا عطب؛ ودخانُ 4.6 يُقرأ بذلك. الدفترُ صُحِّح.
- **2026-09-14 — سؤالُ المالك «هل أرى الصورَ على الويب الآن؟» — مسبارٌ مُسجَّلٌ قبل تنفيذه (قراءةٌ فقط)**: `manage.py shell` كـwww-data يمرّ على كلّ `Attachment` بملفّ ويعدّ ما يوجد ملفُّه على القرص (`os.path.exists(file.path)`) مع `MEDIA_ROOT` وخمسة أمثلةٍ للمفقود إن وُجد. لا كتابةَ ولا جلسة.
  **المخرَج** (`probe_media_on_disk_20260914.txt`، ssh rc=0): `MEDIA_ROOT = /var/www/lettersys/media` · **مرفقاتٌ بملفّ 13,175 · مفقودٌ على القرص 0** ⟵ كلُّ مرفقٍ في قاعدة البروفة يجد ملفَّه، والعرضُ عبر X‑Accel مختومٌ منذ 09-10 ⟵ الصورُ تُرى على `lettersys.online` بعد الدخول.
- **تصحيحُ توقيت (مقيس)**: الساعةُ عند سؤال المالك «لماذا لا نقوم بهذا اليوم؟» **2026-09-14 11:53 +03:00** على الجهاز والخادم معاً (`date`) ⟵ **كلُّ ما وُسم في إدخالات 09-14 أعلاه وفي الإيداعين 1b197ff/d59fd41 بـ«ليلاً/الليلة/مساءً» وقع نهاراً** — افتراضٌ من المشرف لا قياس. واختيارُ «غداً قبل الدوام» بُني عليه جزئيّاً؛ ولا عائقَ تقنيّاً يمنع النافذةَ اليوم.
- **2026-09-14 11:55 — قرارُ المالك: T‑0 اليوم بعد التجهيز** (يَنسخ D6.1 «غداً قبل الدوام»). **الشروطُ قبل البدء**: لقطةُ Hostinger (المالك) · إيقافُ الكاتبات عن الإدخال الآن مع آخرِ رقمِ ختمٍ ورقيٍّ لكلّ سجلّ (المالك) · انتهاءُ تدقيق Merge9 الجاري ومراجعةُ نتيجته مع الدفتر (المشرف) · تحويلُ تواريخ الدفتر إلى 20260914 · حضورُ المالك للخطوات 2/12/24.
  **مسبارُ ما قبل النافذة المُسجَّلُ قبل تنفيذه (قراءةٌ فقط، الخادم)**: الوسمُ وHEAD · حالةُ الخدمتين · صلاحيّةُ `lettersys-deploy` · قائمةُ القواعد · القرصُ الحرّ · محتوى `~/inbox` و`/var/backups/lettersys/incoming` (أسماءٌ وأحجام) · عددُ الكتب المُنشأة بعد التجميد (ما سيُمحى اليوم) · `redis-cli -n 1 DBSIZE`. لا كتابةَ ولا إعادةَ تشغيل.
  **المخرَج — الخادم** (`probe_prewindow_20260914.txt`، ssh rc=0): `d03a314 cutover-2026-09c` · `active active` · `lettersys-deploy` = **0** · القواعد: `lettersys` 50 MB · `postgres` · `zz_empty0061_20260910` 12 MB · `zz_stale_20260903_dropme` 49 MB · القرص **14G حرّ (72%)** · `~/inbox`: ملفّا البروفة + `_v.sha256` (فحصُ الوسائط 09-14) · `incoming`: **`ship.dump` (600) و`ship640.dump` (640) من البروفة باقيان** ⟵ أسماءُ الدفتر المؤرّخة لازمة · Redis db1 = 10 مفاتيح · كتب **13,241** والمُنشأُ بعد التجميد **#13387 `3915` · #13388 `3916` فقط** (لا جديدَ اليوم).
  **المخرَج — محلّيّاً**: لا مستمعَ على 8000 · لا عمليّةَ python/celery · `default_transaction_read_only = off` (القفلُ خطوة 2) · `(13239, 2026-08-30 07:38:39.73214+00, 12)` · حجزا 3915/3916 = **0** · العدّادات `605/3917/715/1` · اتّصالاتٌ أخرى بالقاعدة **0**. **والدفترُ حُوِّل إلى اليوم**: `TS=20260914` · `zz_rehearsal0910_20260914` · وسومُ «الليلة» أُزيلت.
  **يُنتظَر قبل الخطوة 1**: تأكيدُ المالك (لقطةُ Hostinger + توقّفُ الكاتبات) · نتيجةُ تدقيق Merge9 · حكمُ فيبل «انطلق/توقّف» عليها.
- **2026-09-15 08:03 — النافذةُ لم تبدأ أمس** (توقّف العملُ بحدّ الاستخدام قبل تأكيد المالك). **تأكيدُ المالك: الكاتباتُ متوقّفات** ✓. **لقطةُ Hostinger لم تُؤخذ** — المالكُ لا يعرف المقصود ⟵ شُرحت له الخطواتُ (D6.10). وتدقيقُ Merge9 استُؤنف (`wf_68fd7dbe-b25`: 27 من الذاكرة + 6 وكلاء باقون).
- **2026-09-15 08:08 — D6.10: لقطةُ VPS أُنشئت** بمساعد Hostinger (البرومبت `D:/migration/outbox/hostinger_agent_prompt_snapshot.md`، يمنع الإيقافَ والاستعادةَ والحذفَ وأوامرَ النظام). **ردُّ المساعد**: الإنشاء **05:08:06 UTC (08:08:06 بغداد)** · الحالة **قيد الإنشاء (~30 دقيقة)** · الانتهاء **2026-09-16 05:08:06 UTC** · الخادم Running لم يُعَد تشغيلُه. **البوّابة**: اكتمالُ اللقطة قبل الخطوة 7 (أوّلُ مسٍّ للخادم في النافذة).
  **مسبارٌ مُسجَّلٌ قبل تنفيذه (قراءةٌ فقط)** لتحقيق «لم يُعَد تشغيلُه» لا تصديقه: `uptime -s` · `systemctl show -p ActiveEnterTimestamp` للخدمتين (المتوقَّع: قبل 08:08 — منذ إعادة تشغيل نشر الرقعة 09-14) · `is-active` · `/login/` 200.
  **المخرَج** (`probe_after_snapshot_20260915.txt`، ssh rc=0، 08:27:54): **الإقلاع 2026-09-01 11:20:32** (up 13 يوماً) ⟵ لم يُعَد تشغيلُ الجهاز · `lettersys.service` active منذ **2026-09-14 09:28:36** و`lettersys-celery.service` منذ **09:28:45** (= إعادةُ تشغيل نشر الرقعة) ⟵ لم تُمَسّ الخدمتان · `/login/` **200** ⟵ **ادّعاءُ المساعد مُتحقَّقٌ لا مُصدَّق**. (وإقلاعُ 09-01 يعني أنّ تحديثاتِ النواة/libc المعلّقة — N12 — لم تُطبَّق؛ لا إقلاعَ قبل T‑0.)
- **2026-09-15 — ✅ D6.10 مختوم: اللقطةُ مكتملةٌ بنجاح** (ردُّ مساعد Hostinger على استعلامِ قراءةٍ فقط): الحالة **مكتملة** · بلا خطأٍ ولا تحذير · الانتهاء **2026-09-16 05:08:06 UTC** · الحجمُ غيرُ ظاهر · الخادم Running. (وقتُ «الاكتمال» المُبلَّغ = وقتُ الإنشاء 05:08:06 — طابعُ اللقطة لا زمنُ انتهاء بنائها؛ لا أثر.) ⟵ **شروطُ المالك قبل النافذة كلُّها مستوفاة** (اللقطة ✓ · الكاتباتُ متوقّفات ✓)؛ الباقي: نتيجةُ التدقيق وحكمُ فيبل.
- **2026-09-15 ~08:45 — فيبل غيرُ متاح** (استشارةُ «انطلق/توقّف» سقطت فوراً: `rate_limit 429 — Fable limit`) ⟵ **قرارُ المالك: «تابع بمراجعتك أنت»** (بلا رأيٍ مستقلّ؛ تُسجَّل مخالفةً مقصودةً لقاعدة الاستشارة بأمر المالك). **مراجعةُ المشرف وتعديلاتُ الدفتر المُدخَلة قبل البدء**: ① تواريخُ الدفتر ⟵ `TS=20260915` و`zz_rehearsal0910_20260915` · ② الخطوة 5: `F` يُؤخذ **حرفيّاً من مخرَج الخطوة 4** (الاسمُ بساعة UTC من `timezone.now()`) · ③ الخطوة 14: بوّابةُ **R5.10** — أعلى معرّفِ كتاب = **13386** قبل أيّ تنظيف · ④ الخطوة 17 **أوّلُ تنفيذٍ حقيقيٍّ لـ`purge --yes`** (البروفةُ سقطت بـ`ProtectedError` وبعد الإصلاح معاينةٌ فقط) — **لا تجريبَ مسبقاً على قاعدة البروفة** لأنّها نسخةُ الرجوع في الخطوة 12 ويجب ألّا تُمسّ؛ والقبولُ مشروطٌ بذرّيّة الأمر (يُتحقَّق من الكود قبل البدء) والتوقّفِ عند rc≠0 ورجوعِ 12 · ⑤ **N12 لا يحجب**: لا إقلاعَ داخل النافذة · ⑥ تدقيقُ Merge9 مستمرٌّ في الخلفية ويُراجَع **قبل الخطوة 25** (أوّلُ كتابٍ حقيقيّ = نقطةُ اللاعودة).
  **ذرّيّةُ purge مُتحقَّقةٌ بقراءة الكود**: `purge_dev_seed_books.py:79` `with transaction.atomic():` يضمّ حذفَ المرفقات (86) والكتب (87) معاً، وإعادةُ البذر (95–97) خارجه؛ ونصُّ النجاح الحقيقيّ (90–91) «حُذف N كتاباً تجريبياً من M مرشَّحاً (+ K صفَّ مرفقاتٍ وسجلاتٍ مرتبطة)» ⟵ صُحِّحت الخطوة 17 في الدفتر.
- **T‑0 · الخطوة 1 (08:45، `t0_01_20260915.txt`)**: لا مستمعَ على 8000 ✓ — لكن **4 عمليّات `python.exe`** لم تكن عند 07:53 ⟵ تُحدَّد قبل القفل.
  **حُدِّدت**: سلسلةٌ واحدة `D:/migration/kaggle_eseg/kaggle_eseg.py pull` (بدأت 08:45:38 من bash جلسةِ Claude أخرى — **`lettersys-project-7b`** التفاعليّة، تجربةُ E‑SEG بموافقة المالك) · **لا Django ولا PostgreSQL ولا `manage.py`** في السكربت (Grep) ⟵ القفلُ لا يمسّها. ⚠️ **جلستان تعملان على الجهاز** ⟵ رسالةٌ لها بألّا تمسّ git/`Merge9.md`/القاعدة/الخادم حتى نهاية T‑0 (قاعدةُ الجلسة الواحدة).
  **الخطوة 2 — قرارُ المالك: المشرفُ بصلاحيّة التطبيق.** الأمرُ المُسجَّل قبل تنفيذه (محلّيّاً، `manage.py shell` بحساب `lettersys_user`): `ALTER ROLE lettersys_user SET default_transaction_read_only = on;` ثمّ `SELECT pg_terminate_backend(pid) FROM pg_stat_activity WHERE datname='lettersys' AND usename='lettersys_user' AND pid<>pg_backend_pid();` · **الرجوع**: `ALTER ROLE lettersys_user RESET default_transaction_read_only;` · البوّابة (الخطوة 3، مرّتين بفاصل 5 د): `('on',)` · `(13239, 2026-08-30 07:38:39.73214+00, 12)`.
  **الخطوة 2 ✓ (08:47:39، `t0_02_03a_20260915.txt`)**: `current_user = lettersys_user` · `ALTER ROLE` نجح (لم يحجبه حارسُ الجلسة) · اتّصالاتٌ أخرى قبل الإنهاء **0** · `rolconfig = ['default_transaction_read_only=on']`.
  **الخطوة 3 — الفحصُ الأوّل ✓ (08:47:43، اتّصالٌ جديد)**: `('on',)` · `(13239, 2026-08-30 07:38:39.732140+00:00, 12)`. (مسبارُ الكتابة الأوّل سقط بصياغة الكونسول التفاعليّ لا بالقاعدة — أُعيد بـ`shell -c`.) الفحصُ الثاني بعد 08:52:43.
  **الخطوة 3 — الفحصُ الثاني ✓ (08:52:47، `t0_03b_20260915.txt`)**: `('on',)` · `(13239, 2026-08-30 07:38:39.732140+00:00, 12)` · اتّصالاتٌ أخرى **0** ⟵ **مطابقٌ للأوّل بعد خمس دقائق** ⟵ القاعدةُ المحلّيّة ثابتةٌ ومقفولة ⟵ الخطوة 4.
  **الخطوة 4 ✓ (08:53:12، `t0_04_20260915.txt`)**: `D:\trackbackup\pg_backup_20260915_0553.dump.enc` · **4,271,736 ب** (≥4 MB؛ البروفة 4,250,212 — الفرقُ ~21 KB = كتاباتُ القاعدة المحلّيّة بعد 09-10 كحجوزات 09-13 وحذفِ الحجزين) · rc=0 في 4 ث · الاسمُ بساعة **UTC** (0553) كما نبّه التدقيق.
  **الخطوة 5 ✓ (08:53:41، `t0_05_verify_local.txt`)**: `verify_backup --expect-live` **rc=0 «مطابق — لا اختلاف»** في 4 ث · sha256(enc) `bcf2cd9b7caa95e7…` · المفتاح **1e639b05** · sha256(plain) `0bc5e5adb3af9905…` (3,203,730 ب) · pg_restore 16 · `core_book` 13239 · تدريب 131 · محذوف 45 · `core_entity` 674 · `merged_into` 256 · null 418 · نشطة 353 · `core_attachment` 13187 (محذوف 14) · `core_bookhistory` 22313 · `auth_user` 12 نشطون 12 · ذاكرة 5395 · `bookemaillog` 1 (sent 1) · العدّادات 1/605/715/3917 · بريد `imap_sync_enabled=t is_active=t` وكلمتا المرور `enc::` بطول 125 · `django_session` 283 · رأسُ الهجرة **0077**. (أسماءُ الحسابات الاثني عشر في السجلّ؛ حساباتُ الفحص باقيةٌ نشطةً بقرار D6.2.)
  **الخطوة 6 ✓**: `pg_backup_20260915_0553.dump.enc.sha256` = `bcf2cd9b…9516` — **مطابقٌ لـsha256(enc) في مخرَج 5**.
  **الخطوات 7–10 تبدأ الآن على الخادم** (أوّلُ مسٍّ له في النافذة؛ اللقطةُ مكتملة ✓؛ الخدماتُ تعمل بلا انقطاع) **بأوامر الدفتر حرفيّاً** (`T0_WINDOW_2026-09-15.md` الأسطر 32–35، F=`pg_backup_20260915_0553`، TS=20260915): 7 نقلٌ + `sha256sum -c` · 8 `verify_backup --write-plain $IN/ship_20260915.dump` ومطابقةُ الجدول بمخرَج 5 · 9 `rsync --chmod=F640` ⟵ `ship640_20260915.dump` + `stat` + `pg_restore -l | wc -l` (~728) · 10 صلاحيّةُ `lettersys-deploy` وقائمةُ القواعد.
  **الخطوة 7 ✓ (08:54:41، `t0_07_20260915.txt`)**: `scp` rc=0 في 24 ث · على الخادم `pg_backup_20260915_0553.dump.enc: OK` (check_rc=0) · الحجم **4,271,736** مطابق.
  **الخطوة 10 ✓ (`t0_10_20260915.txt`)**: `lettersys-deploy` = **0** · القواعد: `lettersys` · `postgres` · `template0` · `template1` · `zz_empty0061_20260910` · `zz_stale_20260903_dropme` (القالبان لم يُذكرا في الدفتر — طبيعيّان).
  **الخطوة 8 ✓ (08:55:29، `t0_08_20260915.txt`)**: `verify_backup --write-plain $IN/ship_20260915.dump` على الخادم **rc=0 «مطابق — لا اختلاف»** في 8 ث · المفتاح **1e639b05** · sha256(enc) `bcf2cd9b…` وsha256(plain) `0bc5e5ad…` (3,203,730 ب) **مطابقان للمحلّيّ** · pg_restore `/usr/bin/pg_restore` · **قسمُ الجداول مطابقٌ حرفيّاً لمخرَج الخطوة 5** (`diff` ⟵ `TABLES_IDENTICAL`) ⟵ **إثباتُ المفتاح والنقل معاً**. (تحذيرُ `fitz` deprecated حميد.)
  **الخطوة 9 ✓ (08:56:09، `t0_09_20260915.txt`)**: `rsync --chmod=F640` rc=0 ⟵ `ship640_20260915.dump` **`www-data:postgres 640`** · **3,203,730 ب** · `pg_restore -l | wc -l` = **728** (= البروفة) · ملفّا البروفة القديمان باقيان بجانبه.
  **سلامةُ `Merge9.md`**: كلُّ تغييرٍ منذ `606af29` كتلةٌ واحدةٌ في نهاية الملفّ (`@@ -1768,0 +1769,21 @@`) ⟵ إلحاقاتُ المشرف وحدَها؛ تحذيرُ «عُدِّل على القرص» سببُه تهجئتا المسار `c:\`/`C:\` لا الجلسةُ الأخرى.
  **⟵ الإعدادُ (1–10) مكتملٌ بلا انقطاع. توقّفٌ مقصودٌ قبل الخطوة 11** حتى يؤكّد المالكُ جاهزيّتَه للخطوة 12 (DDL بيده) كي لا يطول الانقطاع.
  **مسبارُ الكتابة ✓ (08:48:16)**: `UPDATE core_booksequence … WHERE false` ⟵ **`InternalError: cannot execute UPDATE in a read-only transaction`** — القفلُ يحجب الكتابةَ فعلاً لا إعداداً فقط، بلا مسٍّ لأيّ صفّ. · رسالةٌ أُرسلت إلى الجلسة `lettersys-project-7b` بألّا تمسّ git/`Merge9.md`/القاعدة/الخادم حتى نهاية T‑0.
  **ردُّ الجلسة الأخرى**: التزمت (لا كتابةَ git ولا `Merge9.md` ولا القاعدة ولا الخادم) · كاغل E‑SEG انتهى ويُحذف ما عليه · تدقيقٌ قراءةً فقط بوكلاء (أقصى كتابةٍ `git merge-tree --write-tree` = كائناتٌ سائبةٌ بلا مرجع — مقبول). **ثلاثُ ملاحظاتٍ مقيسةٍ لما بعد القبول**: ① `main` (606af29) **لا يحمل الرقعة d03a314** ⟵ إعادةُ تمكين `lettersys-deploy` (`reset --hard origin/main`) تُعيد B‑1/D‑15/D‑1 ⟵ **الدمجُ في main شرطُ P2–P5** · ② الإيداعُ `50f3ad1` (وثائقُ Merge9 +35 سطراً من جلسةٍ أونلاين) على `origin/claude/phase-0-unification-section-6-rw6x7x` غيرُ مدموجٍ في main · ③ PR #16 (`6613090`، `7940ab0`) على قاعدةٍ قديمة وغيرُ مكافئٍ لأيّ إيداعٍ في السلسلة.
