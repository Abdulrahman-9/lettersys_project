# سجلُّ التنفيذ — 2026-09-17

- 2026-09-17 — **فصلُ Merge9 إلى ثلاثة** (جلسةٌ سحابيّة، فرع `docs/split-merge9` فوق `main@1c65a75`، بقرار المالك): `Merge9.md` **خُتم أرشيفاً** ببانرٍ في رأسه (1,793 سطراً · 34 ألف كلمة · 116 عنواناً — كان خطّةً وسجلّاً وقواعدَ في ملفٍّ واحد يتعارض عند كلّ إعادة قاعدة). **`docs/STATE.md`**: لوحةُ الحال في صفحة — أين نحن (مقيسٌ من المستودع)، التالي بالترتيب، **سجلُّ القرارات الموحّد** (12 قراراً محسوماً بمَن ومتى وأين يُنفَّذ + 7 معلَّقة P1–P7)، والديون. **`docs/RULES.md`**: §0 و7.6 و7.7 و15.6 **حرفيّاً** + 8 قواعد من جلسات 10–17 أيلول (أهمُّها: لا عمليّةَ إنتاجٍ لا رجعةَ فيها من محادثة وكيل). **`docs/log/`**: السطورُ المؤرَّخة 09‑08…09‑10 من سجلّ Merge9، و09‑13/14 من §17، و09‑15 من `START_HERE` — ملفٌّ لكلّ يوم، و`.gitattributes` يجعل دمجَها اتّحاديّاً (`merge=union`) فلا تعارضَ على الإلحاق. `docs/00_START_HERE.md` صار مؤشّراً من خمسة أسطر. **لا كودَ ولا هجرة**؛ الحزمةُ لم تُمَسّ.

- 2026-09-17 — **استشارةُ فيبل على نافذة T‑0، وثلاثُ وقائعَ نقضت لوحةَ الحال** (جلسةٌ محلّيّة):
  ① **لقطةُ Hostinger انتهت 09‑16 05:08:06 UTC** لا «غداً 08:08» — «08:08» وقتُ الإنشاء ببغداد (`Merge9.md:1770`).
  ⟵ **كنّا بلا شبكةِ أمانٍ قصوى منذ أمس**؛ لقطةٌ جديدةٌ شرطُ بدء النافذة.
  ② **القفلُ المحلّيُّ كان ساقطاً**: `rolconfig=['default_transaction_read_only=off']` — **ضُبط صراحةً على `off`،
  لا `RESET`**. البياناتُ نجت (`verify_backup --expect-live` rc=0 «مطابق» · 13239 · `max_id` 13386 · آخرُ إنشاء
  2026‑08‑30) ⟵ أُعيد القفلُ وتُحقِّق باتّصالٍ جديد (`RO=on` · مسبارُ الكتابة `InternalError`). **الدرس**: القفلُ
  نُفِّذ بـ`ALTER ROLE` لا `ALTER DATABASE` (مالكُ القاعدة `postgres`) فدورُ `postgres` يتخطّاه — **الإثباتُ هو
  `--expect-live` لا عدُّ الصفوف**.
  ③ الفحصان المنسوبان إلى «اليوم» (دلتا الوسائط 0 · `check-ignore`) مؤرَّخان **09‑15 09:47/09:50**.
- 2026-09-17 — **نافذةُ T‑0 صارت سكربتاتٍ بنقاط تفتيش** (قاعدةُ «لا عمليّةَ إنتاجٍ لا رجعةَ فيها من محادثة وكيل»):
  `D:/migration/t0/` ⟵ `t0_lib.sh` (المحرّك + حارسُ الإقلاع) · `t0_precheck.sh` (P1…P6 + R1/R2) · `t0_cutover.sh`
  (11→23، `--status`/`--dry-run`/`--until`/`--from`/`--manual-12`) · `t0_rollback.sh` · `remote/{p3_drift,p4_sudo,s11_stop}.sh`
  · `e2e_probe.py` (الخطوة 23 — كانت **بوّابةً بلا أمر**) · `step24_checklist.md`. الحالةُ في
  `D:/migration/state/t0_<RUN_TS>/` ملفّاً لكلّ خطوة + `journal.txt`. **المحرّك** يفحص `post_N` قبل التنفيذ فيتخطّى
  المنجَز، و`N.started` بلا `N.done` ⟵ توقّفٌ يدويٌّ لا إعادةٌ تلقائيّة (يمنع تكرارَ 17 `purge_dev_seed_books --yes`).
  **متغيّران لا واحد**: `DUMP_TS=20260915` (اسمُ الدمب على الخادم) و`RUN_TS` (السجلّات والحالة).
  **حارسُ الإقلاع مُطفَّرٌ خمساً — كلُّها حمراء** (`lettersys-deploy` · `migrate` عارٍ · `sqlmigrate` · مسٌّ بـdeploy
  داخل الملفّ المُستثنى · `dumpdata`) والنظيفُ أخضر. وطفرةٌ سادسةٌ كشفت **حارساً زائفاً**: نمطُ `migrate` كُتب
  `(^|[^-])migrate` بلا `\b` فلم يُمسك شيئاً — أُعيد بناؤه بلا هروبٍ هشّ.
  **فحصُ ما قبل النافذة: NO‑GO** — أخضر P2 (`--expect-live`) · P2ب (القفل · 8000 · العمليّات) · P6 (شجرةٌ نظيفة
  @`1c65a75`)؛ أحمر **P1** (اللقطة) و**P6** (رسالةُ الكاتبات المؤرّخة) — كلاهما بيد المالك. P3/P4/P5 تحتاج ssh.
- 2026-09-17 — **تصحيحُ `docs/RULES.md` §0**: «لا اتّصالَ بالخادم إطلاقاً» نصٌّ **بائد** نُقل حرفيّاً من Merge9 §0
  (08‑08) وأُغفل نقضُه بقرار المالك **D6.16 (09‑10)**؛ وسجلّاتُ 09‑15 (`t0_07…t0_10`) تحمل `ssh rc=0` من الجلسة،
  ودفترُ T‑0 يُسند 11–23 إليها. الصياغةُ الجديدة تُجيز [ن] بحساب `lettersys-admin` وحدَه بقائمة N8، وتُبقي بيد
  المالك: DDL · `sudoers` · `.env` · nginx · `lettersys-deploy`.
- 2026-09-17 — **تصحيحُ `docs/STATE.md`**: `fix/extraction-stale-guard` **فرعٌ معدوم** (لا محلّيّاً ولا على
  `origin` — `ls-remote`) وكان البندَ 3 في «التالي»؛ بديلُه المقيس `64cecf3` (كاشُ `EntityResolver` لا يشفى)
  **ليس مستقلّاً** بل داخل أربعة فروع ⟵ `cherry-pick` فرعاً `fix/entity-cache-heal` **بعد القبول** (حكمُ فيبل).
  و`purge_dev_seed_books --yes` كان مذكوراً بعد T‑0 وهو **الخطوة 17 داخل النافذة**. ووُحِّد مؤشّرا البدء.
- 2026-09-17 — **لقطةُ VPS الجديدة مكتملة** (مساعد Hostinger): الإنشاءُ والاكتمالُ **05:54:36 UTC (08:54:36 بغداد)**،
  فالغطاءُ سارٍ حتّى **2026-09-18 05:54:36 UTC**. ⟵ **P1 أخضر**؛ الفجوةُ التي بلا شبكةٍ (09‑16 05:08 ⟵ 09‑17 05:54)
  أُغلقت. الوسمُ على الخادم لم يتغيّر (`cutover-2026-09c`).
- 2026-09-17 08:56 — **فحصُ ما قبل النافذة، الشقُّ المحلّيُّ كلُّه أخضر**: P1 اللقطة ✓ · P2 `verify_backup
  --expect-live` rc=0 «مطابق — لا اختلاف» ✓ · P2ب `RO=on` ومسبارُ الكتابة محجوب ولا مستمعَ على 8000 ولا
  python/celery ولا اتّصالاتٍ أخرى ✓ · P6 شجرةُ `main` نظيفة @`1c65a75` ✓. **الأحمرُ الوحيد: رسالةُ الكاتبات
  المؤرّخةُ اليوم** (بيد المالك). **P3/P4/P5 لم تُشغَّل** — تحتاج ssh، وقد حجبه مصنّفُ أذونات الجلسة
  (`Production Reads`) فيلزم إذنٌ صريحٌ في `.claude/settings.json`؛ ومثلُه حجب `git push`.
  السجلّات: `D:/migration/logs/t0_precheck_20260917_*.txt`.

- 2026-09-17 09:15 — **الفحصُ الكامل (بشقّه البعيد) — 27 أخضر · أحمرُ واحد: رسالةُ الكاتبات**. الخادمُ مقيسٌ لا مفترَض:
  `d03a314` · صفرُ تعديلٍ متتبَّع (غيرُ المتتبَّع `.env.bak.1788671758` وحدَه، معروفٌ منذ 09‑14) · `active active` ·
  إقلاعُ 09‑01 · `lettersys-deploy`=0 · **كتب 13,241** (⟵ **النافذةُ قبل الخطوة 11 قطعاً**؛ البندُ 1 في STATE مُجاب) ·
  `max_id` 13,388 · الدمبُ بعد يومين سليم (3,203,730 ب · فهرس 728 · `www-data:postgres 640`) · مفتاح `1e639b05` ·
  قرص 11G · ذاكرة 1.8G · طابورُ celery 0 · Redis db1 = 3 مفاتيح (جلساتٌ حيّة على موقع البروفة؛ الخطوة 21 تمسحها).
  **`sudo -n -l`** مطابقٌ لمنحة N8 (الأرشيف §14.2) ويغطّي ما تحتاجه الخطوات 11–23.
- 2026-09-17 — **ستّةُ أعطابٍ في سكربتات T‑0 كُشفت بمقابلتها بالمقيس — كلٌّ منها كان سيكسر النافذةَ أثناءها**:
  ① `bookline` افترض أنّ سطرَ `probe_s0` يبدأ بالعدد، وهو يُطبع `'%-40s %s'` فيبدأ **بالوسم العربيّ** ⟵ الشرطُ البعديُّ
  للخطوة 13 ما كان ليصدق أبداً: **استعادةٌ ناجحةٌ تُعلَّم فاشلةً ويُطلب الرجوع**. ② الخطوة 23 نسخت المسبارَ بـ`cp`/`rm`
  كـwww-data — **غيرُ مُجازَين في sudoers** ⟵ صار base64 داخل `shell -c` (لا ملفّ، لا stdin). ③ استطلاعُ `--manual-12`
  فتح جلسةً على `lettersys` كلَّ 10 ث — **جلسةٌ لحظةَ RENAME تُفشله** ⟵ لا تُمَسّ `lettersys` قبل ثبوت قاعدة البروفة.
  ④ `ssh` بلا `-n` يقرأ stdin ⟵ **يبتلع رمزَ `PURGE-131`** إن كُتب والمعاينةُ تعمل. ⑤ الرجوعُ أبقى `11.done` ⟵ الإعادةُ
  تقفز الإيقافَ فتُجري RENAME **والخدماتُ حيّة**؛ وأبقى السجلّات ⟵ **سجلٌّ قديمٌ أخضر يُصدِّق الاستعادةَ الجديدة دون
  فحصها**. ⑥ المسبارُ اختبر مرفقاً سرّيّاً **خارج** قسم sarah — بوّابةٌ لم تُقَس؛ المقيسُ 09‑14 سرّيّةٌ **داخل** القسم
  (sarah قسم 1 · 11074 قسم 1). وإلغاءُ المالك عند الرمز صار rc=99 نظيفاً (كان يترك `.started` فيحجب الإعادة).
  **التحقّق**: `bookline` على صيغة الطباعة الحقيقيّة ⟵ `13239 | 0 | 131` و`13108 | 0 | 0` · محلِّلُ الدخان يحمرّ على
  `502` وعلى traceback=2 · base64 ذهاباً وإياباً بـsha مطابق · **المسبارُ بالنقل نفسه على القاعدة المحلّيّة المقفولة
  يستنسخ 09‑14 حرفيّاً** (`att=11074 · sarah=404 · superuser=200 · PASS`) **ويحمرّ** حين تُطفَّر البوّابةُ الأمنيّة
  (`sarah=200 · FAIL`) · مسارُ الإلغاء: 99 ⟵ إعادةٌ بالرمز ⟵ تنفيذٌ مرّةً واحدة · الرجوعُ الجافّ يُثبت سباكةَ ssh/psql.
- 2026-09-17 09:2x — **قرارُ المالك (نصّاً): «سجّل خروجهنّ أنت، لا أريد إخبارهنّ وغيرُ ضروريٍّ إخبارهنّ»** ⟵ يحلّ محلَّ
  حاجز P6 (رسالةُ الكاتبات). **مسجَّلٌ قبل التنفيذ — كتابةٌ على الخادم**: حذفُ مفاتيح جلسات Django من Redis db1
  حصراً (`--scan --pattern '*django.contrib.sessions*'` ⟵ `DEL`)، لا `FLUSHDB` (يُبقي `cache_version_book/entity`).
  **المقيسُ قبله**: `SESSION_ENGINE=backends.cache` (لا `cached_db` ⟵ Redis وحدَه مخزنُ الجلسات) · `CACHE_DB=1` ·
  db1 = 3 مفاتيح منها **جلسةٌ حيّةٌ واحدة**. **حدُّه**: الخروجُ لا يمنع الدخولَ ثانيةً؛ الخروجُ الدائمُ هو الخطوة 11
  (إيقافُ الخدمة) و21. ما يُدخَل على موقع البروفة قبلها يُمحى عند 12–13 — خطرٌ قبله المالكُ بقرار 09‑13.
  **المخرَج** (`D:/migration/logs/t0_logout_sessions_20260917.txt`): قبل `جلسات=1 · db1=3` ⟵ `DEL → 1` ⟵ بعد
  `جلسات=0 · db1=2` · الموقعُ حيّ (`/login/` 200). ⟵ `P6_writers.ok` يحمل قرارَ المالك بدلَ الرسالة.
- 2026-09-17 09:25 — **قراران للمالك قبل النافذة (نصّاً: «عطّل الحسابات وسأحوّل إلى الوضع اليدويّ لتبدأ»)**:
  ① **D6.2 محسوم: تعطيلٌ** — مقيسٌ من الدمب (12 نشطاً) ⟵ **خمسةٌ قاطعة** تُعطَّل في الخطوة 18‑ب:
  خمسةُ حساباتِ اختبار (ثلاثةٌ منها superuser، أحدُها لم يدخل قطّ) تُعطَّل في 18‑ب؛ خمسةٌ حقيقيّةٌ لا تُمَسّ؛ حسابان
  superuser ملتبسان بقرارٍ لاحق. `save(update_fields)` لكلّ صفّ (لا إشارةَ `save` على المستخدم —
  مقيس). ⟵ النشطون بعدها 7.
  ② **الوكيلُ يُطلق النافذة والمالكُ يعتمد كلَّ أمرٍ في الوضع اليدويّ** — يُعدِّل عمليّاً «سكربتٌ يُطلقه المالك» (RULES §0):
  يدُ المالك على كلّ خطوةٍ باعتماد الأمر، ورمزا 12/17 يُمرَّران في الأمر المعتمَد نفسه (`T0_CONFIRM_12`/`T0_CONFIRM_17`)
  لأنّ `read` التفاعليّ يقرأ EOF من أداة الوكيل فيُلغي. التنفيذُ خطوةً فخطوة (`--until N`) كي لا يقطع موتُ الجلسة خطوةً
  طويلة؛ وحارسُ `.started` يلتقط أيَّ انقطاع.
- 2026-09-17 09:25→09:30 — **نافذةُ T‑0: الخطوات 11→23 خضراءُ كلُّها** (أطلقها ن، واعتمد المالكُ كلَّ أمرٍ يدويّاً؛ الحالةُ
  في `D:/migration/state/t0_20260917/`، السجلّاتُ `D:/migration/logs/t0_1[1-9]_20260917.txt` و`t0_2[0-3]_…`).
  **11** 09:25:13 `inactive inactive` · صفرُ اتّصالات — **بدءُ الانقطاع**. **12** 09:25:56 `ALTER DATABASE` · `CREATE DATABASE`
  (`zz_rehearsal0910_20260917` محفوظة). **13** `restore_rc=0` في **2.435 ث** · `13239 | 0 | 131`. **14** مطابقٌ للأساس حرفيّاً:
  جهات `674 | 722` · `256 | 353` · محذوف 45 · عدّادات `605/3917/1/715` · ذاكرة 5395 · `sent=1` · بريد `True | 125 | True | True` ·
  `ins=13239 dead=0` · `MAXID=13386` · `0077` (الجلساتُ 283 لا 282 — استعمالٌ محلّيٌّ بين 09‑10 و09‑15؛ مُحيت في 18).
  **15** `migrate --check=0` · `[X]=86` · النماذجُ «سليم» · `no issues` · لا `Applying` (تحذيرُ `TESSERACT_TESSDATA_DIR` قائمٌ من قبل).
  **16** `UPDATE 1` ×2. **17** المعاينةُ 131 (5·103·2·21) · مرفقات 129 · ذاكرةٌ تُفصَل 14 ⟵ `PURGE-131` ⟵ «حُذف 131 من 131
  (+132 صفّاً)» · الذاكرةُ باقية 5395 · **العدّاداتُ لم تنزل** · `purge_rc=0` · `13108 | 0 | 0`. **18** `TRUNCATE` · **5 حساباتٍ
  عُطِّلت** ⟵ النشطون 7. **19** `13108 | 0 | 0` · محذوف 13 · مرفقات 13058 · جلسات 0 · بريد `False | False`. **20 (حاجزٌ صلب)**
  `PWLEN ✓` · `KEYCOUNT=1` · `KEYSHA=1e639b05`. **21** Redis db1=0 · celery 0. **22** 09:29:46 `active active` ·
  `/login/` 200 · traceback 0 · زمنُ الدخول 0.73 ث · `lettersys` 49 MB · قرص 11G حرّ — **نهايةُ الانقطاع (~4.5 د)**.
  **23** `att=11074 · sarah=404 · superuser=200 · SCOPE_GATE=PASS`.
  **الباقي**: 24 بالعين (`D:/migration/t0/step24_checklist.md`) ⟵ 25 القبول. ورسالةٌ من جلسةٍ أخرى (`fix/text-layer-and-markers`
  في `D:/wt/tlm`) أُجيبت: تمضي، ولا تمسّ الخادمَ ولا `main` ولا `D:/migration/{t0,state,logs}` حتّى القبول.
- 2026-09-17 — **قرارُ المالك: تعطيلُ الحسابين الملتبسين** (بعد عرض المتبقّي). **مسجَّلٌ قبل التنفيذ — كتابةٌ على الإنتاج**:
  `remote/deactivate_*.py` (محلّيّ) بـbase64 داخل `shell -c` · `save(update_fields)` لكلّ صفّ · حارسٌ يرفض إن لم يبقَ مديرٌ نشط.
  المتوقَّع: `DEACTIVATED=2` ⟵ النشطون 5 · المديرون 3.
  **المخرَج** (سجلٌّ محلّيٌّ في `D:/migration/logs/`): `DEACTIVATED=2` · النشطون **5** · المديرون 3.
- 2026-09-17 — **الفرعُ أُعيدت كتابتُه قبل دفعه الأوّل** (حكمُ فيبل: التعميةُ في التاريخ لا فوقه — المستودعُ عامّ): إيداعاتُ
  09‑17 السبعة طُويت في إيداعٍ واحدٍ فوق `c326080` مع تعميةِ جردِ الحسابات وأعلامِها · خريطةِ `sudo` · طولِ كلمتَي البريد ·
  تفاصيلِ حساب الخادم في RULES §0؛ وتجريدِ الزمنيّات من RULES؛ ووسمِ سطر الجلسة السحابيّة في 09‑15 «غيرُ مُتحقَّق» **لا
  حذفه**؛ و`text eol=lf` مع `merge=union`؛ واستبدالِ STATE بحال ما بعد النافذة. النسخةُ السابقة محفوظةٌ محلّيّاً
  (`backup/split-merge9-pre-redact`، لا تُدفع).

- 2026-09-17 — **استردادُ عمل جلسة 09‑14 الأونلاين (O2–O4)** — بحكم فيبل وأمر المالك «ادفع». المصدر: `files.zip` في التنزيلات
  (09‑14 08:19، **لم يُفتح ثلاثةَ أيّام**)، نُسخ إلى `D:/migration/backups/git_bundles_20260914/` قبل أيّ شيء. `bundle verify` سليم ⟵
  مراجعُ `refs/recovered/*` ⟵ تحقّق: `16ddfc4` فوق `0601076` بـ**7 إيداعات** (fast‑forward) · **صفرُ هجرات** · 24 ملفّاً +767/−67 ·
  التسليمُ مطابقٌ بالبصمة لنسخة التنزيلات · فحصُ الأسرار: كلماتُ مرورٍ وهميّةٌ لمستخدمي الاختبار فقط.
  **الدفع**: #18 `0601076..16ddfc4` · #19 `f3b6f05...3a1a55d` بـ`--force-with-lease` (القديمُ محفوظٌ محلّيّاً في فرعٍ احتياطيّ وفي حزمة
  09‑13) · `fix/text-layer-and-markers` `54f87d1` (نصوصُ اختباراته بصيغة المراسلات لها سوابقُ في اختبارات `main` المنشورة —
  18 «تحية طيبة» · 5 «التراخيص» — عامّةٌ بلا أسماءٍ ولا أرقام). وأُبلغ التبويبُ الثاني بالحال: **T‑0 لم تنتهِ** (24/25 معلَّقتان).
  **الباقي من العمل الأونلاين: O1 وحدَه** (الحارسُ الشارد، جلسةُ 09‑15).
