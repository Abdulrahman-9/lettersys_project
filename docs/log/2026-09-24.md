# سجلُّ التنفيذ — 2026-09-24

- 2026-09-24 — **قرارُ المالك (نصّاً)**: «استثنِ الرفعَ على الموقع الآن، يهمّني تضبيطُ العمل على الجهاز محلّيّاً، وسأطلقه
  على شبكة لأنّها داخليّة، وهذه الحاسبة ستُعتبر السيرفر». **المرجعُ الحاكم**: حكمُ فيبل (خارج المستودع،
  `D:/migration/fable_local_server_20260924.md`) — 21 خطوةً في خمسة أقسام؛ ورأيٌ ثانٍ مستقلّ محفوظٌ بجانبه
  (`…_second_opinion.md`، §0 فيه فروقٌ تحتاج قرارَ المالك قبل خطوتي الحكم 7–8: نقلُ pagefile إلى D: يعارض سابقةَ تجمّد
  2026‑05، ووكيلُ المسح أحاديُّ الجهاز في الكود الحاليّ، والتطبيقُ يكتب في `.env` وقتَ التشغيل). **أمرُ المالك اليوم**:
  التنفيذُ بالترتيب — تجميدُ VPS ⟵ تجهيزُ الجهاز ⟵ البيانات ⟵ الخدمةُ والقبول. **الخطوتان 24/25 تنتقلان إلى الجهاز
  المحلّيّ** (على VPS ميّتتان). **لا دمجَ** قبل قبولهما محلّيّاً + يومَي استقرار، ثمّ ترتيبُ الحكم (الحارسُ الشارد أوّلاً).
  التنفيذُ بجلستَي وكيل: القسمُ أ (VPS) والوثائق هنا؛ ب فما بعده (الجهاز) في الجلسة الأخرى وحالتُها `D:/migration/state/lan_*`.

- 2026-09-24 10:50→11:10 — **البياناتُ المحلّيّة (خطواتُ الحكم 9–14) نُفِّذت قبل تجميد VPS** (الجلسةُ الأخرى، بموافقة المالك؛
  **الترتيبُ انعكس** عن الحكم): G0 `verify_backup --expect-live` rc=0 مطابق · رُفع قفلُ القراءة (`RESET`) · البريدُ والنسخُ مُطفآن (0/0) ·
  `purge_dev_seed_books --yes` ⟵ **13,108 · تدريب 0 · `max_id` 13,386** · العدّاداتُ 3917/605/715 لم تنزل · الذاكرةُ 5,395 باقية ·
  `TRUNCATE django_session` · حساباتُ الاختبار الخمسة معطَّلة · نسخةُ أساسٍ مشفّرةٌ تُحفظ للأبد (`verify` rc=0 · المفتاح `1e639b05`).
  ⟵ **القاعدةُ المحلّيّة صارت الحقيقة**، والكاتباتُ بدأن العملَ عليها (حجزٌ نشطٌ للرقم 3917 — كان مقرَّراً كتابَ القبول).
  ⟵ لذلك تحمل بوّابةُ F1 أدناه **إعادةَ قياسِ أيّ كتابةٍ على VPS بعد T‑0** لحظةَ الإيقاف.

- 2026-09-24 11:17 — **ثغرةٌ أمنيّةٌ حيّةٌ على الشبكة الداخليّة — أُغلقت بقرار المالك**: الخادمُ المؤقّت الذي تعمل عليه الكاتبات
  (`runserver` على كلّ الواجهات) كان على `.env` التطويريّ (`DEBUG=True`) ⟵ مسارُ الدخول التجريبيّ (`core/views/dev_login.py`)
  **حيٌّ**: POST واحد يُنشئ حسابَ مديرٍ بلا كلمة سرٍّ ويُدخل صاحبَه. **مقيسٌ قبله**: GET ⟵ 200 · الحسابُ التجريبيّ **لم يُنشأ قطّ**.
  **الإصلاح**: الخادمُ نفسُه أُعيد تشغيلُه منفصلاً بمتغيّرات بيئةٍ تغلب `.env` (`DEBUG=False` + تجاوزاتُ الكوكيّ/SSL لـhttp +
  بريدٌ console + `--insecure` للساكن + `--noreload`). **البوّابة**: المسارُ **200 ⟵ 404** (POST ⟵ 403) · الدخولُ 200 محلّيّاً وعلى
  عنوان الشبكة · الساكنُ 200 · `/media/` بلا جلسة ⟵ 302 إلى الدخول · الجلساتُ باقية (مخزَّنةٌ في القاعدة). مؤقّتٌ حتّى خدمة
  waitress (الخطوة 15). **الدرس**: `.env` التطويريّ لا يُشغَّل على واجهةٍ يصلها أحد — «منجَز» الخادمِ يشمل `/dev-login/` = 404.

- 2026-09-24 11:21 — **الحسابان المديران الملتبسان عُطِّلا محلّيّاً** (قرارُ المالك 09‑17 على الإنتاج، أُعيد تأكيدُه اليوم؛ الجلسةُ
  الأخرى عطّلت خمسةً لا سبعة): السكربتُ نفسُه الذي جرى على الإنتاج ⟵ `DEACTIVATED=2 · ACTIVE_TOTAL=5 · 3 مديرين` — **مطابقٌ
  حرفيّاً لسجلّ الإنتاج 09‑17**. الرجوعُ سطر (`is_active=True`).

- 2026-09-24 — **مسجَّلٌ قبل التنفيذ — كتابةٌ على الخادم (القسمُ أ، تجميدُ VPS بثلاث طبقات)**، بسكربتٍ بنقاط تفتيش
  (`D:/migration/t0/vps_freeze.sh` على محرّك t0؛ الرجوع `vps_freeze_rollback.sh` بترتيب R2⟵R1⟵R3؛ الأصولُ محفوظةٌ ومطابقةٌ
  للخادم بالبصمة). **فحصٌ قبليٌّ قراءةً (10:55)**: لا أحدَ في منتصف إدخال — منذ T‑0: 0 كتبٍ منشأة · 0 معدَّلة · 0 مرفقات · 0 دخول
  (آخرُ دخولٍ 09‑13) · جلساتُ Redis 0 · طابورُ celery 0. **مراجعةٌ عدائيّة** (8 وكلاء، قراءةً): 14 عيباً مؤكَّداً أُصلحت كلُّها قبل
  العرض على المالك — أهمُّها أنّ بوّابةَ الموقع تُقاس **على الخادم** بـSNI وشهادةٍ حقيقيّة (شبكةُ المكتب تعترض TLS: curl من هنا
  ⟵ `SEC_E_UNTRUSTED_ROOT`)، ونوعُ محتوى صفحة 503 مفروضٌ `text/html` على كلّ مسار، وحدُّ الرفع 50M باقٍ.
  - **F1** [المالك كـroot — `disable` ليس في منحة الأتمتة]: `systemctl disable --now` للخدمتين. البوّابة: `disabled ×2 · inactive ×2 ·
    اتّصالات 0 · كتاباتُ VPS بعد T‑0 = 0 0 0 0 · max_id 13386`. الرجوع: `enable --now`.
  - **F2** [الوكيل، كلُّ أمرٍ باعتماد المالك]: `ALTER ROLE <دور التطبيق> SET default_transaction_read_only = on`؛ مسبارٌ قبله يُثبت
  أنّ القاعدةَ كاتبة. البوّابة: الإعداداتُ الثلاثة في `rolconfig` + مسبارٌ باتّصالٍ جديد `RO=on WRITE=InternalError`. الرجوع:
  `RESET` **للمعامل وحدَه** (الدورُ يحمل إعدادين آخرين).
  - **F3** [الوكيل، كلُّ أمرٍ باعتماد المالك]: موقعُ nginx ⟵ 503 ثابتةٌ على كلّ مسارٍ بصفحة «انتقل النظام إلى الشبكة الداخلية»؛
  كتلُ TLS والتحويلُ وموقعُ ACME كما هي. البوّابة: `nginx -t` ثمّ reload ثمّ من الخادم `login=503 text/html · العلامة · media=503 ·
  www=301`. الرجوع: الأصلُ بالبصمة ⟵ `nginx -t` ⟵ reload.
  - **لا مسحَ لـVPS** قبل مرآةٍ خارجيّةٍ للمرفقات (VPS اليومَ النسخةُ الثانيةُ الوحيدة لـ13,932 مرفقاً).

- 2026-09-24 11:30 — **تصحيحُ إسنادٍ (إلحاقاً لا كتابةً فوق)**: البندان أعلاه نسبا خطواتِ البيانات 9–14 وتعطيلَ «الخمسة» إلى
  «الجلسة الأخرى» (جلسةِ القسم ب) — **خطأ**: تلك الجلسةُ لم تكتب تحت `D:/migration/state` ولم تُشغّل خطوةَ بياناتٍ، وعلمت برفع القفل
  وبالمحو من رسالتي. الكاتبُ جلسةٌ **ثالثة** (أو المالكُ مباشرةً) — ملفُّ حالتها يقول «تنفيذٌ يدويٌّ من جلسة وكيل بموافقة المالك».
  أُرسلت إليها رسالةُ تنسيق. **الدرس**: لا يُنسب أثرٌ إلى جلسةٍ من مكان ملفّه؛ يُسأل الكاتبُ أو يُقرأ رأسُ الملفّ.
  **قراراتُ المالك المنقولة عبر جلسة القسم ب** (لم تُكتب هنا بيده): (1) F1 بيده كـroot لا البديلُ داخل المنحة · (2) **لا شيءَ
  ينتقل إلى D** — D قرصٌ ميكانيكيّ وC هو SSD ⟵ pagefile وبياناتُ PG تبقيان على C، ويُحرَّر C بقائمةٍ تُعرض عليه قبل أيّ حذف؛
  بوّابةُ «C ≥ 20 GB» والخطوة 7 من الحكم **سقطتا كما كُتبتا** · (3) كتاباتُ `.env` وقتَ التشغيل تُنقل إلى القاعدة **بإصلاح كود**
  (لا بصلاحيّات ملفّ) · (4) المسحُ من أجهزة الكاتبات يُصلَح **بعد** الانطلاق؛ عند الإطلاق NAPS2 ⟵ PDF ⟵ رفع · (5)
  `EMAIL_BACKEND=console` في `.env` الإنتاجيّ.

- 2026-09-24 11:35 — **تصحيحٌ ثانٍ (إلحاقاً)**: (1) **المالكُ: «لا أحدَ يعمل على السيرفر»** ⟵ عبارتا «والكاتباتُ بدأن العملَ عليها»
  و«الخادمُ المؤقّت الذي تعمل عليه الكاتبات» أعلاه **تأويلٌ خاطئ** من جلسةٍ وحجزٍ بحساب إحداهنّ؛ الحقيقةُ المقيسة وحدَها:
  **الحجزُ 415 يمسك الرقمَ 3917 ⟵ عدّادُ الوارد الداخليّ الآن 3918** (لا كتابَ محفوظ: 13,108). (2) **إسنادُ خطوات البيانات 9–14
  وجزءِ الخطوة 5** ثبت من كاتبها نفسِه: الجلسةُ الثالثة — ودليلُها `D:/migration/state/lan_20260924/journal.txt`؛ وجزءُ 5:
  `powercfg` النومُ والسباتُ على التيّار **0** (كانا 60 دقيقة)، وBIOS وساعاتُ النشاط لم تُمسّ. (3) **فخٌّ مقيس**: `prune_old_backups`
  يحذف `*.dump.enc` الأقدمَ من مدّة الاحتفاظ **في جذر مجلّد النسخ وحدَه** (نمطٌ غيرُ تكراريّ) ⟵ النسخُ الثلاث الدائمة (09‑15 ·
  ما قبل الانطلاق · الأساس) في `D:/trackbackup/keep/` كلٌّ بـ`.sha256`.

- 2026-09-24 11:46 — **قرارُ المالك (نصّاً): «نفذ البديل»** — يحلّ محلّ «F1 بيده كـroot». استطلاعُ النمط الحرفيّ (11:23→11:45) لم يلتقط
  تغييراً فأُوقف؛ **صفرُ أثر** مقيس (الخدمتان `enabled/active` · `rolconfig` والموقعُ كما هما · كتاباتُ VPS بعد T‑0 = `0 0 0 0 13386`).
  **مسجَّلٌ قبل التنفيذ — كتابةٌ على الخادم (F1، نمطُ المنحة، الوضعُ اليدويّ بموافقة المالك)**: `systemctl stop` للخدمتين ⟵ `override.conf`
  لكلٍّ منهما = الأصلُ المثبَّت بالبصمة + بندُ `[Unit] ConditionPathExists=` لملفٍّ لا يوجد ولا يُنشأ (يمنع البدءَ عند الإقلاع **وباليد**) ⟵
  تحقّقُ البصمة على الخادم قبل `daemon-reload` ⟵ `daemon-reload` ⟵ برهان: محاولةُ `start` يجب أن تبقى `inactive` (وإلّا إيقافٌ فوريّ).
  **البوّابة**: `inactive ×2 · ConditionResult=no ×2 · overrides=المجمَّدة بالبصمة · اتّصالات 0 · كتاباتُ VPS بعد T‑0 = 0 0 0 0 · max_id 13386`.
  `is-enabled` يبقى `enabled` بالتصميم. **الرجوع**: الأصلان بالبصمة ⟵ `daemon-reload` ⟵ `start` (R1 في `vps_freeze_rollback.sh`).

- 2026-09-24 11:46→11:52 — **تجميدُ VPS: الطبقاتُ الثلاث خضراء** (الوضعُ اليدويّ، كلُّ أمرٍ باعتماد المالك؛ السجلّات `D:/migration/logs/t0_F{1,2,3}_20260924.txt`
  و`vps_freeze_final_verify_20260924.txt`):
  **F1** (نمطُ المنحة) `CONNS 0⟵0` · برهانُ الحاجز: `start` ⟵ `enabled enabled inactive inactive · ConditionResult=no no` · البوّابة:
  `inactive ×2 · ConditionResult=no ×2 · overrides=frozen (بالبصمة) · conns=0 · كتاباتُ VPS بعد T‑0 = 0 · max_id 13386`.
  **F2** مسبارٌ قبله `RO=off WRITE=ok` ⟵ `ALTER ROLE` ⟵ `rolconfig = client_encoding=utf8,TimeZone=Asia/Baghdad,default_transaction_read_only=on` ·
  اتّصالٌ جديد `RO=on WRITE=InternalError`.
  **F3** بصمةُ الملفّ على الخادم = المحلّيّة (`e86346cf…`) ⟵ `nginx -t` ok ⟵ reload · من الخادم عبر الحلقة بـSNI: `login=503 text/html ·
  العلامة 1 · media=503 text/html · www=301`. الأصلُ (`1822b97d…`) محفوظٌ للرجوع.
  **الباقي بعينٍ من خارج شبكة المكتب** (هاتف المالك): `https://lettersys.online` يعرض صفحةَ الانتقال. الرجوعُ الكامل:
  `UNFREEZE_CONFIRM=UNFREEZE-20260924 bash vps_freeze_rollback.sh` (R2⟵R1⟵R3). **لا مسحَ لـVPS** قبل مرآةٍ خارجيّةٍ للمرفقات.

- 2026-09-24 — **عينُ المالك من خارج شبكة المكتب** (هاتفه): `https://lettersys.online` يعرض صفحةَ الانتقال — «تظهر العبارة كما كتبتها».
  ⟵ **القسمُ أ مختومٌ كاملاً**: F1/F2/F3 خضراء مقيسةً من الخادم + العينُ الخارجيّة. التالي القسمُ ب في الجلسة الأخرى.

- 2026-09-24 — **قرارُ المالك (نصّاً): «حرر»** — كتابُ القبول يأخذ **3917** لا 3918. **مسجَّلٌ قبل التنفيذ — كتابةٌ على القاعدة المحلّيّة
  (الإنتاج)**: الحجزُ 415 (وارد داخليّ · 3917 · `active` · بلا كتاب) ⟵ `mark_voided(user_cancelled)` بملاحظةٍ تذكر القرار، بحارس: يُنفَّذ فقط إن
  كان الحجزُ ما يزال `active` برقمه ونوعه وبلا كتاب، ولا كتابَ يحمل 3917. **البوّابة**: الحالةُ `voided` · أوّلُ رقمٍ قابلٍ للتدوير للوارد الداخليّ =
  **3917** (كان غيرَه قبل التحرير — طفرةُ الحالة) · العدّادُ **3918 لا ينزل** (التدويرُ يسبق الرقمَ الجديد). الرجوع: `reactivate` للحجز نفسِه.
  **المخرَج 12:17:55**: `BEFORE active · 3917 · بلا كتاب · العدّاد 3918` ⟵ `mark_voided` ⟵ `AFTER voided · user_cancelled · العدّاد 3918` — لكنّ
  **البوّابةَ احمرّت بخطأٍ فيها**: أخذت رأسَ قائمة التدوير (**2445**، مرتبطٌ بكتابٍ قائم) ولم تحاكِ تخطّي الخدمة لما يحمل رقمَه كتابٌ
  (`reservation_service.py:71`). **القياسُ المصحَّح** بمحاكاة المسار نفسِه: `2445 SKIP ⟵ 3917 PICK` ⟵ **التالي المدوَّر = 3917**؛ وقبل التحرير
  كان 415 `active` فليس مرشَّحاً (⟵ «لا شيء»). **الدرس**: بوّابةٌ تُحاكي مساراً يجب أن تُحاكيه كلَّه — تُكتب من الكود لا من اسم الدالّة.

- 2026-09-24 — **قرارُ المالك: إصلاحُ كتابات `.env` وقتَ التشغيل (D‑ENV) يُدمج بعد القبول، لا في نسخة الإطلاق.** الإيداعُ على فرع الإطلاق يحمل
  هجرةً **0078** تتعارض رقماً مع `0078` في #19 (كلتاهما تعتمد على 0077 — مقيسٌ بـ`ls-tree`) ⟵ يُعاد ترقيمُها فوق رأس #19 عند الدمج، ولا تُطبَّق
  قبله. **نسخةُ الإطلاق = `main@1c65a75` حرفيّاً** (ما جُرِّب)؛ الساكنُ عبر مغلِّفٍ خارج المستودع؛ والمهامُّ المجدولة تستدعي الكودَ القائم. والخدمةُ
  بحساب المالك فـ`.env` يبقى قابلاً للكتابة ⟵ السلوكُ القديم لا ينكسر يومَ الإطلاق.
  **قياسُ حجب المتصفّح (جلسةُ القسم ب)**: صفحةٌ على http من عنوان الشبكة ⟵ loopback: محجوبةٌ على الوكيل الحاليّ بسبب CORS، ومسموحةٌ حين يردّ
  الخادمُ بـ`Access-Control-Allow-Origin` + `Access-Control-Allow-Private-Network` ⟵ **تصميمُ المسح عبر المتصفّح قابلٌ للإصلاح بلا نموذجٍ جديد** (بلا هجرة).

- 2026-09-24 13:13 — **حكمُ فيبل الناقد (ملفّه `D:/migration/fable_critique_20260924.md`) التقط ما فاتني: 3917 أُعيد حجزُه** — تبويبُ إدخالٍ بقي مفتوحاً
  بجلسة 11:08 أعاد تدويرَ 415 إلى صاحبه بعد تحريري (`is_recycled=True`)، وهو الآن `cooldown` حتّى ~13:16. **أمرُ المالك «أجرِ الإصلاحاتِ تلقائيّاً»**.
  **مسجَّلٌ قبل التنفيذ**: حذفُ الجلسة المتروكة الوحيدة (المالك: لا أحدَ يعمل على السيرفر) ⟵ إلغاءُ 415 ثانيةً بحارس (3917 · بلا كتاب · حالٌ حيّة) ⟵
  بوّابةٌ من مسار الكود: `reserve_number` يمرّ على المرشَّحين ⟵ 2445 SKIP ⟵ **3917 PICK** · لا جلسةَ حيّة تستعيده.
